# Continue here

**Last position:** Phase 9 (multi-site-supervision) is **closed**. 12 of 12
plans implemented, 19 of 20 threats verified, summarised in
`.planning/phases/09-multi-site-supervision/09-SUMMARY.md`.

Phases 1 through 9 are executed and closed. **Phase 10 is the only remaining
phase and has no planning artifacts yet.**

Branch: `claude/chatgpt-continuation-hi3y86` (PR #3). No release is authorized
and none was made.

## Evidence at head

- `npm test` — 476 passed
- `npm run test:python` — 691 passed
- `npm run test:e2e` — 81 passed
- `node tools/verify-docs-site.mjs` — 25 sources, 41 byte-identical site files

**The browser override is no longer needed.** `playwright.config.mjs` now
prefers the pinned Chromium revision and uses it silently, substituting an
installed sibling only when the pinned one is absent from disk — and printing
the substitution, because running browser evidence against a different browser
than the one named is a fact about the evidence. The container ships revision
1194 while `@playwright/test` 1.62.1 expects 1234, so the substitution happens
here and the Phase-9 gate's F9-04 passes without any environment help. Setting
`PLAYWRIGHT_CHROMIUM_EXECUTABLE` still wins if you want a specific binary.

## Known blocked, in every phase

T9-20, T8-25, T7-23, T6-21, T5-16, T4-14, T3-14 and T2-16 all stay `planned`.
Each is owned by the composed `test:phaseN:release` leaf, whose
`test:ha-artifacts` leg probes `docker info` across twelve bounded lane
candidates; this environment has no Docker engine. Each carries its exact
failure output in its own register.

`F-01` (release provenance) is blocked separately: all five third-party
repository endpoints answer 403, "GitHub access to this repository is not
enabled for this session". `api.github.com/rate_limit` answers 200, so this is
a repository-scope limit rather than a network one. Attaching five third-party
repositories with credentials to satisfy a provenance check would be a
disproportionate permission change and was not made.

In CI the release leaf fails for a *different* reason: Home Assistant 2026.9.0
was published while `pytest-homeassistant-custom-component` 0.13.362 still pins
`homeassistant==2026.9.0b6`. Not this branch's failure. Which release the
product certifies against is a product decision, so no resolver change was
pushed; it is diagnosed once on PR #3 with a proposed patch.

## Next

1. **Phase 10 — Product-Wide Usability & Release Evidence.** Planning artifacts
   first (CONTEXT, SOURCE-AUDIT, RESEARCH, PATTERNS, THREATS, VALIDATION,
   UI-SPEC), then plans, then execution. It owns I18N-01, A11Y-01 and TEST-01:
   the evidence-linked claim registry, package checksums, and capacity
   scenarios with recorded budgets. **Several earlier phases defer measured
   numbers to it by name** — Phase 9's bounds are shapes, not measurements, and
   say so.

2. **Close-out.**
   - `REQUIREMENTS.md` traceability still reads "Pending" for completed phases.
   - Phases 3 and 4 have **no per-plan summaries**. Their registers and phase
     summaries carry the evidence; the summaries are genuinely missing.
   - Review passes for Phases 2 through 9 and Phase 1's consolidated
     verification are outstanding.

## Standing constraints

- No live Home Assistant writes, remote-site writes, physical-bus writes,
  plant or equipment calls, or credential handling.
- **No release is authorized.** No public HACS repository, GitHub release or
  Pages deployment without an exact target and explicit authorization.
- Browser role checks are UX only. Every shared read, write, control, remote
  call and authoritative audit event is enforced server-side.
- Source of truth: edit the authored modules and generators. An isolated change
  to `dist/glt-flow-card.js` or the Companion `www` copy is never complete.
- Commit and push only on `claude/chatgpt-continuation-hi3y86`.
