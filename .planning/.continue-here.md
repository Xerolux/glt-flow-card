# Continue here

**Last position:** Phase 7 (trends-energy-reports) is **closed**. 20 of 20 plans
implemented, 22 of 23 threats verified, summarised in
`.planning/phases/07-trends-energy-reports/07-SUMMARY.md`.

Branch: `claude/chatgpt-continuation-hi3y86` (PR #3). No release is authorized
and none was made.

## Evidence at head

- `npm test` — 455 passed
- `npm run test:python` — 510 passed, 1 deselected
- `npm run test:e2e` — 61 passed (requires the browser override below)
- `node tools/verify-docs-site.mjs` — 24 sources, 41 byte-identical site files
- `node tools/phase7-red-gate.mjs` — 0 controlled RED, 11 implemented, 0 broken

The exact-dist suites need the override `playwright.config.mjs` documents:

```
PLAYWRIGHT_CHROMIUM_EXECUTABLE=/opt/pw-browsers/chromium-1194/chrome-linux/chrome
```

The container ships browser revision 1194; `@playwright/test` 1.62.1 expects
1234. Without it the browser cannot launch, and the phase gate's F7-04 — which
does not set it — fails.

## Next

1. **Phase 8** — planning artifacts first (still `Plans: TBD` in the roadmap,
   as are 9 and 10).
2. **Open work carried out of Phase 7, not closed:**
   - `glt_flow_card/history/coverage` and `.../history/export` are still shells
     returning a stated `unavailable`. Coverage needs the period's **expected
     instants**, and that grid's bucket step is a decision that wants a corpus
     behind it — inferring it from the rows that came back is exactly the defect
     `expected_instants` exists to prevent, so it was left open rather than
     half-built. This deserves its own plan.
3. **Review passes** still outstanding for Phases 2–7 (code review, security
   audit, UI audit, drift checks, goal verification) and Phase 1's consolidated
   verification.

## Environment limits (four, all recorded in `07-THREATS.md`)

1. Browser revision 1194 vs pinned 1234 — see the override above.
2. **Correction to the Phase-6 record.** GitHub API is *not* blocked at the
   host: `api.github.com/rate_limit` answers 200. Only third-party repository
   endpoints 403 ("GitHub access to this repository is not enabled for this
   session"), which is what blocks F-01 and therefore every gate recursing into
   Phase 1. Attaching five third-party repos with credentials to satisfy a
   provenance check would be a disproportionate permission change; not done.
3. No Docker engine — blocks T7-23 and the five earlier release leaves.
4. In CI the same leaf fails *differently*: HA 2026.9.0 was published while
   `pytest-homeassistant-custom-component` 0.13.362 still pins `2026.9.0b6`.
   Not this branch's failure; diagnosed in a PR comment, no resolver change
   pushed.

## Standing constraints

- No live Home Assistant writes, remote-site writes, physical-bus writes,
  plant/equipment calls, or credential handling.
- No release is authorized. No public HACS repo, GitHub release or Pages deploy
  without an exact target and explicit authorization.
- Browser role checks are UX only; shared reads, writes, controls, remote calls
  and authoritative audit events require server-side enforcement.
- Source of truth: edit authored modules and generators. An isolated change to
  `dist/glt-flow-card.js` or the Companion `www` copy is never complete.
- Commit order: source → build → stage → commit manifest.
- Develop, commit and push only on `claude/chatgpt-continuation-hi3y86`.
