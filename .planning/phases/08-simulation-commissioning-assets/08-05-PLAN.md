---
phase: 08-simulation-commissioning-assets
plan: 5
type: execute
wave: 1
depends_on: [2, 3]
files_modified: [custom_components/glt_flow_card/simulation_session.py, tests/components/glt_flow_card/test_simulation_session.py]
autonomous: true
requirements: [SIM-01]
must_haves:
  truths:
    - "Simulation state is site runtime state the Companion owns."
    - "A session expires without intervention."
    - "An over-long request is refused, never capped."
---

## Phase Goal

**As an** operator, **I want** a rehearsal to end by itself, **so that** the plant does not become unoperable because somebody forgot.

<objective>Give the simulation session an owner, a boundary and an end.</objective>

<tasks>
<task type="auto">
  <name>Task 1: The session belongs to the Companion</name>
  <files>custom_components/glt_flow_card/simulation_session.py</files>
  <action>Fix T8-02. `simulation.enabled` in the project document is operator input acting as an authorization — Phase 6's notification-allowlist defect with plant writes behind it. The session records who started it, when, for which project, and when it expires, and a project document can neither enable nor exempt itself from it.</action>
  <acceptance_criteria>
    - A project document containing `simulation.enabled` does not start, stop or bypass a session.
    - The session names its actor and its expiry.
    - Starting and stopping are audited.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_simulation_session.py -q -x</automated></verify>
  <done>T8-02 passes.</done>
</task>

<task type="auto">
  <name>Task 2: Bounded, and refused rather than capped</name>
  <files>custom_components/glt_flow_card/simulation_session.py</files>
  <action>Fix T8-06. A rehearsal that never ends makes the plant unoperable and someone will work around the block, which is worse than no block. Bound the TTL with an explicit maximum and **refuse** a longer request rather than truncating it — Phase 6 shipped a 90-day shelve silently cut to 7 and the operator walked away believing the alarm was quiet for three months.</action>
  <acceptance_criteria>
    - An over-long TTL is refused with the maximum named.
    - An expired session stops suppressing dispatch without anyone acting.
    - An unreadable expiry does **not** extend the session.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_simulation_session.py -q -x</automated></verify>
  <done>T8-06 passes.</done>
</task>
</tasks>

<output>Create `08-05-SUMMARY.md` when done.</output>
