---
phase: 08-simulation-commissioning-assets
plan: 9
type: execute
wave: 2
depends_on: [2]
files_modified: [custom_components/glt_flow_card/commissioning.py, tests/components/glt_flow_card/test_commissioning.py]
autonomous: true
requirements: [DIAG-01]
must_haves:
  truths:
    - "The diagnostic is computed by the Companion from the registries and the state machine."
    - "A reference is declared, never guessed from a string."
    - "Registry and state machine are two questions with four answers."
---

## Phase Goal

**As an** engineer, **I want** to ask whether the installation is ready and be told the truth, **so that** I can act on the answer.

<objective>Move the diagnostic to where the facts are, and stop it inventing findings.</objective>

<tasks>
<task type="auto">
  <name>Task 1: The diagnostic moves to the Companion</name>
  <files>custom_components/glt_flow_card/commissioning.py</files>
  <action>Fix T8-10. `diagnoseConfig(config, hassStates)` reads the state map the card was handed, so registry provenance — which integration, which device, which config entry — is not merely missing but unreachable from where the code runs. `entity_registry` and `device_registry` exist only in the Companion. This is the third phase to draw this line (alarms, trends, now commissioning), which suggests it is the product's architecture rather than a per-phase choice.</action>
  <acceptance_criteria>
    - Provenance comes from `RegistryEntry.platform`, `config_entry_id` and `device_id`, read rather than inferred from the entity id.
    - The browser renders the Companion's answer and computes no diagnosis.
    - Two clients asking simultaneously get the same answer.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_commissioning.py -q -x</automated></verify>
  <done>T8-10 passes.</done>
</task>

<task type="auto">
  <name>Task 2: References are declared, not guessed</name>
  <files>custom_components/glt_flow_card/commissioning.py</files>
  <action>Fix T8-11. `collect()` treats any string containing a dot as an entity id, so a version string, a filename and a decimal written as text each become a "missing entity". A diagnostic that reports things that are not true stops being read, and then nothing else it reports matters. Collect from declared locations — profile slots, control definitions, alarm conditions, datapoint bindings, energy meters — and have each finding name where it came from.</action>
  <acceptance_criteria>
    - A project containing `"1.2.3"` and `"docs/readme.md"` produces no entity findings for them.
    - Every finding names its declaring site, not just the entity.
    - A reference reachable only by pattern-matching is not a reference.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_commissioning.py -q -x</automated></verify>
  <done>T8-11 passes.</done>
</task>

<task type="auto">
  <name>Task 3: Four diagnoses, not one</name>
  <files>custom_components/glt_flow_card/commissioning.py</files>
  <action>Fix T8-13. Registry membership and state-machine membership are independent, and the four combinations are four different problems: `present`, `registered_not_loaded` (disabled, or its integration failed to set up), `unregistered` (a template or YAML entity — fine, but with no provenance), and `missing`. Collapsing them into `missing` sends an engineer to look for a typo when the real answer is that an integration failed to load.</action>
  <acceptance_criteria>
    - All four combinations from the registry corpus produce their own diagnosis.
    - `unregistered` is not an error — it states the absence of provenance.
    - Staleness is computed server-side with an age, so a wrong client clock cannot invent one.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_commissioning.py -q -x</automated></verify>
  <done>T8-13 passes, and T8-17's staleness half with it.</done>
</task>
</tasks>

<output>Create `08-09-SUMMARY.md` when done.</output>
