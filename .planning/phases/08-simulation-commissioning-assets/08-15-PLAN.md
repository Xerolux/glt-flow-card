---
phase: 08-simulation-commissioning-assets
plan: 15
type: execute
wave: 4
depends_on: [12]
files_modified: [custom_components/glt_flow_card/maintenance_plans.py, tests/components/glt_flow_card/test_maintenance_plans.py]
autonomous: true
requirements: [ASSET-01]
must_haves:
  truths:
    - "Due logic is computed from a declared plan, never typed by hand."
    - "An operating-hour plan states its coverage."
---

## Phase Goal

**As a** maintenance planner, **I want** next-due computed from the plan, **so that** it is not whatever somebody last wrote in a box.

<objective>Give due dates a derivation.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Interval and operating-hour plans</name>
  <files>custom_components/glt_flow_card/maintenance_plans.py</files>
  <action>Fix T8-21. `due` is a date string typed by a human, with no interval plan, no operating-hour plan, no next-due calculation and no reminder — four of ASSET-01's named capabilities. Reuse Phase 7's period vocabulary and resolution for interval plans rather than paralleling them, since a second notion of "a month" is how two parts of a product start disagreeing.</action>
  <acceptance_criteria>
    - An interval plan resolves next-due on local-calendar boundaries via Phase 7's resolver.
    - An operating-hour plan computes from measured hours, not from wall time.
    - Overdue is derived, not stored.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_maintenance_plans.py -q -x</automated></verify>
  <done>The interval half of T8-21 passes.</done>
</task>

<task type="auto">
  <name>Task 2: An operating-hour plan states its coverage</name>
  <files>custom_components/glt_flow_card/maintenance_plans.py</files>
  <action>An operating-hour figure computed over a window the Recorder only half covers is a smaller number presented with the same confidence as a whole one — Phase 7's D16 in a new subject. Carry Phase 7's `measured_value` shape so hours arrive with their coverage, and refuse to declare a service due or not-due when coverage is below a stated threshold, saying so rather than guessing.</action>
  <acceptance_criteria>
    - Operating hours carry coverage and gaps.
    - Below the threshold the answer is "not determinable", with the coverage stated.
    - The threshold is configuration with a conservative default documented as a site decision.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_maintenance_plans.py -q -x</automated></verify>
  <done>T8-21 passes.</done>
</task>
</tasks>

<output>Create `08-15-SUMMARY.md` when done.</output>
