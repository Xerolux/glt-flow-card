---
phase: 08-simulation-commissioning-assets
plan: 11
type: execute
wave: 3
depends_on: [10]
files_modified: [custom_components/glt_flow_card/commissioning.py, tests/components/glt_flow_card/test_commissioning.py]
autonomous: true
requirements: [DIAG-01]
must_haves:
  truths:
    - "A full diagnostic run produces an empty service ledger, proven by execution."
    - "Readiness is counts per diagnosis, not an invented percentage."
---

## Phase Goal

**As an** engineer, **I want** asking whether the plant is ready to change nothing, **so that** the question is safe to ask at any time.

<objective>Prove read-only, and stop reporting a number nobody defined.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Read-only, by execution</name>
  <files>tests/components/glt_flow_card/test_commissioning.py</files>
  <action>Fix T8-14. "It is read-only by construction" is a claim by inspection, which is exactly what D16 records. Run a full diagnostic over a project that references controls, alarms, schedules and meters, and assert the dispatch ledger is empty across every kind. The assertion must also confirm the run actually did work — a diagnostic that produced no findings because it examined nothing would satisfy an empty ledger, which is the vacuous pass Phase 4 and Phase 7 each corrected once.</action>
  <acceptance_criteria>
    - Every ledger count is zero after a full run.
    - The same test asserts findings were produced, so an empty ledger cannot come from an empty run.
    - Remediation is a link and never an action.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_commissioning.py -q -x</automated></verify>
  <done>T8-14 passes.</done>
</task>

<task type="auto">
  <name>Task 2: Counts, not a score</name>
  <files>custom_components/glt_flow_card/commissioning.py</files>
  <action>Fix T8-17. `100 - issues.length / refs.size * 100` counts issues rather than entities, so two findings on one entity subtract twice and thirty findings on ten entities give a negative clamped to zero — presented as a readiness percentage. Replace it with counts per diagnosis. Do **not** replace it with a better-computed invented score: that is the same defect with a nicer formula, and the honest answer to "how ready is this?" is a list of what is wrong.</action>
  <acceptance_criteria>
    - The response carries a count per diagnosis and no aggregate percentage.
    - A project with two findings on one entity reports one affected entity.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_commissioning.py -q -x</automated></verify>
  <done>T8-17 passes.</done>
</task>
</tasks>

<output>Create `08-11-SUMMARY.md` when done.</output>
