---
phase: 08-simulation-commissioning-assets
plan: 4
type: execute
wave: 1
depends_on: [2, 3]
files_modified: [custom_components/glt_flow_card/scenarios.py, tests/components/glt_flow_card/test_scenarios.py]
autonomous: true
requirements: [SIM-01]
must_haves:
  truths:
    - "A scenario is a pure function of definition and tick."
    - "The same definition and tick yield byte-identical state, on any machine, at any time."
    - "A scenario evaluates with an empty state machine."
---

## Phase Goal

**As an** engineer, **I want** to rehearse a sequence before the entities exist, **so that** a design can be tested rather than only drawn.

<objective>Make a scenario repeatable by construction.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Scenarios as pure functions</name>
  <files>custom_components/glt_flow_card/scenarios.py</files>
  <action>Fix T8-07. Home Assistant offers an integration no virtual clock, so a repeatable scenario cannot be built by moving its clock — and a pure function from definition and tick is stronger anyway: reproducible by construction, needing no wall time to pass, and evaluable for entities that do not exist yet (D7), because nothing is read from the state machine. Support value ramps, pump and valve state sequences, and injected faults.</action>
  <acceptance_criteria>
    - `evaluate(definition, tick)` reads nothing outside its arguments.
    - The corpus's varying scenario changes per tick and is byte-identical on re-run.
    - A scenario naming entities that do not exist evaluates without error.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_scenarios.py -q -x</automated></verify>
  <done>T8-07 passes.</done>
</task>

<task type="auto">
  <name>Task 2: A simulated value is validated before it is stored</name>
  <files>custom_components/glt_flow_card/scenarios.py</files>
  <action>Fix T8-08. The shipped path stores `querySelector("[data-value]").value` verbatim — no number, no unit, no device class, no range. Validate against the profile's declared unit and device class at authoring time, refusing with the reason, so a scenario cannot assert something the entity could never report.</action>
  <acceptance_criteria>
    - A value whose unit contradicts the profile is refused with both sides named.
    - A non-numeric value for a numeric slot is refused.
    - The refusal reasons come from the closed set.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_scenarios.py -q -x</automated></verify>
  <done>T8-08 passes.</done>
</task>
</tasks>

<output>Create `08-04-SUMMARY.md` when done.</output>
