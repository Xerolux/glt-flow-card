---
phase: 08-simulation-commissioning-assets
plan: 6
type: execute
wave: 2
depends_on: [5]
files_modified: [custom_components/glt_flow_card/dispatch_gate.py, custom_components/glt_flow_card/__init__.py, tests/components/glt_flow_card/test_simulation_gate.py]
autonomous: true
requirements: [SIM-01]
must_haves:
  truths:
    - "Every dispatch path consults one decision at the point of dispatch."
    - "A physical kind is refused while a session is active."
    - "An unreadable simulation state refuses, with its own reason."
---

## Phase Goal

**As an** engineer rehearsing a sequence, **I want** the plant to be genuinely unreachable, **so that** the interface's promise is true.

<objective>Close the headline defect: make simulation block something.</objective>

<tasks>
<task type="auto">
  <name>Task 1: One decision, consulted at the point of dispatch</name>
  <files>custom_components/glt_flow_card/dispatch_gate.py, custom_components/glt_flow_card/__init__.py</files>
  <action>Fix T8-01. This is a safety defect, not a correctness one: no server path reads `simulation.enabled`, `ws_controls_execute` calls `hass.services.async_call` unconditionally and `ws_remote_control` forwards an arbitrary domain and service, so the product blocks *nothing* while displaying "Simulationsmodus aktiv". An engineer rehearsing on a Saturday is operating the plant and has been told they are not. `decide_dispatch(kind, simulation)` returns dispatch, simulated or refused, and every path asks it immediately before the effect — not at the start of a handler, where a later branch can slip past.</action>
  <acceptance_criteria>
    - A control, a remote control and a schedule service are each refused during a session, asserted from the effect ledger rather than the return value.
    - The refusal reason is `simulation_active`.
    - With no session, each dispatches exactly once.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_simulation_gate.py -q -x</automated></verify>
  <done>T8-01 passes.</done>
</task>

<task type="auto">
  <name>Task 2: Fail closed, and say which way</name>
  <files>custom_components/glt_flow_card/dispatch_gate.py</files>
  <action>Fix T8-04. If simulation state cannot be read, refuse every physical kind. An unknown resolving to "go ahead" is worse than not having the feature, because the feature is what persuaded the engineer they were safe. Carry `simulation_state_unavailable` distinctly from `simulation_active`: one means "you are rehearsing", the other "the Companion is unwell and is protecting you", and they call for different responses.</action>
  <acceptance_criteria>
    - An injected read failure refuses, and the effect ledger is empty.
    - The two reasons are distinct and both reach the caller.
    - The refusal is audited.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_simulation_gate.py -q -x</automated></verify>
  <done>T8-04 passes.</done>
</task>
</tasks>

<output>Create `08-06-SUMMARY.md` when done.</output>
