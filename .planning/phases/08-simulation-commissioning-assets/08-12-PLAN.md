---
phase: 08-simulation-commissioning-assets
plan: 12
type: execute
wave: 3
depends_on: [3]
files_modified: [custom_components/glt_flow_card/work_orders.py, custom_components/glt_flow_card/__init__.py, tests/components/glt_flow_card/test_work_orders.py]
autonomous: true
requirements: [ASSET-01]
must_haves:
  truths:
    - "A work order is a sequence of entries, not a mutable row."
    - "Status is derived from the entries, so record and display cannot disagree."
    - "An invalid transition is refused before an entry is appended."
---

## Phase Goal

**As a** technician's colleague six months later, **I want** to know who did what and when, **so that** the record answers the question it exists for.

<objective>Make the maintenance record able to survive being written to.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Append-only entries</name>
  <files>custom_components/glt_flow_card/work_orders.py</files>
  <action>Fix T8-18. `save_work_order` does `{**old, **work_order}`, so completing an order erases who opened it and when, and a completed record is indistinguishable from a rewritten one. Entries are append-only with actor and time; a correction is a **new entry naming what it corrects**, never an edit. Current status is derived from the entries rather than stored beside them, so the record and the display cannot disagree. Reconcile D20's two stores: the Companion's store is authoritative and the project document no longer carries work orders.</action>
  <acceptance_criteria>
    - Completing an order preserves the opening entry with its original actor and time.
    - A correction appends and names its target; nothing is edited in place.
    - The project document is no longer a second, disagreeing store.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_work_orders.py -q -x</automated></verify>
  <done>T8-18 passes.</done>
</task>

<task type="auto">
  <name>Task 2: A closed transition table</name>
  <files>custom_components/glt_flow_card/work_orders.py</files>
  <action>Fix T8-19. Any status string is currently accepted, so `"banana"` is a valid status and a completed order can silently return to open. Check the transition **before** the entry is appended, and name both the current status and the attempted one in the refusal — "invalid transition" alone leaves the operator guessing which half was wrong. A reopen is its own transition carrying a reason, not a silent revert.</action>
  <acceptance_criteria>
    - Every transition not in the table is refused with both statuses named.
    - Reopening requires a reason and is recorded as a reopen.
    - The refusal happens before anything is stored.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_work_orders.py -q -x</automated></verify>
  <done>T8-19 passes.</done>
</task>
</tasks>

<output>Create `08-12-SUMMARY.md` when done.</output>
