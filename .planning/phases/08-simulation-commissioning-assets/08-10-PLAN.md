---
phase: 08-simulation-commissioning-assets
plan: 10
type: execute
wave: 3
depends_on: [9]
files_modified: [custom_components/glt_flow_card/commissioning.py, tests/components/glt_flow_card/test_commissioning.py]
autonomous: true
requirements: [DIAG-01]
must_haves:
  truths:
    - "Services are checked alongside entities."
    - "A unit or device class contradicting the profile is a finding naming both sides."
    - "Suggestions are bounded and the bound is stated."
---

## Phase Goal

**As an** engineer, **I want** the readiness answer to cover what actually breaks, **so that** commissioning finds the fault before an operator does.

<objective>Check the things the shipped diagnostic never looked at.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Services are references too</name>
  <files>custom_components/glt_flow_card/commissioning.py</files>
  <action>Fix T8-15. The criterion says every referenced entity *and service*; services are never collected, so a control naming a service that does not exist is discovered when an operator presses the button — the same "fails at the call, not the request" shape Phase 4 closed for controls and Phase 6 for calendar bindings.</action>
  <acceptance_criteria>
    - A control naming an absent service produces `service_missing` naming the control.
    - A service that exists produces no finding.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_commissioning.py -q -x</automated></verify>
  <done>T8-15 passes.</done>
</task>

<task type="auto">
  <name>Task 2: Units, device classes and duplicates</name>
  <files>custom_components/glt_flow_card/commissioning.py</files>
  <action>Fix T8-16. Compare the entity's registry unit and device class against the profile's expectation, and detect one entity bound to two slots. Report both sides — "the profile expects °C, the entity reports %" — because a finding that names only one side leaves the engineer to guess which is wrong, and Phase 5 established that a refusal without both sides is a tool disagreeing with a human without saying why.</action>
  <acceptance_criteria>
    - A unit mismatch names expected and actual.
    - A device-class mismatch is distinct from a unit mismatch.
    - Two slots bound to one entity produce `duplicate_binding` naming both slots.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_commissioning.py -q -x</automated></verify>
  <done>T8-16 passes.</done>
</task>

<task type="auto">
  <name>Task 3: Bounded suggestions</name>
  <files>custom_components/glt_flow_card/commissioning.py</files>
  <action>Fix T8-12. `unused` returns every entity in the installation the project does not reference — on a real Home Assistant, thousands of rows rendered into a modal. Bound it, and state the bound in the answer so a truncated list cannot read as a complete one.</action>
  <acceptance_criteria>
    - The suggestion list is capped and the cap is in the response.
    - A truncated answer says it was truncated.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_commissioning.py -q -x</automated></verify>
  <done>T8-12 passes.</done>
</task>
</tasks>

<output>Create `08-10-SUMMARY.md` when done.</output>
