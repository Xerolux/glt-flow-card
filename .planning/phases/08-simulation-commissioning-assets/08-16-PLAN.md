---
phase: 08-simulation-commissioning-assets
plan: 16
type: execute
wave: 5
depends_on: [14, 15]
files_modified: [docs/wiki/Simulation-Commissioning.md, docs/wiki/Assets-Maintenance.md, .planning/phases/08-simulation-commissioning-assets/08-THREATS.md, .planning/STATE.md, .planning/ROADMAP.md]
autonomous: true
requirements: [SIM-01, DIAG-01, ASSET-01]
must_haves:
  truths:
    - "Every threat row is marked from its own owner command, run at head."
    - "The documentation states what the product does not have."
    - "A blocked command is recorded with its exact failure."
---

## Phase Goal

**As an** engineer, **I want** the phase closed against evidence rather than recollection, **so that** the register means what it says.

<objective>Document the contract and close the phase honestly.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Document it, including what it is not</name>
  <files>docs/wiki/Simulation-Commissioning.md, docs/wiki/Assets-Maintenance.md</files>
  <action>German pages with English README sections. State plainly: simulation blocks physical dispatch server-side and *marks* notification rather than silencing it, and why. Commissioning is read-only and this is proven by execution, not asserted. Maintenance is a bounded workflow and **not a CMMS** — REQUIREMENTS rules out conformance claims and the page must not imply one. `Assets-Maintenance.md` already exists and describes the shipped behaviour, so it is rewritten rather than appended to.</action>
  <acceptance_criteria>
    - The "simulation blocks physical dispatch, marks notification" rule is explicit with its reason.
    - The "not a CMMS" statement is explicit.
    - `node tools/verify-docs-site.mjs` passes with the new sources counted.
  </acceptance_criteria>
  <verify><automated>node tools/verify-docs-site.mjs</automated></verify>
  <done>The contract is written down.</done>
</task>

<task type="auto">
  <name>Task 2: Close the register from commands</name>
  <files>.planning/phases/08-simulation-commissioning-assets/08-THREATS.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <action>Run each threat's owner command at head and mark each row only from its own command passing — where rows share a command, run it per row. Any row whose evidence is a grep over `dist/` must carry its paired outcome assertion. Record a blocked command with its exact failure output rather than its likely cause: Phase 7 found four independent environment limits and corrected one Phase 6 had recorded wrongly.</action>
  <acceptance_criteria>
    - Every marked row has its own at-head run.
    - Blocked rows carry exact failure output.
    - The summary states the limitations, including anything raised rather than taken.
  </acceptance_criteria>
  <verify><automated>npm run test:phase8</automated></verify>
  <done>The phase is closed against evidence.</done>
</task>
</tasks>

<output>Create `08-16-SUMMARY.md` and `08-SUMMARY.md` when done.</output>
