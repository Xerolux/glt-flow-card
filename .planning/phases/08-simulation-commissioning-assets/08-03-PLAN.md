---
phase: 08-simulation-commissioning-assets
plan: 3
type: execute
wave: 1
depends_on: [2]
files_modified: [custom_components/glt_flow_card/content_id.py, src/v100/content-id.mjs, test/content-id.test.mjs, tests/components/glt_flow_card/test_content_id.py]
autonomous: true
requirements: [ASSET-01]
must_haves:
  truths:
    - "An id is derived from content, identically in both runtimes."
    - "Two records created in the same millisecond have different ids."
    - "The same record re-created yields the same id."
---

## Phase Goal

**As an** engineer, **I want** a record's identity to come from what it is, **so that** it can be reproduced and cannot collide.

<objective>Close the clock-derived id pattern once, in a shared helper.</objective>

<tasks>
<task type="auto">
  <name>Task 1: One content-id helper, both runtimes</name>
  <files>custom_components/glt_flow_card/content_id.py, src/v100/content-id.mjs</files>
  <action>Fix T8-22. `wo_${Date.now()}` is the **third** occurrence of this defect: Phase 5 fixed it in paste, Phase 7 in report runs, and this phase would be the third independent fix. Close it once — `content_id(kind, payload)` hashing canonical bytes, with the kind as a prefix so an id says what it identifies. Reuse Phase 7's `canonical_number` so the canonical bytes are the project's existing ones rather than a fourth notion of canonical.</action>
  <acceptance_criteria>
    - Byte-identical ids across both runtimes over a committed corpus.
    - Two records differing only in a nested value get different ids.
    - The same content yields the same id, and the test says so by re-deriving rather than by comparing to a literal.
  </acceptance_criteria>
  <verify><automated>node --test test/content-id.test.mjs && py -3.13 -m pytest tests/components/glt_flow_card/test_content_id.py -q -x</automated></verify>
  <done>T8-22 passes and the pattern has one home.</done>
</task>
</tasks>

<output>Create `08-03-SUMMARY.md` when done.</output>
