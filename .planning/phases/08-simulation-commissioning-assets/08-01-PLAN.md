---
phase: 08-simulation-commissioning-assets
plan: 1
type: execute
wave: 0
depends_on: []
files_modified: [tools/verify-phase8.mjs, tools/phase8-red-gate.mjs, tests/components/glt_flow_card/dispatch_factory.py, tests/components/glt_flow_card/fixtures/scenario_corpus.json, tests/components/glt_flow_card/fixtures/registry_corpus.json]
autonomous: true
requirements: [SIM-01, DIAG-01, ASSET-01]
must_haves:
  truths:
    - "The gate proves one acyclic path to test:phase8:release and runs the Phase-7 gate."
    - "A test that dispatches a real service fails, and the ledger says which path did it."
    - "The corpora are committed before anything reads them."
---

## Phase Goal

**As an** engineer, **I want** the phase's evidence machinery standing before any behaviour, **so that** a claim about safety is a command someone can run.

<objective>Stand up the Phase-8 gate, the dispatch effect ledger and the two fixture corpora.</objective>

<tasks>
<task type="auto">
  <name>Task 1: The Phase-8 gate</name>
  <files>tools/verify-phase8.mjs</files>
  <action>Bind requirements, roadmap truths, plans, threats and assumptions to evidence, prove the command graph acyclic with exactly one path to `test:phase8:release`, and run the Phase-7 gate as F8-06. A Phase-8 claim resting on Phase-7 guarantees nobody re-ran is resting on nothing.</action>
  <acceptance_criteria>
    - The graph is proven acyclic and reaches the release leaf exactly once.
    - Every F8-nn command is named with its own hash.
    - The Phase-7 gate is mandatory.
  </acceptance_criteria>
  <verify><automated>npm run test:phase8</automated></verify>
  <done>The gate exists and reports honestly, including when it fails.</done>
</task>

<task type="auto">
  <name>Task 2: The dispatch effect ledger</name>
  <files>tests/components/glt_flow_card/dispatch_factory.py</files>
  <action>A recording double for every effect that can leave this integration: service calls, remote calls, notifications. It records rather than performs, and raises `LiveDispatchReached` if a test wires a real one. This is the Phase-7 recorder-factory pattern applied to the thing that can move plant, so the same class of mistake fails loudly instead of quietly succeeding.</action>
  <acceptance_criteria>
    - Every recorded call carries kind, domain, service and target.
    - A live dispatch raises rather than returning.
    - Counts are emitted as `PHASE8_*_EFFECTS` and are zero unless the test names them.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_dispatch_enumeration.py -q -x</automated></verify>
  <done>A test cannot reach a real service without failing.</done>
</task>

<task type="auto">
  <name>Task 3: The scenario and registry corpora</name>
  <files>tests/components/glt_flow_card/fixtures/scenario_corpus.json, tests/components/glt_flow_card/fixtures/registry_corpus.json</files>
  <action>Commit both before anything reads them. The scenario corpus must contain at least one scenario whose value *changes per tick* — a corpus of constants would prove reproducibility on something that cannot vary. The registry corpus must contain all four registry/state combinations, since collapsing them is T8-13.</action>
  <acceptance_criteria>
    - The scenario corpus contains a varying scenario, a fault scenario and a scenario whose entities do not exist.
    - The registry corpus contains present, registered-not-loaded, unregistered and missing.
    - Both are committed in this plan, not generated by the code under test later.
  </acceptance_criteria>
  <verify><automated>node tools/phase8-red-gate.mjs</automated></verify>
  <done>The evidence exists before the claims do.</done>
</task>
</tasks>

<output>Create `08-01-SUMMARY.md` when done.</output>
