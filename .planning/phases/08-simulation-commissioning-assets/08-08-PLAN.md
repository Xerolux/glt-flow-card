---
phase: 08-simulation-commissioning-assets
plan: 8
type: execute
wave: 2
depends_on: [6]
files_modified: [custom_components/glt_flow_card/notifications.py, tests/components/glt_flow_card/test_simulation_gate.py]
autonomous: true
requirements: [SIM-01, ALM-02]
must_haves:
  truths:
    - "A notification during a rehearsal is delivered, and says the plant was simulated."
    - "Blocking it would be a safety defect in the other direction."
---

## Phase Goal

**As an** operator, **I want** a real fault during a rehearsal to still reach me, **so that** the test does not become a blind spot.

<objective>Mark notifications rather than silencing them.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Marked, not blocked</name>
  <files>custom_components/glt_flow_card/notifications.py</files>
  <action>Fix T8-05. The obvious reading of "block everything during simulation" silences alarms, which turns a rehearsal into a window where nobody is told about a real fault. Notification is `marked`: it is delivered, and the message states the plant was simulated. Report delivery is marked for the same reason — a report produced from simulated inputs must say so on its face rather than fail to arrive, since a missing report is noticed late and a wrong one is not noticed at all.</action>
  <acceptance_criteria>
    - A notification during a session is delivered and its body states the simulation.
    - The delivery is recorded with the simulation marked, so the audit distinguishes it later.
    - Blocking it fails the test, with a message saying why silencing is the wrong answer.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_simulation_gate.py -q -x</automated></verify>
  <done>T8-05 passes.</done>
</task>
</tasks>

<output>Create `08-08-SUMMARY.md` when done.</output>
