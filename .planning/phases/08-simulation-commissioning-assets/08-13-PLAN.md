---
phase: 08-simulation-commissioning-assets
plan: 13
type: execute
wave: 4
depends_on: [12]
files_modified: [custom_components/glt_flow_card/work_orders.py, custom_components/glt_flow_card/attachments.py, tests/components/glt_flow_card/test_work_orders.py]
autonomous: true
requirements: [ASSET-01]
must_haves:
  truths:
    - "Attachments and history are bounded, with the limits stated."
    - "An over-limit attachment is refused, never truncated."
---

## Phase Goal

**As an** installation owner, **I want** maintenance evidence not to grow without limit, **so that** the feature does not become the reason storage fills up.

<objective>Bound the evidence, and say what the bounds are.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Bounded attachments</name>
  <files>custom_components/glt_flow_card/attachments.py</files>
  <action>Fix T8-20's attachment half. Completion evidence — photos, documents, parts — has no size, count or type limit today because it does not exist; adding it without bounds would create the leak rather than inherit it. Limits are stated **before** an attachment is chosen, refused rather than truncated, and the type is checked by content rather than by filename extension.</action>
  <acceptance_criteria>
    - An over-size or over-count attachment is refused with the limit named.
    - A type not on the allowlist is refused, and the check does not trust the extension.
    - The limits are readable by the surface so they can be shown in advance.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_work_orders.py -q -x</automated></verify>
  <done>The attachment half of T8-20 passes.</done>
</task>

<task type="auto">
  <name>Task 2: Bounded history and store</name>
  <files>custom_components/glt_flow_card/work_orders.py</files>
  <action>Fix T8-20's storage half. `manager.data["work_orders"]` is only ever written to — Phase 6 named this shape: unbounded state is a leak with a friendly name. Retain by an explicit, configurable policy with a conservative default, documented as a site decision the way Phase 6 documented every alarm default, and **never** drop an entry from an open work order.</action>
  <acceptance_criteria>
    - The retention policy is configuration with a stated default.
    - Only completed orders past the horizon are dropped; an open one is never pruned.
    - Pruning is recorded, so a missing record is explicable rather than mysterious.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_work_orders.py -q -x</automated></verify>
  <done>T8-20 passes.</done>
</task>
</tasks>

<output>Create `08-13-SUMMARY.md` when done.</output>
