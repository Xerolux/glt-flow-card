---
phase: 08-simulation-commissioning-assets
plan: 7
type: execute
wave: 2
depends_on: [6]
files_modified: [tests/components/glt_flow_card/test_dispatch_enumeration.py, custom_components/glt_flow_card/dispatch_vocabulary.py]
autonomous: true
requirements: [SIM-01]
must_haves:
  truths:
    - "Every path that can leave this integration is declared."
    - "A declared path that does not consult the gate fails the test."
    - "An undeclared path that dispatches fails the test."
---

## Phase Goal

**As an** engineer, **I want** the block to have the shape of the product rather than of somebody's memory, **so that** the next dispatch path cannot quietly escape it.

<objective>Prove no dispatch path escapes the gate.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Walk every declared path</name>
  <files>tests/components/glt_flow_card/test_dispatch_enumeration.py</files>
  <action>Fix T8-03. A gate applied where somebody remembered to apply it is a gate with the shape of somebody's memory. Enumerate the six paths the research measured — configured control, legacy control, remote control, notification, schedule service, report delivery — start a session, exercise each, and assert the ledger. The test must fail if a path is declared but never actually invoked, or the enumeration becomes a list that proves nothing about code that ran.</action>
  <acceptance_criteria>
    - Every declared kind is exercised and its outcome asserted.
    - A path that reaches the ledger without consulting the gate fails with the path named.
    - The test asserts each path was actually invoked, not merely listed.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_dispatch_enumeration.py -q -x</automated></verify>
  <done>T8-03 passes.</done>
</task>
</tasks>

<output>Create `08-07-SUMMARY.md` when done.</output>
