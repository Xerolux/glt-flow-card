---
phase: 08-simulation-commissioning-assets
plan: 14
type: execute
wave: 4
depends_on: [11, 12]
files_modified: [src/v100/project-assets.js, src/v100/entry.js, test/e2e/project-assets.spec.mjs, tools/run-exact-dist-playwright.mjs]
autonomous: true
requirements: [SIM-01, DIAG-01, ASSET-01]
must_haves:
  truths:
    - "A simulated value is marked as text and shape, never by colour."
    - "Every workflow is keyboard-reachable and every state is announced as text."
    - "Operator text reaches the DOM as text and still reaches the reader."
---

## Phase Goal

**As an** operator on a monochrome control-room kiosk, **I want** to see that a value is simulated, **so that** a rehearsal cannot read as commissioned plant.

<objective>Ship the three surfaces to `08-UI-SPEC.md`.</objective>

<tasks>
<task type="auto">
  <name>Task 1: The surfaces</name>
  <files>src/v100/project-assets.js, src/v100/entry.js</files>
  <action>Simulation session banner, scenario table, commissioning table, work-order entries, attachment limits. Form fields, never `prompt()` — the third time this rule is written down, after Phase 6's acknowledgement comment and Phase 7's report schedule. Register the new spec in the exact-dist runner's spec list, which now refuses when the list and `test/e2e/` disagree.</action>
  <acceptance_criteria>
    - Every element defines in the shipped artifact.
    - The session banner names who started it and when it ends; an expired session says so rather than disappearing.
    - No `prompt()` remains on any Phase-8 path.
  </acceptance_criteria>
  <verify><automated>node tools/run-exact-dist-playwright.mjs --grep=phase-8-simulation</automated></verify>
  <done>The surfaces exist in the artifact.</done>
</task>

<task type="auto">
  <name>Task 2: Simulated is a word and a shape</name>
  <files>test/e2e/project-assets.spec.mjs</files>
  <action>Fix T8-09. The whole hazard of this phase is that a successful rehearsal reads as commissioned plant, so this is a safety assertion rather than a styling one. Assert the word and the shape are present with colour removed, and that the provider is stated next to the value.</action>
  <acceptance_criteria>
    - The marking survives with colour stripped.
    - The provider is stated next to the value, not only in a banner.
    - A refusal caused by simulation is worded distinctly from one caused by unreadable state.
  </acceptance_criteria>
  <verify><automated>node tools/run-exact-dist-playwright.mjs --grep=phase-8-simulation</automated></verify>
  <done>T8-09 passes.</done>
</task>

<task type="auto">
  <name>Task 3: Injection structurally, and keyboard reachability</name>
  <files>test/e2e/project-assets.spec.mjs</files>
  <action>Fix T8-23 and T8-24. Assert injection by **structure** — no elements created, no `on*` attributes, no global written — and separately that the operator's text still reaches the reader, because escaping that swallows the name is a second defect rather than a fix. Assert every workflow is reachable by keyboard and every state is announced as text.</action>
  <acceptance_criteria>
    - Structural injection assertions, not substring matching.
    - The operator's text is still rendered.
    - Each workflow completes with keyboard only.
  </acceptance_criteria>
  <verify><automated>node tools/run-exact-dist-playwright.mjs --grep=phase-8-simulation</automated></verify>
  <done>T8-23 and T8-24 pass.</done>
</task>
</tasks>

<output>Create `08-14-SUMMARY.md` when done.</output>
