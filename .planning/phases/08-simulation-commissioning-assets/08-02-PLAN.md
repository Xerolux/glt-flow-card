---
phase: 08-simulation-commissioning-assets
plan: 2
type: execute
wave: 0
depends_on: [1]
files_modified: [custom_components/glt_flow_card/dispatch_vocabulary.py, src/v100/dispatch-vocabulary.mjs, schemas/project/7.schema.json, custom_components/glt_flow_card/project_migrations.py, src/v100/project-migrations.mjs]
autonomous: true
requirements: [SIM-01, DIAG-01, ASSET-01]
must_haves:
  truths:
    - "Dispatch kinds, diagnoses and work-order transitions are closed sets in both runtimes."
    - "Schema 7 closes every field this phase reads, with a sequential 6-7 migration."
    - "An unknown member is refused, never interpreted as its nearest match."
---

## Phase Goal

**As an** engineer, **I want** the phase's vocabulary fixed before the behaviour, **so that** two parts of the product cannot disagree about what a word means.

<objective>Close the dispatch, diagnosis and transition vocabularies, and introduce schema 7.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Three closed vocabularies, mirrored</name>
  <files>custom_components/glt_flow_card/dispatch_vocabulary.py, src/v100/dispatch-vocabulary.mjs</files>
  <action>DISPATCH_KINDS (control, remote_control, schedule_service, notification, audit, report_delivery), DISPATCH_OUTCOMES (dispatch, simulated, refused), DIAGNOSES (present, registered_not_loaded, unregistered, missing, wrong_unit, wrong_device_class, duplicate_binding, stale, service_missing) and WORK_ORDER_STATES with their transitions. Mirrored between runtimes rather than generated from one, with a test comparing them — Phase 6 shipped four disagreeing severity vocabularies and this is how that is prevented rather than regretted.</action>
  <acceptance_criteria>
    - Every set is closed and an unknown member raises with the member named.
    - A parity test compares both runtimes directly.
    - No set is derived from another.
  </acceptance_criteria>
  <verify><automated>node --test test/dispatch-vocabulary.test.mjs</automated></verify>
  <done>One word means one thing.</done>
</task>

<task type="auto">
  <name>Task 2: Schema 7 and the sequential 6-7 migration</name>
  <files>schemas/project/7.schema.json, custom_components/glt_flow_card/project_migrations.py, src/v100/project-migrations.mjs</files>
  <action>Close the scenario, maintenance-plan, work-order and attachment shapes. Migrate the shipped `simulation.enabled` project flag out: it was an authorization living in operator data (D2) and must not survive into schema 7 as a field anything reads. Migrate `work_orders[].id` from its clock-derived form and quarantine what cannot be migrated rather than guessing.</action>
  <acceptance_criteria>
    - Every field this phase reads is declared, with `model` and `plan` required where the phase depends on them.
    - The migration is sequential from 6, receipted, and identical in both runtimes.
    - `simulation.enabled` is removed with a receipt, not silently dropped.
  </acceptance_criteria>
  <verify><automated>node --test test/v100-migrations.test.mjs && py -3.13 -m pytest tests/components/glt_flow_card/test_project_migrations.py -q -x</automated></verify>
  <done>`period: "sometimes"` for assets cannot be written.</done>
</task>
</tasks>

<output>Create `08-02-SUMMARY.md` when done.</output>
