---
phase: 09-multi-site-supervision
plan: 8
type: execute
wave: 3
depends_on: [7]
files_modified: [custom_components/glt_flow_card/__init__.py, tests/components/glt_flow_card/test_remote_authority.py]
autonomous: true
requirements: [SITE-01, OPS-02]
must_haves:
  truths:
    - "A remote control carries the same four outcomes and the same audit as a local one."
    - "A timeout is effect_unknown, never failed."
---

## Phase Goal

**As an** operator, **I want** to know whether a remote command actually moved the plant, **so that** I do not run it twice.

<objective>Give remote controls the evidence local ones have.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Four outcomes and a trusted audit</name>
  <files>custom_components/glt_flow_card/__init__.py</files>
  <action>Fix T9-11. Phase 4 established *accepted*, *sent*, *confirmed* and *failed after dispatch* and a trusted audit for local controls; a remote control produces the remote's JSON on success and a generic `remote_failed` on any failure. `POST /api/services/...` returns the states it changed, which is exactly the readback `confirmed` needs — and the shipped code discards it.</action>
  <acceptance_criteria>
    - All four outcomes are reachable and distinguished.
    - The service response is used as the readback rather than discarded.
    - Every attempt is audited, successful or not.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_remote_authority.py -q -x</automated></verify>
  <done>T9-11 passes.</done>
</task>

<task type="auto">
  <name>Task 2: A timeout is not a failure</name>
  <files>custom_components/glt_flow_card/__init__.py</files>
  <action>Fix T9-12. The distinction between "we do not know whether it happened" and "it did not happen" matters **more** over a network, not less: a timeout on a POST is the canonical case where the service may well have run. Reporting it as failed invites a retry, and a retry after an unknown is how plant gets operated twice.</action>
  <acceptance_criteria>
    - A timed-out remote call reports `effect_unknown`.
    - It is distinct from `failed` in both the result and the audit.
    - Nothing in the surface offers a retry beside an unknown.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_remote_authority.py -q -x</automated></verify>
  <done>T9-12 passes.</done>
</task>
</tasks>

<output>Create `09-08-SUMMARY.md` when done.</output>
