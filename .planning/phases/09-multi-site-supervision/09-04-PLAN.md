---
phase: 09-multi-site-supervision
plan: 4
type: execute
wave: 1
depends_on: [2]
files_modified: [custom_components/glt_flow_card/site_health.py, tests/components/glt_flow_card/test_site_health.py]
autonomous: true
requirements: [SITE-01]
must_haves:
  truths:
    - "Unreachability belongs to the site and is never written as an entity state."
    - "A circuit breaker opens, says it is open, and closes through a bounded probe."
---

## Phase Goal

**As an** operator, **I want** to tell a site that is down from an entity that is unavailable, **so that** I know which problem I have.

<objective>Give a site a health state, and stop absence being written as a value.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Absent is never `unavailable`</name>
  <files>custom_components/glt_flow_card/site_health.py</files>
  <action>Fix T9-13. `result[entity_id] = {"state": "unavailable", "error": resp.status}` writes a **real Home Assistant state** for an entity we could not ask about. The two are different facts and must not produce the same word — Phase 7's whole subject, one network hop out. Unreachability attaches to the site; the entity simply has no reading.</action>
  <acceptance_criteria>
    - A failed read produces no entity state at all.
    - A genuinely unavailable remote entity is still reported as `unavailable`, so the two remain distinguishable in both directions.
    - Freshness and latency travel with every site answer.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_site_health.py -q -x</automated></verify>
  <done>T9-13 passes.</done>
</task>

<task type="auto">
  <name>Task 2: A circuit breaker that states its state</name>
  <files>custom_components/glt_flow_card/site_health.py</files>
  <action>Fix T9-15. A site that is down is retried by every client on every request, so the cost of a dead site grows with the number of people watching. Open, closed and half-open, and the answer says which — a site skipped because its breaker is open has been broken for a while, and a site that was asked and did not answer just failed. Showing them identically hides how long the problem has existed.</action>
  <acceptance_criteria>
    - Repeated failures open the breaker and subsequent requests are not attempted.
    - `circuit_open` is distinct from `unreachable` in the answer.
    - A half-open probe closes the breaker, and the probe itself is bounded.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_site_health.py -q -x</automated></verify>
  <done>T9-15 passes.</done>
</task>
</tasks>

<output>Create `09-04-SUMMARY.md` when done.</output>
