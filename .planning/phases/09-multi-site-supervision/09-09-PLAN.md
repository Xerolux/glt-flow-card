---
phase: 09-multi-site-supervision
plan: 9
type: execute
wave: 3
depends_on: [5]
files_modified: [custom_components/glt_flow_card/site_rollup.py, tests/components/glt_flow_card/test_site_rollup.py]
autonomous: true
requirements: [SITE-01, NAV-01]
must_haves:
  truths:
    - "Every roll-up states which sites answered, which did not, and why."
    - "An aggregate whose completeness is not stated is not shown."
---

## Phase Goal

**As a** person watching one screen instead of five, **I want** a missing site to be visible, **so that** the screen I am watching is not hiding a plant.

<objective>Make partial a first-class answer.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Roll-ups carry their completeness</name>
  <files>custom_components/glt_flow_card/site_rollup.py</files>
  <action>Fix T9-14. A portfolio figure computed while one site was silent is a smaller number presented with the same confidence as a complete one — Phase 7's D16, one hop out. Reuse Phase 7's coverage vocabulary rather than inventing a parallel one: a second notion of "how complete is this" is how two parts of a product start disagreeing.</action>
  <acceptance_criteria>
    - Every aggregate carries answered and absent site lists with reasons.
    - Failing the whole roll-up when one site is down is a test failure, not a pass — four healthy sites made invisible is worse than the missing one.
    - An aggregate with no stated completeness is refused rather than rendered.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_site_rollup.py -q -x</automated></verify>
  <done>T9-14 passes.</done>
</task>
</tasks>

<output>Create `09-09-SUMMARY.md` when done.</output>
