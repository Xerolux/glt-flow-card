---
phase: 09-multi-site-supervision
plan: 7
type: execute
wave: 2
depends_on: [5]
files_modified: [custom_components/glt_flow_card/__init__.py, custom_components/glt_flow_card/policy.py, tests/components/glt_flow_card/policy_contract.py, tests/components/glt_flow_card/test_remote_authority.py]
autonomous: true
requirements: [SITE-01, SEC-01]
must_haves:
  truths:
    - "Every remote route enforces what its local equivalent enforces."
    - "Site access is checked against the caller's projects, not merely a role."
    - "A listing is filtered before it is limited."
---

## Phase Goal

**As an** administrator, **I want** remote access governed by the same rules as local access, **so that** a network hop is not a way around them.

<objective>Make remote a scoped path rather than a second product.</objective>

<tasks>
<task type="auto">
  <name>Task 1: `remote/states` enforces something</name>
  <files>custom_components/glt_flow_card/__init__.py</files>
  <action>Fix T9-08. `ws_remote_states` has no role check, no capability check and no project scoping: any caller who can reach the websocket reads any entity of any configured site.</action>
  <acceptance_criteria>
    - An unauthorized caller is refused, opaquely.
    - The refusal does not distinguish "no such site" from "not allowed".
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_remote_authority.py -q -x</automated></verify>
  <done>T9-08 passes.</done>
</task>

<task type="auto">
  <name>Task 2: A site belongs to projects</name>
  <files>custom_components/glt_flow_card/__init__.py</files>
  <action>Fix T9-09. `remote_control` checks the service domain — which is right and is the only check. The caller's `project_id` drives a role check but is never checked against the *site*, so an operator on project A can operate site B.</action>
  <acceptance_criteria>
    - An operator authorized on one project cannot reach a site bound to another.
    - The site-to-project binding is server-owned configuration.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_remote_authority.py -q -x</automated></verify>
  <done>T9-09 passes.</done>
</task>

<task type="auto">
  <name>Task 3: Filter, then limit</name>
  <files>custom_components/glt_flow_card/__init__.py</files>
  <action>Fix T9-10. `remote/list` returns every configured site to any caller. Filter to what the caller may see and apply the limit **after** filtering — Phase 6 established this for `alarms/list` and Phase 7 for `history/series`, because limiting first turns the limit into a count oracle for rows the caller may not see.</action>
  <acceptance_criteria>
    - A caller sees only their sites.
    - The limit is applied after filtering, asserted by a caller whose sites are outnumbered by others'.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_remote_authority.py -q -x</automated></verify>
  <done>T9-10 passes.</done>
</task>
</tasks>

<output>Create `09-07-SUMMARY.md` when done.</output>
