---
phase: 09-multi-site-supervision
plan: 5
type: execute
wave: 2
depends_on: [3, 4]
files_modified: [custom_components/glt_flow_card/remote_fanout.py, custom_components/glt_flow_card/__init__.py, tests/components/glt_flow_card/test_remote_fanout.py]
autonomous: true
requirements: [SITE-01]
must_haves:
  truths:
    - "One request per site, not one per entity."
    - "The total deadline belongs to the request and is not divisible among sites."
    - "A truncated answer says it was truncated."
---

## Phase Goal

**As an** operator with five sites, **I want** the view to load, **so that** one unresponsive site does not make the product unusable.

<objective>Close the phase's headline performance and availability defect.</objective>

<tasks>
<task type="auto">
  <name>Task 1: One request per site</name>
  <files>custom_components/glt_flow_card/remote_fanout.py</files>
  <action>Fix T9-01. The shipped read is `for entity_id in entity_ids[:200]` with an awaited request and a 15-second timeout inside — two hundred entities against one unresponsive site is fifty minutes inside a websocket handler. The roadmap forbids working around it, and it is right to: a handler holding for fifty minutes is an availability defect, and the obvious mitigations make the answer *more* incomplete rather than faster. Home Assistant serves `/api/states` in one request; filter locally.</action>
  <acceptance_criteria>
    - The fixture records one request per site regardless of entity count.
    - Sites are asked concurrently up to a declared bound.
    - The bound is a configured number, not a literal.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_remote_fanout.py -q -x</automated></verify>
  <done>T9-01 passes.</done>
</task>

<task type="auto">
  <name>Task 2: A total deadline owned by the request</name>
  <files>custom_components/glt_flow_card/remote_fanout.py</files>
  <action>Fix T9-02. Bounded concurrency alone still lets *n* sites times a timeout accumulate. The deadline belongs to the request and is not divisible among sites. A request that hits it returns what it has, with the rest stated absent — never an error, because failing the whole roll-up makes four healthy sites invisible, which is worse than the missing one.</action>
  <acceptance_criteria>
    - With one site hanging, the request returns within the deadline.
    - The hanging site is reported absent with a reason, and the others carry their values.
    - Latency is injected rather than slept, so the test runs in milliseconds.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_remote_fanout.py -q -x</automated></verify>
  <done>T9-02 passes.</done>
</task>

<task type="auto">
  <name>Task 3: Truncation says so</name>
  <files>custom_components/glt_flow_card/remote_fanout.py</files>
  <action>Fix T9-17. `entity_ids[:200]` silently drops the rest — the same shape as Phase 7's row cap before it was made to state itself, and Phase 8's suggestion list before the same fix.</action>
  <acceptance_criteria>
    - A request beyond the limit reports the limit and that it truncated.
    - A request within it does not claim truncation.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_remote_fanout.py -q -x</automated></verify>
  <done>T9-17 passes.</done>
</task>
</tasks>

<output>Create `09-05-SUMMARY.md` when done.</output>
