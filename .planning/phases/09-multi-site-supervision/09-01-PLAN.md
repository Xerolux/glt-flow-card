---
phase: 09-multi-site-supervision
plan: 1
type: execute
wave: 0
depends_on: []
files_modified: [tools/verify-phase9.mjs, tests/components/glt_flow_card/site_factory.py, tests/components/glt_flow_card/fixtures/site_corpus.json]
autonomous: true
requirements: [SITE-01]
must_haves:
  truths:
    - "The gate proves one acyclic path to test:phase9:release and runs the Phase-8 gate."
    - "A test that opens a real socket fails, and says which site it reached for."
    - "The corpus is committed before anything reads it."
---

## Phase Goal

**As an** engineer, **I want** the phase's evidence machinery standing before any behaviour, **so that** a claim about failure isolation is a command someone can run.

<objective>Stand up the Phase-9 gate, the socket ledger and the site corpus.</objective>

<tasks>
<task type="auto">
  <name>Task 1: The Phase-9 gate</name>
  <files>tools/verify-phase9.mjs</files>
  <action>Derive everything from `PHASE`, bind requirements, roadmap truths, plans, threats and assumptions to evidence, and run the Phase-8 gate as F9-06.</action>
  <acceptance_criteria>
    - The graph is acyclic and reaches the release leaf exactly once.
    - No literal names the phase where a derivation would do.
    - The Phase-8 gate is mandatory.
  </acceptance_criteria>
  <verify><automated>npm run test:phase9</automated></verify>
  <done>The gate exists and fails honestly.</done>
</task>

<task type="auto">
  <name>Task 2: The socket ledger and site fixtures</name>
  <files>tests/components/glt_flow_card/site_factory.py</files>
  <action>A fixture transport that records requests and never opens a socket, raising `LiveSocketReached` if one is attempted. It must be able to simulate: a fast site, a slow site, a site that times out, a site that refuses the connection, and a site that returns malformed JSON. Latency is simulated by an injected clock rather than by sleeping, so a test of a total deadline takes milliseconds — a suite that sleeps to prove a timeout is a suite nobody runs.</action>
  <acceptance_criteria>
    - A real socket attempt raises rather than being recorded.
    - Latency is injected, not slept.
    - The ledger counts `socket`, `remote`, `service` and `network`.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_remote_fanout.py -q -x</automated></verify>
  <done>A test cannot reach the network without failing.</done>
</task>

<task type="auto">
  <name>Task 3: The site corpus</name>
  <files>tests/components/glt_flow_card/fixtures/site_corpus.json</files>
  <action>Committed before anything reads it. Must contain the destinations that matter: an allowlisted public host, a host not on the allowlist, a host that resolves to loopback, one that resolves to 169.254.169.254, one to a private range, one to a unique-local IPv6 address, and one whose resolution *changes* between validation and connection.</action>
  <acceptance_criteria>
    - The metadata address appears by name.
    - The rebinding case exists, since it is what makes the second check necessary.
    - No entry contains a real credential.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_site_destinations.py -q -x</automated></verify>
  <done>The evidence exists before the claims.</done>
</task>
</tasks>

<output>Create `09-01-SUMMARY.md` when done.</output>
