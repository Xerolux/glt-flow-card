---
phase: 09-multi-site-supervision
plan: 3
type: execute
wave: 1
depends_on: [2]
files_modified: [custom_components/glt_flow_card/site_destinations.py, tests/components/glt_flow_card/test_site_destinations.py]
autonomous: true
requirements: [SITE-01]
must_haves:
  truths:
    - "A destination is on a server-owned allowlist or the request is not made."
    - "The resolved address is checked at connection time, not only at validation time."
    - "Disabled certificate verification is declared, recorded and stated."
---

## Phase Goal

**As an** installation owner, **I want** the Companion to connect only where I said, **so that** a configuration field is not a request-forgery primitive.

<objective>Close the SSRF surface.</objective>

<tasks>
<task type="auto">
  <name>Task 1: A server-owned allowlist</name>
  <files>custom_components/glt_flow_card/site_destinations.py</files>
  <action>Fix T9-03. `configure_remote_sites` accepts any `url`, and the Companion then makes an authenticated request to it and returns the body to the browser. The allowlist is **site configuration**, never project data — the third time this rule appears, after Phase 6's notification targets and Phase 8's simulation gate, which makes it the product's security model rather than a precaution.</action>
  <acceptance_criteria>
    - A host not on the allowlist is refused before any connection is attempted.
    - The refusal reason is from the closed set and names no internal detail.
    - A project document cannot add a destination.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_site_destinations.py -q -x</automated></verify>
  <done>T9-03 passes.</done>
</task>

<task type="auto">
  <name>Task 2: Check the address, not the name</name>
  <files>custom_components/glt_flow_card/site_destinations.py</files>
  <action>Fix T9-04. An allowlist alone does not hold: a name that resolves publicly at validation time may resolve to 127.0.0.1 at connection time, which is DNS rebinding. Check the **resolved address** against loopback, link-local, private and unique-local ranges. 169.254.169.254 is named in the test because it is the cloud metadata address and it is why this is not theoretical.</action>
  <acceptance_criteria>
    - Every corpus address class is refused, the metadata address by name.
    - The rebinding case — resolution changing between validation and connection — is refused at connection time.
    - A legitimate public address is allowed, so the check is not simply "refuse everything".
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_site_destinations.py -q -x</automated></verify>
  <done>T9-04 passes.</done>
</task>

<task type="auto">
  <name>Task 3: Unverified TLS is declared and visible</name>
  <files>custom_components/glt_flow_card/site_destinations.py</files>
  <action>Fix T9-05. `verify_ssl: false` currently produces no warning, no audit entry and no indication in the UI. Require it to be explicitly declared, record it, and carry it into every answer that site produces so an operator can see which figures came over an unauthenticated channel.</action>
  <acceptance_criteria>
    - Verification off requires an explicit declaration and is recorded.
    - Every answer from such a site states it.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_site_destinations.py -q -x</automated></verify>
  <done>T9-05 passes.</done>
</task>
</tasks>

<output>Create `09-03-SUMMARY.md` when done.</output>
