---
phase: 09-multi-site-supervision
plan: 10
type: execute
wave: 3
depends_on: [4]
files_modified: [custom_components/glt_flow_card/site_subscriptions.py, tests/components/glt_flow_card/test_site_subscriptions.py]
autonomous: true
requirements: [SITE-01]
must_haves:
  truths:
    - "Subscriptions are bounded per site and in total."
    - "A subscription names its entities rather than taking every event."
---

## Phase Goal

**As an** installation owner, **I want** a supervision view not to become a load generator, **so that** watching a site does not degrade it.

<objective>Bound the held resources.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Named entities, bounded counts</name>
  <files>custom_components/glt_flow_card/site_subscriptions.py</files>
  <action>Fix T9-16. `subscribe_events` with `state_changed` delivers **every** state change on the remote instance — for twenty watched entities on a site with two thousand, a hundredfold amplification. `subscribe_entities` takes an entity list and delivers compressed deltas, which is what makes a bound meaningful rather than nominal. A subscription is a *held* resource, so bound them per site and in total.</action>
  <acceptance_criteria>
    - A subscription names its entities.
    - Both bounds are enforced and a request past either is refused with the limit named.
    - Releasing a subscription frees its slot, asserted rather than assumed.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_site_subscriptions.py -q -x</automated></verify>
  <done>T9-16 passes.</done>
</task>
</tasks>

<output>Create `09-10-SUMMARY.md` when done.</output>
