---
phase: 09-multi-site-supervision
plan: 2
type: execute
wave: 0
depends_on: [1]
files_modified: [custom_components/glt_flow_card/site_vocabulary.py, src/v100/site-vocabulary.mjs]
autonomous: true
requirements: [SITE-01]
must_haves:
  truths:
    - "Site health states, failure reasons and outcomes are closed sets in both runtimes."
    - "`unreachable` is a site state and never an entity state."
---

## Phase Goal

**As an** engineer, **I want** one vocabulary for what a site can be, **so that** two parts of the product cannot disagree about whether a site answered.

<objective>Close the site vocabulary in both runtimes.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Site states, failure reasons, outcomes</name>
  <files>custom_components/glt_flow_card/site_vocabulary.py, src/v100/site-vocabulary.mjs</files>
  <action>SITE_STATES (healthy, slow, unreachable, circuit_open), REMOTE_FAILURES (a closed set replacing `str(err)`), and the four command outcomes reused from Phase 4 rather than redefined. Mirrored between runtimes and compared as canonical bytes — the byte trap has caught this codebase four times, so the canonical form comes first rather than after.</action>
  <acceptance_criteria>
    - `unreachable` is declared as a site state and a test asserts it is never used as an entity state.
    - The failure set contains no free-text member.
    - Both runtimes compare byte-identically.
  </acceptance_criteria>
  <verify><automated>node --test test/site-vocabulary.test.mjs</automated></verify>
  <done>One word means one thing.</done>
</task>
</tasks>

<output>Create `09-02-SUMMARY.md` when done.</output>
