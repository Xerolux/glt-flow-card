---
phase: 09-multi-site-supervision
plan: 11
type: execute
wave: 4
depends_on: [9, 10]
files_modified: [src/v100/project-sites.js, src/v100/entry.js, test/e2e/project-sites.spec.mjs, tools/run-exact-dist-playwright.mjs]
autonomous: true
requirements: [SITE-01, A11Y-01]
must_haves:
  truths:
    - "A view missing a site says so, in the view."
    - "Every remote value carries its age and its site's health, as text and shape."
    - "Nothing offers a retry beside an unknown."
---

## Phase Goal

**As an** operator on a monochrome kiosk, **I want** to see that a site is missing, **so that** the screen replacing five screens is not hiding one.

<objective>Ship the surfaces to `09-UI-SPEC.md`.</objective>

<tasks>
<task type="auto">
  <name>Task 1: The surfaces</name>
  <files>src/v100/project-sites.js, src/v100/entry.js</files>
  <action>Site health badge, portfolio roll-up with stated completeness, remote value with age, and a refusal that distinguishes the four site states. Register the spec in the exact-dist runner's list, which refuses when the list and `test/e2e/` disagree.</action>
  <acceptance_criteria>
    - Every element defines in the shipped artifact.
    - The four site states are four distinct words.
    - `circuit_open` and `unreachable` are visibly different, since one has been broken a while and the other just failed.
  </acceptance_criteria>
  <verify><automated>node tools/run-exact-dist-playwright.mjs --grep=phase-9-sites</automated></verify>
  <done>The surfaces exist in the artifact.</done>
</task>

<task type="auto">
  <name>Task 2: Age and health, with colour removed</name>
  <files>test/e2e/project-sites.spec.mjs</files>
  <action>Fix T9-18. A value read an hour ago from a site unreachable since reads exactly like a current one. Assert age and health survive with colour stripped, and that a roll-up missing a site states it in the view rather than behind a hover.</action>
  <acceptance_criteria>
    - Age and health render with colour removed.
    - A partial roll-up names its absent sites.
    - No retry is offered beside `effect_unknown`.
  </acceptance_criteria>
  <verify><automated>node tools/run-exact-dist-playwright.mjs --grep=phase-9-sites</automated></verify>
  <done>T9-18 passes.</done>
</task>

<task type="auto">
  <name>Task 3: Remote text is text</name>
  <files>test/e2e/project-sites.spec.mjs</files>
  <action>Fix T9-19. A site name and a remote entity's attributes are authored somewhere this installation does not control, which makes them the most hostile input the product handles. Assert structurally, and assert the text still reaches the reader.</action>
  <acceptance_criteria>
    - No elements created, no `on*` attributes, no global written.
    - The remote name is still rendered.
  </acceptance_criteria>
  <verify><automated>node tools/run-exact-dist-playwright.mjs --grep=phase-9-sites</automated></verify>
  <done>T9-19 passes.</done>
</task>
</tasks>

<output>Create `09-11-SUMMARY.md` when done.</output>
