---
phase: 09-multi-site-supervision
plan: 12
type: execute
wave: 5
depends_on: [11]
files_modified: [docs/wiki/Multi-Site.md, .planning/phases/09-multi-site-supervision/09-THREATS.md, .planning/STATE.md, .planning/ROADMAP.md]
autonomous: true
requirements: [SITE-01]
must_haves:
  truths:
    - "Every threat row is marked from its own owner command, run at head."
    - "The documentation states what the product does not have."
    - "A blocked command is recorded with its exact failure."
---

## Phase Goal

**As an** engineer, **I want** the phase closed against evidence, **so that** the register means what it says.

<objective>Document the contract and close the phase.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Document it, including what it is not</name>
  <files>docs/wiki/Multi-Site.md</files>
  <action>`Multi-Site.md` already exists and describes the shipped behaviour, so it is rewritten. State the allowlist and why a destination is site configuration; that partial is a first-class answer and why failing the whole roll-up would be worse; that a timeout is not a failure; and that there is **no remote engineering** — reading state and operating authorized controls, not editing a remote project.</action>
  <acceptance_criteria>
    - The SSRF rule and its two halves are explicit.
    - "Partial is an answer, not an error" is explicit.
    - `node tools/verify-docs-site.mjs` passes.
  </acceptance_criteria>
  <verify><automated>node tools/verify-docs-site.mjs</automated></verify>
  <done>The contract is written down.</done>
</task>

<task type="auto">
  <name>Task 2: Close the register from commands</name>
  <files>.planning/phases/09-multi-site-supervision/09-THREATS.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <action>Run each threat's owner command at head, mark each row only from its own run, and run shared commands per row. Record a blocked command with its exact failure output rather than its likely cause.</action>
  <acceptance_criteria>
    - Every marked row has its own at-head run.
    - Blocked rows carry exact failure output.
    - The summary states the limitations, including anything raised rather than taken.
  </acceptance_criteria>
  <verify><automated>npm run test:phase9</automated></verify>
  <done>The phase is closed against evidence.</done>
</task>
</tasks>

<output>Create `09-12-SUMMARY.md` and `09-SUMMARY.md` when done.</output>
