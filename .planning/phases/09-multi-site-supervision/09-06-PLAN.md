---
phase: 09-multi-site-supervision
plan: 6
type: execute
wave: 2
depends_on: [5]
files_modified: [custom_components/glt_flow_card/remote_fanout.py, custom_components/glt_flow_card/__init__.py, tests/components/glt_flow_card/test_remote_failures.py]
autonomous: true
requirements: [SITE-01]
must_haves:
  truths:
    - "A remote failure is one of a closed set of reasons."
    - "No credential appears in any response, log, export or error."
---

## Phase Goal

**As an** installation owner, **I want** a failure to tell the operator what to do without telling everyone what my network looks like.

<objective>Stop failures leaking, and prove credentials do not.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Failures are a closed set</name>
  <files>custom_components/glt_flow_card/remote_fanout.py</files>
  <action>Fix T9-06. `connection.send_error(msg["id"], "remote_failed", str(err))` returns the exception, and `aiohttp` connection errors carry the host and port they failed to reach — so a caller can enumerate internal hostnames by triggering failures. Report a declared reason and log the exception server-side. Same rule as Phase 8's dispatch refusals, and for the same second reason: an error string is an interface, and one that varies with a library version is not one.</action>
  <acceptance_criteria>
    - Every failure path returns a member of the closed set.
    - No returned message contains a host, port, path or token.
    - The exception is still available server-side.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_remote_failures.py -q -x</automated></verify>
  <done>T9-06 passes.</done>
</task>

<task type="auto">
  <name>Task 2: The credential appears nowhere</name>
  <files>tests/components/glt_flow_card/test_remote_failures.py</files>
  <action>Fix T9-07. Assert it directly: seed a token with a distinctive sentinel value, exercise every remote path including every failure branch, and assert the sentinel appears in no response, no audit row, no export and no error. This is the Phase-2 seeded-secret pattern applied one hop out — a claim that a secret does not leak is worth exactly as much as the search that looked for it.</action>
  <acceptance_criteria>
    - The sentinel is searched for across every output of every remote path.
    - Failure branches are exercised, not only success.
    - The test fails if the sentinel is added to any response.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_remote_failures.py -q -x</automated></verify>
  <done>T9-07 passes.</done>
</task>
</tasks>

<output>Create `09-06-SUMMARY.md` when done.</output>
