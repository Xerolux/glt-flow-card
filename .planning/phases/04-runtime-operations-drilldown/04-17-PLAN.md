---
phase: 04-runtime-operations-drilldown
plan: 17
type: execute
wave: 12
depends_on: [04-01, 04-02, 04-03, 04-04, 04-05, 04-06, 04-07, 04-08, 04-09, 04-10, 04-11, 04-12, 04-13, 04-14, 04-15, 04-16]
files_modified:
  - tools/verify-phase4.mjs
  - test/phase4-gate.test.mjs
  - .planning/phases/04-runtime-operations-drilldown/04-THREATS.md
  - .planning/phases/04-runtime-operations-drilldown/04-VALIDATION.md
autonomous: true
requirements: [OPS-02, NAV-01]
must_haves:
  truths:
    - "The gate binds every requirement, roadmap truth, plan, threat and assumption to current evidence."
    - "The command graph is acyclic with exactly one path to the release leaf."
    - "A threat row is marked verified only after its owner command has actually run and passed."
---

## Phase Goal

**As a** maintainer, **I want** one command that proves Phase 4 is done, **so that** closure is an evidence check rather than a judgement call.

<objective>Build the Phase-4 gate and close the phase honestly.</objective>

<tasks>
<task type="auto">
  <name>Task 1: The orchestrator</name>
  <files>tools/verify-phase4.mjs</files>
  <action>Follow the Phase-2 and Phase-3 orchestrators. Bind OPS-02 and NAV-01, the five roadmap truths, all seventeen plans, T4-01..T4-14 and the six resolved assumptions A1-A6 from 04-RESEARCH to current evidence. Execute each unique command once. Prove the command graph is acyclic with exactly one path to the `test:phase4:release` leaf owned by T4-14, using the same source/script edge distinction so advice inside an error message is not read as an edge.</action>
  <acceptance_criteria>
    - The graph check returns exactly one path to the leaf.
    - Every plan, threat, requirement and assumption resolves to a command that exists.
    - No command reachable from the leaf re-invokes the gate.
  </acceptance_criteria>
  <verify><automated>node tools/verify-phase4.mjs</automated></verify>
  <done>The plan loads and the graph is proven acyclic.</done>
</task>

<task type="auto">
  <name>Task 2: Gate mutation tests</name>
  <files>test/phase4-gate.test.mjs</files>
  <action>Mutate the gate the way the Phase-3 gate tests do: an unbound requirement, an unowned threat, a missing plan, a duplicated command, an introduced cycle, a second path to the leaf, and a threat marked verified without evidence. Each mutation must fail the gate.</action>
  <acceptance_criteria>
    - Every mutation fails; none passes silently.
    - A threat row marked verified with no passing owner command is rejected by the gate itself.
  </acceptance_criteria>
  <verify><automated>node --test test/phase4-gate.test.mjs</automated></verify>
  <done>The gate cannot be satisfied by a document edit alone.</done>
</task>

<task type="auto">
  <name>Task 3: Close the phase honestly</name>
  <files>.planning/phases/04-runtime-operations-drilldown/04-THREATS.md, .planning/phases/04-runtime-operations-drilldown/04-VALIDATION.md</files>
  <action>Run every runnable owner command at head. Mark only the rows whose owner actually passed. Leave T4-14 `planned` if Docker is unavailable and write the Evidence Status note explaining exactly why, as Phases 2 and 3 did. Write 04-SUMMARY.md and the phase's .continue-here.md.</action>
  <acceptance_criteria>
    - No row is marked verified on the strength of its parts passing separately.
    - The Evidence Status note names the environment limit rather than implying a defect.
    - STATE.md, HANDOFF.json and HANDOFF.md are updated in the same commit.
  </acceptance_criteria>
  <verify><automated>node tools/phase4-red-gate.mjs && node tools/verify-phase4.mjs</automated></verify>
  <done>Phase 4 is closed with an honest evidence record.</done>
</task>
</tasks>

<output>Create `04-17-SUMMARY.md` when done.</output>
