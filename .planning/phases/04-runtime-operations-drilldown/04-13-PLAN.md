---
phase: 04-runtime-operations-drilldown
plan: 13
type: execute
wave: 8
depends_on: [04-10, 04-11, 04-12]
files_modified:
  - src/v100/project-operations.js
  - src/v100/entry.js
  - src/v100/project-safety-i18n.mjs
  - src/v040-extension.part02
  - src/v040-extension.part06
autonomous: true
requirements: [OPS-02, NAV-01]
must_haves:
  truths:
    - "No Phase-4 surface can reach hass.callService or hass.callApi."
    - "The legacy browser permission check and direct service fallback are retired and provably inert."
    - "Every new string exists in German and English."
---

## Phase Goal

**As an** operator, **I want** the runtime surfaces in the shipped card, **so that** the workflow exists where I actually use it.

<objective>Ship the five operations elements and retire the legacy operate path.</objective>

<tasks>
<task type="auto">
  <name>Task 1: The operations surfaces</name>
  <files>src/v100/project-operations.js, src/v100/entry.js, src/v100/project-safety-i18n.mjs</files>
  <action>Define `glt-flow-card-object-panel`, `glt-flow-card-breadcrumbs`, `glt-flow-card-drilldown-list`, `glt-flow-card-outcome-strip` and `glt-flow-card-view-staleness` against the reducers, reusing the Phase-2 and Phase-3 elements unchanged. Wire the module into `entry.js`. Add every new string to the existing DE/EN catalog; a missing key is a build failure.</action>
  <acceptance_criteria>
    - The five elements appear in the generated artifact.
    - No surface constructs a control, a permission or a count of its own.
    - Both languages are complete.
  </acceptance_criteria>
  <verify><automated>npm run build && node --test test/exact-dist.test.mjs</automated></verify>
  <done>The surfaces ship in dist/glt-flow-card.js.</done>
</task>

<task type="auto">
  <name>Task 2: Retire the legacy operate path</name>
  <files>src/v040-extension.part02, src/v040-extension.part06</files>
  <action>Retire `canOperate` in both files and the `call-service` / `perform-action` branches of `runTap`, the way Phase 2 retired `glt_flow_card/control/execute`: the path stays reachable by a test and produces zero effects. Retarget the `navigate` branch to the new address form. Replace `confirmControl`'s window.confirm with the Phase-2 control-confirm element.</action>
  <acceptance_criteria>
    - No tap action reaches hass.callService, proven by the effect ledger rather than by source inspection.
    - `canOperate` no longer returns true merely because no permission list is configured.
    - The `navigate` action still works, against the new address form.
    - No window.confirm or alert remains on an authorization path.
  </acceptance_criteria>
  <verify><automated>node tools/run-exact-dist-playwright.mjs --grep=phase-4-legacy-retired</automated></verify>
  <done>T4-11 is GREEN.</done>
</task>
</tasks>

<output>Create `04-13-SUMMARY.md` when done.</output>
