---
phase: 04-runtime-operations-drilldown
plan: 6
type: execute
wave: 2
depends_on: [04-02]
files_modified:
  - custom_components/glt_flow_card/view_stream.py
  - custom_components/glt_flow_card/policy.py
  - custom_components/glt_flow_card/policy_sessions.py
  - custom_components/glt_flow_card/__init__.py
autonomous: true
requirements: [OPS-02]
must_haves:
  truths:
    - "A snapshot and its sequence are read in one critical section."
    - "Every streamed event carries a monotonic sequence a client can check."
    - "Snapshot concurrency and resync rate are bounded, and exceeding them answers rate_limited."
---

## Phase Goal

**As an** operator, **I want** an incremental view that tells me when it has lost track, **so that** I never read stale numbers as live ones.

<objective>Give the browser a resumable, bounded, authority-checked view stream.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Snapshot and stream</name>
  <files>custom_components/glt_flow_card/view_stream.py, custom_components/glt_flow_card/policy_sessions.py</files>
  <action>Return {snapshot, sequence} from one critical section over the SubscriptionRegistry's counter, then stream events each carrying `_next_sequence()`. Re-authorize per emission using the registry's existing hook. Bind the subscription to the runtime generation so a post-unload callback is inert. Add a snapshot rate class and a per-subscription in-flight limit.</action>
  <acceptance_criteria>
    - An event emitted between the snapshot read and its stamp is structurally impossible.
    - A client that exceeds the snapshot rate receives `rate_limited` and the stream stays open.
    - Concurrent snapshots per subscription are capped at one.
    - A revoked principal stops receiving events at the next emission, not at the next snapshot.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_view_stream.py -q -x</automated></verify>
  <done>T4-10 is GREEN and T4-09's server half is in place.</done>
</task>

<task type="auto">
  <name>Task 2: Declare and register the route</name>
  <files>custom_components/glt_flow_card/policy.py, custom_components/glt_flow_card/__init__.py, tests/components/glt_flow_card/policy_contract.py</files>
  <action>Declare `glt_flow_card/views/subscribe` in both policy tables, project-scoped with `project.read`, registering the unsubscribe callback under the message id per the Home Assistant contract, and move the route count (39 → 40). Honour MAX_SUBSCRIPTIONS_PER_CONNECTION rather than introducing a second limit.</action>
  <acceptance_criteria>
    - The registration oracle passes.
    - Teardown removes every subscription and the resource ledger reaches zero.
    - A ninth subscription on one connection is refused.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_policy.py tests/components/glt_flow_card/test_init.py -q -x</automated></verify>
  <done>The stream route is declared, registered and bounded.</done>
</task>
</tasks>

<output>Create `04-06-SUMMARY.md` when done.</output>
