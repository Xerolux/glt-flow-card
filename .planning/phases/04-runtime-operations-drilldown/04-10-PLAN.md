---
phase: 04-runtime-operations-drilldown
plan: 10
type: execute
wave: 5
depends_on: [04-04, 04-05]
files_modified:
  - src/v100/panel-model.mjs
autonomous: true
requirements: [OPS-02]
must_haves:
  truths:
    - "The browser renders the regions it is given, in the order it is given them."
    - "The browser derives no role, capability or control list."
    - "A region the server did not send is not rendered."
---

## Phase Goal

**As an** operator, **I want** the panel to show exactly what the server says I may do, **so that** nothing I can click is something the server would refuse.

<objective>Render the server-composed panel without adding browser authority.</objective>

<tasks>
<task type="auto">
  <name>Task 1: The panel reducer</name>
  <files>src/v100/panel-model.mjs</files>
  <action>Reduce a panel-model response into render state: ordered regions, the Phase-3 state badge, the provenance summary, the values and runtime regions, the alarms region, the controls region and the trend region in its declared history_unavailable state. Reject an unknown region kind. Render the declared empty text for an empty region and render nothing for an absent one.</action>
  <acceptance_criteria>
    - A response with an unknown region kind is rejected, not partially rendered.
    - No code path constructs a control entry the response did not contain.
    - An absent region and an empty region render differently and both are declared.
  </acceptance_criteria>
  <verify><automated>node --test test/panel-model.test.mjs</automated></verify>
  <done>The panel reducer matches the 04-UI-SPEC region table.</done>
</task>
</tasks>

<output>Create `04-10-SUMMARY.md` when done.</output>
