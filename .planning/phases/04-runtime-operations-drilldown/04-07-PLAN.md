---
phase: 04-runtime-operations-drilldown
plan: 7
type: execute
wave: 3
depends_on: [04-03, 04-05]
files_modified:
  - custom_components/glt_flow_card/navigation.py
  - custom_components/glt_flow_card/policy.py
  - custom_components/glt_flow_card/__init__.py
autonomous: true
requirements: [NAV-01]
must_haves:
  truths:
    - "Every resolve re-authorizes from scratch."
    - "An unauthorized address and a non-existent one answer byte-identically."
    - "Address depth and length are bounded before any tree walk."
---

## Phase Goal

**As a** user, **I want** a link I was sent to open only what I am allowed to see, **so that** a shared URL never becomes a way around permissions.

<objective>Turn address resolution GREEN with no enumeration oracle.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Bounded address resolution</name>
  <files>custom_components/glt_flow_card/navigation.py</files>
  <action>Validate a caller-supplied address for length and depth before parsing, re-asserting the Phase-3 max_depth of 32, then resolve it against the Phase-3 hierarchy under a fresh authorization. Return the node, its authorized children and its ancestry for breadcrumbs. Answer `not_found_or_denied` for an unauthorized target, a missing target and a deferred remote address, with identical bodies.</action>
  <acceptance_criteria>
    - An overlong or over-deep address is rejected before any tree walk.
    - The three denial cases produce equal responses under a full-response comparison.
    - An ancestor the caller cannot open appears in the ancestry as a non-linkable entry with no indication a link was withheld.
    - Resolution never consults a cached authorization.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_navigation.py -q -x</automated></verify>
  <done>T4-03 and T4-05 are GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Declare and register the route</name>
  <files>custom_components/glt_flow_card/policy.py, custom_components/glt_flow_card/__init__.py, tests/components/glt_flow_card/policy_contract.py</files>
  <action>Declare `glt_flow_card/navigation/resolve` in both policy tables, project-scoped with `project.read` and opaque enumeration, register the handler and move the route count (40 → 41).</action>
  <acceptance_criteria>
    - The registration oracle passes with declared and registered sets equal.
    - The route appears in the policy matrix probe with a stable error code.
    - A remote address is refused identically to a missing one.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_policy.py tests/components/glt_flow_card/test_init.py -q -x</automated></verify>
  <done>The final Phase-4 route is declared and registered; the count is 41.</done>
</task>
</tasks>

<output>Create `04-07-SUMMARY.md` when done.</output>
