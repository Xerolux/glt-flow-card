---
phase: 04-runtime-operations-drilldown
plan: 4
type: execute
wave: 1
depends_on: [04-01]
files_modified:
  - test/navigation.test.mjs
  - test/panel-model.test.mjs
  - test/command-outcome.test.mjs
  - test/view-resync.test.mjs
  - test/e2e/project-operations.spec.mjs
autonomous: true
requirements: [OPS-02, NAV-01]
must_haves:
  truths:
    - "Only readback_confirmed may render as success."
    - "A sequence gap marks the view stale in the same transition."
    - "The exact generated artifact is what the UI sentinel loads."
---

## Phase Goal

**As a** maintainer, **I want** the browser guarantees specified as failing tests, **so that** the surfaces are written against a contract.

<objective>Write the four browser RED sentinels.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Panel, navigation, outcome and resync reducers</name>
  <files>test/panel-model.test.mjs, test/navigation.test.mjs, test/command-outcome.test.mjs, test/view-resync.test.mjs</files>
  <action>Specify, as failing tests: a panel response reducing to the ordered regions of the 04-UI-SPEC table, with an unknown region kind rejected and an absent region rendering differently from an empty one; address round-trip over a corpus including hostile inputs; time and alarm context surviving every transition; popstate re-resolving rather than replaying; the nine control result states rendering distinctly with only readback_confirmed as success and no retry affordance for an unknown-effect state; and gap/reconnect/revocation each producing stale in one transition with operator input preserved.</action>
  <acceptance_criteria>
    - Each file fails exactly once with its marker.
    - The outcome test enumerates all nine states from CONTROL_RESULT_STATES rather than a chosen subset.
    - The resync test asserts no interpolation occurs.
  </acceptance_criteria>
  <verify><automated>node tools/phase4-red-gate.mjs</automated></verify>
  <done>phase4-panel-model, phase4-navigation-reducer, phase4-command-outcome and phase4-view-resync report controlled RED.</done>
</task>

<task type="auto">
  <name>Task 2: The exact-dist UI sentinel</name>
  <files>test/e2e/project-operations.spec.mjs</files>
  <action>Specify, as failing tests against the exact generated artifact, the full 04-VALIDATION exact-dist matrix: four layouts, two languages, the named scenarios, plus the cross-cutting focus, non-color, 200%, forced-colors, live-region and zero-effect assertions. Include the `phase-4-legacy-retired` and `phase-4-outcome` scenarios.</action>
  <acceptance_criteria>
    - The spec loads dist/glt-flow-card.js, not a source module.
    - The kiosk scenario is one continuous keyboard traversal, not per-element assertions.
    - The file fails exactly once.
  </acceptance_criteria>
  <verify><automated>node tools/phase4-red-gate.mjs</automated></verify>
  <done>phase4-ui reports controlled RED; all ten sentinels are now RED.</done>
</task>
</tasks>

<output>Create `04-04-SUMMARY.md` when done.</output>
