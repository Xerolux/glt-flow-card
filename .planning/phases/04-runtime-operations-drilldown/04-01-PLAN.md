---
phase: 04-runtime-operations-drilldown
plan: 1
type: execute
wave: 0
depends_on: []
files_modified:
  - tools/phase4-red-gate.mjs
  - tools/assert-red.mjs
  - tests/components/glt_flow_card/panel_factory.py
  - test/fixtures/operations/site.project.json
  - tools/exact-dist-effect-ledger.mjs
  - package.json
autonomous: true
requirements: [OPS-02, NAV-01]
must_haves:
  truths:
    - "A Phase-4 sentinel is accepted only with its named marker and a matching effect ledger."
    - "The operations corpus contains at least one object each principal may see differently."
    - "The effect ledger fails on any callService or callApi from a Phase-4 surface."
---

## Phase Goal

**As a** maintainer, **I want** the Phase-4 evidence machinery in place before any feature, **so that** every later plan has one honest way to fail.

<objective>Stand up the Phase-4 gate scaffolding, corpus and effect ledger.</objective>

<tasks>
<task type="auto">
  <name>Task 1: The Phase-4 sentinel classifier</name>
  <files>tools/phase4-red-gate.mjs, tools/assert-red.mjs, package.json</files>
  <action>Add the ten Phase-4 sentinel entries and their owner commands, following the Phase-3 gate exactly: a run is accepted as controlled RED only when exactly one named sentinel fails with a literal EXPECTED_RED[...] marker and the task-specific effect-ledger prefix. Reject harness failures, zero-test runs and skips. Add the `test:phase4`, `test:phase4:quick` and `test:phase4:release` scripts.</action>
  <acceptance_criteria>
    - A harness failure (a missing browser, an import error) is rejected, not counted as RED.
    - A sentinel that passes reports `implemented`; one that fails without its marker reports `broken`.
    - The classifier has its own mutation tests, as Phases 2 and 3 do.
  </acceptance_criteria>
  <verify><automated>node tools/phase4-red-gate.mjs</automated></verify>
  <done>The gate runs and reports 10 controlled RED once the sentinels land.</done>
</task>

<task type="auto">
  <name>Task 2: The operations corpus</name>
  <files>tests/components/glt_flow_card/panel_factory.py, test/fixtures/operations/site.project.json</files>
  <action>Build a schema-3 project with a two-site portfolio, a plant with two subsystems, profiled equipment from the Phase-3 IDM corpus, datapoints with declared hours and starts, and alarms. Give it at least one object visible to an engineer and hidden from an operator, one hidden from both, and one subtree whose only alarm-bearing child is unauthorized — the count oracle from 04-RESEARCH section 3.</action>
  <acceptance_criteria>
    - Every one of the seven Phase-2 principals sees a materially different authorized subtree.
    - The corpus is generated from a compact manifest, not committed as expanded bodies.
    - No fixture references a live entity or a real site.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_panel_factory.py -q -x</automated></verify>
  <done>The corpus can express every enumeration case the threat register names.</done>
</task>

<task type="auto">
  <name>Task 3: The Phase-4 effect ledger</name>
  <files>tools/exact-dist-effect-ledger.mjs</files>
  <action>Extend the browser effect ledger to record and fail on any `hass.callService`, any `hass.callApi`, any `window.confirm`/`alert`, and any local or session storage write, attributed to the Phase-4 surfaces. Seed it with a deliberate violation to prove it fails.</action>
  <acceptance_criteria>
    - A seeded callService in a Phase-4 surface fails the ledger.
    - The legacy base card's existing callApi is attributed to the legacy surface and does not mask a new one.
    - The ledger reports zero effects for a clean run.
  </acceptance_criteria>
  <verify><automated>node tools/run-exact-dist-playwright.mjs --grep=phase-4-ledger-seed</automated></verify>
  <done>T4-11's evidence path exists before the code it will judge.</done>
</task>
</tasks>

<output>Create `04-01-SUMMARY.md` when done.</output>
