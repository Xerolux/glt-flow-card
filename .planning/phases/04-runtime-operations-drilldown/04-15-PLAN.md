---
phase: 04-runtime-operations-drilldown
plan: 15
type: execute
wave: 10
depends_on: [04-13]
files_modified:
  - test/e2e/project-operations.spec.mjs
  - src/v100/project-operations.js
  - src/v100/project-safety-i18n.mjs
autonomous: true
requirements: [OPS-02, NAV-01]
must_haves:
  truths:
    - "The complete workflow is keyboard-traversable on the pointerless kiosk layout."
    - "Every state is conveyed by symbol and text, never by color alone."
    - "German and English are complete at every layout."
---

## Phase Goal

**As an** operator on a control-room screen with no mouse, **I want** to complete the whole workflow from the keyboard, **so that** the product is usable where it is actually installed.

<objective>Close the exact-dist accessibility and localization matrix.</objective>

<tasks>
<task type="auto">
  <name>Task 1: The four-layout, two-language matrix</name>
  <files>test/e2e/project-operations.spec.mjs</files>
  <action>Implement every cell of the 04-VALIDATION exact-dist matrix against the generated artifact: mobile 320x640, tablet 768x1024, widescreen 1920x1080 and pointerless kiosk, each in German and English, with the named scenarios. Apply the cross-cutting assertions to every cell: visible focus, symbol-plus-text state cues, 200% zoom, forced colors, polite live regions, and a zero callService / zero callApi ledger.</action>
  <acceptance_criteria>
    - The kiosk cell is one continuous keyboard traversal: open panel, execute a control, read its outcome, drill down, return.
    - No cell relies on color alone to distinguish a state.
    - Nothing scrolls the page horizontally at 320px; wide content scrolls in its own container.
    - A missing translation key fails the build.
  </acceptance_criteria>
  <verify><automated>node tools/run-exact-dist-playwright.mjs --grep=phase-4-ui</automated></verify>
  <done>T4-13 is GREEN.</done>
</task>
</tasks>

<output>Create `04-15-SUMMARY.md` when done.</output>
