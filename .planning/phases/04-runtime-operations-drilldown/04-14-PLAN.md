---
phase: 04-runtime-operations-drilldown
plan: 14
type: execute
wave: 9
depends_on: [04-06, 04-13]
files_modified:
  - custom_components/glt_flow_card/__init__.py
  - tools/stage-hacs-packages.mjs
  - tools/validate-hacs-staging.mjs
  - test/hacs-staging.test.mjs
autonomous: true
requirements: [OPS-02, NAV-01]
must_haves:
  truths:
    - "Unload leaves no ghost subscription, panel cache or in-flight resync task."
    - "Runtime becomes unavailable before teardown, and the resource ledger reaches zero."
    - "Every new Companion module is in all three packaging lists."
---

## Phase Goal

**As a** maintainer, **I want** a reload to leave nothing behind, **so that** a long-running installation does not accumulate the state this phase adds.

<objective>Close the lifecycle and packaging obligations of the new surfaces.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Lifecycle and resource ledger</name>
  <files>custom_components/glt_flow_card/__init__.py</files>
  <action>Add the panel cache and the view-stream registry to the generation-stamped invalidation in `async_invalidate` and to the lifecycle resource ledger. Make the runtime unavailable first, then await teardown, so a late callback finds an unavailable runtime rather than a half-torn-down one. Prove a snapshot taken before a reload is not served after it.</action>
  <acceptance_criteria>
    - After unload the ledger reports zero subscriptions, zero cached panels and zero in-flight resyncs.
    - A callback that fires after unload is inert rather than raising.
    - A pre-reload snapshot sequence is not accepted post-reload.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_phase4_lifecycle.py -q -x</automated></verify>
  <done>T4-12 is GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Three packaging lists</name>
  <files>tools/stage-hacs-packages.mjs, tools/validate-hacs-staging.mjs, test/hacs-staging.test.mjs</files>
  <action>Add `panels.py`, `view_stream.py` and `navigation.py` to the stager, to the independent validator and to the drift guard. Import the staged component the way the HA lane does, so a missing module fails here rather than in `ha-artifacts`.</action>
  <acceptance_criteria>
    - All three lists contain all three modules.
    - Importing the staged component succeeds.
    - Removing a module from any one list fails the drift guard.
  </acceptance_criteria>
  <verify><automated>npm run validate:hacs-staging && node --test test/hacs-staging.test.mjs</automated></verify>
  <done>The packaging gap Phase 3 found cannot recur for Phase-4 modules.</done>
</task>
</tasks>

<output>Create `04-14-SUMMARY.md` when done.</output>
