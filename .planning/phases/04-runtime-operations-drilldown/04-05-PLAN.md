---
phase: 04-runtime-operations-drilldown
plan: 5
type: execute
wave: 2
depends_on: [04-02]
files_modified:
  - custom_components/glt_flow_card/panels.py
  - custom_components/glt_flow_card/policy.py
  - custom_components/glt_flow_card/__init__.py
  - tests/components/glt_flow_card/policy_contract.py
autonomous: true
requirements: [OPS-02]
must_haves:
  truths:
    - "The panel model is composed server-side from the verified head."
    - "A control the caller may not execute now is absent from the response."
    - "No panel response carries a domain, service or target."
---

## Phase Goal

**As an** operator, **I want** one consistent panel for every profiled object, **so that** I do not need a hand-built popup per equipment type.

<objective>Turn the panel half of OPS-02 GREEN behind the Phase-2 policy boundary.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Compose the panel model</name>
  <files>custom_components/glt_flow_card/panels.py</files>
  <action>Compose the ordered regions defined in 04-UI-SPEC from the instantiated Phase-3 profile, the project's datapoints, the Phase-3 resolved operational state, the Phase-3 provenance summary and the authorized alarms. Build the authorized set first and serialize only that. Emit control entries as {control_id, label, kind, confirm_required} and nothing more. Emit the trend region in its declared `history_unavailable` state. Bound regions per model and datapoints per panel.</action>
  <acceptance_criteria>
    - Every profile in the corpus produces a model with the same region kinds in the same order.
    - A principal without control.execute receives no controls region content and no hint of what was withheld.
    - No response field can be traced to a domain, service or entity target.
    - An undeclared region kind is an error, not a passthrough.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_panels.py -q -x</automated></verify>
  <done>T4-01 is GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Declare and register the route</name>
  <files>custom_components/glt_flow_card/policy.py, custom_components/glt_flow_card/__init__.py, tests/components/glt_flow_card/policy_contract.py</files>
  <action>Declare `glt_flow_card/panels/get` in both policy tables, project-scoped with `project.read` and opaque enumeration, register the handler, and move the exact route count in the lifecycle ledger with it (38 → 39). Resolve only objects belonging to the named project.</action>
  <acceptance_criteria>
    - Hidden and missing answers are byte-identical across the whole response.
    - The registration oracle passes with declared and registered sets equal.
    - A panel for an object outside the named project cannot be requested through this route.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_panel_enumeration.py tests/components/glt_flow_card/test_policy.py tests/components/glt_flow_card/test_init.py -q -x</automated></verify>
  <done>T4-02 is GREEN.</done>
</task>
</tasks>

<output>Create `04-05-SUMMARY.md` when done.</output>
