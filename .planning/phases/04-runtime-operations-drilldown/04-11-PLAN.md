---
phase: 04-runtime-operations-drilldown
plan: 11
type: execute
wave: 6
depends_on: [04-04, 04-10]
files_modified:
  - src/v100/command-outcome.mjs
autonomous: true
requirements: [OPS-02]
must_haves:
  truths:
    - "Only readback_confirmed renders as success."
    - "No outcome state offers a retry affordance."
    - "The displayed target and result match the authoritative audit record for the same command id."
---

## Phase Goal

**As an** operator, **I want** to know the difference between 'sent' and 'it actually happened', **so that** I never walk away from a command that did not land.

<objective>Surface Phase 2's nine control states as four visibly distinct operator outcomes.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Outcome presentation</name>
  <files>src/v100/command-outcome.mjs</files>
  <action>Map each of the nine CONTROL_RESULT_STATES to the presentation and affordances in the 04-UI-SPEC table. Give the three unknown-effect states a path to current state and trusted audit and no retry. Carry the command correlation id on every row. Do not add a tenth state and do not widen CONTROL_SUCCESS_STATES.</action>
  <acceptance_criteria>
    - All nine states are covered; adding a state to the Phase-2 list without covering it here fails the test.
    - `accepted` and `dispatched` never render with success styling.
    - No unknown-effect state exposes a retry entry point.
  </acceptance_criteria>
  <verify><automated>node --test test/command-outcome.test.mjs</automated></verify>
  <done>T4-07 is GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Outcome matches the audit record</name>
  <files>test/e2e/project-operations.spec.mjs</files>
  <action>In the exact generated artifact, execute a corpus control through the fake Companion, then compare the rendered target and result against the trusted audit row for the same correlation id. Cover a confirmed, a timed-out and a denied command.</action>
  <acceptance_criteria>
    - The comparison is against the audit row, not against the request the browser sent.
    - A seeded mismatch fails the test.
    - The effect ledger reports zero service effects.
  </acceptance_criteria>
  <verify><automated>node tools/run-exact-dist-playwright.mjs --grep=phase-4-outcome</automated></verify>
  <done>T4-08 is GREEN.</done>
</task>
</tasks>

<output>Create `04-11-SUMMARY.md` when done.</output>
