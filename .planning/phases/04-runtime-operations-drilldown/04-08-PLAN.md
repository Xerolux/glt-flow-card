---
phase: 04-runtime-operations-drilldown
plan: 8
type: execute
wave: 4
depends_on: [04-07]
files_modified:
  - custom_components/glt_flow_card/navigation.py
autonomous: true
requirements: [NAV-01]
must_haves:
  truths:
    - "A count is computed over the authorized scope only."
    - "An authorized-but-empty subtree is indistinguishable from an unauthorized one."
---

## Phase Goal

**As a** user, **I want** roll-up numbers I can trust, **so that** a badge never tells me something exists that I am not allowed to know about.

<objective>Close the aggregate-count oracle at every level.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Authorized-scope roll-ups</name>
  <files>custom_components/glt_flow_card/navigation.py</files>
  <action>Compute alarm, fault and child counts by walking only the authorized subtree, at every level from portfolio to equipment. Emit no count field at all when the authorized count is zero, so a rendered zero cannot distinguish an empty authorized subtree from an unauthorized one. Bound the walk by the Phase-3 node bound.</action>
  <acceptance_criteria>
    - The corpus subtree whose only alarm-bearing child is unauthorized reports no count.
    - An empty authorized subtree and an unauthorized subtree produce equal responses.
    - Roll-ups are asserted at portfolio, site, plant, subsystem and equipment level, not only at the leaf.
    - The walk cannot exceed the Phase-3 max_nodes bound.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_navigation_counts.py -q -x</automated></verify>
  <done>T4-04 is GREEN.</done>
</task>
</tasks>

<output>Create `04-08-SUMMARY.md` when done.</output>
