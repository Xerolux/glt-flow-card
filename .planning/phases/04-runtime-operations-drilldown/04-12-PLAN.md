---
phase: 04-runtime-operations-drilldown
plan: 12
type: execute
wave: 7
depends_on: [04-04, 04-06, 04-09]
files_modified:
  - src/v100/view-resync.mjs
autonomous: true
requirements: [OPS-02]
must_haves:
  truths:
    - "A gap marks the view stale in the same transition."
    - "Nothing is interpolated across a gap."
    - "Operator input survives a resync."
---

## Phase Goal

**As an** operator, **I want** the screen to tell me when it has lost the thread, **so that** I never mistake a frozen number for a live one.

<objective>Detect gaps honestly and recover without a page reload.</objective>

<tasks>
<task type="auto">
  <name>Task 1: The resync state machine</name>
  <files>src/v100/view-resync.mjs</files>
  <action>Hold `expected = snapshot.sequence + 1`. Treat a mismatched event sequence, a reconnect, a revocation and an incompatible snapshot each as a loss event producing `stale` in one transition, then request a fresh snapshot subject to the server's bound. Render previous values with an explicit age while stale, never as live. Preserve the operator's in-flight input across the resync, per the Phase-2 candidate-preservation pattern.</action>
  <acceptance_criteria>
    - Every loss event reaches stale in exactly one transition, with no intermediate interactive render.
    - No code path fills a gap by interpolating or by reusing the last event.
    - A typed setpoint and a selected time window survive a resync.
    - A `rate_limited` answer leaves the view stale rather than escalating.
  </acceptance_criteria>
  <verify><automated>node --test test/view-resync.test.mjs</automated></verify>
  <done>T4-09's browser half is GREEN.</done>
</task>
</tasks>

<output>Create `04-12-SUMMARY.md` when done.</output>
