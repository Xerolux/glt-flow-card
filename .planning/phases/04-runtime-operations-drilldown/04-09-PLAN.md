---
phase: 04-runtime-operations-drilldown
plan: 9
type: execute
wave: 4
depends_on: [04-04, 04-07]
files_modified:
  - src/v100/navigation.mjs
autonomous: true
requirements: [NAV-01]
must_haves:
  truths:
    - "The URL is the only source of navigation state."
    - "Back and forward re-resolve through the server; a cached view is never replayed."
    - "Time and alarm context survive every transition."
---

## Phase Goal

**As a** user, **I want** to bookmark and share exactly the view I am looking at, **so that** deep links and browser history behave the way the rest of the web does.

<objective>Make the address the state, and make history correct after a revocation.</objective>

<tasks>
<task type="auto">
  <name>Task 1: The address model</name>
  <files>src/v100/navigation.mjs</files>
  <action>Write a pure serializer and parser for the address: node path, time window and selected alarm. Round-trip them over a corpus that includes hostile inputs — overlong paths, control characters, percent-encoding tricks, duplicated keys. Export a frozen closed set of level kinds and staleness reasons; an unknown member is an error.</action>
  <acceptance_criteria>
    - Every corpus address round-trips to itself.
    - A hostile address is rejected by the parser rather than partially accepted.
    - Nothing needed to reconstruct a view lives outside the address.
  </acceptance_criteria>
  <verify><automated>node --test test/navigation.test.mjs</automated></verify>
  <done>The address round-trip corpus passes.</done>
</task>

<task type="auto">
  <name>Task 2: History without replay</name>
  <files>src/v100/navigation.mjs</files>
  <action>Push only the address string as history state. On popstate, re-resolve through `navigation/resolve` rather than restoring a cached view, and do not react to the reducer's own pushes. Build breadcrumbs from the server-returned ancestry only.</action>
  <acceptance_criteria>
    - A revocation between two views makes Back land on the neutral unavailable page, not on cached content.
    - A self-initiated push produces exactly one resolve, not two.
    - Breadcrumbs are never built from a locally cached tree.
  </acceptance_criteria>
  <verify><automated>node --test test/navigation.test.mjs</automated></verify>
  <done>T4-06 is GREEN.</done>
</task>
</tasks>

<output>Create `04-09-SUMMARY.md` when done.</output>
