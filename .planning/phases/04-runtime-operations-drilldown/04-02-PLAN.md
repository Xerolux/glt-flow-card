---
phase: 04-runtime-operations-drilldown
plan: 2
type: execute
wave: 1
depends_on: [04-01]
files_modified:
  - tests/components/glt_flow_card/test_panels.py
  - tests/components/glt_flow_card/test_panel_enumeration.py
  - tests/components/glt_flow_card/test_view_stream.py
autonomous: true
requirements: [OPS-02]
must_haves:
  truths:
    - "Each RED file carries exactly one sentinel and lists every unmet guarantee as a gap."
    - "A RED test asserts behavior, never the absence of a symbol."
---

## Phase Goal

**As a** maintainer, **I want** the backend guarantees written down as failing tests first, **so that** the implementation has a specification it cannot quietly narrow.

<objective>Write the three backend RED sentinels: panel composition, panel enumeration, view stream.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Panel composition and enumeration sentinels</name>
  <files>tests/components/glt_flow_card/test_panels.py, tests/components/glt_flow_card/test_panel_enumeration.py</files>
  <action>Specify, as failing tests: one composed model per corpus profile with its ordered regions; a control absent rather than disabled for a principal without `control.execute`; no `domain`, `service` or `target` anywhere in the response; hidden and missing objects answering byte-identically; and the region set being closed.</action>
  <acceptance_criteria>
    - Each file fails exactly once, with its EXPECTED_RED marker.
    - The enumeration assertions compare two full responses for equality, not merely both being errors.
    - No test asserts on a source token.
  </acceptance_criteria>
  <verify><automated>node tools/phase4-red-gate.mjs</automated></verify>
  <done>phase4-panels and phase4-panel-enumeration report controlled RED.</done>
</task>

<task type="auto">
  <name>Task 2: The view stream sentinel</name>
  <files>tests/components/glt_flow_card/test_view_stream.py</files>
  <action>Specify, as failing tests: a snapshot carrying the sequence it was read at, taken in one critical section; events carrying monotonic sequences; a gap being observable to the client; bounded concurrent snapshots and a bounded resync rate answering `rate_limited`; and a subscription bound to the runtime generation being inert after unload.</action>
  <acceptance_criteria>
    - An event emitted between the snapshot read and its stamp is proven impossible, not merely unobserved.
    - The rate bound is asserted with a number, not a shrug.
    - The file fails exactly once.
  </acceptance_criteria>
  <verify><automated>node tools/phase4-red-gate.mjs</automated></verify>
  <done>phase4-view-stream reports controlled RED.</done>
</task>
</tasks>

<output>Create `04-02-SUMMARY.md` when done.</output>
