---
phase: 04-runtime-operations-drilldown
plan: 3
type: execute
wave: 1
depends_on: [04-01]
files_modified:
  - tests/components/glt_flow_card/test_navigation.py
  - tests/components/glt_flow_card/test_navigation_counts.py
autonomous: true
requirements: [NAV-01]
must_haves:
  truths:
    - "An unauthorized address and a non-existent one are indistinguishable."
    - "A count over an unauthorized subtree is never rendered, not even as zero."
---

## Phase Goal

**As a** maintainer, **I want** the navigation oracles specified before the resolver exists, **so that** the resolver is built to close them rather than patched afterwards.

<objective>Write the navigation and count RED sentinels.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Address resolution sentinel</name>
  <files>tests/components/glt_flow_card/test_navigation.py</files>
  <action>Specify, as failing tests: resolution of each level from portfolio to datapoint; re-authorization on every resolve; byte-identical answers for unauthorized and non-existent targets; a deferred remote address answering the same way; ancestry returned for breadcrumbs; and address depth and length bounded before parse, with the Phase-3 max_depth of 32 re-asserted.</action>
  <acceptance_criteria>
    - The unauthorized/non-existent comparison covers the full response, including any error detail.
    - A cyclic or overlong address is rejected before any tree walk.
    - The file fails exactly once.
  </acceptance_criteria>
  <verify><automated>node tools/phase4-red-gate.mjs</automated></verify>
  <done>phase4-navigation reports controlled RED.</done>
</task>

<task type="auto">
  <name>Task 2: Count oracle sentinel</name>
  <files>tests/components/glt_flow_card/test_navigation_counts.py</files>
  <action>Specify, as failing tests: counts computed over the authorized scope only; the corpus subtree whose only alarm-bearing child is unauthorized reporting no count at all; and an authorized-but-empty subtree being indistinguishable from an unauthorized one.</action>
  <acceptance_criteria>
    - The test distinguishes 'renders 0' from 'renders nothing' and requires the latter.
    - Roll-ups at every level are covered, not only the leaf.
    - The file fails exactly once.
  </acceptance_criteria>
  <verify><automated>node tools/phase4-red-gate.mjs</automated></verify>
  <done>phase4-navigation-counts reports controlled RED.</done>
</task>
</tasks>

<output>Create `04-03-SUMMARY.md` when done.</output>
