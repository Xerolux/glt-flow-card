---
phase: 02-authoritative-policy-controls-collaboration
plan: 10
type: execute
wave: 6
depends_on: [02-04, 02-09]
files_modified:
  - custom_components/glt_flow_card/trusted_evidence.py
  - custom_components/glt_flow_card/const.py
  - custom_components/glt_flow_card/__init__.py
  - tests/components/glt_flow_card/test_trusted_evidence.py
  - tests/components/glt_flow_card/test_evidence_pagination.py
autonomous: true
requirements: [SEC-01, COLLAB-01]
must_haves:
  truths:
    - "Only server workflows can create trusted audit/control evidence with server actor/time and bounded details."
    - "Client telemetry is permanently untrusted, separately stored, authorized, size/rate/retention bounded, and cannot impersonate trusted history."
    - "Authorized evidence pagination is project-filtered, non-enumerating, cursor-bound, and retains already loaded truth on transient failure."
  artifacts:
    - path: "custom_components/glt_flow_card/trusted_evidence.py"
      provides: "Trusted evidence, untrusted telemetry, projection repair, filtering and pagination"
      exports: ["TrustedEvidenceRepository", "TelemetryRepository", "EvidenceCoordinator"]
  key_links:
    - from: "custom_components/glt_flow_card/trusted_evidence.py"
      to: "custom_components/glt_flow_card/policy_sessions.py"
      via: "authorized opaque cursor pages"
---

## Phase Goal

**As a** shared-project user, **I want to** have every shared operation authorized by the Companion and every concurrent edit guarded, **so that** I can operate and engineer without privilege bypass or lost updates.

<objective>Deliver a usable, authoritative evidence surface with provenance-separated bounded telemetry and no browser-trusted audit path.</objective>
<execution_context>@C:/Users/basti/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/02-authoritative-policy-controls-collaboration/02-04-SUMMARY.md @.planning/phases/02-authoritative-policy-controls-collaboration/02-09-SUMMARY.md @custom_components/glt_flow_card/project_repository.py</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: GREEN trusted evidence and separate telemetry repositories</name>
  <files>custom_components/glt_flow_card/trusted_evidence.py, custom_components/glt_flow_card/const.py, tests/components/glt_flow_card/test_trusted_evidence.py</files>
  <read_first>
    - tests/components/glt_flow_card/test_trusted_evidence.py
    - custom_components/glt_flow_card/project_repository.py
    - custom_components/glt_flow_card/project_transactions.py
  </read_first>
  <action>Implement immutable trusted event creation only from server workflow inputs: server actor/time, stable event/result code, normalized project/target summary, correlation and bounded sanitized details. Bind resolved A1 by enforcing exactly 8 KiB/event and existing `max_audit` plus 32 MiB oldest-complete-event retention. Create a different Store key/version/factory/schema for client telemetry, label it untrusted, replace claimed actor/result trust with received server metadata, and enforce the resolved A1 defaults of 4 KiB, 30/user/minute burst 10, 1,000 rows and 4 MiB. Strip CR/LF/raw tokens/payloads and use deep-copy/read-back/fault-safe writes. GREEN equal/above-boundary, separation and failure suites; changes to these constants require recorded measurements rather than silent relaxation.</action>
  <acceptance_criteria>
    - Browser `audit/add` is removed or maps only to bounded untrusted telemetry; it cannot create trusted event types.
    - No shared constructor/store/export/filter can combine trusted and untrusted rows.
    - Boundary and repeated-abuse tests keep memory/store growth within declared caps.</acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_trusted_evidence.py -q -x</automated></verify>
  <done>T2-09 provenance and bounds are GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire policy-scoped evidence reads, cursors, subscriptions and exports</name>
  <files>custom_components/glt_flow_card/trusted_evidence.py, custom_components/glt_flow_card/__init__.py, tests/components/glt_flow_card/test_evidence_pagination.py</files>
  <read_first>
    - tests/components/glt_flow_card/test_evidence_pagination.py
    - custom_components/glt_flow_card/policy_sessions.py
    - custom_components/glt_flow_card/policy.py
  </read_first>
  <action>Replace broad `audit/list` with project-authorized trusted pages and independent telemetry pages. Filter project IDs before pagination, page exactly 50, expose no hidden totals, bind cursors to filter/provenance/authority scope, and reauthorize each page/event. Bind resolved A5: cursor values are short-lived server-state tokens whose offset/filter state never leaves the registry and restart always invalidates them. Require explicit `audit.read`, `audit.export`, or `telemetry.read`; telemetry append stays separately bounded for assigned users. Emit separate provenance-labeled exports and minimal revocation events. Return stable errors without raw exceptions.</action>
  <acceptance_criteria>
    - Hidden projects/events never influence returned rows or counts.
    - Revocation clears subsequent pages/subscriptions; already loaded browser rows can be marked stale by the client rather than replaced with false empty data.
    - Trusted and telemetry route/result schemas are distinct.
    - Resolved A5 restart invalidation returns `cursor_invalid` and no row/total leak rather than resuming a self-contained cursor.</acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_trusted_evidence.py tests/components/glt_flow_card/test_evidence_pagination.py tests/components/glt_flow_card/test_policy_enumeration.py -q -x</automated></verify>
  <done>Authorized users can consume trustworthy evidence without enumeration or telemetry confusion.</done>
</task>
</tasks>

<artifacts_this_phase_produces>
- `trusted_evidence.py`: `TrustedEvent`, `TelemetryEvent`, `TrustedEvidenceRepository`, `TelemetryRepository`, `EvidenceCoordinator`, sanitized serializers/repair records.
- `const.py`: trusted/telemetry store keys, byte/row/rate bounds.
- `__init__.py`: guarded trusted-audit/telemetry page, subscribe, append and export adapters.
</artifacts_this_phase_produces>
<threat_model>
| Threats | Disposition | Mitigation / test |
|---|---|---|
| T2-04 | mitigate; HIGH/blocking | Source-filtered project evidence and scoped cursors. |
| T2-09 | mitigate; HIGH/blocking | Separate stores/factories/contracts, fixed provenance labels, byte/rate/retention caps. |
| T2-15 | mitigate | Evidence cursors/subscriptions/rate state join runtime cleanup. |
</threat_model>
<verification>Run trusted evidence, pagination, enumeration, policy and lifecycle-focused suites; scan trusted outputs for telemetry sentinels.</verification>
<success_criteria>Trusted evidence is server-authored and usable, telemetry stays explicitly untrusted, and neither leaks hidden projects or grows unbounded.</success_criteria>
<output>Create `.planning/phases/02-authoritative-policy-controls-collaboration/02-10-SUMMARY.md` when done.</output>
