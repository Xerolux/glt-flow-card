---
phase: 02-authoritative-policy-controls-collaboration
plan: 14
type: execute
wave: 10
depends_on: [02-13]
files_modified:
  - custom_components/glt_flow_card/const.py
  - custom_components/glt_flow_card/config_flow.py
  - custom_components/glt_flow_card/__init__.py
  - tests/components/glt_flow_card/test_phase2_migration.py
  - tests/components/glt_flow_card/test_phase2_lifecycle.py
autonomous: true
requirements: [SEC-01, COLLAB-01]
must_haves:
  truths:
    - "Legacy authority data migrates once, conservatively and idempotently without deriving live authority from imported/project content."
    - "Old-card/new-Companion incompatibility becomes shared read-only and never enables a privileged fallback."
    - "Setup, reload and unload become unavailable first and leave every Phase-2 runtime resource at zero; old ephemeral authority never revives."
  artifacts:
    - path: "tests/components/glt_flow_card/test_phase2_migration.py"
      provides: "Conservative migration and compatibility matrix"
    - path: "tests/components/glt_flow_card/test_phase2_lifecycle.py"
      provides: "Unavailable-first resource-zero lifecycle matrix"
  key_links:
    - from: "custom_components/glt_flow_card/__init__.py"
      to: "Phase-2 policy/lease/cursor/subscription/control managers"
      via: "generation-bound setup, reload, unload and late-callback cleanup"
---

## Phase Goal

**As a** shared-project user, **I want to** have every shared operation authorized by the Companion and every concurrent edit guarded, **so that** I can operate and engineer without privilege bypass or lost updates.

<objective>Close conservative authority migration, compatibility negotiation and resource-zero lifecycle behavior before exact artifacts are staged.</objective>
<execution_context>@C:/Users/basti/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/02-authoritative-policy-controls-collaboration/02-04-SUMMARY.md
@.planning/phases/02-authoritative-policy-controls-collaboration/02-13-SUMMARY.md
@.planning/phases/02-authoritative-policy-controls-collaboration/02-RESEARCH.md
@.planning/phases/02-authoritative-policy-controls-collaboration/02-THREATS.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: GREEN conservative authority migration and compatibility</name>
  <files>custom_components/glt_flow_card/const.py, custom_components/glt_flow_card/config_flow.py, custom_components/glt_flow_card/__init__.py, tests/components/glt_flow_card/test_phase2_migration.py</files>
  <read_first>
    - tests/components/glt_flow_card/test_phase2_migration.py
    - custom_components/glt_flow_card/__init__.py
    - custom_components/glt_flow_card/config_flow.py
    - custom_components/glt_flow_card/const.py
  </read_first>
  <action>Confirm the classified `phase2-migration-lifecycle` RED from 02-04, then implement one-time active-head-only conservative legacy-permissions bootstrap; imported/candidate content never bootstraps access. Clear legacy persisted locks without creating leases, label monolithic client audit `legacy_untrusted`, preserve Phase-1 trusted transaction evidence, and fail closed on malformed/partial migration with exact backup/read-back guarantees. Bind resolved A1 by migrating `default_lock_ttl` idempotently into the exact 60..900 range with default 300. Bind resolved A2 by ensuring conservative bootstrap never turns HA-admin status into content membership and the minimal membership-admin exception remains outside project JSON. Implement old-card/new-Companion policy-version negotiation as incompatible shared read-only with no local/direct privileged fallback.</action>
  <acceptance_criteria>
    - Repeated migration produces byte/shape-equivalent safe ACL/evidence/options state and no role elevation.
    - Active-head legacy assignments can bootstrap only within fixed roles and HA ceilings; import, rollback and candidates cannot change access.
    - The exact A1 lease TTL range/default and A2 no-implicit-content rule pass below/equal/above and malformed legacy cases.
    - Incompatible clients can inspect only the permitted read-only shell and produce zero privileged effects.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_phase2_migration.py tests/components/glt_flow_card/test_options.py -q -x</automated></verify>
  <done>The migration half of T2-15 is GREEN without broadening authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN unavailable-first resource-zero lifecycle</name>
  <files>custom_components/glt_flow_card/__init__.py, tests/components/glt_flow_card/test_phase2_lifecycle.py</files>
  <read_first>
    - tests/components/glt_flow_card/test_phase2_lifecycle.py
    - tests/components/glt_flow_card/test_init.py
    - tests/components/glt_flow_card/test_options.py
    - custom_components/glt_flow_card/__init__.py
  </read_first>
  <action>Extend setup/reload/unload so runtime becomes unavailable before cleanup; cancel and await subscriptions, tasks, lease/cursor/rate registries, control readback listeners/waits and pending callbacks; then clear all generation-bound memory. Ignore late callbacks and invalidate every old token/cursor/session binding. Add before/after persistence failures, failed reload restoration, active-resource unload, re-setup, late-event and ghost-command cases on both supported HA APIs. Keep every service fake and require the complete lifecycle effect ledger to return to zero.</action>
  <acceptance_criteria>
    - Setup/reload/unload/re-setup passes the supported HA APIs with all Phase-2 resource counters at zero after teardown.
    - Failed reload restores the previous effective safe runtime without resurrecting any prior ephemeral authority.
    - Late callbacks, old tokens/cursors/connections and ghost command adapters stay inert and create zero service/network/plant effects.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_phase2_lifecycle.py tests/components/glt_flow_card/test_init.py tests/components/glt_flow_card/test_options.py -q -x</automated></verify>
  <done>T2-15 lifecycle behavior is GREEN and produces a clean handoff to artifact staging.</done>
</task>
</tasks>

<artifacts_this_phase_produces>
- Migration-compatible constants/options and setup logic.
- `test_phase2_migration.py`: idempotent/no-elevation/compatibility evidence.
- `test_phase2_lifecycle.py`: resource-zero/late-callback/failure-recovery evidence.
</artifacts_this_phase_produces>
<threat_model>
| Threats | Disposition | Mitigation / test |
|---|---|---|
| T2-05 | mitigate; HIGH/blocking | Migration never derives implicit HA-admin content authority or trusts imported ACL content. |
| T2-15 | mitigate; HIGH/blocking | Conservative idempotent migration and unavailable-first resource-zero lifecycle. |
</threat_model>
<verification>Run migration, lifecycle, options and init suites; require zero skips, zero live effects and a zero resource ledger.</verification>
<success_criteria>Migration and lifecycle are release-ready before Plan 02-15 builds and installs exact artifacts.</success_criteria>
<output>Create `.planning/phases/02-authoritative-policy-controls-collaboration/02-14-SUMMARY.md` when done.</output>
