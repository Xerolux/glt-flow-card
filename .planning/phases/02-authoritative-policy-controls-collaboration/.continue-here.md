# Continue Here — Phase 2

**Updated:** 2026-09-02
**Position:** Phase 2 execution complete; all 17 plans implemented
**Scope:** 17 plans, 61 planned tasks, 17 implemented

## Read First

1. `.planning/HANDOFF.json`
2. `.planning/STATE.md`
3. `.planning/phases/02-authoritative-policy-controls-collaboration/02-THREATS.md`
4. The plan summaries `02-01-SUMMARY.md` through `02-17-SUMMARY.md`

## Exact Resume Point

Every Phase-2 plan is implemented. `npm run test:phase2:quick` reports
**12 implemented, 0 controlled RED, 0 broken**, and T2-01 through T2-15 are
marked `verified` in the threat register and the validation map after their
owner commands were run at this head.

**One thing is genuinely open.** T2-16 stays `planned`. Its owner,
`npm run test:phase2:release`, installs the exact stage on two digest-pinned
Home Assistant images and needs a Docker engine. This container has none, so
`npm run test:phase2` reaches F2-05 and then fails closed at F2-06 — the
Phase-1 gate, whose dependency-provenance step reaches package source metadata
that this network policy answers with HTTP 403. Both are environment limits,
not repository defects. The next session with Docker and open provenance
endpoints should:

1. Run `npm run test:phase2`.
2. If it passes, mark T2-16 `verified` in `02-THREATS.md` and `02-VALIDATION.md`
   and update the Evidence Status note.
3. If it fails, root-cause it — do not mark the row.

Marking T2-16 verified on the strength of its parts passing separately is
exactly the failure the register exists to prevent.

After that: the Phase-2 code review, security audit, UI audit, drift checks and
independent goal verification, then Phase 3.

Two environment notes for this container. Run Playwright with
`PLAYWRIGHT_CHROMIUM_EXECUTABLE=/opt/pw-browsers/chromium-1194/chrome-linux/chrome`,
and read the **full** `# fail` counter of a test run — a `tail` that cuts it off
has already produced one false green.

Working on the Python component? Commit the source first, then `npm run build`,
then commit the regenerated `build-manifest.json`: the manifest records the last
commit touching a canonical source, so building before committing always leaves
`verify:release` red.

## Completed

- Phase 1: 13/13 implementation plans and 14/14 recorded review fixes.
- Phase 2 planning: all artifacts and 17 plans; the bounded plan check passed.
- Plans 02-01..02-05: the twelve RED sentinels, the seven authenticated
  principals, the strict RED classifier, the browser effect ledger and the
  shared two-browser authority coordinator.
- Plans 02-06..02-11: the deny-default policy boundary, the server-owned ACL,
  connection-bound leases, membership administration, subscriptions and opaque
  cursors, the in-lock mutation guard, bounded merge, trusted evidence separated
  from telemetry, and configured controls resolved from the verified head.
- Plan 02-12: the fail-closed browser authority reducer, the authority state
  bar, capability-aware Overview cards, the access work surface and the
  separated evidence sections. It also added `glt_flow_card/capabilities/get`,
  which the UI contract and the exact-dist fixture had assumed since the RED
  wave but no backend plan had declared.
- Plan 02-13: the collaboration and configured-control state machines and the
  lease, conflict-recovery, control-confirmation and evidence-view surfaces.
- Plan 02-14: conservative upgrade of legacy authority and the unavailable-first
  resource-zero lifecycle.
- Plan 02-15: the HA lanes fail on a collapsed or skipped run, and
  `verify:release` consumes the staged packages.
- Plan 02-16: bilingual documentation of the tested contract and a validated,
  least-privilege docs delivery workflow.
- Plan 02-17: `tools/verify-phase2.mjs`, which binds every requirement, roadmap
  truth, plan, threat and resolved assumption to current evidence and proves its
  own command graph is acyclic with exactly one path to the T2-16 leaf.

## Still Open

- [ ] T2-16: run `npm run test:phase2` where Docker and the package-provenance
      endpoints are reachable, then mark the row.
- [ ] The server does not yet return a role-to-capability matrix, so
      `RoleMatrixDisclosure` renders its declared `unavailable` state. The
      browser must not invent one.
- [ ] `actions/configure-pages`, `actions/upload-pages-artifact` and
      `actions/deploy-pages` are pinned to tags, not digests: this session's
      GitHub access is scoped to this repository, so their release digests
      could not be verified here.
- [ ] Phase 2 code review, security audit, UI audit, drift checks, goal verification.
- [ ] Phase 1 consolidated security/UI/drift/goal verification.
- [ ] Phases 3 through 10, the final milestone audit and the first release.
