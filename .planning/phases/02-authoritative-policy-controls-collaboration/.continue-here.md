# Continue Here — Phase 2

**Updated:** 2026-09-02
**Position:** Phase 2 execution; plans 02-01 through 02-13 complete
**Scope:** 17 plans, 61 planned tasks, 13 implemented

## Read First

1. `.planning/HANDOFF.json`
2. `.planning/STATE.md`
3. `.planning/phases/02-authoritative-policy-controls-collaboration/02-PLAN-CHECK.md`
4. The summaries `02-01-SUMMARY.md` through `02-06-SUMMARY.md`
5. The next plan you are executing

## Exact Resume Point

The bounded final plan check **passed** on 2026-09-02 and is recorded in
`02-PLAN-CHECK.md`; that item from the pause handoff is closed. Execution has
run through plan 02-13: every Phase-2 sentinel is implemented.

Run `npm run test:phase2` first. It classifies every Phase-2 sentinel as
*controlled RED*, *implemented*, or *broken*, so it tells you exactly where the
phase stands without reading anything else. Current state: **12 implemented,
0 controlled RED, 0 broken**. Every Phase-2 threat owner command from T2-01 to
T2-14 passes.

Next plan is **02-14** (migration and lifecycle). After that: 02-15 (packaging
and the HA lanes), 02-16 (DE/EN documentation) and 02-17 (`tools/verify-phase2.mjs`,
which replaces `tools/phase2-red-gate.mjs` as the phase gate).

`tools/run-unit-tests.mjs` no longer excludes anything: both browser sentinels
are green and run in `npm test` as ordinary regression tests.

Two environment notes for this container. Run Playwright with
`PLAYWRIGHT_CHROMIUM_EXECUTABLE=/opt/pw-browsers/chromium-1194/chrome-linux/chrome`,
and read the **full** `# fail` counter of a test run - a `tail` that cuts it off
has already produced one false green.

## Completed

- Phase 1: 13/13 implementation plans and 14/14 recorded review fixes.
- Phase 2 planning: all artifacts and 17 plans; the bounded plan check passed.
- Plan 02-01: threat register status column, seven authenticated HA principals,
  controlled fake service, Phase-2 resource ledger, the twelve-entry strict RED
  classifier with mutation tests, and the extended browser effect ledger.
- Plans 02-02..02-05: all twelve RED sentinels registered and accepted by the
  classifier, plus the shared two-browser authority coordinator.
- Plan 02-06: the deny-default policy boundary and the server-owned ACL.
  T2-01, T2-02 and T2-03 pass their owner commands.
- Plan 02-08: connection-bound exclusive leases with rotating bearers, exact
  expiry and generation invalidation (T2-10).
- Plan 02-07: membership administration, per-event subscriptions, opaque
  cursors (T2-04, T2-05).
- Plan 02-09: the in-lock mutation guard and bounded three-way merge
  (T2-11, T2-12).
- Plan 02-10: trusted evidence separated from bounded telemetry (T2-09).
- Plan 02-11: configured controls resolved from the head, dispatched at most
  once, with honest evidence (T2-06, T2-07, T2-08).
- Plan 02-12: the fail-closed browser authority reducer, the persistent
  authority state bar, capability-aware Overview cards, the in-dialog access
  work surface and the separated trusted/untrusted evidence sections
  (T2-13 core). It also added `glt_flow_card/capabilities/get`, which the UI
  contract and the exact-dist fixture had assumed since the RED wave but no
  backend plan had declared; the exact route count moved 36 -> 37.
- Plan 02-13: the collaboration and configured-control state machines plus the
  lease, conflict-recovery, control-confirmation and evidence-view surfaces
  (T2-13, T2-14). No bearer exists in the collaboration module at all, conflict
  recovery has no overwrite branch, and the control module has no retry entry
  point.

## Still Open

- [ ] Plans 02-14 through 02-17.
- [ ] Threats T2-15 and T2-16.
- [ ] The server does not yet return a role-to-capability matrix, so
      `RoleMatrixDisclosure` renders its declared `unavailable` state. The
      browser must not invent one.
- [ ] Phase 2 code review, security audit, UI audit, drift checks, goal verification.
- [ ] Phase 1 consolidated security/UI/drift/goal verification.
- [ ] Phases 3 through 10, the final milestone audit and the first release.

## Constraints

- No live Home Assistant writes, remote-site writes, physical-bus writes,
  plant/equipment calls, or credential handling.
- No release is authorized.
- Preserve the security decisions already implemented: server-owned authority,
  default deny, filtered collections, one non-enumerating denial shape, exact
  configured targets, trusted audit, revision-plus-lease collaboration, and
  fail-closed shared behavior.
- A shared mutation is checked twice: at the policy boundary and again inside
  the coordinator lock immediately before the PREPARED journal. Both are load
  bearing; do not remove either.
- A new Companion Python module must be added to `tools/stage-hacs-packages.mjs`,
  `tools/validate-hacs-staging.mjs` and the list in `test/hacs-staging.test.mjs`,
  or it will not ship. The guard in that test file enforces it and has already
  caught the drift twice.
- The CI `ha-artifacts` job runs the whole pytest suite inside immutable Home
  Assistant containers with `-x`, so a *Python* controlled-RED sentinel turns
  that job red for as long as it stays RED. Every Python sentinel is currently
  implemented and the three remaining ones are browser-side, so this is not
  biting today. Plan 02-15 owns `tools/test-ha-artifacts.mjs`; if a later phase
  introduces a Python RED, that job needs the same treatment `npm test` got in
  `tools/run-unit-tests.mjs`.
- Read the *full* counters from `npm test` (`# tests`, `# pass`, `# fail`,
  `# skipped`). A short `tail` can cut off the `# fail` line and make a red run
  look green; that is how the packaging drift reached CI.

## Environment

- **Python.** `tools/python-launcher.mjs` resolves the pinned 3.13 interpreter:
  `GLT_PYTHON`, then the Windows `py -3.13` launcher, then a repository `.venv`
  or `python3.13`. It refuses anything that is not exactly 3.13. Plan commands
  written as `py -3.13 …` are satisfied by the resolved equivalent.
- **Playwright.** Set `PLAYWRIGHT_CHROMIUM_EXECUTABLE` when the container ships
  a Chromium revision that does not match the pinned Playwright version.
- **Build.** `package.json` is hashed into the canonical build manifest, so run
  `npm run build` after changing it or the HACS staging gates will report drift.

## Git State

- Branch: `claude/chatgpt-continuation-hi3y86`, pushed to `origin`.
- Pull request: https://github.com/Xerolux/glt-flow-card/pull/3 (draft).
- No subagent, worktree or background process is running.
