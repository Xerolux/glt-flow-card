---
phase: 02-authoritative-policy-controls-collaboration
plan: 05
type: tdd
wave: 1
depends_on: [02-01]
files_modified:
  - test/phase2-authority.test.mjs
  - test/phase2-collaboration.test.mjs
  - test/e2e/fixtures/shared-authority.mjs
  - test/e2e/project-authority.spec.mjs
  - tools/run-exact-dist-playwright.mjs
autonomous: true
requirements: [SEC-01, COLLAB-01]
must_haves:
  truths:
    - "Pure browser reducers have RED contracts for fail-closed authority, candidate retention, leases, conflicts, and control results."
    - "Two exact-dist browser contexts share one authoritative coordinator and receive distinct HA identities."
    - "DE/EN, keyboard, focus, live-region, responsive, forced-color, and secret/effect cases are automated before UI implementation."
  artifacts:
    - path: "test/e2e/fixtures/shared-authority.mjs"
      provides: "Shared two-session policy/lease/revision/evidence coordinator"
    - path: "test/e2e/project-authority.spec.mjs"
      provides: "Exact-dist Phase-2 UI acceptance scenarios"
    - path: "test/phase2-authority.test.mjs"
      provides: "Authority reducer contract"
  key_links:
    - from: "test/e2e/project-authority.spec.mjs"
      to: "dist/glt-flow-card.js"
      via: "manifest-hash checked loopback runner in two isolated contexts"
---

## Phase Goal

**As a** shared-project user, **I want to** have every shared operation authorized by the Companion and every concurrent edit guarded, **so that** I can operate and engineer without privilege bypass or lost updates.

<objective>Specify the complete fail-closed browser and two-session exact-dist behavior before authored UI modules are added.</objective>
<execution_context>@C:/Users/basti/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/02-authoritative-policy-controls-collaboration/02-UI-SPEC.md
@src/v100/project-safety.js
@test/e2e/project-safety.spec.mjs
@test/e2e/fixtures/fake-ha.mjs
@tools/assert-red.mjs
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Specify authority and collaboration reducers</name>
  <files>test/phase2-authority.test.mjs, test/phase2-collaboration.test.mjs</files>
  <read_first>
    - src/v100/project-safety.js
    - src/v100/project-safety-i18n.mjs
    - .planning/phases/02-authoritative-policy-controls-collaboration/02-UI-SPEC.md
  </read_first>
  <behavior>
    - Missing/loading/stale/sequence-gap/rejected/incompatible/role-revoked authority becomes shared read-only in the same reducer transition and removes protected data.
    - Candidate stays in memory across expiry, disconnect, conflict, role loss, and merge failure; clears only on authoritative success or explicit discard.
    - Lease token is never returned in render/export/diagnostic/telemetry state; reconnect requires fresh capability/head/lease.
    - Control result states never equate accepted/dispatched with confirmed and never schedule an automatic retry.
  </behavior>
  <action>Create deterministic Node tests for every UI-SPEC state transition, stable server error code, TTL threshold (dirty auto-renew at 40%, manual prompt at 50%, clean idle no auto), conflict/merge choice, and effect intent. Finish with the exact `phase2-authority-reducers` sentinel and reducer-effect evidence only after modules import and fixtures run; commit controlled RED for missing product behavior. The RED wrapper must reject a missing module/import error rather than treating it as expected.</action>
  <acceptance_criteria>
    - Reducer outputs contain affordance/recovery state but no bearer token or hidden project data.
    - Every stale/gap/revoke event produces a same-step read-only output.</acceptance_criteria>
  <verify><automated>node tools/assert-red.mjs --expected=phase2-authority-reducers -- node --test test/phase2-authority.test.mjs test/phase2-collaboration.test.mjs</automated></verify>
  <done>T2-13 reducer and T2-12 browser recovery behavior are specified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build a shared authoritative two-browser fixture</name>
  <files>test/e2e/fixtures/shared-authority.mjs, test/e2e/project-authority.spec.mjs, tools/run-exact-dist-playwright.mjs</files>
  <read_first>
    - test/e2e/fixtures/fake-ha.mjs
    - tools/run-exact-dist-playwright.mjs
    - playwright.config.mjs
  </read_first>
  <behavior>
    - One Node coordinator owns users, roles, access/content revisions, lease/session bindings, event sequence, trusted/telemetry pages, configured-control results, and injected disconnects for both contexts.
    - Pages load the exact same generated bytes but receive distinct authenticated identities/connections.
    - Runner verifies manifest hash/dist-www equality and refuses skip/zero tests/non-loopback effects.
  </behavior>
  <action>Implement only the test coordinator/runner seam, not product behavior. Make its server decisions match stable codes and bound outputs, seed unique secret/current/other-user sentinels, and expose deterministic time/fault controls. Add one exact-dist seed scenario whose fixture assertions pass and whose final product-completeness assertion emits the exact `phase2-ui-fixture-seed` marker plus exact-dist effect evidence. Commit the fixture and controlled product RED separately; import/launch/load/timeout/effect failures must remain rejected by the wrapper.</action>
  <acceptance_criteria>
    - Two contexts observe one shared revision/lease history and isolated browser storage.
    - The fixture itself cannot allow raw domain/service/target fields or live network/service effects.
    - The verification exits zero only when the exact seed product gap occurs after both contexts and all zero-effect sentinels are proven.</acceptance_criteria>
  <verify><automated>node tools/assert-red.mjs --expected=phase2-ui-fixture-seed -- node tools/run-exact-dist-playwright.mjs --grep=phase-2-fixture-seed</automated></verify>
  <done>Exact-dist Phase-2 tests have a deterministic shared authority without live infrastructure and one strictly classified product RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Specify all exact-dist Phase-2 UI scenarios</name>
  <files>test/e2e/project-authority.spec.mjs</files>
  <read_first>
    - .planning/phases/02-authoritative-policy-controls-collaboration/02-UI-SPEC.md
    - test/e2e/project-safety.spec.mjs
    - test/e2e/fixtures/shared-authority.mjs
  </read_first>
  <action>Translate all 22 UI-SPEC acceptance scenarios into behavioral Playwright tests with named grep groups `authority`, `two-session`, and `phase-2-ui`. Cover five principals, non-enumeration, access management, authority state bar, lease lifecycle, concurrent edit/conflict/merge/retry/discard, control/evidence states, trusted/untrusted split, Companion loss in every in-flight state, explicit local-only separation, DE/EN, dark/light, 320px and 200% zoom, keyboard/focus/live regions, reduced-motion/forced-colors, and sentinel/effect absence. Use one exact terminal completeness sentinel registered as `phase2-ui`; all other harness, accessibility, effect and fixture assertions must pass before that marker is emitted. Commit controlled RED against exact generated bytes.</action>
  <acceptance_criteria>
    - Tests interact through visible Shadow DOM behavior, not source tokens or screenshots alone.
    - Every denial/loss scenario asserts zero callService/shared storage/direct-target/network effects and candidate/token invariants.
  </acceptance_criteria>
  <verify><automated>node tools/assert-red.mjs --expected=phase2-ui -- node tools/run-exact-dist-playwright.mjs --grep=phase-2-ui</automated></verify>
  <done>T2-13/T2-14 exact-dist behavior is fully specified before UI GREEN plans.</done>
</task>
</tasks>

<artifacts_this_phase_produces>
- `test/phase2-authority.test.mjs`: pure authority/control reducer cases.
- `test/phase2-collaboration.test.mjs`: lease/candidate/conflict/merge reducer cases.
- `test/e2e/fixtures/shared-authority.mjs`: two-context authoritative coordinator.
- `test/e2e/project-authority.spec.mjs`: all 22 exact-dist UI scenarios.
- `tools/run-exact-dist-playwright.mjs`: Phase-2 grep/evidence support.
</artifacts_this_phase_produces>
<threat_model>
| Threats | Disposition | Mitigation / test |
|---|---|---|
| T2-12, T2-13 | mitigate; HIGH/blocking | Pure reducer candidate/read-only/no-retry contracts. |
| T2-14 | mitigate; HIGH/blocking | Exact-dist two-context accessibility/leak/effect suite. |
</threat_model>
<verification>Fixture tests pass while product scenarios reach controlled RED with exact artifact identity and zero prohibited effects.</verification>
<success_criteria>All browser product behavior is specified before Plans 02-12/13 implement it.</success_criteria>
<output>Create `.planning/phases/02-authoritative-policy-controls-collaboration/02-05-SUMMARY.md` when done.</output>
