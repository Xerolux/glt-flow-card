---
phase: 02-authoritative-policy-controls-collaboration
plan: 06
type: tdd
wave: 2
depends_on: [02-02]
files_modified:
  - custom_components/glt_flow_card/policy.py
  - custom_components/glt_flow_card/project_access.py
  - custom_components/glt_flow_card/const.py
  - custom_components/glt_flow_card/__init__.py
  - tests/components/glt_flow_card/test_policy.py
  - tests/components/glt_flow_card/test_policy_enumeration.py
autonomous: true
requirements: [SEC-01, COLLAB-01]
must_haves:
  truths:
    - "Every registered legacy/new route is declared once in a deny-default policy manifest and no route can bypass it."
    - "Authenticated HA connection identity, server ACL role, and HA ceiling are the only authority inputs."
    - "Authorized users can list/read projects while unassigned/unauthorized users cannot enumerate them; every not-yet-hardened effect route is safely unavailable."
  artifacts:
    - path: "custom_components/glt_flow_card/policy.py"
      provides: "Capabilities, fixed roles, command manifest, centralized decisions and stable errors"
      exports: ["COMMAND_POLICIES", "ROLE_CAPABILITIES", "PolicyCoordinator"]
    - path: "custom_components/glt_flow_card/project_access.py"
      provides: "Server-owned access read/bootstrap repository"
      exports: ["ProjectAccessRepository"]
  key_links:
    - from: "custom_components/glt_flow_card/__init__.py"
      to: "custom_components/glt_flow_card/policy.py"
      via: "single wrapper applied to every registered WebSocket handler"
---

## Phase Goal

**As a** shared-project user, **I want to** have every shared operation authorized by the Companion and every concurrent edit guarded, **so that** I can operate and engineer without privilege bypass or lost updates.

<objective>Turn the policy RED matrix GREEN with one centralized deny-default boundary and a usable authorized list/get slice, while fail-closing every legacy effect path not yet hardened.</objective>
<execution_context>@C:/Users/basti/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>
@custom_components/glt_flow_card/__init__.py
@custom_components/glt_flow_card/project_repository.py
@.planning/phases/02-authoritative-policy-controls-collaboration/02-PATTERNS.md
@.planning/phases/02-authoritative-policy-controls-collaboration/02-02-SUMMARY.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: GREEN the immutable capability and command policy contract</name>
  <files>custom_components/glt_flow_card/policy.py, custom_components/glt_flow_card/const.py, tests/components/glt_flow_card/test_policy.py</files>
  <read_first>
    - tests/components/glt_flow_card/test_policy.py
    - custom_components/glt_flow_card/__init__.py
    - .planning/phases/02-authoritative-policy-controls-collaboration/02-RESEARCH.md
  </read_first>
  <action>Confirm the committed RED, then implement fixed capability IDs/Viewer-Operator-Engineer-Admin matrix and immutable `COMMAND_POLICIES` entries for every current and planned route. Each entry declares scope, capability, project-field source, non-enumeration, revision/lease needs, rate class, and implemented/fail-closed state. Bind resolved A2 by declaring a distinct HA-admin ceiling path only for minimal membership inventory/read/write and administration-purpose lease acquisition; it is not a project role and cannot authorize project content/control/audit/telemetry/subscriptions. `PolicyCoordinator` derives actor only from active `connection.user`, intersects role with HA/entity authority, uses server time, maps exceptions to the stable public codes, and rejects undeclared/duplicate/incompatible routes. Bind resolved A6: remote transport and every unsafe legacy save/control/audit-add route remain declared but effect-unavailable until their owning GREEN plan. Commit GREEN after the generated route/principal matrix passes.</action>
  <acceptance_criteria>
    - Runtime registered names exactly equal `COMMAND_POLICIES` keys.
    - Client actor/role/capability/time/ACL fields cannot change a decision or evidence actor.
    - All unimplemented mutation/control/remote paths return stable zero-effect errors.
    - Resolved A2 permits HA-admin-without-membership only the minimal membership cells needed to bootstrap/manage fixed assignments; every content/control/audit cell remains denied.
    - Resolved A6 leaves remote paths declared and unavailable with zero legacy transport effects through Phase 9.</acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_policy.py -q -x</automated></verify>
  <done>T2-01/T2-03 are GREEN and no later plan is needed to make a registered route safe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN server-owned access bootstrap and authorized reads</name>
  <files>custom_components/glt_flow_card/project_access.py, custom_components/glt_flow_card/__init__.py, tests/components/glt_flow_card/test_policy_enumeration.py</files>
  <read_first>
    - tests/components/glt_flow_card/test_policy_enumeration.py
    - custom_components/glt_flow_card/project_repository.py
    - custom_components/glt_flow_card/project_contract.py
    - custom_components/glt_flow_card/__init__.py
  </read_first>
  <action>Implement the separate versioned `glt_flow_card.project_access` repository with deep copies, read-back verification, monotonic `access_revision`, and conservative one-time bootstrap only from verified active heads. Ignore project/import/rollback permission fields for live decisions. Wire `projects/list`, `projects/get`, and capability snapshot through policy: filter at repository source, omit hidden totals/counts, return identical `project_unavailable` for missing/unauthorized item reads, and filter entity-derived fields through HA read permission. Expose only a minimal HA-admin membership inventory shell (project ID, access revision, opaque content revision guard, assignments; no title/content/control/audit/counts) so first assignment cannot deadlock; the write remains fail-closed until 02-07.</action>
  <acceptance_criteria>
    - Viewer through Admin can read only assigned projects; unassigned and HA-admin-without-membership see no content.
    - Malformed/missing ACL fails project access closed instead of falling back to project JSON.
    - List/get denial has zero store/service/subscription effects.</acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_policy.py tests/components/glt_flow_card/test_policy_enumeration.py -q -x</automated></verify>
  <done>A real authorized read slice works through the new server boundary; all other paths remain safe.</done>
</task>
</tasks>

<artifacts_this_phase_produces>
- `policy.py`: capability constants, `ROLE_CAPABILITIES`, `CommandPolicy`, `COMMAND_POLICIES`, `PolicyDecision`, `PolicyError`, `PolicyCoordinator`.
- `project_access.py`: `ProjectAccessRepository`, access assignment/access-revision records, conservative bootstrap marker.
- `const.py`: access-store keys/versions and Phase-2 policy/bound constants.
- `__init__.py`: centralized guarded command registration and safe list/get/snapshot adapters.
</artifacts_this_phase_produces>
<threat_model>
| Threats | Disposition | Mitigation / test |
|---|---|---|
| T2-01, T2-02, T2-03 | mitigate; HIGH/blocking | Server identity/ceiling and exact command-manifest equality. |
| T2-04, T2-05 | mitigate; HIGH/blocking | Source filtering and separate access bootstrap; write routes stay fail-closed. |
</threat_model>
<verification>Run policy/enumeration suites, then full Python suite; inspect effect ledgers for every unimplemented legacy route.</verification>
<success_criteria>SEC-01 has a non-bypassable backend boundary and usable authorized reads without exposing any unfinished unsafe capability.</success_criteria>
<output>Create `.planning/phases/02-authoritative-policy-controls-collaboration/02-06-SUMMARY.md` when done.</output>
