---
phase: 02-authoritative-policy-controls-collaboration
plan: 11
type: tdd
wave: 7
depends_on: [02-04, 02-10]
files_modified:
  - custom_components/glt_flow_card/configured_controls.py
  - custom_components/glt_flow_card/policy.py
  - custom_components/glt_flow_card/trusted_evidence.py
  - custom_components/glt_flow_card/__init__.py
  - tests/components/glt_flow_card/test_configured_controls.py
  - tests/components/glt_flow_card/test_control_evidence.py
autonomous: true
requirements: [SEC-01, COLLAB-01]
must_haves:
  truths:
    - "Operator/Engineer/Admin can preview and execute only a server-known control from the verified current project head."
    - "Invalid, stale, unauthorized, gated, oversized, or rate-limited requests cause zero Home Assistant service attempts."
    - "One accepted request causes at most one exact normalized service attempt and evidence never overclaims confirmation or retries after uncertainty."
  artifacts:
    - path: "custom_components/glt_flow_card/configured_controls.py"
      provides: "Current-head control resolver, bounded input validator, preview/rate/dispatch/readback coordinator"
      exports: ["ConfiguredControlCoordinator", "ControlError"]
  key_links:
    - from: "custom_components/glt_flow_card/configured_controls.py"
      to: "Home Assistant services and states"
      via: "exact normalized target/data/context and configured readback predicate"
---

## Phase Goal

**As a** shared-project user, **I want to** have every shared operation authorized by the Companion and every concurrent edit guarded, **so that** I can operate and engineer without privilege bypass or lost updates.

<objective>Turn exact configured-control and evidence RED matrices GREEN as a safe operational vertical slice, without adding Phase-4 rich object UI or live targets.</objective>
<execution_context>@C:/Users/basti/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/02-authoritative-policy-controls-collaboration/02-04-SUMMARY.md @.planning/phases/02-authoritative-policy-controls-collaboration/02-10-SUMMARY.md @schemas/project.schema.json</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: GREEN current-head configured-control resolution</name>
  <files>custom_components/glt_flow_card/configured_controls.py, custom_components/glt_flow_card/policy.py, tests/components/glt_flow_card/test_configured_controls.py</files>
  <read_first>
    - tests/components/glt_flow_card/test_configured_controls.py
    - custom_components/glt_flow_card/project_repository.py
    - custom_components/glt_flow_card/project_contract.py
    - custom_components/glt_flow_card/policy.py
  </read_first>
  <action>Confirm RED, then implement strict control ID lookup from the verified current head; validate current revision/digest and exact declared user-input schema; reconstruct immutable domain/service plus disjoint entity/device/area target and immutable service data; reject unknown keys, target/service/context overrides, templates, nested calls, unsafe service pairs, missing maintenance/simulation gates, and HA entity-control denial. Bind resolved A1 exactly at 4 KiB/depth4/nodes64/keys16/string512/array32 and preview 30/min, and bind resolved A4 by authorizing this operational path through fresh capability+head+preview+HA permission without acquiring or validating an engineering lease. Return an opaque actor/project/head/control-bound preview, never raw editable service data. Commit GREEN after exact/boundary/Operator-no-lease/zero-call cases pass.</action>
  <acceptance_criteria>
    - Client request contains no accepted domain/service/target/immutable field.
    - Stale/removed control and every malicious/boundary failure produce zero service attempts.
    - Preview output is bounded and safe for confirmation without becoming caller authority.
    - Resolved A1/A4 pass explicitly: exact equal/above limits are enforced and an authorized Operator succeeds without any lease while stale/unauthorized callers make zero service attempts.</acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_configured_controls.py -q -x</automated></verify>
  <done>T2-06/T2-07 resolution is GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN one-attempt dispatch and authoritative readback evidence</name>
  <files>custom_components/glt_flow_card/configured_controls.py, custom_components/glt_flow_card/trusted_evidence.py, tests/components/glt_flow_card/test_control_evidence.py</files>
  <read_first>
    - tests/components/glt_flow_card/test_control_evidence.py
    - custom_components/glt_flow_card/trusted_evidence.py
    - tests/components/glt_flow_card/conftest.py
  </read_first>
  <action>Persist a bounded trusted `accepted` record before dispatch; if that append fails, do not call HA. Reauthorize capability/head/control/HA permission immediately before one `hass.services.async_call` with authenticated context and exact target/data. Record `dispatched`, then only configured state/attribute predicate within bounded timeout may produce `readback-confirmed`; missing/wrong/late/revoked yields timed-out/denied. Distinguish failed-before-dispatch from failed/result-unknown after a service attempt. Store a correlation-stable pending projection and repair evidence forward after restart; never automatically repeat a service. Enforce execute 10/min burst3 and clean listeners/tasks.</action>
  <acceptance_criteria>
    - Every fault-injection row has the exact evidence sequence and zero or one service attempts as specified.
    - `accepted`/`dispatched` never renders or serializes as confirmed.
    - Post-dispatch store failure/restart repairs evidence only and performs no second call.</acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_control_evidence.py -q -x</automated></verify>
  <done>T2-08 is GREEN without duplicate-effect risk.</done>
</task>

<task type="auto">
  <name>Task 3: Replace legacy control/remote adapters with safe contracts</name>
  <files>custom_components/glt_flow_card/__init__.py, tests/components/glt_flow_card/test_policy.py</files>
  <read_first>
    - custom_components/glt_flow_card/__init__.py
    - custom_components/glt_flow_card/configured_controls.py
    - custom_components/glt_flow_card/policy.py
  </read_first>
  <action>Expose preview/execute/result through thin guarded routes accepting only project/head/control/preview/input contract fields. Remove the caller-authored legacy domain/service/entity/service_data behavior or return `policy_incompatible` with zero effects. Bind resolved A6: keep remote list/states/control explicitly policy-declared and `feature_unavailable` until Phase 9; do not preserve current outbound REST/control transport as a compatibility path.</action>
  <acceptance_criteria>
    - Route inventory has no registered caller-selected control or active remote transport path.
    - All control/remote principal and malicious payload tests pass with exact zero/one attempt ledgers.</acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_policy.py tests/components/glt_flow_card/test_configured_controls.py tests/components/glt_flow_card/test_control_evidence.py -q -x</automated></verify>
  <done>The Companion control boundary is safe and Phase-9 transport remains deferred.</done>
</task>
</tasks>

<artifacts_this_phase_produces>
- `configured_controls.py`: `ControlPreview`, `ControlAttempt`, `ControlError`, `ConfiguredControlCoordinator.preview/execute/repair`, input/rate/readback helpers.
- `policy.py`: exact service-pair/control capability declarations and remote fail-closed policies.
- `trusted_evidence.py`: correlation-stable control evidence/pending repair projections.
- `__init__.py`: configured-control adapters and disabled legacy/remote effects.
</artifacts_this_phase_produces>
<threat_model>
| Threats | Disposition | Mitigation / test |
|---|---|---|
| T2-06, T2-07 | mitigate; HIGH/blocking | Current-head resolution, strict schemas/bounds/rates/HA permissions, zero-call denial. |
| T2-08 | mitigate; HIGH/blocking | Durable accepted event, one attempt, predicate readback, forward-only repair. |
</threat_model>
<verification>Run configured control/evidence/policy/full Python suites; controlled service is the only permitted effect and each valid execute has exactly one normalized attempt.</verification>
<success_criteria>Configured controls are server-resolved, bounded, HA-authorized and evidence-correct, with no confused-deputy or duplicate-dispatch path.</success_criteria>
<output>Create `.planning/phases/02-authoritative-policy-controls-collaboration/02-11-SUMMARY.md` when done.</output>
