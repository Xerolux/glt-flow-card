---
phase: 02-authoritative-policy-controls-collaboration
plan: 03
type: tdd
wave: 1
depends_on: [02-01]
files_modified:
  - tests/components/glt_flow_card/test_project_leases.py
  - tests/components/glt_flow_card/test_collaboration.py
  - tests/components/glt_flow_card/test_merge.py
autonomous: true
requirements: [SEC-01, COLLAB-01]
must_haves:
  truths:
    - "Lease lifecycle, replay, and race behavior is specified before tokens can guard writes."
    - "Every shared mutation has a generated missing/wrong/expired lease and stale revision zero-effect case."
    - "Merge recovery preserves candidates and never performs last-writer-wins or stale patch replay."
  artifacts:
    - path: "tests/components/glt_flow_card/test_project_leases.py"
      provides: "Connection-bound lease state-machine tests"
    - path: "tests/components/glt_flow_card/test_collaboration.py"
      provides: "Atomic mutation race tests"
    - path: "tests/components/glt_flow_card/test_merge.py"
      provides: "Bounded three-way merge tests"
  key_links:
    - from: "tests/components/glt_flow_card/test_collaboration.py"
      to: "ProjectTransactionCoordinator critical section"
      via: "deterministic barriers around immediate precommit guard"
---

## Phase Goal

**As a** shared-project user, **I want to** have every shared operation authorized by the Companion and every concurrent edit guarded, **so that** I can operate and engineer without privilege bypass or lost updates.

<objective>Write controlled RED contracts for leases, atomic shared mutations, and bounded non-destructive conflict recovery.</objective>
<execution_context>@C:/Users/basti/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/02-authoritative-policy-controls-collaboration/02-CONTEXT.md
@.planning/phases/02-authoritative-policy-controls-collaboration/02-VALIDATION.md
@custom_components/glt_flow_card/project_transactions.py
@src/v100/project-diff.mjs
@tools/assert-red.mjs
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Specify connection-bound exclusive lease lifecycle</name>
  <files>tests/components/glt_flow_card/test_project_leases.py</files>
  <read_first>
    - custom_components/glt_flow_card/__init__.py
    - custom_components/glt_flow_card/const.py
    - tests/components/glt_flow_card/user_factory.py
  </read_first>
  <behavior>
    - One exclusive engineering lease per project; reads remain concurrent.
    - TTL accepts 60, 300, 900 and rejects outside bounds; renewal rotates token; expiry has no grace.
    - Token binds project, actor, connection/session, purpose, access revision, and runtime generation.
    - Same-user second tab, another user, reconnect, old renewal token, disconnect, unload, and restart replay all fail without owner/token leak.
  </behavior>
  <action>Add simultaneous-acquire, just-before/at/after-expiry, disconnect-during-renewal, owner-safe release, role-loss, access-revision-change, and resource-bound cases using injected clock/random seams. Finish with the exact `phase2-leases` sentinel only after the lifecycle/effect ledger executes; commit controlled RED.</action>
  <acceptance_criteria>
    - Losing callers receive anonymous `lease_held` or stable invalid/expired codes.
    - No token is persisted or appears in logs/diagnostics/evidence.</acceptance_criteria>
  <verify><automated>node tools/assert-red.mjs --expected=phase2-leases -- py -3.13 -m pytest tests/components/glt_flow_card/test_project_leases.py -q</automated></verify>
  <done>T2-10 is completely specified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Specify the immediate-precommit collaboration guard</name>
  <files>tests/components/glt_flow_card/test_collaboration.py</files>
  <read_first>
    - custom_components/glt_flow_card/project_transactions.py
    - custom_components/glt_flow_card/project_repository.py
    - tests/components/glt_flow_card/test_project_transactions.py
  </read_first>
  <behavior>
    - Save/apply/rollback/delete/import/metadata/member changes require exact revision and valid purpose lease.
    - Role, capability policy version, access revision, lease, content revision, and digest are rechecked inside the project lock immediately before journal/commit.
    - Races before/after guard, journal, snapshot, and head yield exactly one authorized commit, one stable conflict/denial, no lost update, and no duplicate audit.
  </behavior>
  <action>Generate mutation-path cases from the policy manifest so no legacy/new route is omitted. Bind resolved A3: access changes carry exact `access_revision`, content changes carry exact `revision`, and both validate the same purpose-bound engineering lease without conflating their revision streams. Reuse Phase-1 fault barriers and assert store, audit, service, and resource ledgers. Finish with the exact `phase2-collaboration-guard` sentinel only after zero-effect evidence; commit controlled RED.</action>
  <acceptance_criteria>
    - Optional revision or lease paths are forbidden for shared mutation.
    - Authorization performed only before entering the lock is demonstrated insufficient by a deterministic race test.</acceptance_criteria>
  <verify><automated>node tools/assert-red.mjs --expected=phase2-collaboration-guard -- py -3.13 -m pytest tests/components/glt_flow_card/test_collaboration.py -q</automated></verify>
  <done>T2-11 has executable race and zero-effect coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Specify bounded three-way merge and retry recovery</name>
  <files>tests/components/glt_flow_card/test_merge.py</files>
  <read_first>
    - custom_components/glt_flow_card/project_diff.py
    - src/v100/project-diff.mjs
    - .planning/phases/02-authoritative-policy-controls-collaboration/02-UI-SPEC.md
  </read_first>
  <behavior>
    - Response includes authorized bounded base/current/candidate revisions and semantic operations, maximum 100 operations/256 KiB with explicit truncation.
    - Non-overlap selection can preview/apply; overlap, dependency lock, invalid candidate, stale lease/policy, and second writer cause a new conflict.
    - Candidate remains intact until authoritative success or explicit discard; no auto merge, LWW, or stale patch replay.
  </behavior>
  <action>Create server-side merge and race cases tied to Phase-1 stable operation IDs/dependencies. Include discard/refresh/retry, unauthorized-content redaction, second conflict, and exact revision/digest revalidation. Finish with the exact `phase2-merge` sentinel only after candidate and zero-mutation evidence; commit controlled RED.</action>
  <acceptance_criteria>
    - Merge tests prove both successful non-overlap and every blocked overlap/dependency/staleness case.
    - Failed merge causes zero project mutation and preserves the supplied candidate fixture.</acceptance_criteria>
  <verify><automated>node tools/assert-red.mjs --expected=phase2-merge -- py -3.13 -m pytest tests/components/glt_flow_card/test_merge.py -q</automated></verify>
  <done>T2-12 backend behavior is specified before implementation.</done>
</task>
</tasks>

<artifacts_this_phase_produces>
- `test_project_leases.py`: lease state/replay/race contract.
- `test_collaboration.py`: generated shared-mutation and atomic guard contract.
- `test_merge.py`: bounded three-way semantic recovery contract.
</artifacts_this_phase_produces>
<threat_model>
| Threats | Disposition | Mitigation / test |
|---|---|---|
| T2-10 | mitigate; HIGH/blocking | Opaque connection/session-bound lease lifecycle tests. |
| T2-11 | mitigate; HIGH/blocking | Immediate in-lock guard with deterministic barriers. |
| T2-12 | mitigate; HIGH/blocking | Bounded server-recomputed merge and second-conflict tests. |
</threat_model>
<verification>All three suites reach valid controlled RED states with no service or durable mutation effect.</verification>
<success_criteria>COLLAB-01 algorithms have complete RED coverage before Plans 02-08 and 02-09.</success_criteria>
<output>Create `.planning/phases/02-authoritative-policy-controls-collaboration/02-03-SUMMARY.md` when done.</output>
