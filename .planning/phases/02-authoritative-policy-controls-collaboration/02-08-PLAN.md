---
phase: 02-authoritative-policy-controls-collaboration
plan: 08
type: tdd
wave: 3
depends_on: [02-03, 02-06]
files_modified:
  - custom_components/glt_flow_card/project_leases.py
  - custom_components/glt_flow_card/const.py
  - custom_components/glt_flow_card/__init__.py
  - tests/components/glt_flow_card/test_project_leases.py
autonomous: true
requirements: [SEC-01, COLLAB-01]
must_haves:
  truths:
    - "Assigned Engineer/Admin can acquire one exclusive engineering lease; an authenticated unassigned HA administrator can acquire only the administration-purpose variant needed for minimal membership recovery, while all authorized reads remain concurrent."
    - "Renewal rotates the opaque bearer and expiry/release/disconnect/reconnect/unload has no grace or resurrection."
    - "Lease validation binds project, user, connection/session, purpose, access revision, and runtime generation without leaking owner/token."
  artifacts:
    - path: "custom_components/glt_flow_card/project_leases.py"
      provides: "Ephemeral exclusive lease manager"
      exports: ["ProjectLeaseManager", "LeaseError"]
  key_links:
    - from: "custom_components/glt_flow_card/__init__.py"
      to: "custom_components/glt_flow_card/project_leases.py"
      via: "acquire/renew/release adapters and disconnect cleanup"
---

## Phase Goal

**As a** shared-project user, **I want to** have every shared operation authorized by the Companion and every concurrent edit guarded, **so that** I can operate and engineer without privilege bypass or lost updates.

<objective>Turn the lease RED state machine GREEN as an independently usable exclusive-editing capability.</objective>
<execution_context>@C:/Users/basti/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/02-authoritative-policy-controls-collaboration/02-03-SUMMARY.md @.planning/phases/02-authoritative-policy-controls-collaboration/02-06-SUMMARY.md</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: GREEN the opaque lease algorithm</name>
  <files>custom_components/glt_flow_card/project_leases.py, custom_components/glt_flow_card/const.py, tests/components/glt_flow_card/test_project_leases.py</files>
  <read_first>
    - tests/components/glt_flow_card/test_project_leases.py
    - custom_components/glt_flow_card/policy.py
    - custom_components/glt_flow_card/project_transactions.py
    - custom_components/glt_flow_card/const.py
  </read_first>
  <action>Confirm RED, then implement `ProjectLeaseManager` with `secrets.token_urlsafe(32)`, stored SHA-256 digest and constant-time comparison, injected monotonic clock/token factory, TTL default 300 and accepted range 60..900, one project lease, purpose binding, user+connection+refresh-session+access-revision+runtime-generation binding, token rotation on renewal, deterministic expiry/prune, and no persistence/grace/owner disclosure. Cap lease operations at 30/user/project/minute. Commit GREEN when the complete same/different-user connection, replay, bound, and simultaneous-acquire suite passes.</action>
  <acceptance_criteria>
    - Exactly one simultaneous acquire succeeds and the loser learns no owner identity.
    - Old tokens fail immediately after renewal, reconnect, role/access revision change, expiry, disconnect, unload, or restart.
    - Only token digests reside in manager state; raw bearer exists only in response/in-memory caller.</acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_project_leases.py -q -x</automated></verify>
  <done>The core T2-10 algorithm is GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Wire lease endpoints and runtime cleanup</name>
  <files>custom_components/glt_flow_card/__init__.py, tests/components/glt_flow_card/test_project_leases.py</files>
  <read_first>
    - custom_components/glt_flow_card/__init__.py
    - custom_components/glt_flow_card/policy.py
    - custom_components/glt_flow_card/project_leases.py
  </read_first>
  <action>Replace legacy persisted `projects/lock`/`unlock` semantics with policy-declared acquire/renew/release behavior (retain compatibility names only if they enforce the same exact contract). Require `lease.acquire/renew/release`, derive binding from the active connection, register disconnect cleanup, publish anonymous held state/capability events, and include the manager in unavailable-first unload. Bind resolved A2: assigned Engineer/Admin may acquire engineering purposes allowed by their fixed role; an authenticated HA administrator without project assignment may acquire only `membership_admin`, sees no content/owner detail, and the token validates only on the minimal membership mutation boundary. Never log, diagnose, audit, render, persist, or echo the bearer outside its acquisition/renewal response.</action>
  <acceptance_criteria>
    - Assigned Engineer/Admin can complete acquire-renew-release; Viewer/Operator/ordinary-unassigned cannot; unassigned HA admin succeeds only for `membership_admin` and remains denied for every content/engineering/control purpose.
    - Disconnect and unload synchronously invalidate lease use and leave the lifecycle ledger at zero.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_project_leases.py tests/components/glt_flow_card/test_policy.py -q -x</automated></verify>
  <done>Lease lifecycle is usable over HA WebSocket and safe under authority/runtime loss.</done>
</task>
</tasks>

<artifacts_this_phase_produces>
- `project_leases.py`: `LeaseRecord`, `LeaseError`, `ProjectLeaseManager.acquire`, `.renew`, `.validate`, `.release`, `.release_connection`, `.clear`.
- `const.py`: lease TTL/rate/purpose bounds.
- `__init__.py`: guarded lease WebSocket routes and disconnect/runtime cleanup.
</artifacts_this_phase_produces>
<threat_model>
| Threats | Disposition | Mitigation / test |
|---|---|---|
| T2-10 | mitigate; HIGH/blocking | CSPRNG/digest/constant-time, full scope binding, rotation, deterministic cleanup. |
| T2-15 | mitigate | Unavailable-first disconnect/unload invalidation and resource ledger. |
</threat_model>
<verification>Focused lease suite, policy suite, then lifecycle smoke; scan logs/diagnostics/stores for seeded token sentinel.</verification>
<success_criteria>One exclusive connection-bound engineering lease is available and no token survives or leaks beyond its valid session.</success_criteria>
<output>Create `.planning/phases/02-authoritative-policy-controls-collaboration/02-08-SUMMARY.md` when done.</output>
