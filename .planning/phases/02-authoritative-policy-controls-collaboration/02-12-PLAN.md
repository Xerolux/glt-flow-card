---
phase: 02-authoritative-policy-controls-collaboration
plan: 12
type: tdd
wave: 8
depends_on: [02-05, 02-07, 02-10, 02-11]
files_modified:
  - src/v100/project-authority.mjs
  - src/v100/project-safety.js
  - src/v100/project-safety-i18n.mjs
  - src/v100/entry.js
  - test/phase2-authority.test.mjs
  - test/e2e/project-authority.spec.mjs
autonomous: true
requirements: [SEC-01, COLLAB-01]
must_haves:
  truths:
    - "Shared authority loss becomes visible read-only in the same render cycle, removes protected rows/actions, and never falls back."
    - "Project safety retains exactly five tabs and adds one persistent authority state bar plus capability-aware Overview/access/evidence surfaces."
    - "Trusted audit and untrusted telemetry remain visually, semantically, accessibly, and export-wise separate in English and German."
  artifacts:
    - path: "src/v100/project-authority.mjs"
      provides: "Pure authority/capability/evidence reducer and Companion adapter"
      exports: ["reduceProjectAuthority", "createProjectAuthorityClient"]
    - path: "src/v100/project-safety.js"
      provides: "AuthorityStateBar, access management and separated evidence UI"
  key_links:
    - from: "src/v100/project-safety.js"
      to: "src/v100/project-authority.mjs"
      via: "server snapshot/subscription actions reduced before every render/action"
---

## Phase Goal

**As a** shared-project user, **I want to** have every shared operation authorized by the Companion and every concurrent edit guarded, **so that** I can operate and engineer without privilege bypass or lost updates.

<objective>Turn authority/access/evidence browser RED contracts GREEN while preserving the existing Project safety shell and standalone/local boundary.</objective>
<execution_context>@C:/Users/basti/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/02-authoritative-policy-controls-collaboration/02-UI-SPEC.md @.planning/phases/02-authoritative-policy-controls-collaboration/02-05-SUMMARY.md @src/v100/project-safety.js</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: GREEN the fail-closed authority reducer and Companion adapter</name>
  <files>src/v100/project-authority.mjs, src/v100/entry.js, test/phase2-authority.test.mjs</files>
  <read_first>
    - test/phase2-authority.test.mjs
    - src/v100/project-safety.js
    - src/v100/project-safety-i18n.mjs
  </read_first>
  <action>Confirm RED, then implement pure normalized state for shared/local mode, policy compatibility/freshness, own role/effective capabilities, access/content revision, lease summary (never bearer), authorized project/evidence rows, and stable errors. `createProjectAuthorityClient` uses HA WebSocket snapshot/subscription/page APIs only; reauthorizes server-side on every action. Loading/stale/gap/reject/incompatible/disconnect/role revoke transitions shared state to read-only immediately, clears unauthorized rows/actions, preserves candidate reference only in memory, and never invokes callService/localStorage/IndexedDB/Lovelace/direct target/network fallback. Keep explicit local-only mode separate and labeled. Commit GREEN.</action>
  <acceptance_criteria>
    - Reducer tests pass all stale/revoke/sequence/error transitions in one step.
    - No server bearer, hidden project, raw exception, control target, or other-user identity enters renderable/exportable state.
    - Shared mode never auto-converts to local-only.</acceptance_criteria>
  <verify><automated>node --test test/phase2-authority.test.mjs</automated></verify>
  <done>T2-13 core browser authority is GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Add AuthorityStateBar, capability-aware Overview and access work surface</name>
  <files>src/v100/project-safety.js, src/v100/project-safety-i18n.mjs, test/e2e/project-authority.spec.mjs</files>
  <read_first>
    - src/v100/project-safety.js
    - src/v100/project-safety-i18n.mjs
    - .planning/phases/02-authoritative-policy-controls-collaboration/02-UI-SPEC.md
  </read_first>
  <action>Preserve the one Project safety trigger and exact five-tab order. Insert the persistent `AuthorityStateBar` between scope banner and tabs, with shared/local, Companion state, own role, lease summary, current/expected revision and accessible live announcements. Add Overview cards Shared authority/My access/Collaboration/Control policy and an in-dialog access surface shown only for `members.manage`: server-returned eligible users, exactly four roles, read-only capability matrix, exact revisions and lease, safe confirmations, last-admin/ceiling/stale denial recovery. Use native controls/textContent, adjacent disabled reasons, 44px targets, existing color/type/spacing tokens, responsive/forced-colors/reduced-motion/focus behavior, and complete DE/EN stable-code copy.</action>
  <acceptance_criteria>
    - Five tabs remain exact and authority bar appears on all tabs without exposing hidden IDs/counts.
    - Membership UI cannot accept arbitrary user IDs/capability checkboxes and never treats browser roles as authority.
    - Authority loss removes actions/rows and restores focus safely.</acceptance_criteria>
  <verify><automated>node tools/run-exact-dist-playwright.mjs --grep=authority</automated></verify>
  <done>Shared authority and access state is continuously understandable and fail-closed.</done>
</task>

<task type="auto">
  <name>Task 3: Separate trusted audit and untrusted telemetry UI</name>
  <files>src/v100/project-safety.js, src/v100/project-safety-i18n.mjs, test/e2e/project-authority.spec.mjs</files>
  <read_first>
    - .planning/phases/02-authoritative-policy-controls-collaboration/02-UI-SPEC.md
    - src/v100/project-authority.mjs
    - src/v100/project-safety.js
  </read_first>
  <action>Extend Evidence with independent Trusted audit and Client telemetry (untrusted) bordered sections, filters, load-next-50 cursors, error/empty/stale states, accessible headings/labels, and separate provenance-preserving exports only when capability permits. A failed next page keeps loaded rows marked stale; revocation removes unauthorized rows. Never show a combined total/timeline or style client actor/time/result as trusted. Add DE/EN strings for every state/code.</action>
  <acceptance_criteria>
    - Trusted and telemetry have distinct queries, DOM sections, exports, filters and names.
    - Pagination failure and authority revocation follow the UI-SPEC recovery matrix.
    - Sentinels/tokens/raw telemetry payloads never appear in DOM, URL, storage, console or export.</acceptance_criteria>
  <verify><automated>node tools/run-exact-dist-playwright.mjs --grep=authority</automated></verify>
  <done>Evidence provenance remains honest and accessible in both languages.</done>
</task>
</tasks>

<artifacts_this_phase_produces>
- `project-authority.mjs`: authority state enum, `reduceProjectAuthority`, `authorityAffordances`, `createProjectAuthorityClient`, sanitized error/evidence adapters.
- `project-safety.js`: `AuthorityStateBar`, `RoleCapabilitySummary`, `MemberAccessTable`, `RoleMatrixDisclosure`, `TrustedAuditTable`, `UntrustedTelemetryTable`, `ReadOnlyAuthorityBanner`, live region integration.
- `project-safety-i18n.mjs`: complete English/German authority/access/evidence/error copy.
- `entry.js`: project-authority module wiring before Project safety UI.
</artifacts_this_phase_produces>
<threat_model>
| Threats | Disposition | Mitigation / test |
|---|---|---|
| T2-04, T2-05, T2-09 | mitigate; HIGH/blocking | Server-returned/filtered rows, fixed membership UI, separate evidence provenance. |
| T2-13 | mitigate; HIGH/blocking | Same-cycle read-only reducer, protected-data removal, exhaustive no-fallback effects. |
| T2-14 | mitigate; HIGH/blocking | DE/EN keyboard/focus/reflow/leak exact-dist coverage. |
</threat_model>
<verification>Node authority suite and exact-dist authority/UI grep pass against rebuilt output with zero prohibited effects.</verification>
<success_criteria>Users can understand and administer current server authority and evidence without the browser becoming an authority or retaining protected data.</success_criteria>
<output>Create `.planning/phases/02-authoritative-policy-controls-collaboration/02-12-SUMMARY.md` when done.</output>
