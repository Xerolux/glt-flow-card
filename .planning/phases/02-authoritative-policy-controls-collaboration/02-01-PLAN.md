---
phase: 02-authoritative-policy-controls-collaboration
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/02-authoritative-policy-controls-collaboration/02-THREATS.md
  - tests/components/glt_flow_card/user_factory.py
  - tests/components/glt_flow_card/conftest.py
  - test/e2e/fixtures/fake-ha.mjs
  - tools/assert-red.mjs
  - test/assert-red.test.mjs
  - package.json
autonomous: true
requirements: [SEC-01, COLLAB-01]
must_haves:
  truths:
    - "All Phase-2 security claims have stable T2-01..T2-16 owners and executable blocking commands."
    - "Tests can create five real HA principals and multiple independently authenticated WebSocket connections without using client-supplied identity."
    - "Backend and browser effect ledgers fail on unintended service, network, shared-storage, token, cursor, subscription, or lifecycle effects."
  artifacts:
    - path: ".planning/phases/02-authoritative-policy-controls-collaboration/02-THREATS.md"
      provides: "Canonical ASVS L1 threat register"
      contains: "T2-16"
    - path: "tests/components/glt_flow_card/user_factory.py"
      provides: "Five-principal authenticated HA fixture factory"
    - path: "test/e2e/fixtures/fake-ha.mjs"
      provides: "Extended browser effect ledger"
    - path: "tools/assert-red.mjs"
      provides: "Strict Phase-2 named RED classifier"
    - path: "test/assert-red.test.mjs"
      provides: "Mutation tests for accepting only the intended RED identity/marker/evidence"
  key_links:
    - from: "tests/components/glt_flow_card/user_factory.py"
      to: "Home Assistant auth/WebSocket fixtures"
      via: "token-specific authenticated connections"
    - from: "package.json"
      to: "Phase-2 behavioral gates"
      via: "test:phase2:quick and outer test:phase2 scripts; Plan 02-17 adds the non-recursive T2-16 leaf"
---

## Phase Goal

**As a** shared-project user, **I want to** have every shared operation authorized by the Companion and every concurrent edit guarded, **so that** I can operate and engineer without privilege bypass or lost updates.

<objective>
Create the Wave-0 identity, effect-ledger, command, and threat scaffolding that every Phase-2 slice must prove before implementation.

Purpose: Make unsafe RED states distinguishable from fixture failure and make every later GREEN claim evidence-producing.
Output: Canonical T2 register, multi-principal HA fixtures, extended zero-effect ledgers, and Phase-2 script entry points.
</objective>

<execution_context>
@C:/Users/basti/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/basti/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-authoritative-policy-controls-collaboration/02-CONTEXT.md
@.planning/phases/02-authoritative-policy-controls-collaboration/02-RESEARCH.md
@.planning/phases/02-authoritative-policy-controls-collaboration/02-VALIDATION.md
@.planning/phases/02-authoritative-policy-controls-collaboration/02-THREATS.md
@.planning/phases/01-trusted-contract-release-foundation/01-12-SUMMARY.md
</context>

<red_contracts>
| Registry key | Exact sentinel identity | Exact marker | Required evidence prefix |
|---|---|---|---|
| `phase2-policy-matrix` | `tests/components/glt_flow_card/test_policy.py::test_expected_red_phase2_policy_matrix` | `EXPECTED_RED[phase2-policy-matrix]: centralized deny-default policy matrix is unavailable` | `PHASE2_POLICY_EFFECTS ` |
| `phase2-access-revocation` | `tests/components/glt_flow_card/test_project_access.py::test_expected_red_phase2_access_revocation` | `EXPECTED_RED[phase2-access-revocation]: server-owned access and revocation are unavailable` | `PHASE2_ACCESS_EFFECTS ` |
| `phase2-evidence-pagination` | `tests/components/glt_flow_card/test_evidence_pagination.py::test_expected_red_phase2_evidence_pagination` | `EXPECTED_RED[phase2-evidence-pagination]: scoped opaque evidence pagination is unavailable` | `PHASE2_CURSOR_EFFECTS ` |
| `phase2-leases` | `tests/components/glt_flow_card/test_project_leases.py::test_expected_red_phase2_leases` | `EXPECTED_RED[phase2-leases]: connection-bound engineering leases are unavailable` | `PHASE2_LEASE_EFFECTS ` |
| `phase2-collaboration-guard` | `tests/components/glt_flow_card/test_collaboration.py::test_expected_red_phase2_collaboration_guard` | `EXPECTED_RED[phase2-collaboration-guard]: immediate-precommit collaboration guard is unavailable` | `PHASE2_COLLAB_EFFECTS ` |
| `phase2-merge` | `tests/components/glt_flow_card/test_merge.py::test_expected_red_phase2_merge` | `EXPECTED_RED[phase2-merge]: bounded three-way merge recovery is unavailable` | `PHASE2_MERGE_EFFECTS ` |
| `phase2-configured-controls` | `tests/components/glt_flow_card/test_configured_controls.py::test_expected_red_phase2_configured_controls` | `EXPECTED_RED[phase2-configured-controls]: authoritative configured controls are unavailable` | `PHASE2_CONTROL_EFFECTS ` |
| `phase2-control-evidence` | `tests/components/glt_flow_card/test_control_evidence.py::test_expected_red_phase2_control_evidence` | `EXPECTED_RED[phase2-control-evidence]: trusted control evidence lifecycle is unavailable` | `PHASE2_EVIDENCE_EFFECTS ` |
| `phase2-migration-lifecycle` | `tests/components/glt_flow_card/test_phase2_lifecycle.py::test_expected_red_phase2_migration_lifecycle` | `EXPECTED_RED[phase2-migration-lifecycle]: conservative migration and resource cleanup are unavailable` | `PHASE2_LIFECYCLE_EFFECTS ` |
| `phase2-authority-reducers` | `[expected-red:phase2-authority-reducers]` | `EXPECTED_RED[phase2-authority-reducers]: fail-closed authority reducers are unavailable` | `PHASE2_REDUCER_EFFECTS ` |
| `phase2-ui-fixture-seed` | `[expected-red:phase2-ui-fixture-seed]` | `EXPECTED_RED[phase2-ui-fixture-seed]: exact-dist shared-authority UI is unavailable` | `PHASE2_FIXTURE_EFFECTS ` |
| `phase2-ui` | `[expected-red:phase2-ui]` | `EXPECTED_RED[phase2-ui]: complete exact-dist Phase-2 UI is unavailable` | `PHASE2_UI_EFFECTS ` |
</red_contracts>

<tasks>

<task type="auto">
  <name>Task 1: Lock the canonical ASVS threat/evidence contract</name>
  <files>.planning/phases/02-authoritative-policy-controls-collaboration/02-THREATS.md</files>
  <read_first>
    - .planning/phases/02-authoritative-policy-controls-collaboration/02-THREATS.md
    - .planning/phases/02-authoritative-policy-controls-collaboration/02-VALIDATION.md
    - .planning/config.json
  </read_first>
  <action>Validate that T2-01 through T2-16 are unique, stable, mapped to SEC-01/COLLAB-01, ASVS L1 V2-V6, an owner plan, and one behavioral command. Add a machine-checkable status/owner table if the executor needs it, but do not renumber or weaken any threat. Treat every HIGH threat, missing command, skip, zero-test run, live target, or non-zero unintended effect as release-blocking.</action>
  <acceptance_criteria>
    - T2-01..T2-16 appear exactly once in the canonical table and every row has an owner and command.
    - The ledger states the no-live-HA/remote/fieldbus/plant-write boundary and the fail-closed release rule.
  </acceptance_criteria>
  <verify><automated>py -3.13 -c "from pathlib import Path; p=Path('.planning/phases/02-authoritative-policy-controls-collaboration/02-THREATS.md').read_text(encoding='utf-8'); assert all(p.count(f'| T2-{i:02d} |') == 1 for i in range(1,17)); assert 'ASVS L1' in p and 'blocks release' in p"</automated></verify>
  <done>The threat register is a deterministic, complete release gate rather than advisory prose.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create authenticated multi-principal HA and lifecycle fixtures</name>
  <files>tests/components/glt_flow_card/user_factory.py, tests/components/glt_flow_card/conftest.py</files>
  <read_first>
    - tests/components/glt_flow_card/conftest.py
    - tests/components/glt_flow_card/test_websocket.py
    - .planning/phases/02-authoritative-policy-controls-collaboration/02-VALIDATION.md
  </read_first>
  <behavior>
    - Viewer, Operator, Engineer, Admin, HA-admin-without-membership, and unassigned users have distinct server identities and access tokens.
    - The same Engineer can open two distinct connections, a second Engineer can connect concurrently, and reconnect produces a new connection/session identity.
    - LifecycleEffects records and resets services, listeners, tasks, subscriptions, cursors, leases, control waits, rate buckets, stores, sessions, and late callbacks.
  </behavior>
  <action>RED: add focused fixture self-tests that fail until the user factory returns real token-authenticated HA WebSocket clients and the resource ledger accounts for every Phase-2 runtime resource. GREEN: implement reusable factories using the supported HA fixtures on both declared lanes; never accept a caller-authored actor as fixture truth. Keep the global service blocker and expose a named controlled fake service only to tests that explicitly request it. Commit RED and GREEN separately.</action>
  <acceptance_criteria>
    - Two connections for one user are distinguishable and one reconnect cannot reuse the prior session binding.
    - The controlled-service fixture captures exact domain/service/data/target/context and defaults to zero allowed service calls.
    - Teardown asserts all Phase-2 resource counters return to zero.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_init.py -q -x</automated></verify>
  <done>Later policy/lease/control tests can prove identity and zero-effect behavior on supported HA fixtures.</done>
</task>

<task type="auto">
  <name>Task 3: Extend browser effects and add strict Phase-2 RED command entry points</name>
  <files>test/e2e/fixtures/fake-ha.mjs, tools/assert-red.mjs, test/assert-red.test.mjs, package.json</files>
  <read_first>
    - test/e2e/fixtures/fake-ha.mjs
    - tools/run-exact-dist-playwright.mjs
    - tools/assert-red.mjs
    - package.json
    - .planning/phases/02-authoritative-policy-controls-collaboration/02-UI-SPEC.md
  </read_first>
  <action>Extend the exact-dist effect ledger to capture callService, WS request bodies, non-loopback network, localStorage, IndexedDB, sessionStorage, URL/history, clipboard, console, DOM text/attributes, diagnostics/export, subscriptions, and seeded token/current-project/other-user sentinels. Extend `tools/assert-red.mjs` with exact Phase-2 registry entries `phase2-policy-matrix`, `phase2-access-revocation`, `phase2-evidence-pagination`, `phase2-leases`, `phase2-collaboration-guard`, `phase2-merge`, `phase2-configured-controls`, `phase2-control-evidence`, `phase2-migration-lifecycle`, `phase2-authority-reducers`, `phase2-ui-fixture-seed`, and `phase2-ui`. Each entry must bind one literal `EXPECTED_RED[...]` marker, one task-specific effect-ledger prefix, and the exact expected identity: pytest sentinel nodeids use `test_expected_red_` plus the registry key with hyphens changed to underscores; Node/Playwright sentinel titles use `[expected-red:registry-key]`. Accept exit 1 only when exactly that named product-completeness sentinel fails after fixtures/effect assertions run; reject any unlisted FAILED/not-ok test, import/collection/syntax/launch/timeout/setup/teardown/zero-test/skip, prohibited effect, other RED marker, signal, or non-1 exit. Add `test/assert-red.test.mjs` that spawns deterministic fake outputs and mutation-tests the valid case plus every rejection class. Add `test:phase2:quick` and the outer `test:phase2` script that may initially execute these controlled RED suites but never skip or report zero tests. Do not assign T2-16 to the outer command: Plan 02-17 adds its sole non-recursive `test:phase2:release` leaf and the exact-once aggregation/cycle checks. Preserve every Phase-1 script and make no test reach a live HA, remote, bus, or plant.</action>
  <acceptance_criteria>
    - Effect assertions can distinguish permitted fake controlled-service attempts from every unintended effect.
    - Phase-2 scripts are additive and retain `test:phase1` unchanged.
    - Seeded secrets can be scanned across all declared browser sinks.
    - Every controlled Phase-2 RED command exits zero only for its exact named missing-behavior sentinel plus required zero-effect evidence; unrelated test or harness failure remains non-zero.
  </acceptance_criteria>
  <verify><automated>node --test test/assert-red.test.mjs test/phase1-gate.test.mjs &amp;&amp; node -e "const p=require('./package.json'); if(!p.scripts['test:phase2:quick']||!p.scripts['test:phase2']) process.exit(1)"</automated></verify>
  <done>One command can run Phase-2 gates, and browser tests can prove absence rather than infer it.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- `.planning/phases/02-authoritative-policy-controls-collaboration/02-THREATS.md`: canonical T2-01..T2-16 register.
- `tests/components/glt_flow_card/user_factory.py`: `Phase2UserFactory`, principal records, connection/session helpers.
- `tests/components/glt_flow_card/conftest.py`: controlled service and expanded `LifecycleEffects` fixtures.
- `test/e2e/fixtures/fake-ha.mjs`: extended exact-dist effect/leak ledger helpers.
- `test/assert-red.test.mjs`: strict classifier acceptance/rejection mutation tests.
- `package.json`: `test:phase2:quick` and `test:phase2` scripts.
</artifacts_this_phase_produces>

<threat_model>
| Threats | Disposition | Mitigation / test |
|---|---|---|
| T2-01..T2-16 | mitigate; HIGH/blocking | Stable owners, real multi-user identity, controlled-service default deny, comprehensive effect ledger, non-skippable scripts. |
</threat_model>

<verification>
Run the focused fixture tests, Phase-1 regression gate, threat uniqueness check, and inspect that no user setup or live endpoint was introduced.
</verification>

<success_criteria>
Wave-0 scaffolding is deterministic, both requirements are mapped, and later RED/GREEN plans can fail only for missing product behavior rather than missing identity/effect infrastructure.
</success_criteria>

<output>Create `.planning/phases/02-authoritative-policy-controls-collaboration/02-01-SUMMARY.md` when done.</output>
