---
phase: 02-authoritative-policy-controls-collaboration
plan: 15
type: execute
wave: 11
depends_on: [02-14]
files_modified:
  - tools/build.mjs
  - tools/stage-hacs-packages.mjs
  - tools/validate-hacs-staging.mjs
  - tools/test-ha-artifacts.mjs
  - tools/verify-release.mjs
  - package.json
  - .github/workflows/validate.yml
  - .github/workflows/hacs.yml
autonomous: true
requirements: [SEC-01, COLLAB-01]
must_haves:
  truths:
    - "One canonical build produces byte-identical dist, Companion www and editor artifacts from authored Phase-2 modules."
    - "Plugin and integration HACS stages plus the Companion ZIP contain exactly the allowed files and the same manifest-hashed bytes."
    - "Immutable HA 2024.8.0 and 2026.8.3 lanes execute the full Phase-2 backend matrix with zero unintended effects."
  artifacts:
    - path: "tools/test-ha-artifacts.mjs"
      provides: "Dual immutable-HA exact-stage executor"
    - path: "tools/validate-hacs-staging.mjs"
      provides: "Independent HACS/ZIP allowlist and hash validator"
    - path: ".github/workflows/hacs.yml"
      provides: "Read-only full-SHA exact-stage CI transfer"
  key_links:
    - from: "tools/build.mjs"
      to: "dist/glt-flow-card.js, custom_components/glt_flow_card/www/glt-flow-card.js, docs/editor/app.js"
      via: "one canonical authored-source build and exact SHA-256 comparisons"
---

## Phase Goal

**As a** shared-project user, **I want to** have every shared operation authorized by the Companion and every concurrent edit guarded, **so that** I can operate and engineer without privilege bypass or lost updates.

<objective>Regenerate, stage and install the exact Phase-2 card/Companion artifacts across HACS and both supported immutable HA lanes.</objective>
<execution_context>@C:/Users/basti/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/02-authoritative-policy-controls-collaboration/02-14-SUMMARY.md
@.planning/phases/01-trusted-contract-release-foundation/01-12-SUMMARY.md
@.planning/phases/01-trusted-contract-release-foundation/01-13-SUMMARY.md
@tools/verify-phase1.mjs
</context>

<tasks>
<task type="auto">
  <name>Task 1: Regenerate and independently validate exact build and HACS artifacts</name>
  <files>tools/build.mjs, tools/stage-hacs-packages.mjs, tools/validate-hacs-staging.mjs, package.json, .github/workflows/validate.yml, .github/workflows/hacs.yml</files>
  <read_first>
    - tools/build.mjs
    - tools/stage-hacs-packages.mjs
    - tools/validate-hacs-staging.mjs
    - package.json
    - .github/workflows/validate.yml
    - .github/workflows/hacs.yml
  </read_first>
  <action>Extend canonical source and packaging allowlists/manifests for every new authored JavaScript/Python module and authority/schema copy. Build once from authored sources, atomically replace then compare `dist/glt-flow-card.js`, Companion `www/glt-flow-card.js`, and `docs/editor/app.js`, and record exact source/output hashes plus clean/dirty provenance. Stage and independently validate both HACS categories and the component-relative Companion ZIP; reject missing/extra/symlink/traversal/case-collision/stale/generated bytes. Keep CI read-only, full-SHA, build-once and transfer only the same manifest-hashed stage; introduce no credential, public endpoint or live target.</action>
  <acceptance_criteria>
    - Two clean builds are byte-identical and all three checked-in generated card copies match the canonical output.
    - HACS plugin/integration stages and ZIP include exactly the Phase-2 runtime modules and no source, test, secret or mutable-path surplus.
    - CI consumes the locked dependency tree and exact build manifest without rebuilding a divergent artifact.
  </acceptance_criteria>
  <verify><automated>npm run build &amp;&amp; npm test &amp;&amp; npm run test:python &amp;&amp; npm run validate:hacs-staging</automated></verify>
  <done>Canonical and HACS artifacts are exact, clean and independently validated.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the exact stage on both immutable Home Assistant lanes</name>
  <files>tools/test-ha-artifacts.mjs, tools/verify-release.mjs, package.json, .github/workflows/validate.yml, .github/workflows/hacs.yml</files>
  <read_first>
    - tools/test-ha-artifacts.mjs
    - tools/verify-release.mjs
    - .planning/phases/02-authoritative-policy-controls-collaboration/02-VALIDATION.md
  </read_first>
  <action>Extend isolated artifact tests to install the exact manifest-hashed HACS stage and execute the complete policy/principal, HA-admin membership exception, ACL, subscription/cursor, lease/race/merge, configured-control/evidence, migration and lifecycle suites on immutable HA 2024.8.0 and 2026.8.3. Use only the controlled fake service and require zero unintended service/session/network/remote/fieldbus/plant effects, non-skipped/non-zero test counts and exact output/ZIP hashes. Extend the local release preflight to consume—not regenerate—the same stage. Keep `validate:hacs-staging`, `test:ha-artifacts`, and `verify:release` as non-recursive leaf helpers: none may invoke `test:phase2`, `tools/verify-phase2.mjs`, or `test:phase2:release`; Plan 02-17 alone aggregates them under T2-16.</action>
  <acceptance_criteria>
    - Both immutable lanes execute every Phase-2 backend owner suite with non-zero counts and no skip.
    - The unassigned-HA-admin membership tests and all ordinary unassigned content/control/audit denials pass on both HA APIs.
    - Release preflight and HA lanes consume the exact HACS manifest hashes produced by Task 1 and never rebuild.
    - No live Home Assistant, remote site, fieldbus or plant service is contacted.
  </acceptance_criteria>
  <verify><automated>npm run test:ha-artifacts &amp;&amp; npm run verify:release</automated></verify>
  <done>The prerequisite exact-artifact evidence for T2-16 passes both supported HA lanes and local release preflight without owning or invoking the outer Phase-2 gate.</done>
</task>
</tasks>

<artifacts_this_phase_produces>
- Exact canonical card/Companion/editor artifacts and manifest hashes.
- Independently validated HACS plugin/integration stages and Companion ZIP.
- Immutable HA 2024.8.0 and 2026.8.3 non-skipped behavioral evidence.
</artifacts_this_phase_produces>
<threat_model>
| Threats | Disposition | Mitigation / test |
|---|---|---|
| T2-01..T2-15 | mitigate; HIGH/blocking | Exact staged bytes execute every existing backend/browser owner suite. |
| T2-16 | mitigate; HIGH/blocking | Build-once hashes, independent HACS allowlists, immutable dual-HA install and no-live-effect gate. |
</threat_model>
<verification>Run canonical build, Node/Python, HACS validation, dual immutable HA and release-preflight gates; require a clean diff.</verification>
<success_criteria>The same exact Phase-2 bytes are proven across generated copies, HACS packaging and both supported HA lanes.</success_criteria>
<output>Create `.planning/phases/02-authoritative-policy-controls-collaboration/02-15-SUMMARY.md` when done.</output>
