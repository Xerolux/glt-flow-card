---
phase: 02-authoritative-policy-controls-collaboration
plan: 07
type: execute
wave: 4
depends_on: [02-06, 02-08]
files_modified:
  - custom_components/glt_flow_card/policy_sessions.py
  - custom_components/glt_flow_card/project_access.py
  - custom_components/glt_flow_card/__init__.py
  - tests/components/glt_flow_card/test_project_access.py
  - tests/components/glt_flow_card/test_policy_subscriptions.py
  - tests/components/glt_flow_card/test_evidence_pagination.py
autonomous: true
requirements: [SEC-01, COLLAB-01]
must_haves:
  truths:
    - "Project Admins and authenticated HA administrators can perform bounded fixed-role membership administration; HA administrators need no project assignment for this minimal surface but gain no project content/control/audit authority."
    - "Capability/project events reauthorize on every emission and revoke/remove detail immediately."
    - "Opaque pagination never exposes offsets/totals and cannot be replayed across authority/session scope."
  artifacts:
    - path: "custom_components/glt_flow_card/policy_sessions.py"
      provides: "Capability subscriptions, opaque cursor registry, scoped rate/resource lifecycle"
    - path: "custom_components/glt_flow_card/project_access.py"
      provides: "Atomic guarded membership APIs"
  key_links:
    - from: "custom_components/glt_flow_card/policy_sessions.py"
      to: "custom_components/glt_flow_card/policy.py"
      via: "reauthorization at event/page time"
---

## Phase Goal

**As a** shared-project user, **I want to** have every shared operation authorized by the Companion and every concurrent edit guarded, **so that** I can operate and engineer without privilege bypass or lost updates.

<objective>Deliver server-owned membership reads/changes, live capability revocation, and non-enumerating cursor sessions as one usable administration/observation slice.</objective>
<execution_context>@C:/Users/basti/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/02-authoritative-policy-controls-collaboration/02-06-SUMMARY.md @.planning/phases/02-authoritative-policy-controls-collaboration/02-02-SUMMARY.md</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: GREEN server-owned membership administration</name>
  <files>custom_components/glt_flow_card/project_access.py, custom_components/glt_flow_card/__init__.py, tests/components/glt_flow_card/test_project_access.py</files>
  <read_first>
    - tests/components/glt_flow_card/test_project_access.py
    - custom_components/glt_flow_card/project_access.py
    - custom_components/glt_flow_card/policy.py
  </read_first>
  <action>Implement eligible-user listing from Home Assistant and fixed-role assignment updates with exact `access_revision`, exact current content revision, maximum 512 assignments, active-user validation, fixed Viewer/Operator/Engineer/Admin roles, HA assignable ceilings, no arbitrary capability vectors, no self-grant, and no last-permitted-Admin removal/demotion. Bind resolved A2 explicitly: either an assigned project Admin within the HA ceiling or an authenticated HA administrator may use this minimal membership surface; the HA administrator may be unassigned, may obtain/validate only an administration-purpose lease, and may read only project ID, assignments, access revision, and the opaque content-revision guard—not project content, controls, trusted audit, telemetry, counts, or protected subscriptions. This exception permits first-Admin assignment and later recovery without becoming implicit membership. Bind resolved A3 by rechecking separate exact access/content revisions plus the shared administration-purpose engineering lease inside the access repository's per-project critical section immediately before persistence, then re-read policy/access/content state; Plan 02-09 will reuse and generalize this already-safe guard for every content mutation. GREEN ACL fault/restart/idempotence, unassigned-HA-admin bootstrap/recovery, and assigned-project-Admin cases.</action>
  <acceptance_criteria>
    - Content/import/rollback cannot change assignments.
    - Failed/stale/above-ceiling writes persist nothing; authorized guarded writes read back exactly.
    - An unassigned authenticated HA administrator can list the minimal membership inventory, acquire an administration-purpose lease, assign the first eligible fixed-role Admin, and recover membership without arbitrary IDs/capabilities; the same principal remains denied for content/control/audit/telemetry/subscription APIs before and after the membership operation unless separately assigned.
    - An assigned project Admin can manage membership only within the intersection of its fixed capability and HA ceiling; an ordinary unassigned user cannot enumerate even the membership inventory.
    - Resolved A3 remains exact: ACL commits advance only `access_revision`, content commits advance only `revision`, and the guarded membership commit validates both current values plus the shared purpose-bound lease without conflating the streams.</acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_project_access.py -q -x</automated></verify>
  <done>T2-05 is GREEN with mutation safety already fail-closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN per-event capability/project subscriptions</name>
  <files>custom_components/glt_flow_card/policy_sessions.py, custom_components/glt_flow_card/__init__.py, tests/components/glt_flow_card/test_policy_subscriptions.py</files>
  <read_first>
    - tests/components/glt_flow_card/test_policy_subscriptions.py
    - custom_components/glt_flow_card/policy.py
    - custom_components/glt_flow_card/__init__.py
  </read_first>
  <action>Implement at most eight subscriptions per connection with monotonic sequence, current policy/access revision, per-event `PolicyCoordinator` authorization, minimal revocation events, and immediate unsubscribe/detail removal on revoke/disconnect/unload. Never cache an authorization decision in a callback or expose owner/hidden counts. Bind callbacks to runtime generation so late events are ignored.</action>
  <acceptance_criteria>
    - Revocation/sequence-gap tests receive no protected event after authority changes.
    - Disconnect/unload removes every listener/subscription and late callbacks are inert.</acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_policy_subscriptions.py -q -x</automated></verify>
  <done>Open subscriptions cannot outlive current authorization.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN scoped opaque cursor registry</name>
  <files>custom_components/glt_flow_card/policy_sessions.py, custom_components/glt_flow_card/__init__.py, tests/components/glt_flow_card/test_evidence_pagination.py</files>
  <read_first>
    - tests/components/glt_flow_card/test_evidence_pagination.py
    - custom_components/glt_flow_card/policy_sessions.py
  </read_first>
  <action>Implement server-state opaque cursors with page exactly 50, five-minute idle expiry, maximum 32/connection and 256/integration, injected monotonic clock/token factory, binding to user/connection/session/project/filter/access-policy revision, constant-time token-digest lookup, and deterministic lazy prune/unload cleanup. Reauthorize before each page and return no hidden total.</action>
  <acceptance_criteria>
    - Replay/tamper/restart/revoke/cross-scope cases return `cursor_invalid` without rows or count leak.
    - Bounds and cleanup pass below/equal/above tests.</acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_evidence_pagination.py -q -x</automated></verify>
  <done>T2-04 cursor/subscription paths are GREEN.</done>
</task>
</tasks>

<artifacts_this_phase_produces>
- `policy_sessions.py`: `PolicySessionRegistry`, `CapabilitySubscription`, `OpaqueCursorRegistry`, cursor/subscription cleanup APIs.
- `project_access.py`: eligible-member and exact access-revision mutation APIs.
- `__init__.py`: members/snapshot/subscribe/cursor route adapters.
</artifacts_this_phase_produces>
<threat_model>
| Threats | Disposition | Mitigation / test |
|---|---|---|
| T2-04 | mitigate; HIGH/blocking | Per-event reauth, minimal revocation, source filtering, scoped opaque cursors. |
| T2-05 | mitigate; HIGH/blocking | Separate access store, fixed roles/HA ceiling/exact revision/lease-required writes. |
| T2-15 | mitigate | Runtime-generation cleanup hooks covered now and closed finally in 02-14. |
</threat_model>
<verification>Run access, subscription, cursor, policy and lifecycle-focused suites; all denied paths must have zero effects.</verification>
<success_criteria>Admins can safely inspect/manage authorized access, users receive live authority changes, and pagination/subscriptions cannot leak after revocation.</success_criteria>
<output>Create `.planning/phases/02-authoritative-policy-controls-collaboration/02-07-SUMMARY.md` when done.</output>
