---
phase: 02-authoritative-policy-controls-collaboration
plan: 13
type: tdd
wave: 9
depends_on: [02-09, 02-11, 02-12]
files_modified:
  - src/v100/project-collaboration.mjs
  - src/v100/configured-control.mjs
  - src/v100/project-safety.js
  - src/v100/project-safety-i18n.mjs
  - src/v100/entry.js
  - test/phase2-collaboration.test.mjs
  - test/e2e/project-authority.spec.mjs
autonomous: true
requirements: [SEC-01, COLLAB-01]
must_haves:
  truths:
    - "Two browsers share concurrent reads but exactly one exclusive editor, and reconnect/expiry/role loss never resurrects a token or overwrites data."
    - "A dirty candidate remains in session memory through conflict/authority loss and can be discarded, refreshed/retried, or non-destructively merged."
    - "Configured control UI sends only control ID plus declared input and reports accepted/dispatched/confirmed/timeout/denied/failed without optimistic success or automatic retry."
  artifacts:
    - path: "src/v100/project-collaboration.mjs"
      provides: "Lease/candidate/conflict/merge state machine and in-memory token vault"
      exports: ["createCollaborationController", "reduceCollaboration"]
    - path: "src/v100/configured-control.mjs"
      provides: "Configured-control preview/confirm/result state machine"
      exports: ["createConfiguredControlClient", "reduceControlEvidence"]
  key_links:
    - from: "src/v100/project-collaboration.mjs"
      to: "server lease/mutation/merge routes"
      via: "fresh capability/head plus memory-only token and exact revisions"
---

## Phase Goal

**As a** shared-project user, **I want to** have every shared operation authorized by the Companion and every concurrent edit guarded, **so that** I can operate and engineer without privilege bypass or lost updates.

<objective>Complete the exact-dist two-session collaboration and configured-control user slice with candidate safety, accessibility and no fallback.</objective>
<execution_context>@C:/Users/basti/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/02-authoritative-policy-controls-collaboration/02-UI-SPEC.md @.planning/phases/02-authoritative-policy-controls-collaboration/02-05-SUMMARY.md @.planning/phases/02-authoritative-policy-controls-collaboration/02-12-SUMMARY.md</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: GREEN collaboration, lease and candidate state machines</name>
  <files>src/v100/project-collaboration.mjs, src/v100/entry.js, test/phase2-collaboration.test.mjs</files>
  <read_first>
    - test/phase2-collaboration.test.mjs
    - src/v100/project-authority.mjs
    - src/v100/project-diff.mjs
  </read_first>
  <action>Confirm RED, then implement an in-memory-only bearer vault and pure collaboration reducer for available/acquiring/held-self/held-other/renewing/expired/lost states, current/expected/base/candidate revisions, and candidate clean/dirty/preserved/merge-preview/applied/discarded states. Dirty lease auto-renew begins at 40% remaining, manual renewal is offered at 50%, clean idle does not auto-renew. Reconnect discards token, refreshes capability/head, reacquires, and rebases; errors preserve candidate. Merge sends only opaque preview and selected stable IDs; no client patch/LWW. Commit GREEN.</action>
  <acceptance_criteria>
    - Bearer is never part of reducer/render/export/telemetry state and old tokens are never retried.
    - Candidate clears only after authoritative success or explicit accessible destructive discard.
    - Second conflict returns to preserved conflict state without patch replay.</acceptance_criteria>
  <verify><automated>node --test test/phase2-collaboration.test.mjs</automated></verify>
  <done>T2-12/T2-13 browser collaboration logic is GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Add lease bar and conflict recovery to Migrate & compare</name>
  <files>src/v100/project-safety.js, src/v100/project-safety-i18n.mjs, test/e2e/project-authority.spec.mjs</files>
  <read_first>
    - src/v100/project-safety.js
    - src/v100/project-collaboration.mjs
    - .planning/phases/02-authoritative-policy-controls-collaboration/02-UI-SPEC.md
  </read_first>
  <action>Add `EngineeringLeaseBar`, `CandidateStateChip`, `RevisionTriplet`, `ConflictRecoveryPanel`, and `MergePreview` to the existing Migrate & compare tab without weakening Phase-1 validation/diff/backup prerequisites. Support acquire/renew/release, anonymous held elsewhere, expiry/disconnect/role loss, candidate-preserving close, refresh/retry, dependency-locked semantic selection, second conflict, and explicit discard. Use native keyboard-operable controls, focus restoration, polite/assertive announcements, responsive table/card conversion and complete DE/EN copy. No owner/token leak or storage persistence.</action>
  <acceptance_criteria>
    - Two contexts prove one editor, concurrent reads, deterministic expiry/release, and no lost update.
    - Conflict UI shows bounded authorized evidence and every recovery path preserves or explicitly discards candidate.</acceptance_criteria>
  <verify><automated>node tools/run-exact-dist-playwright.mjs --grep=two-session</automated></verify>
  <done>COLLAB-01 is usable and visible in the exact generated card.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN configured-control confirmation and evidence states</name>
  <files>src/v100/configured-control.mjs, src/v100/project-safety.js, src/v100/project-safety-i18n.mjs, src/v100/entry.js, test/e2e/project-authority.spec.mjs</files>
  <read_first>
    - test/phase2-authority.test.mjs
    - src/v100/project-authority.mjs
    - .planning/phases/02-authoritative-policy-controls-collaboration/02-UI-SPEC.md
  </read_first>
  <action>Implement reusable `ConfiguredControlConfirm` and `ControlEvidenceStatus` primitives at existing control affordances, without the deferred rich Phase-4 object panel. Client sends only control ID/current head plus declared input to preview, renders server-normalized target summary read-only, confirms safely with Cancel focused, then reports authoritative accepted/dispatched/readback-confirmed/timed-out/denied/failed/cancelled-before-dispatch states and correlation. Timeout/result-unknown directs the user to current state/evidence and never auto-retries or calls `hass.callService`. Complete DE/EN, keyboard, 320px/200%, forced-colors/reduced-motion cases.</action>
  <acceptance_criteria>
    - DOM/forms/WS bodies contain no editable or caller-authored domain/service/target fields.
    - Evidence UI agrees with backend sequence and never calls accepted/dispatched confirmed.
    - Malicious input/authority loss/timeout/failure asserts zero unintended or duplicate effects.</acceptance_criteria>
  <verify><automated>node tools/run-exact-dist-playwright.mjs --grep=phase-2-ui</automated></verify>
  <done>T2-14 full exact-dist interaction/accessibility/effect contract is GREEN.</done>
</task>
</tasks>

<artifacts_this_phase_produces>
- `project-collaboration.mjs`: memory-only token vault, collaboration reducer/controller, merge orchestration.
- `configured-control.mjs`: configured-control client/reducer, confirmation/result models.
- `project-safety.js`: lease/candidate/revision/conflict/merge/control components.
- `project-safety-i18n.mjs`: complete DE/EN lease/conflict/control/recovery copy.
- `entry.js`: collaboration/control module wiring.
</artifacts_this_phase_produces>
<threat_model>
| Threats | Disposition | Mitigation / test |
|---|---|---|
| T2-06, T2-08 | mitigate; HIGH/blocking | Read-only server preview, exact result states, no fallback/retry. |
| T2-10, T2-12, T2-13 | mitigate; HIGH/blocking | Memory-only rotated lease, candidate preservation, fresh reconnect, no LWW. |
| T2-14 | mitigate; HIGH/blocking | Two-context exact-dist DE/EN/a11y/leak/effect suite. |
</threat_model>
<verification>Node reducers plus exact-dist two-session and phase-2-ui groups pass; all seeded bearer/other-user/current-project sentinels are absent from forbidden sinks.</verification>
<success_criteria>Two engineers cannot overwrite one another and operators can safely use configured controls with honest authoritative evidence.</success_criteria>
<output>Create `.planning/phases/02-authoritative-policy-controls-collaboration/02-13-SUMMARY.md` when done.</output>
