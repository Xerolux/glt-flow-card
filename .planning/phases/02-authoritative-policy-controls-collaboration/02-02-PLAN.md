---
phase: 02-authoritative-policy-controls-collaboration
plan: 02
type: tdd
wave: 1
depends_on: [02-01]
files_modified:
  - tests/components/glt_flow_card/test_policy.py
  - tests/components/glt_flow_card/test_policy_enumeration.py
  - tests/components/glt_flow_card/test_project_access.py
  - tests/components/glt_flow_card/test_policy_subscriptions.py
  - tests/components/glt_flow_card/test_evidence_pagination.py
autonomous: true
requirements: [SEC-01, COLLAB-01]
must_haves:
  truths:
    - "The complete registered-route/principal matrix has a controlled failing specification before policy implementation."
    - "Missing and unauthorized objects, aggregates, cursors, and subscription events are behaviorally indistinguishable where enumeration would leak."
    - "Project content cannot create or elevate server-owned ACL assignments, while HA-admin minimal membership administration can bootstrap a first fixed-role assignment without self-grant or content access."
  artifacts:
    - path: "tests/components/glt_flow_card/test_policy.py"
      provides: "Command inventory and fixed role/HA ceiling oracle"
    - path: "tests/components/glt_flow_card/test_project_access.py"
      provides: "ACL ownership and access revision scenarios"
    - path: "tests/components/glt_flow_card/test_policy_subscriptions.py"
      provides: "Per-event reauthorization scenarios"
  key_links:
    - from: "tests/components/glt_flow_card/test_policy.py"
      to: "COMMAND_POLICIES and registered commands"
      via: "exact set equality and generated allow/deny cases"
---

## Phase Goal

**As a** shared-project user, **I want to** have every shared operation authorized by the Companion and every concurrent edit guarded, **so that** I can operate and engineer without privilege bypass or lost updates.

<objective>Write controlled RED behavioral contracts for centralized authorization, access ownership, non-enumeration, subscriptions, and cursors before any backend security implementation.</objective>

<execution_context>@C:/Users/basti/.codex/gsd-core/workflows/execute-plan.md</execution_context>

<context>
@.planning/phases/02-authoritative-policy-controls-collaboration/02-THREATS.md
@.planning/phases/02-authoritative-policy-controls-collaboration/02-VALIDATION.md
@custom_components/glt_flow_card/__init__.py
@tests/components/glt_flow_card/user_factory.py
@tools/assert-red.mjs
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Specify deny-default command and principal behavior</name>
  <files>tests/components/glt_flow_card/test_policy.py, tests/components/glt_flow_card/test_policy_enumeration.py</files>
  <read_first>
    - custom_components/glt_flow_card/__init__.py
    - .planning/phases/02-authoritative-policy-controls-collaboration/02-RESEARCH.md
    - tests/components/glt_flow_card/user_factory.py
  </read_first>
  <behavior>
    - Registered `glt_flow_card/*` routes exactly equal the policy manifest; duplicates/undeclared handlers fail startup/tests.
    - Viewer/Operator/Engineer/Admin/unassigned and HA-admin-without-membership receive the fixed allow/deny result for every route; the HA-admin-only membership inventory/read/write and administration-purpose lease cells are allowed without granting content/control/audit authority.
    - Client identity/role/capability/timestamp/project authority mutations never change server actor or authorization.
    - Missing and unauthorized item/collection/count/search/audit/control/remote results have the stable non-enumerating shape and zero effects.
  </behavior>
  <action>Create parameterized integration tests from runtime registration, not source-token assertions. Assert stable codes and response shapes, filtered source results, HA entity read/control ceilings, and zero store/service/subscription changes on denial. Bind resolved A6: every remote route is declared and returns the same fail-closed unavailable contract until Phase 9. Finish with the exact sentinel nodeid/marker/evidence registered as `phase2-policy-matrix`; emit it only after the complete matrix and zero-effect ledger execute, so `tools/assert-red.mjs` rejects fixture, import, syntax, collection, extra-test, or unrelated assertion failures. Run and commit this controlled RED.</action>
  <acceptance_criteria>
    - Every current route and every principal is generated into at least one allow and one deny/missing-object case as applicable.
    - Resolved A6 is enforced: remote routes are policy-declared but fail closed unavailable until Phase 9, with no legacy remote transport call.
  </acceptance_criteria>
  <verify><automated>node tools/assert-red.mjs --expected=phase2-policy-matrix -- py -3.13 -m pytest tests/components/glt_flow_card/test_policy.py tests/components/glt_flow_card/test_policy_enumeration.py -q</automated></verify>
  <done>RED evidence names T2-01..T2-04 product gaps and reports no prohibited effect.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Specify server-owned access and subscription revocation</name>
  <files>tests/components/glt_flow_card/test_project_access.py, tests/components/glt_flow_card/test_policy_subscriptions.py</files>
  <read_first>
    - custom_components/glt_flow_card/project_repository.py
    - custom_components/glt_flow_card/project_transactions.py
    - .planning/phases/02-authoritative-policy-controls-collaboration/02-CONTEXT.md
  </read_first>
  <behavior>
    - ACL is independent of project JSON; import/rollback/content edits cannot self-grant.
    - Access revision is exact; eligible users come from HA; arbitrary capabilities, self-grant, last-admin removal, and above-ceiling roles reject atomically.
    - Every subscription event reauthorizes; role/access change sends a minimal sequenced revocation and no later protected detail.
  </behavior>
  <action>Add bootstrap/read/save/read-back/restart fault injection and multi-user subscription tests. Bind resolved A2: an authenticated HA administrator without a project assignment can list only the minimal eligible-user/current-assignment/access-revision data and perform guarded fixed-role membership administration, including first-Admin assignment, but cannot read project content, control, trusted audit, telemetry, counts, or subscription detail. Cover malformed legacy ACL, repeated migration, inactive/deleted users, 512-member bound, sequence gaps, disconnect cleanup, and same render-visible revocation data without owner/token leak. Finish with the exact `phase2-access-revocation` sentinel only after effect assertions execute; commit controlled RED.</action>
  <acceptance_criteria>
    - Failed ACL writes expose no partial/elevated membership.
    - Subscription revocation removes detail and resource teardown reaches zero.
    - Resolved A2 prevents bootstrap deadlock without turning HA administration into implicit project content/control/audit authority.
  </acceptance_criteria>
  <verify><automated>node tools/assert-red.mjs --expected=phase2-access-revocation -- py -3.13 -m pytest tests/components/glt_flow_card/test_project_access.py tests/components/glt_flow_card/test_policy_subscriptions.py -q</automated></verify>
  <done>T2-05 and subscription portions of T2-04 have executable RED cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Specify opaque scoped evidence pagination</name>
  <files>tests/components/glt_flow_card/test_evidence_pagination.py</files>
  <read_first>
    - custom_components/glt_flow_card/project_repository.py
    - .planning/phases/02-authoritative-policy-controls-collaboration/02-UI-SPEC.md
  </read_first>
  <behavior>
    - Page size is exactly 50 with no hidden total.
    - Cursor is bound to user, connection/session, project/filter, access/policy revision, and five-minute idle expiry.
    - Cross-user/connection/project/filter/restart/tamper/revocation replay returns `cursor_invalid` without data or state growth.
    - Registry bounds are 32 per connection and 256 per integration with deterministic eviction/cleanup.
  </behavior>
  <action>Create boundary tests at below/equal/above limits and restart/unload cases. Bind resolved A5: cursors are short-lived server-state tokens and restart invalidates them rather than decoding or resuming a self-contained offset. Use injected clock/token seams. Finish with the exact `phase2-evidence-pagination` sentinel only after zero information/service effect evidence is emitted; commit controlled RED.</action>
  <acceptance_criteria>
    - Cursor tests never inspect or rely on a caller-readable offset.
    - Revoked users cannot receive the next page or infer total/hidden rows.
    - Resolved A5 is enforced: restart always invalidates the server-state cursor and requires a fresh authorized first page.
  </acceptance_criteria>
  <verify><automated>node tools/assert-red.mjs --expected=phase2-evidence-pagination -- py -3.13 -m pytest tests/components/glt_flow_card/test_evidence_pagination.py -q</automated></verify>
  <done>Opaque pagination has deterministic RED coverage before implementation.</done>
</task>
</tasks>

<artifacts_this_phase_produces>
- `test_policy.py`: command/principal manifest oracle.
- `test_policy_enumeration.py`: object/collection non-enumeration matrix.
- `test_project_access.py`: ACL/HA-ceiling/access-revision contract.
- `test_policy_subscriptions.py`: per-event reauthorization contract.
- `test_evidence_pagination.py`: opaque cursor/bounds contract.
</artifacts_this_phase_produces>

<threat_model>
| Threats | Disposition | Mitigation / test |
|---|---|---|
| T2-01, T2-02, T2-03 | mitigate; HIGH/blocking | Complete runtime-generated route/principal matrix. |
| T2-04, T2-05 | mitigate; HIGH/blocking | Source filtering, uniform errors, server ACLs, revocation/cursor replay tests. |
</threat_model>

<verification>Each new test command reaches a controlled product RED with valid fixtures and a zero-effect ledger.</verification>
<success_criteria>Backend authorization behavior is fully specified before Plan 02-06 turns it GREEN.</success_criteria>
<output>Create `.planning/phases/02-authoritative-policy-controls-collaboration/02-02-SUMMARY.md` when done.</output>
