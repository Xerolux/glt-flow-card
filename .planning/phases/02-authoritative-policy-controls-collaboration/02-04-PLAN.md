---
phase: 02-authoritative-policy-controls-collaboration
plan: 04
type: tdd
wave: 1
depends_on: [02-01]
files_modified:
  - tests/components/glt_flow_card/test_configured_controls.py
  - tests/components/glt_flow_card/test_control_evidence.py
  - tests/components/glt_flow_card/test_trusted_evidence.py
  - tests/components/glt_flow_card/test_phase2_migration.py
  - tests/components/glt_flow_card/test_phase2_lifecycle.py
autonomous: true
requirements: [SEC-01, COLLAB-01]
must_haves:
  truths:
    - "Configured-control requests cannot choose service or target and all malformed/bounded/rate cases prove zero service calls."
    - "Trusted control/audit evidence is server-authored and distinct from bounded untrusted telemetry."
    - "Legacy migration and lifecycle cleanup are specified before new persistent/runtime resources exist."
  artifacts:
    - path: "tests/components/glt_flow_card/test_configured_controls.py"
      provides: "Exact current-head control normalization and zero-call matrix"
    - path: "tests/components/glt_flow_card/test_control_evidence.py"
      provides: "Dispatch/readback/failure evidence matrix"
    - path: "tests/components/glt_flow_card/test_phase2_lifecycle.py"
      provides: "Runtime resource-zero contract"
  key_links:
    - from: "tests/components/glt_flow_card/test_control_evidence.py"
      to: "controlled HA service fixture"
      via: "one-attempt capture and readback fault injection"
---

## Phase Goal

**As a** shared-project user, **I want to** have every shared operation authorized by the Companion and every concurrent edit guarded, **so that** I can operate and engineer without privilege bypass or lost updates.

<objective>Write controlled RED tests for exact configured controls, trusted evidence/telemetry separation, conservative migration, and complete lifecycle cleanup.</objective>
<execution_context>@C:/Users/basti/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/02-authoritative-policy-controls-collaboration/02-VALIDATION.md
@.planning/phases/02-authoritative-policy-controls-collaboration/02-THREATS.md
@custom_components/glt_flow_card/__init__.py
@custom_components/glt_flow_card/project_repository.py
@tools/assert-red.mjs
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Specify configured-control normalization and bounds</name>
  <files>tests/components/glt_flow_card/test_configured_controls.py</files>
  <read_first>
    - custom_components/glt_flow_card/__init__.py
    - schemas/project.schema.json
    - .planning/phases/02-authoritative-policy-controls-collaboration/02-RESEARCH.md
  </read_first>
  <behavior>
    - Request accepts only `project_id`, current revision/digest, `control_id`, and the declared bounded user input.
    - Server resolves exact domain/service and disjoint entity/device/area target plus immutable data from verified current head and supplies authenticated HA context.
    - Unknown/override/template/nested/unsafe/simulation/maintenance/permission failures reject before zero service calls.
    - Boundaries: 4 KiB, depth 4, 64 nodes, 16 keys, 512-char strings, arrays 32; preview 30/user/project/min; execute 10/min burst 3.
  </behavior>
  <action>Build below/equal/above and malicious mutation cases against the named fake service only. Bind resolved A1 exactly to the declared 4 KiB/depth 4/64 nodes/16 keys/512-character/array 32 and preview/execute rate boundaries, and resolved A4 by proving Operator configured-control execution uses fresh capability+revision/digest+preview+HA permission but no engineering lease. Cover stale head/control removal, multiple target forms, HA entity permission, preview/execute separation, rate eviction, and no caller-authored `service_data.entity_id`. Finish with the exact `phase2-configured-controls` sentinel only after zero-call evidence; commit controlled RED.</action>
  <acceptance_criteria>
    - Every rejection asserts an empty controlled-service attempt ledger.
    - One valid case asserts exact domain/service/data/target/context bytes/shape.
    - Resolved A1/A4 are normative: the exact declared bounds are release gates and operational controls do not acquire or validate an engineering lease.
  </acceptance_criteria>
  <verify><automated>node tools/assert-red.mjs --expected=phase2-configured-controls -- py -3.13 -m pytest tests/components/glt_flow_card/test_configured_controls.py -q</automated></verify>
  <done>T2-06 and T2-07 are behaviorally specified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Specify control evidence and trusted/untrusted provenance</name>
  <files>tests/components/glt_flow_card/test_control_evidence.py, tests/components/glt_flow_card/test_trusted_evidence.py</files>
  <read_first>
    - custom_components/glt_flow_card/project_repository.py
    - custom_components/glt_flow_card/project_transactions.py
    - .planning/phases/02-authoritative-policy-controls-collaboration/02-UI-SPEC.md
  </read_first>
  <behavior>
    - Accepted must be durable before dispatch; dispatched, readback-confirmed, timed-out, denied, failed-before, and failed/result-unknown after dispatch are distinct correlation-stable events.
    - Missing/late/wrong readback or permission loss never becomes confirmed; listeners clean up; no automatic repeat.
    - Trusted event max 8 KiB/32 MiB retention; telemetry max 4 KiB, 30/user/min burst 10, 1,000 rows/4 MiB; separate stores/cursors/filters/exports.
    - Client actor/time/result/control claims remain untrusted and cannot enter trusted evidence.
  </behavior>
  <action>Add durable-store and service/readback barriers before accepted append, before handler, during/after dispatch, after state change, after evidence append, restart repair, and telemetry injection/rate/size boundaries. Bind resolved A1 to the exact trusted/telemetry byte, row, retention, and rate constants. Assert one service attempt maximum and forward evidence repair only. Finish with the exact `phase2-control-evidence` sentinel only after trusted/untrusted and service-effect evidence; commit controlled RED.</action>
  <acceptance_criteria>
    - Failure before durable accepted event dispatches nothing.
    - Failure after dispatch records result-unknown/repair state without redispatch.
    - Trusted and telemetry outputs can never be combined or relabeled.</acceptance_criteria>
  <verify><automated>node tools/assert-red.mjs --expected=phase2-control-evidence -- py -3.13 -m pytest tests/components/glt_flow_card/test_control_evidence.py tests/components/glt_flow_card/test_trusted_evidence.py -q</automated></verify>
  <done>T2-08 and T2-09 are behaviorally specified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Specify Phase-2 migration and runtime cleanup</name>
  <files>tests/components/glt_flow_card/test_phase2_migration.py, tests/components/glt_flow_card/test_phase2_lifecycle.py</files>
  <read_first>
    - custom_components/glt_flow_card/const.py
    - custom_components/glt_flow_card/__init__.py
    - tests/components/glt_flow_card/test_init.py
    - tests/components/glt_flow_card/test_options.py
  </read_first>
  <behavior>
    - Active-head legacy permissions bootstrap once conservatively; imported candidates never bootstrap ACL; legacy locks clear; monolithic audit becomes `legacy_untrusted`; options TTL tightens idempotently to 60..900.
    - Old generated browser/new Companion reports incompatible/read-only, never fallback.
    - Setup/reload/unload marks unavailable first, cancels/unsubscribes/clears every subscription/task/cursor/lease/rate bucket/control wait, ignores late callbacks, and re-setup uses a fresh generation.
  </behavior>
  <action>Add before/after persistence failures, malformed/repeated migration, upgrade/rollback artifact shape, active-resource unload, late-event and ghost-command tests. Keep all services fake and assert zero unintended effects. Finish with the exact `phase2-migration-lifecycle` sentinel only after migration and resource-ledger evidence; commit controlled RED.</action>
  <acceptance_criteria>
    - Migration is copy-safe, idempotent, bounded, and never elevates.
    - Resource ledger is zero after unload and old tokens/cursors/callbacks stay invalid.</acceptance_criteria>
  <verify><automated>node tools/assert-red.mjs --expected=phase2-migration-lifecycle -- py -3.13 -m pytest tests/components/glt_flow_card/test_phase2_migration.py tests/components/glt_flow_card/test_phase2_lifecycle.py -q</automated></verify>
  <done>T2-15 has controlled RED coverage before runtime changes.</done>
</task>
</tasks>

<artifacts_this_phase_produces>
- `test_configured_controls.py`: exact service/target/input/rate contract.
- `test_control_evidence.py`: dispatch/readback/evidence fault matrix.
- `test_trusted_evidence.py`: trusted versus untrusted storage/provenance/bounds contract.
- `test_phase2_migration.py`: legacy access/lock/audit/options/compatibility migration contract.
- `test_phase2_lifecycle.py`: Phase-2 runtime resource ledger contract.
</artifacts_this_phase_produces>
<threat_model>
| Threats | Disposition | Mitigation / test |
|---|---|---|
| T2-06, T2-07 | mitigate; HIGH/blocking | Current-head resolution, strict bounds/rates, exact zero-call assertions. |
| T2-08, T2-09 | mitigate; HIGH/blocking | Durable accepted event, one-attempt/forward repair, store separation. |
| T2-15 | mitigate; HIGH/blocking | Conservative migration and resource-zero lifecycle failure injection. |
</threat_model>
<verification>All suites reach controlled RED only for missing product behavior and every denied/fault path has zero unintended effects.</verification>
<success_criteria>Control, evidence, migration, and lifecycle invariants are test-first and release-blocking.</success_criteria>
<output>Create `.planning/phases/02-authoritative-policy-controls-collaboration/02-04-SUMMARY.md` when done.</output>
