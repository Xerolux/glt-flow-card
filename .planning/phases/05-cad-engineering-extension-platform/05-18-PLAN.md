---
phase: 05-cad-engineering-extension-platform
plan: 18
type: execute
wave: 4
depends_on: [05-17]
files_modified:
  - custom_components/glt_flow_card/__init__.py
  - tools/stage-hacs-packages.mjs
  - tools/validate-hacs-staging.mjs
  - test/hacs-staging.test.mjs
autonomous: true
requirements: [SDK-01]
must_haves:
  truths:
    - "Removal is refused while a project still references a contribution."
    - "Unload leaves no registration and no resolvable stale symbol."
---

## Phase Goal

**As a** maintainer, **I want** a reload to leave nothing behind, **so that** a long-running installation does not accumulate extension state.

<objective>Close the Phase-5 lifecycle and packaging obligations.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Lifecycle and referential safety</name>
  <files>custom_components/glt_flow_card/__init__.py</files>
  <action>Add the registry to the generation-stamped invalidation and the resource ledger. Make the runtime unavailable before releasing. Refuse removal of a pack a project still references, naming the referrers — a dangling symbol reference is a diagram that silently stops meaning something.</action>
  <acceptance_criteria>
    - After unload the ledger reports zero registrations.
    - A late callback is inert rather than raising.
    - Removal while referenced is refused and names the referring projects.
    - A stale symbol is not resolvable after removal.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_phase5_lifecycle.py -q -x</automated></verify>
  <done>T5-15 is GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Three packaging lists</name>
  <files>tools/stage-hacs-packages.mjs, tools/validate-hacs-staging.mjs, test/hacs-staging.test.mjs</files>
  <action>Add `sdk_manifest.py`, `sdk_registry.py`, `ports.py` and `schemas/project/4.schema.json` to the stager, the independent validator and the drift guard. Import the staged component the way the HA lane does.</action>
  <acceptance_criteria>
    - All three lists contain every new module and schema.
    - Importing the staged component succeeds.
    - Removing an entry from any one list fails the drift guard.
  </acceptance_criteria>
  <verify><automated>npm run validate:hacs-staging && node --test test/hacs-staging.test.mjs</automated></verify>
  <done>The packaging gap Phase 3 found cannot recur for Phase-5 modules.</done>
</task>
</tasks>

<output>Create `05-18-SUMMARY.md` when done.</output>
