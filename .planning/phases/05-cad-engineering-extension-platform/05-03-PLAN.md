---
phase: 05-cad-engineering-extension-platform
plan: 3
type: execute
wave: 1
depends_on: [05-01]
files_modified:
  - test/routing-determinism.test.mjs
  - test/routing-geometry.test.mjs
  - test/routing-incremental.test.mjs
  - test/designer-transactions.test.mjs
  - test/designer-clipboard.test.mjs
  - test/sdk-manifest.test.mjs
  - test/e2e/project-cad.spec.mjs
autonomous: true
requirements: [ENG-02, CAD-01, SDK-01]
must_haves:
  truths:
    - "A router that is not deterministic cannot be tested for anything else."
    - "Undo is proven as a property over generated sequences, not one click-path."
    - "A contribution that executes fails the ledger, not a code review."
---

## Phase Goal

**As a** maintainer, **I want** routing, designer and SDK guarantees specified before any of them exist, **so that** each is built against a contract.

<objective>Write the routing, designer and SDK RED sentinels.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Routing sentinels</name>
  <files>test/routing-determinism.test.mjs, test/routing-geometry.test.mjs, test/routing-incremental.test.mjs</files>
  <action>Specify, as failing tests: byte-identical routes across repeated runs; bounded path change under a one-pixel move; obstacle avoidance over the corpus; routes leaving the side a port declares; medium honoured; stable junctions across unrelated reroutes; explicit crossings; parallel spacing; and a bound on segments recomputed per interaction, asserted as a number.</action>
  <acceptance_criteria>
    - Determinism is asserted before geometry, so a flaky router fails first.
    - The incremental bound is a number, not a shrug.
    - Each file fails exactly once.
  </acceptance_criteria>
  <verify><automated>node tools/phase5-red-gate.mjs</automated></verify>
  <done>The three routing sentinels report controlled RED.</done>
</task>

<task type="auto">
  <name>Task 2: Designer sentinels</name>
  <files>test/designer-transactions.test.mjs, test/designer-clipboard.test.mjs</files>
  <action>Specify, as failing tests: `invert(apply(s, c), c)` equals `s` over a generated corpus of operation sequences covering every command kind; a failed operation leaving state untouched; a bounded undo depth; cross-project paste remapping ids and rewriting every internal reference; and a bounded clipboard payload.</action>
  <acceptance_criteria>
    - The inverse property is checked over generated sequences, not scripted paths.
    - A paste that keeps a source id fails.
    - Each file fails exactly once.
  </acceptance_criteria>
  <verify><automated>node tools/phase5-red-gate.mjs</automated></verify>
  <done>phase5-designer and phase5-clipboard report controlled RED.</done>
</task>

<task type="auto">
  <name>Task 3: SDK and exact-dist sentinels</name>
  <files>test/sdk-manifest.test.mjs, test/e2e/project-cad.spec.mjs</files>
  <action>Specify, as failing tests: a manifest validated against a closed schema with element and attribute allowlists; a pack shipping script, an `onload` attribute, a remote `href` or a `foreignObject` all refused; and the exact-dist matrix for `phase-5-catalog`, `phase-5-routing`, `phase-5-designer` and `phase-5-sdk` including the continuous keyboard scenario.</action>
  <acceptance_criteria>
    - The validator allowlists rather than denylists.
    - The keyboard scenario is one continuous traversal.
    - Each file fails exactly once.
  </acceptance_criteria>
  <verify><automated>node tools/phase5-red-gate.mjs</automated></verify>
  <done>phase5-sdk and phase5-ui report controlled RED; all sentinels are RED.</done>
</task>
</tasks>

<output>Create `05-03-SUMMARY.md` when done.</output>
