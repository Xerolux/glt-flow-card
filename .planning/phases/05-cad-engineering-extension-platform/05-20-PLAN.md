---
phase: 05-cad-engineering-extension-platform
plan: 20
type: execute
wave: 10
depends_on: [05-01, 05-02, 05-03, 05-04, 05-05, 05-06, 05-07, 05-08, 05-09, 05-10, 05-11, 05-12, 05-13, 05-14, 05-15, 05-16, 05-17, 05-18, 05-19]
files_modified:
  - tools/verify-phase5.mjs
  - test/phase5-gate.test.mjs
  - .planning/phases/05-cad-engineering-extension-platform/05-THREATS.md
  - .planning/phases/05-cad-engineering-extension-platform/05-VALIDATION.md
autonomous: true
requirements: [CAT-01, ENG-01, ENG-02, CAD-01, SDK-01]
must_haves:
  truths:
    - "The gate binds every requirement, roadmap truth, plan, threat and assumption to current evidence."
    - "The command graph is acyclic with exactly one path to the release leaf."
    - "A threat row is marked verified only after its owner command has actually run and passed."
---

## Phase Goal

**As a** maintainer, **I want** one command that proves Phase 5 is done, **so that** closure is an evidence check rather than a judgement call.

<objective>Build the Phase-5 gate and close the phase honestly.</objective>

<tasks>
<task type="auto">
  <name>Task 1: The orchestrator</name>
  <files>tools/verify-phase5.mjs</files>
  <action>Follow the Phase-4 orchestrator. Bind CAT-01, ENG-01, ENG-02, CAD-01 and SDK-01, the five roadmap truths, all twenty plans, T5-01..T5-16 and the six resolved assumptions to current evidence. Prove the graph acyclic with exactly one path to the `test:phase5:release` leaf owned by T5-16, with F5-06 being `npm run test:phase4`. Generating this from the Phase-4 tool introduces two predictable bugs — a roadmap slice whose bounds collapse to the same string, and a plan regex still matching the previous phase number — so check both before trusting a green load.</action>
  <acceptance_criteria>
    - The graph check returns exactly one path to the leaf.
    - Every plan, threat, requirement and assumption resolves to a command that exists.
    - The roadmap slice and plan regex are verified to return non-empty text.
    - No command reachable from the leaf re-invokes the gate.
  </acceptance_criteria>
  <verify><automated>node tools/verify-phase5.mjs</automated></verify>
  <done>The plan loads and the graph is proven acyclic.</done>
</task>

<task type="auto">
  <name>Task 2: Gate mutation tests</name>
  <files>test/phase5-gate.test.mjs</files>
  <action>Mutate the gate as the Phase-4 tests do: an unbound requirement, an unowned threat, a missing plan, a duplicated command, an introduced cycle, a second path to the leaf, and a threat marked verified without evidence. Each must fail the gate.</action>
  <acceptance_criteria>
    - Every mutation fails; none passes silently.
    - A threat marked verified with no passing owner command is rejected by the gate itself.
  </acceptance_criteria>
  <verify><automated>node --test test/phase5-gate.test.mjs</automated></verify>
  <done>The gate cannot be satisfied by a document edit alone.</done>
</task>

<task type="auto">
  <name>Task 3: Close the phase honestly</name>
  <files>.planning/phases/05-cad-engineering-extension-platform/05-THREATS.md, .planning/phases/05-cad-engineering-extension-platform/05-VALIDATION.md</files>
  <action>Run every runnable owner command at head. Mark only the rows whose owner actually passed. Leave T5-16 planned if Docker is unavailable and write the Evidence Status note. Write 05-SUMMARY.md and the phase's .continue-here.md, and update STATE.md, HANDOFF.json and HANDOFF.md in the same commit.</action>
  <acceptance_criteria>
    - No row is marked verified on the strength of its parts passing separately.
    - The Evidence Status note names the environment limit rather than implying a defect.
    - If the catalog evidence reports fewer than 300 distinct variants, that is reported and symbols are added -- the test is not adjusted.
  </acceptance_criteria>
  <verify><automated>node tools/phase5-red-gate.mjs && node tools/verify-phase5.mjs</automated></verify>
  <done>Phase 5 is closed with an honest evidence record.</done>
</task>
</tasks>

<output>Create `05-20-SUMMARY.md` when done.</output>
