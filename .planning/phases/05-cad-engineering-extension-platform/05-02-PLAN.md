---
phase: 05-cad-engineering-extension-platform
plan: 2
type: execute
wave: 1
depends_on: [05-01, 05-04]
files_modified:
  - test/catalog-evidence.test.mjs
  - test/port-compatibility.test.mjs
  - test/port-identity.test.mjs
autonomous: true
requirements: [CAT-01, ENG-01]
must_haves:
  truths:
    - "Each RED file carries one sentinel and reports every unmet guarantee as a gap."
    - "Distinctness is asserted by digest, never by inspection."
---

## Phase Goal

**As a** maintainer, **I want** the catalog and port guarantees written as failing tests first, **so that** the implementation has a specification it cannot narrow.

<objective>Write the catalog and port RED sentinels.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Catalog evidence sentinel</name>
  <files>test/catalog-evidence.test.mjs</files>
  <action>Specify, as a failing test: a generated manifest whose count comes from rendering every variant; per-base geometry digests all distinct; per-style token digests all distinct; the published count at least 300; and fire/electrical representation present as base symbols rather than as style multiples.</action>
  <acceptance_criteria>
    - The count assertion reads the generated manifest, never an array length.
    - Two bases with identical geometry would fail.
    - The file fails exactly once with its marker.
  </acceptance_criteria>
  <verify><automated>node tools/phase5-red-gate.mjs</automated></verify>
  <done>phase5-catalog reports controlled RED.</done>
</task>

<task type="auto">
  <name>Task 2: Port compatibility and identity sentinels</name>
  <files>test/port-compatibility.test.mjs, test/port-identity.test.mjs</files>
  <action>Specify, as failing tests: every incompatible pair in the corpus refused with a stable reason code and no bare boolean anywhere; multiplicity enforced; and endpoint identity surviving an edit, a copy/paste, a bundle round trip and a 3→4 migration.</action>
  <acceptance_criteria>
    - A refusal without a reason code fails.
    - The identity test covers all four survival paths, not one.
    - Each file fails exactly once.
  </acceptance_criteria>
  <verify><automated>node tools/phase5-red-gate.mjs</automated></verify>
  <done>phase5-ports and phase5-port-identity report controlled RED.</done>
</task>
</tasks>

<output>Create `05-02-SUMMARY.md` when done.</output>
