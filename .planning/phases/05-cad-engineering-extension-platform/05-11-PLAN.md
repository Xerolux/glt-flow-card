---
phase: 05-cad-engineering-extension-platform
plan: 11
type: execute
wave: 6
depends_on: [05-10]
files_modified:
  - src/v100/routing.mjs
  - src/v040-extension.part02
autonomous: true
requirements: [ENG-02]
must_haves:
  truths:
    - "Only routes whose obstacle set changed are recomputed."
    - "The work per interaction is bounded by a number that is asserted."
---

## Phase Goal

**As an** engineer dragging a pump, **I want** the editor to stay responsive, **so that** a large diagram is still editable.

<objective>Make rerouting incremental and bounded, and retire the full-sweep reroute.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Incremental rerouting</name>
  <files>src/v100/routing.mjs</files>
  <action>Keep a spatial index of obstacles and, per route, the corridor it occupies. A move invalidates only routes whose corridor intersects the changed region. Assert the bound as segments recomputed per interaction — a wall-clock bound would be a capacity claim, and Phase 10 owns those.</action>
  <acceptance_criteria>
    - Moving one object recomputes only the routes near it, asserted by count.
    - The recomputed set is proven correct: a full recompute produces the same result.
    - The bound is a number in the test, not a description.
  </acceptance_criteria>
  <verify><automated>node --test test/routing-incremental.test.mjs</automated></verify>
  <done>The full-sweep reroute is no longer needed.</done>
</task>

<task type="auto">
  <name>Task 2: Retire the eight-line router</name>
  <files>src/v040-extension.part02</files>
  <action>Replace `autoRoute` and `reroute` with the new router, keeping the old entry points reachable so a test proves the replacement supersedes them rather than deleting them into a place nothing checks. Prove in the exact artifact that a drag does not freeze the editor.</action>
  <acceptance_criteria>
    - No path is routed by the midpoint Z-shape any more.
    - A drag in the exact artifact completes without a frozen frame.
    - The old entry points produce the new router's output.
  </acceptance_criteria>
  <verify><automated>node tools/run-exact-dist-playwright.mjs --grep=phase-5-routing</automated></verify>
  <done>T5-08 is GREEN.</done>
</task>
</tasks>

<output>Create `05-11-SUMMARY.md` when done.</output>
