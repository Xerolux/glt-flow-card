---
phase: 05-cad-engineering-extension-platform
plan: 7
type: execute
wave: 2
depends_on: [05-02, 05-04]
files_modified:
  - src/v100/ports.mjs
autonomous: true
requirements: [ENG-01]
must_haves:
  truths:
    - "Compatibility returns a reason code, never a bare boolean."
    - "Port kinds and reason codes are frozen closed sets."
---

## Phase Goal

**As an** engineer, **I want** the tool to tell me why a connection is impossible, **so that** I can tell whether the tool or my diagram is wrong. Nothing checks compatibility today, so today every impossible connection is drawn.

<objective>Make an impossible connection impossible, and explain it.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Typed ports and explained compatibility</name>
  <files>src/v100/ports.mjs</files>
  <action>Export frozen `PORT_KINDS` (process, signal, power), `MULTIPLICITY` and `REFUSAL_REASONS`. Implement `checkCompatibility(source, target, existing)` returning `{compatible, reason, detail}`: medium mismatch, direction conflict, kind mismatch, multiplicity exceeded, self-connection, and duplicate connection. An unknown kind is an error, not a passthrough.</action>
  <acceptance_criteria>
    - Every refusal carries a code from the frozen set.
    - Every incompatible pair in the corpus is refused for the right reason.
    - A compatible pair is accepted, so the check is not vacuously strict.
  </acceptance_criteria>
  <verify><automated>node --test test/port-compatibility.test.mjs</automated></verify>
  <done>T5-04 is GREEN.</done>
</task>
</tasks>

<output>Create `05-07-SUMMARY.md` when done.</output>
