---
phase: 05-cad-engineering-extension-platform
plan: 1
type: execute
wave: 0
depends_on: []
files_modified:
  - tools/phase5-red-gate.mjs
  - tools/assert-red.mjs
  - package.json
  - test/e2e/fixtures/fake-ha.mjs
  - tests/components/glt_flow_card/cad_factory.py
autonomous: true
requirements: [CAT-01, ENG-01, ENG-02, CAD-01, SDK-01]
must_haves:
  truths:
    - "A Phase-5 sentinel is accepted only with its named marker and a matching effect ledger."
    - "The effect ledger refuses any evaluation of contributed content and any contributed network fetch."
    - "The CAD corpus contains obstacles that force a router to make a real decision."
---

## Phase Goal

**As a** maintainer, **I want** the Phase-5 evidence machinery before any feature, **so that** every later plan has one honest way to fail.

<objective>Stand up the Phase-5 gate, corpus and extended effect ledger.</objective>

<tasks>
<task type="auto">
  <name>Task 1: The Phase-5 sentinel classifier</name>
  <files>tools/phase5-red-gate.mjs, tools/assert-red.mjs, package.json</files>
  <action>Register the Phase-5 sentinels and their owner commands, following the Phase-4 gate exactly. Add `test:phase5`, `test:phase5:quick` and `test:phase5:release`. Extend the registry invariant test with the Phase-5 count.</action>
  <acceptance_criteria>
    - A harness failure is rejected, not counted as controlled RED.
    - Every Phase-5 entry conforms to the sentinel-identity rule.
    - The gate reports controlled RED, implemented or broken per sentinel.
  </acceptance_criteria>
  <verify><automated>node tools/phase5-red-gate.mjs</automated></verify>
  <done>The gate runs and reports every Phase-5 sentinel.</done>
</task>

<task type="auto">
  <name>Task 2: The CAD corpus</name>
  <files>tests/components/glt_flow_card/cad_factory.py</files>
  <action>Build a schema-4 project whose geometry forces real routing decisions: equipment directly between two ports so a straight line is wrong, two routes sharing a corridor so spacing matters, a route that must cross another, and a junction where three routes meet. Include ports of every kind and multiplicity, and one deliberately incompatible pair.</action>
  <acceptance_criteria>
    - A naive midpoint router visibly fails at least three fixtures.
    - Every port kind and multiplicity value appears.
    - The corpus validates against the schema and the semantic model.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_cad_factory.py -q -x</automated></verify>
  <done>The corpus can express every geometry case the register names.</done>
</task>

<task type="auto">
  <name>Task 3: The Phase-5 effect ledger</name>
  <files>test/e2e/fixtures/fake-ha.mjs</files>
  <action>Extend the browser effect ledger to record and refuse `eval`, `new Function`, dynamic `import()` of a contributed path, `<script>` insertion, and any network fetch attributed to a contribution. Seed one deliberate violation of each to prove the ledger fails.</action>
  <acceptance_criteria>
    - Each seeded violation fails the ledger.
    - A clean run reports empty for every new counter.
    - Effects carry an origin so a contributed effect is distinguishable from a first-party one.
  </acceptance_criteria>
  <verify><automated>node tools/run-exact-dist-playwright.mjs --grep=phase-5-ledger-seed</automated></verify>
  <done>T5-12's evidence path exists before the code it will judge.</done>
</task>
</tasks>

<output>Create `05-01-SUMMARY.md` when done.</output>
