---
phase: 05-cad-engineering-extension-platform
plan: 4
type: execute
wave: 0
depends_on: []
files_modified:
  - schemas/project/4.schema.json
  - src/v100/project-migrations.mjs
  - custom_components/glt_flow_card/project_migrations.py
  - tests/components/glt_flow_card/cad_factory.py
  - tools/generate-project-validators.mjs
autonomous: true
requirements: [ENG-01, SDK-01]
must_haves:
  truths:
    - "A port has a validated closed shape carrying kind and multiplicity."
    - "The 3 to 4 migration is sequential, receipted and lossless."
    - "Contributions are a validated collection, not an open object."
    - "The CAD corpus contains obstacles that force a router to make a real decision."
---

## Phase Goal

**As an** engineer, **I want** the project to record which ports a connection means, **so that** an endpoint can survive an edit at all.

<objective>Introduce schema 4 and the sequential 3 to 4 migration.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Schema 4</name>
  <files>schemas/project/4.schema.json, tools/generate-project-validators.mjs</files>
  <action>Generate schema 4 from schema 3, preserving every collection. Give `profile.ports` a validated closed shape -- id, medium, direction, side, `kind` and `multiplicity` -- replacing the current `$ref: openObject`, which validates nothing. `from_port`/`to_port` already exist on `path` and are already consumed by `smartRoute`, so the path shape needs no change. Add a `contributions` collection with a closed contribution shape. Raise the schema_version maximum and register the schema with the validator generator. Derive every schema index rather than using a literal, since Phase 3 found four latent bugs from literal indices.</action>
  <acceptance_criteria>
    - No index into the schema list is a literal.
    - The contribution shape is deny-default, so an unknown field cannot appear.
    - Both runtimes compile the new validator.
  </acceptance_criteria>
  <verify><automated>npm run generate:contract:validators && node --test test/v100-contract.test.mjs</automated></verify>
  <done>Schema 4 exists and validates in both runtimes.</done>
</task>

<task type="auto">
  <name>Task 2: The 3 to 4 migration</name>
  <files>src/v100/project-migrations.mjs, custom_components/glt_flow_card/project_migrations.py</files>
  <action>Add `stepThreeToFour` and its Python mirror on the existing sequential, receipted, dry-run-first machinery. Derive `from_port`/`to_port` where a single unambiguous port exists on each side, and leave them absent where it is ambiguous rather than guessing — a guessed endpoint is the detachment this phase exists to prevent.</action>
  <acceptance_criteria>
    - An ambiguous endpoint is left absent and reported, never guessed.
    - Nothing in a schema-3 project is dropped.
    - Both runtimes produce identical receipts over the corpus.
  </acceptance_criteria>
  <verify><automated>node --test test/v100-migrations.test.mjs && py -3.13 -m pytest tests/components/glt_flow_card/test_project_migrations.py -q -x</automated></verify>
  <done>Schema 3 projects migrate losslessly.</done>
</task>
<task type="auto">
  <name>Task 3: The CAD corpus</name>
  <files>tests/components/glt_flow_card/cad_factory.py</files>
  <action>Build a schema-4 project whose geometry forces real routing decisions: equipment directly between two ports so a straight line is wrong, two routes sharing a corridor so spacing matters, a route that must cross another, and a junction where three routes meet. Include ports of every kind and multiplicity, and one deliberately incompatible pair.</action>
  <acceptance_criteria>
    - A naive midpoint router visibly fails at least three fixtures.
    - Every port kind and multiplicity value appears.
    - The corpus validates against the schema and the semantic model.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_cad_factory.py -q -x</automated></verify>
  <done>The corpus can express every geometry case the register names.</done>
</task>
</tasks>

<output>Create `05-04-SUMMARY.md` when done.</output>
