---
phase: 05-cad-engineering-extension-platform
plan: 12
type: execute
wave: 2
depends_on: [05-03, 05-04]
files_modified:
  - src/v100/designer-commands.mjs
autonomous: true
requirements: [CAD-01]
must_haves:
  truths:
    - "Every operation is a value with an inverse, proven over generated sequences."
    - "A failed operation leaves the diagram exactly as it was."
---

## Phase Goal

**As an** engineer, **I want** every edit to be undoable, **so that** I can work without fear of an irreversible mistake.

<objective>Model editing as commands, so undo is a consequence rather than a feature.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Commands and their inverses</name>
  <files>src/v100/designer-commands.mjs</files>
  <action>Export a frozen `COMMAND_KINDS` and implement `apply` and `invert` for each: move, resize, add, delete, reorder, group, ungroup, layer visibility, layer lock, align, distribute, connect, disconnect, master instantiate. Assert `invert(apply(s, c), c) === s` over a generated corpus of sequences covering every kind. Bound the undo depth.</action>
  <acceptance_criteria>
    - The inverse property holds for every command kind over generated sequences.
    - A rejected command leaves state byte-identical.
    - An unknown command kind is an error, not a no-op.
    - The undo depth bound is asserted.
  </acceptance_criteria>
  <verify><automated>node --test test/designer-transactions.test.mjs</automated></verify>
  <done>T5-09 is GREEN.</done>
</task>
</tasks>

<output>Create `05-12-SUMMARY.md` when done.</output>
