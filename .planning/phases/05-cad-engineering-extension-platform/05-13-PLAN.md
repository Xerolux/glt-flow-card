---
phase: 05-cad-engineering-extension-platform
plan: 13
type: execute
wave: 3
depends_on: [05-12]
files_modified:
  - src/v100/designer-clipboard.mjs
autonomous: true
requirements: [CAD-01]
must_haves:
  truths:
    - "Paste remaps every id and rewrites every internal reference with it."
    - "The clipboard payload is bounded before it is interpreted."
---

## Phase Goal

**As an** engineer, **I want** to copy a subsystem into another project, **so that** I do not redraw what I have already drawn.

<objective>Make cross-project copy/paste safe by construction.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Id-remapping clipboard</name>
  <files>src/v100/designer-clipboard.mjs</files>
  <action>The existing paste in `showCAD` regenerates an id from `Date.now()` and `Math.random()` and rewrites no reference to the old one, so a pasted connection still points at the source objects and the same paste is not reproducible. Replace it. Serialize a selection with its equipment, ports, connections and group structure. On paste, mint fresh ids and rewrite every internal reference — connection endpoints, group membership, master references, layer assignment — using the same map. Bound the payload before parsing.</action>
  <acceptance_criteria>
    - No pasted object keeps a source id, and id minting is deterministic rather than clock- and random-seeded.
    - Every internal reference resolves after paste.
    - A payload over the bound is refused before interpretation.
    - Pasting into the source project produces a genuine copy, not an alias.
  </acceptance_criteria>
  <verify><automated>node --test test/designer-clipboard.test.mjs</automated></verify>
  <done>T5-10 is GREEN.</done>
</task>
</tasks>

<output>Create `05-13-SUMMARY.md` when done.</output>
