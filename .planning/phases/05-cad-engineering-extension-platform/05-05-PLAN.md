---
phase: 05-cad-engineering-extension-platform
plan: 5
type: execute
wave: 2
depends_on: [05-02, 05-04]
files_modified:
  - tools/generate-catalog-evidence.mjs
  - src/v100/catalog.mjs
  - src/v100/core.mjs
  - catalog-evidence.json
autonomous: true
requirements: [CAT-01]
must_haves:
  truths:
    - "The published count is generated by rendering, never read from an array length."
    - "Base geometry and style tokens are each proven distinct by digest."
    - "Fire and electrical representation exist as base symbols."
---

## Phase Goal

**As a** buyer, **I want** the catalog count to be evidence, **so that** the number in the documentation is one somebody proved.

<objective>Turn CAT-01's count into generated evidence, and fill the domain hole.</objective>

<tasks>
<task type="auto">
  <name>Task 1: The evidence generator</name>
  <files>tools/generate-catalog-evidence.mjs, catalog-evidence.json</files>
  <action>`core.mjs:symbolCatalogStats()` already reports a count, by measuring array lengths -- that is the claim this replaces. Render every variant, digest each base's geometry and each style's token set, and write a canonical manifest with the count, the digests and the per-category breakdown. Add a `--check` mode, as the validator and parity-corpus generators have, so a stale manifest fails in the Node suite.</action>
  <acceptance_criteria>
    - The count in the manifest equals the number of rendered variants.
    - All base geometry digests are distinct; all style token digests are distinct.
    - `--check` fails when the manifest is stale, proven by seeding one.
  </acceptance_criteria>
  <verify><automated>node tools/generate-catalog-evidence.mjs --check && node --test test/catalog-evidence.test.mjs</automated></verify>
  <done>T5-01 and T5-02 are GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Fire and electrical representation</name>
  <files>src/v100/catalog.mjs</files>
  <action>Add base symbols for fire and electrical representation — detection, suppression, fire-rated separation, distribution, protection and isolation — with distinct geometry. Multiplying an absent domain by six styles leaves it absent, so this is base symbols, not styles.</action>
  <acceptance_criteria>
    - Every new symbol has a geometry digest distinct from every existing one.
    - The category breakdown shows fire and electrical as their own categories.
    - The published count rises and the manifest records the new number.
  </acceptance_criteria>
  <verify><automated>node tools/generate-catalog-evidence.mjs && node --test test/catalog-evidence.test.mjs</automated></verify>
  <done>The required domain spread is complete and proven.</done>
</task>
</tasks>

<output>Create `05-05-SUMMARY.md` when done.</output>
