---
phase: 05-cad-engineering-extension-platform
plan: 9
type: execute
wave: 3
depends_on: [05-03, 05-07]
files_modified:
  - src/v100/routing.mjs
autonomous: true
requirements: [ENG-02]
must_haves:
  truths:
    - "The same diagram routes to the same bytes, every time."
    - "A one-pixel move changes a bounded amount of the path."
---

## Phase Goal

**As an** engineer, **I want** the diagram to look the same when I open it again, **so that** I can trust what changed to be what I changed.

<objective>Establish routing determinism, the precondition for every other routing claim.</objective>

<tasks>
<task type="auto">
  <name>Task 1: A deterministic router core</name>
  <files>src/v100/routing.mjs</files>
  <action>Write the router as a pure function of `{ports, obstacles, options}` with no clock, no randomness and no iteration over an unordered collection. Every tie is broken by a declared rule — lower id first, then lower coordinate — so two runs cannot disagree. Assert byte equality across repeated runs and bounded path change under a one-pixel move.</action>
  <acceptance_criteria>
    - Repeated runs over the corpus produce byte-identical routes.
    - A one-pixel move changes at most the declared number of segments.
    - No tie is resolved by collection order.
  </acceptance_criteria>
  <verify><automated>node --test test/routing-determinism.test.mjs</automated></verify>
  <done>T5-06 is GREEN, and the other routing plans have something testable to build on.</done>
</task>
</tasks>

<output>Create `05-09-SUMMARY.md` when done.</output>
