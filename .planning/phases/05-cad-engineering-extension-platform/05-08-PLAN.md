---
phase: 05-cad-engineering-extension-platform
plan: 8
type: execute
wave: 4
depends_on: [05-07]
files_modified:
  - src/v100/ports.mjs
  - custom_components/glt_flow_card/ports.py
  - src/v100/project-bundle.mjs
autonomous: true
requirements: [ENG-01]
must_haves:
  truths:
    - "An endpoint is a port id, and geometry is derived from it."
    - "Endpoint identity survives edits, copy/paste, bundles and migration."
---

## Phase Goal

**As an** engineer, **I want** my connections to still mean what I meant after I move things, **so that** a diagram survives being worked on.

<objective>Make endpoint identity stable across every path that could break it.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Stable endpoint identity</name>
  <files>src/v100/ports.mjs, custom_components/glt_flow_card/ports.py, src/v100/project-bundle.mjs</files>
  <action>Resolve a connection through `{equipment_id, port_id}` in both runtimes, deriving geometry from the resolved port. Carry port ids through bundle export and import, and through the 3→4 migration. A port id that no longer resolves is reported as a broken endpoint with both ends named, never silently reattached.</action>
  <acceptance_criteria>
    - An edit that moves equipment leaves both endpoints resolving to the same ports.
    - A bundle round trip preserves every endpoint.
    - A cross-project paste remaps ids and endpoints follow the remap.
    - An unresolvable endpoint is reported, not reattached.
  </acceptance_criteria>
  <verify><automated>node --test test/port-identity.test.mjs && py -3.13 -m pytest tests/components/glt_flow_card/test_port_identity.py -q -x</automated></verify>
  <done>T5-05 is GREEN.</done>
</task>
</tasks>

<output>Create `05-08-SUMMARY.md` when done.</output>
