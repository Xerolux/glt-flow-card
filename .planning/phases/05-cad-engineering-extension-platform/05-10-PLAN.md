---
phase: 05-cad-engineering-extension-platform
plan: 10
type: execute
wave: 5
depends_on: [05-09]
files_modified:
  - src/v100/routing.mjs
autonomous: true
requirements: [ENG-02]
must_haves:
  truths:
    - "A route leaves a port on the side the port declares."
    - "A route never crosses equipment."
    - "Junctions and crossings are distinct and stable."
---

## Phase Goal

**As an** engineer, **I want** routes that go around plant instead of through it, **so that** the drawing means what a drawing is supposed to mean.

<objective>Make the geometry correct: obstacles, sides, media, junctions, crossings, spacing.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Obstacle-aware orthogonal routing</name>
  <files>src/v100/routing.mjs</files>
  <action>Route on a grid derived from obstacle bounds with a declared clearance. Leave and enter on the sides the ports declare. Refuse to route between incompatible media, deferring to the Phase-5 compatibility reasons rather than inventing a second rule set. Bound segments per route and routes per view.</action>
  <acceptance_criteria>
    - No route in the corpus intersects an obstacle.
    - Every route leaves and enters on the declared sides.
    - A route between incompatible media is refused with the compatibility reason.
    - The corpus fixtures a midpoint router fails are all routed correctly.
  </acceptance_criteria>
  <verify><automated>node --test test/routing-geometry.test.mjs</automated></verify>
  <done>Obstacle avoidance and port direction are GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Junctions, crossings and parallel spacing</name>
  <files>src/v100/routing.mjs</files>
  <action>Merge shared trunks into junctions with stable positions; draw a crossing as an explicit hop distinct from a junction; separate parallel runs in a shared corridor by a declared spacing. Junction positions must not move when an unrelated route is rerouted.</action>
  <acceptance_criteria>
    - A junction stays put across an unrelated reroute.
    - A crossing is structurally distinguishable from a junction.
    - Two routes in one corridor never overlap.
    - All of it stays deterministic: the 05-09 assertions still pass.
  </acceptance_criteria>
  <verify><automated>node --test test/routing-geometry.test.mjs && node --test test/routing-determinism.test.mjs</automated></verify>
  <done>T5-07 is GREEN.</done>
</task>
</tasks>

<output>Create `05-10-SUMMARY.md` when done.</output>
