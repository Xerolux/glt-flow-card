---
phase: 05-cad-engineering-extension-platform
plan: 14
type: execute
wave: 7
depends_on: [05-12, 05-13]
files_modified:
  - src/v100/project-designer.js
  - src/v100/entry.js
  - src/v040-extension.part03
  - src/v040-extension.part04
autonomous: true
requirements: [CAD-01]
must_haves:
  truths:
    - "The designer emits commands and mutates nothing directly."
    - "No destructive editor path uses a window dialog."
---

## Phase Goal

**As an** engineer, **I want** the editing tools in the shipped card, **so that** the workflow exists where I actually draw.

<objective>Ship the designer surfaces and retire the legacy editor dialogs.</objective>

<tasks>
<task type="auto">
  <name>Task 1: The designer surfaces</name>
  <files>src/v100/project-designer.js, src/v100/entry.js</files>
  <action>Define `glt-flow-card-designer-canvas`, `glt-flow-card-layer-panel`, `glt-flow-card-minimap` and `glt-flow-card-port-inspector` against the command model. Selection, guides, snapping, alignment, distribution, lasso, groups, nested groups and masters all emit commands. Wire into `entry.js`; both languages complete.</action>
  <acceptance_criteria>
    - No surface mutates project state directly.
    - Every capability CAD-01 names is reachable.
    - Both languages carry the same keys.
  </acceptance_criteria>
  <verify><automated>npm run build && node --test test/exact-dist.test.mjs</automated></verify>
  <done>The designer surfaces ship in dist/glt-flow-card.js.</done>
</task>

<task type="auto">
  <name>Task 2: Retire the legacy editor dialogs</name>
  <files>src/v040-extension.part03, src/v040-extension.part04</files>
  <action>Replace the `confirm()` and `alert()` calls on destructive editor paths with the Phase-2 `glt-flow-card-control-confirm` element. Phase 4 deliberately left these for this phase; retire them the same way, reachable and provably inert.</action>
  <acceptance_criteria>
    - No destructive editor path calls window.confirm or alert.
    - The effect ledger reports zero dialogs for the designer surfaces.
    - Every retired path still reaches its replacement.
  </acceptance_criteria>
  <verify><automated>node tools/run-exact-dist-playwright.mjs --grep=phase-5-designer</automated></verify>
  <done>The last authorization-shaped browser dialogs are gone.</done>
</task>
</tasks>

<output>Create `05-14-SUMMARY.md` when done.</output>
