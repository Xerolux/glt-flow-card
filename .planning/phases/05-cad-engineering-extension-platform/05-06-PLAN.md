---
phase: 05-cad-engineering-extension-platform
plan: 6
type: execute
wave: 4
depends_on: [05-05]
files_modified:
  - test/e2e/project-cad.spec.mjs
  - src/v100/project-catalog.js
autonomous: true
requirements: [CAT-01]
must_haves:
  truths:
    - "Every state is legible in every style, by contrast and by non-colour cue."
    - "The browser displays the generated count, never one it computed."
---

## Phase Goal

**As an** operator, **I want** every symbol readable in whichever style my site uses, **so that** a theme choice never costs me legibility.

<objective>Prove the catalog renders legibly, and ship the browser.</objective>

<tasks>
<task type="auto">
  <name>Task 1: State and contrast evidence</name>
  <files>test/e2e/project-cad.spec.mjs</files>
  <action>For every style, render a representative symbol in every operational state and assert a contrast ratio against its background and a non-colour cue for the state. Cover forced-colors mode.</action>
  <acceptance_criteria>
    - A state distinguished only by colour fails.
    - A symbol below the contrast threshold in any style fails.
    - Forced-colors mode keeps every symbol visible.
  </acceptance_criteria>
  <verify><automated>node tools/run-exact-dist-playwright.mjs --grep=phase-5-catalog</automated></verify>
  <done>T5-03 is GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: The symbol browser</name>
  <files>src/v100/project-catalog.js</files>
  <action>Define `glt-flow-card-symbol-browser` rendering from the generated manifest, with category, domain, style and free-text filters, each variant labelled in words, and the published count taken from the manifest.</action>
  <acceptance_criteria>
    - The count shown equals the manifest's count.
    - An empty filter result says so rather than rendering an empty grid.
    - Every variant carries text, not only a picture.
  </acceptance_criteria>
  <verify><automated>npm run build && node --test test/exact-dist.test.mjs</automated></verify>
  <done>The browser ships in the generated artifact.</done>
</task>
</tasks>

<output>Create `05-06-SUMMARY.md` when done.</output>
