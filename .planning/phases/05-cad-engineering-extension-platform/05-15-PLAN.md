---
phase: 05-cad-engineering-extension-platform
plan: 15
type: execute
wave: 8
depends_on: [05-14]
files_modified:
  - test/e2e/project-cad.spec.mjs
autonomous: true
requirements: [CAD-01]
must_haves:
  truths:
    - "The complete editing workflow is reachable without a pointer."
    - "Nudge, connect, group and reorder all have keyboard paths."
---

## Phase Goal

**As an** engineer at a control-room terminal with no mouse, **I want** to draw and edit, **so that** the product is usable where it is installed.

<objective>Close the non-pointer requirement as one continuous scenario.</objective>

<tasks>
<task type="auto">
  <name>Task 1: The keyboard editing scenario</name>
  <files>test/e2e/project-cad.spec.mjs</files>
  <action>Drive the complete workflow by keyboard alone in both languages: select, extend selection, nudge coarse and fine, resize, group, align, distribute, reorder, connect two ports, route, undo, redo, delete. Assert it as one continuous traversal rather than per-element focusability, and include the minimap.</action>
  <acceptance_criteria>
    - The scenario completes with no pointer event at any step.
    - A refused connection announces its reason in the live region.
    - Undo and redo are reachable and correct within the scenario.
    - Both languages complete the same traversal.
  </acceptance_criteria>
  <verify><automated>node tools/run-exact-dist-playwright.mjs --grep=phase-5-designer</automated></verify>
  <done>T5-11 is GREEN.</done>
</task>
</tasks>

<output>Create `05-15-SUMMARY.md` when done.</output>
