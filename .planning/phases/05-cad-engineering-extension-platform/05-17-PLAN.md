---
phase: 05-cad-engineering-extension-platform
plan: 17
type: execute
wave: 6
depends_on: [05-16]
files_modified:
  - custom_components/glt_flow_card/sdk_registry.py
  - custom_components/glt_flow_card/policy.py
  - custom_components/glt_flow_card/__init__.py
autonomous: true
requirements: [SDK-01]
must_haves:
  truths:
    - "A conflict refuses the whole installation and names both sides."
    - "An unsupported schema version refuses rather than degrading."
---

## Phase Goal

**As an** installation owner, **I want** a failed install to change nothing, **so that** I never have to guess which half applied.

<objective>Make installation all-or-nothing, namespaced and version-checked.</objective>

<tasks>
<task type="auto">
  <name>Task 1: The registry</name>
  <files>custom_components/glt_flow_card/sdk_registry.py</files>
  <action>Resolve contributions by `<namespace>/<local-id>`. Refuse two packs claiming one namespace, and any id collision, naming both sides. Refuse a pack declaring unsupported project schema versions. Apply an installation atomically: validate everything, then commit, so a refusal leaves the registry byte-identical. Enforce packs-per-installation and symbols-per-pack bounds.</action>
  <acceptance_criteria>
    - A conflicting install leaves the registry unchanged and names both packs and the contested id.
    - An unsupported schema version refuses rather than partially applying.
    - Every bound is enforced and asserted with a number.
    - A pack on a project the caller cannot see appears in no listing, count or conflict message.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_sdk_registry.py -q -x</automated></verify>
  <done>T5-13 and T5-14 are GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: The authorized routes</name>
  <files>custom_components/glt_flow_card/policy.py, custom_components/glt_flow_card/__init__.py</files>
  <action>Declare `glt_flow_card/extensions/list`, `/install` and `/remove` in both policy tables with component scope and the appropriate capabilities, register the handlers, and move the exact route count with them. Give every request field an optional default so the generic policy prober reaches a decision rather than a schema rejection.</action>
  <acceptance_criteria>
    - The registration oracle passes with declared and registered sets equal.
    - Install and remove require a write capability; list is filtered.
    - The policy matrix reports a decision, not invalid_format.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_policy.py tests/components/glt_flow_card/test_init.py -q -x</automated></verify>
  <done>The extension routes are declared, registered and bounded.</done>
</task>
</tasks>

<output>Create `05-17-SUMMARY.md` when done.</output>
