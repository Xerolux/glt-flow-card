---
phase: 06-alarms-notifications-schedules
plan: 4
type: execute
wave: 1
depends_on: [1, 2, 3]
files_modified:
  - tests/components/glt_flow_card/test_alarm_lifecycle.py
  - tests/components/glt_flow_card/test_alarm_suppression.py
  - tests/components/glt_flow_card/test_alarm_restart.py
  - tests/components/glt_flow_card/test_alarm_index.py
  - tests/components/glt_flow_card/test_alarm_retention.py
  - tools/phase6-red-gate.mjs
autonomous: true
requirements: [ALM-01]
must_haves:
  truths:
    - "Each RED sentinel names the behaviour it wants and fails for that reason alone."
    - "The delay test uses two different delays on one entity, because one delay cannot show the defect."
    - "The restart test restarts; it does not assert that a guard exists."
    - "Every sentinel carries its EXPECTED_RED marker and its effect-ledger prefix."
---

## Phase Goal

**As a** maintainer, **I want** the alarm defects specified as failing tests before anything is written, **so that** the implementation is judged against a contract rather than against itself.

<objective>Specify the alarm lifecycle, suppression, restart, index and retention RED contracts.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Lifecycle sentinels</name>
  <files>tests/components/glt_flow_card/test_alarm_lifecycle.py</files>
  <action>Write failing tests, under controlled time, for: a threshold condition with hysteresis producing the declared transitions; **two alarms on one entity with different delays, each annunciating at its own delay** (one delay cannot expose D2 — the closure binds the last value, so a single-alarm test passes on the broken code); a sensor oscillating above threshold faster than its delay, which must annunciate rather than never annunciate (D10); deterministic cancellation of a pending task when the condition clears; and simultaneous state changes on two entities.</action>
  <acceptance_criteria>
    - The two-delay test fails on current code with the five-minute alarm firing at five seconds or vice versa.
    - The oscillation test fails on current code by never annunciating.
    - Time is controlled; no test sleeps.
  </acceptance_criteria>
  <verify><automated>node tools/assert-red.mjs --expected=alarm-lifecycle -- py -3.13 -m pytest tests/components/glt_flow_card/test_alarm_lifecycle.py -q -x</automated></verify>
  <done>T6-02 and T6-03 have failing owners.</done>
</task>

<task type="auto">
  <name>Task 2: Suppression sentinels</name>
  <files>tests/components/glt_flow_card/test_alarm_suppression.py</files>
  <action>Write failing tests for: a shelved alarm that must not process and must not notify (D1 — today `shelved_until` is written by nothing and read by nothing, so the test asserts the *effect* of shelving, never the presence of the field); a shelf that has expired and must resume; maintenance suppression; acknowledgement suppression; a shelve request beyond the site maximum, refused with a reason; and a suppressed decision that records **which** suppression applied.</action>
  <acceptance_criteria>
    - No test asserts that `shelved_until` is set; each asserts what shelving did.
    - The decision record's reason is asserted, not just its truthiness.
    - Every test fails on current code.
  </acceptance_criteria>
  <verify><automated>node tools/assert-red.mjs --expected=alarm-suppression -- py -3.13 -m pytest tests/components/glt_flow_card/test_alarm_suppression.py -q -x</automated></verify>
  <done>T6-01 has a failing owner.</done>
</task>

<task type="auto">
  <name>Task 3: Restart, index and retention sentinels</name>
  <files>tests/components/glt_flow_card/test_alarm_restart.py, tests/components/glt_flow_card/test_alarm_index.py, tests/components/glt_flow_card/test_alarm_retention.py</files>
  <action>Restart: drive an active, acknowledged, shelved alarm through a genuine restart — entity to `unavailable`, integration unloaded and reloaded from the persisted store, entity back with the same value — and assert no re-notification, acknowledgement preserved, shelf preserved, and the pending delay re-armed. Index: assert the entity→alarm index equals a full rescan after every mutation path (alarm added, removed, renamed, project deleted, project pasted with remapped ids), and that a state change on an unrelated entity touches no alarm. Retention: assert `schedule_runs` actually drops entries past the window (D8 — today the comparison is `"30" >= "2026-08-19"`, true forever), that `ack_alarm` respects the same cap `alarm_transition` does (D9), and that `alarm_state` holds no entry for a remapped or deleted alarm (D14).</action>
  <acceptance_criteria>
    - The restart test restarts. It does not assert that a guard exists.
    - The index test compares against a full rescan, not against itself.
    - The retention test executes the current prune and shows it dropping nothing.
  </acceptance_criteria>
  <verify><automated>node tools/assert-red.mjs --expected=alarm-restart -- py -3.13 -m pytest tests/components/glt_flow_card/test_alarm_restart.py -q -x</automated></verify>
  <done>T6-04, T6-06 and T6-16 have failing owners.</done>
</task>
</tasks>

<output>Create `06-04-SUMMARY.md` when done.</output>
