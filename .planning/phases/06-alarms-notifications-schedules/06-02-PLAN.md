---
phase: 06-alarms-notifications-schedules
plan: 2
type: execute
wave: 0
depends_on: [1]
files_modified:
  - src/v100/alarm-vocabulary.mjs
  - custom_components/glt_flow_card/alarm_vocabulary.py
  - test/alarm-vocabulary.test.mjs
  - tests/components/glt_flow_card/test_alarm_vocabulary.py
autonomous: true
requirements: [ALM-01]
must_haves:
  truths:
    - "There is exactly one priority vocabulary, and it is closed."
    - "Every string stored by any of today's four vocabularies maps to a declared member."
    - "Suppression reasons, notification outcomes and escalation stage kinds are closed sets too."
    - "An alarm marked critical in the editor is counted by the roll-up that counts criticals."
---

## Phase Goal

**As an** operator, **I want** the word the engineer chose to be the word the roll-up counts, **so that** a critical alarm is not invisible to every summary in the product.

<objective>Close the alarm vocabulary and migrate the four that disagree.</objective>

<tasks>
<task type="auto">
  <name>Task 1: The closed vocabularies</name>
  <files>src/v100/alarm-vocabulary.mjs, custom_components/glt_flow_card/alarm_vocabulary.py</files>
  <action>Declare frozen closed sets, mirrored in both runtimes: `ALARM_PRIORITIES` (ordered, so "higher than" is a fact rather than a convention), `ALARM_STATES`, `SUPPRESSION_REASONS`, `NOTIFICATION_OUTCOMES`, `ESCALATION_STAGE_KINDS` and `SCHEDULE_BINDING_KINDS`. Each gets a membership test, and an unknown member is an error rather than a passthrough. Record in the module why the priority vocabulary is the one Phase-6 setting that is deliberately *not* configurable: sites differ on how many classes they use and what they escalate — both configuration — but not on whether the editor's word and the roll-up's word are the same word.</action>
  <acceptance_criteria>
    - Every set is frozen and every membership test rejects an unknown member.
    - Priorities are ordered and the order is asserted, not assumed.
    - The two runtimes export identical members in identical order.
  </acceptance_criteria>
  <verify><automated>node --test test/alarm-vocabulary.test.mjs && py -3.13 -m pytest tests/components/glt_flow_card/test_alarm_vocabulary.py -q -x</automated></verify>
  <done>One vocabulary exists in both runtimes.</done>
</task>

<task type="auto">
  <name>Task 2: The migration from four vocabularies to one</name>
  <files>src/v100/alarm-vocabulary.mjs, custom_components/glt_flow_card/alarm_vocabulary.py</files>
  <action>Write the mapping as data, not as a branch: every string the four current sources produce — the editor's `critical | warning | info`, `navigation.py`'s `("fault", "warning")`, `project-operations.js`'s `"fault"`, and `alarm_transition`'s `"warning"` default — maps to exactly one declared priority. An unrecognised stored string maps to the *most severe* interpretation available and is reported, never dropped and never silently downgraded: a site whose alarm was already miscounted must not have it miscounted quieter.</action>
  <acceptance_criteria>
    - Every string appearing in the corpus maps to a declared member.
    - An unknown string maps to the most severe interpretation and is reported.
    - The mapping is data and the same data is used by both runtimes.
  </acceptance_criteria>
  <verify><automated>node --test test/alarm-vocabulary.test.mjs && py -3.13 -m pytest tests/components/glt_flow_card/test_alarm_vocabulary.py -q -x</automated></verify>
  <done>No stored severity string is unmapped.</done>
</task>

<task type="auto">
  <name>Task 3: The counting test</name>
  <files>test/alarm-vocabulary.test.mjs, tests/components/glt_flow_card/test_alarm_vocabulary.py</files>
  <action>Assert the defect closed end to end: author an alarm with each priority the editor can write, run the navigation roll-up and the panel badge over it, and assert the counts. Today a `critical` alarm is counted by nothing; the test must fail on the current code for exactly that reason.</action>
  <acceptance_criteria>
    - Every priority the editor can write is counted by the roll-up.
    - The test fails against the pre-migration counting code.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_alarm_vocabulary.py -q -x</automated></verify>
  <done>T6-17 has an executable proof.</done>
</task>
</tasks>

<output>Create `06-02-SUMMARY.md` when done.</output>
