# 06-19 Summary — documentation

**Status:** complete. `verify-docs-site` passes: 22 sources, 41 generated files
byte-identical across two builds.

`Alarms-Controls.md` rewritten and `Schedules.md` added, both German.
README sections in English and German.

Every default appears with its rationale as a site decision. Both DST
transitions are named with concrete dates and the actual tick sequence, because
"handles DST correctly" reads as true whatever the code does. The out-of-scope
statements are explicit: no holiday table, no reimplemented calendar.

No release, HACS repository or Pages deployment is claimed.
