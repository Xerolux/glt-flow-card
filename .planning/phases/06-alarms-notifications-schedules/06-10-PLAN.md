---
phase: 06-alarms-notifications-schedules
plan: 10
type: execute
wave: 2
depends_on: [6, 9]
files_modified:
  - custom_components/glt_flow_card/__init__.py
  - custom_components/glt_flow_card/const.py
autonomous: true
requirements: [ALM-01]
must_haves:
  truths:
    - "Every retained collection has a bound, and the bound is a configured number."
    - "The schedule_runs prune actually drops entries."
    - "Alarm state holds no entry for an alarm the project no longer has."
---

## Phase Goal

**As an** installation running for three years, **I want** the store to stop growing, **so that** retention is a decision somebody made rather than a leak with a friendly name.

<objective>Bound retention and reconcile alarm state against the project.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Fix the prune</name>
  <files>custom_components/glt_flow_card/__init__.py</files>
  <action>Fix D8. The cutoff compares `k.split(":")[-1][:10]` against a date, but `run_key` is `f"{project_id}:{sched_id}:{key_minute}"` and `key_minute` itself contains a colon, so the last segment is the minute — `"30" >= "2026-08-19"` is lexicographically true forever and nothing is ever dropped. Stop parsing the key: store the run's instant as the value and prune on the value. A key that has to be parsed to be understood is a key that will be parsed wrongly.</action>
  <acceptance_criteria>
    - The prune drops entries past the window, proven by count.
    - Nothing derives a date by splitting a composite key.
    - The retention window is configuration with a conservative default.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_alarm_retention.py -q -x</automated></verify>
  <done>`schedule_runs` is bounded.</done>
</task>

<task type="auto">
  <name>Task 2: One history cap, on every path</name>
  <files>custom_components/glt_flow_card/__init__.py, custom_components/glt_flow_card/const.py</files>
  <action>Fix D9. `alarm_transition` caps history at `MAX_AUDIT`; `ack_alarm` inserts with no cap, so acknowledgement is the unbounded path. Route every insertion through one append-and-trim function so the cap cannot be forgotten by the next writer. Make the bound configurable with the conservative default, oldest dropped, recorded in `06-CONTEXT.md` as a site decision.</action>
  <acceptance_criteria>
    - Exactly one function appends to history; grep proves no second insertion site.
    - Repeated acknowledgement does not grow history past the bound.
    - The bound is configuration, and the default is the documented one.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_alarm_retention.py -q -x</automated></verify>
  <done>History is bounded on every path.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile alarm state</name>
  <files>custom_components/glt_flow_card/__init__.py</files>
  <action>Fix D14. `alarm_state` is keyed `f"{project_id}:{alarm_id}"` and nothing reconciles it, so an alarm that was deleted, or whose id was remapped by `ports.py`'s paste remapping, leaves a permanent entry that no project can clear and no UI can show. Reconcile from the same place the index is rebuilt — the two answer the same question about which alarms exist — and record what was dropped rather than dropping it silently.</action>
  <acceptance_criteria>
    - A deleted alarm leaves no state entry.
    - A remapped alarm's state follows the remapping or is dropped with a record; it is never orphaned.
    - Reconciliation runs from the same single place as the index rebuild.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_alarm_retention.py -q -x</automated></verify>
  <done>T6-16 passes.</done>
</task>
</tasks>

<output>Create `06-10-SUMMARY.md` when done.</output>
