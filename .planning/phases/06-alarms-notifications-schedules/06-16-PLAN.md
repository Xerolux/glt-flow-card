---
phase: 06-alarms-notifications-schedules
plan: 16
type: execute
wave: 3
depends_on: [11, 15]
files_modified:
  - src/v100/project-alarms.js
  - src/v100/index.js
autonomous: true
requirements: [ALM-01, ALM-02]
must_haves:
  truths:
    - "Every row shows priority as a word and a shape, never colour alone."
    - "A suppressed row says why and until when."
    - "A failed delivery is visible on the row, and the alarm is not downgraded for it."
    - "Operator text is set as text content and never interpolated into markup."
---

## Phase Goal

**As an** operator, **I want** the alarm list to tell me what is suppressed and what failed to reach anybody, **so that** silence is something I chose rather than something that happened to me.

<objective>Ship the alarm surface: list, detail, acknowledgement and shelving.</objective>

<tasks>
<task type="auto">
  <name>Task 1: The list and the detail</name>
  <files>src/v100/project-alarms.js</files>
  <action>Build `glt-flow-card-alarm-list` and `glt-flow-card-alarm-detail` per the UI contract. Priority renders as a word and a shape. A suppressed row states the reason, the author and the expiry — "shelved by anna until 09.09.2026, 14:00" — because "quiet" without a reason is exactly the defect D1 shipped. A row whose last delivery failed says so on the row, and is neither hidden nor sorted below the successful ones. The detail lists every delivery attempt with service, target, outcome and error, and links to plant, equipment and trend context. An installation with no configured targets says so plainly rather than showing an empty list that reads as nothing having happened. Use `DOMParser`, not `innerHTML`, as Plan 05-14 established.</action>
  <acceptance_criteria>
    - No state is distinguished by colour alone.
    - An empty list says there are no active alarms; it does not render a permanent loading state.
    - No operator-authored string reaches `innerHTML`.
  </acceptance_criteria>
  <verify><automated>node tools/run-exact-dist-playwright.mjs --grep=phase-6-alarms</automated></verify>
  <done>The authoritative state is legible.</done>
</task>

<task type="auto">
  <name>Task 2: Acknowledge and shelve</name>
  <files>src/v100/project-alarms.js</files>
  <action>Build `glt-flow-card-alarm-actions`. Acknowledgement takes an optional comment, stored and displayed as text content with its author and time — and not through `prompt()`, which Phase 5 left on five editor naming paths and this phase must not extend. Shelving offers durations up to the site maximum and refuses longer ones with the reason, while the server enforces the same bound independently: the browser check is UX, exactly as every phase since Phase 2 has required. Both actions post and re-read. Neither paints optimistically — an optimistic acknowledgement the server refused is a lie the operator will act on.</action>
  <acceptance_criteria>
    - No `prompt()`, `alert()` or bare `confirm()` on either path in the shipped artifact.
    - A refused action leaves the previous state visible and says what failed.
    - The shipped artifact contains no client-side authorization gate standing in for the server's.
  </acceptance_criteria>
  <verify><automated>node --test test/shipped-dialogs.test.mjs && node tools/run-exact-dist-playwright.mjs --grep=phase-6-alarms</automated></verify>
  <done>T6-20 passes.</done>
</task>
</tasks>

<output>Create `06-16-SUMMARY.md` when done.</output>
