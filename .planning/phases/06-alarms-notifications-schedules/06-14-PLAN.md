---
phase: 06-alarms-notifications-schedules
plan: 14
type: execute
wave: 2
depends_on: [13]
files_modified:
  - custom_components/glt_flow_card/schedule_bindings.py
  - custom_components/glt_flow_card/__init__.py
autonomous: true
requirements: [SCH-01]
must_haves:
  truths:
    - "Holidays, exceptions, vacations, special days and operating periods bind to Home Assistant capabilities; this card does not reimplement a calendar."
    - "A binding reads the entity's supported_features before offering an edit."
    - "An authoring path a non-admin cannot use says so before it is attempted."
    - "The interval model and the instant model are never silently converted into each other."
---

## Phase Goal

**As an** engineer, **I want** the holidays my site already maintains in Home Assistant to be the holidays the plant honours, **so that** there is one calendar and not two.

<objective>Bind schedules to supported Home Assistant capabilities.</objective>

<tasks>
<task type="auto">
  <name>Task 1: The binding kinds</name>
  <files>custom_components/glt_flow_card/schedule_bindings.py</files>
  <action>Implement the closed binding kinds from Plan 06-02 against the capabilities the research established in Home Assistant 2026.2.3: an **operating period** binds to a `schedule.*` entity (an interval model, `from`/`to` per weekday, with `next_event`); **holidays** bind to a `binary_sensor.workday`-style entity, which already carries country, `province`, `add_holidays` and `remove_holidays` — German public holidays are per-Bundesland and a table we invented would be wrong for half the country; **exceptions, vacations and special days** bind to a `calendar.*` entity, read through `calendar.get_events`. Record for each binding whether it is an interval or an instant, and refuse to convert between them: an HA schedule says the plant is in day mode between these hours, and our runner says call this service at this minute, and those are different claims.</action>
  <acceptance_criteria>
    - Every binding kind names its model, interval or instant, and no code path converts one to the other.
    - No holiday table ships in this repository.
    - A binding to a missing entity is a declared refusal, not a silent no-op.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_schedule_bindings.py -q -x</automated></verify>
  <done>The four binding kinds resolve against real capabilities.</done>
</task>

<task type="auto">
  <name>Task 2: Read the capability before offering the affordance</name>
  <files>custom_components/glt_flow_card/schedule_bindings.py, custom_components/glt_flow_card/__init__.py</files>
  <action>`calendar.create_event` is registered with `required_features=[CalendarEntityFeature.CREATE_EVENT]`, and the websocket create, update and delete paths gate on the matching flags — so a calendar that cannot be written to will reject the call, not the request. Read `supported_features` and report what the binding can and cannot do. Do the same for the admin gate: `schedule/create`, `schedule/update` and `schedule/delete` are each wrapped in `websocket_api.require_admin`, and a card "engineer" is not necessarily a Home Assistant admin, so an authoring path must degrade honestly rather than fail opaquely. This is the same defect shape Phase 4 closed for controls — an affordance whose feasibility was never checked.</action>
  <acceptance_criteria>
    - A calendar without `CREATE_EVENT` is reported read-only, with that reason.
    - A non-admin authoring attempt is refused before the websocket call, with a distinct reason from a capability refusal.
    - Both refusals carry declared codes and neither is silent.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_schedule_bindings.py -q -x</automated></verify>
  <done>T6-15 passes.</done>
</task>
</tasks>

<output>Create `06-14-SUMMARY.md` when done.</output>
