---
phase: 06-alarms-notifications-schedules
plan: 7
type: execute
wave: 2
depends_on: [6]
files_modified:
  - custom_components/glt_flow_card/alarm_engine.py
  - custom_components/glt_flow_card/__init__.py
autonomous: true
requirements: [ALM-01]
must_haves:
  truths:
    - "Suppression is evaluated in one function, at the point of decision."
    - "A suppressed decision records which suppression applied."
    - "A shelve beyond the site maximum is refused with a reason, server-side."
    - "Shelving writes an audit row, as acknowledgement already does."
---

## Phase Goal

**As an** operator, **I want** shelving an alarm to actually make it quiet, **so that** the product's report of success is true.

<objective>Make suppression real, consulted where the decision is made.</objective>

<tasks>
<task type="auto">
  <name>Task 1: One suppression function</name>
  <files>custom_components/glt_flow_card/alarm_engine.py</files>
  <action>Fix D1. Write `suppression_for(alarm, state, now, settings)` returning either nothing or a record naming the reason from the closed set — `shelved`, `maintenance`, `acknowledged` — with its expiry and its author. Call it from the one place the decision is made, so processing and notification cannot disagree about whether an alarm is suppressed. Today `shelved_until` is written twice, cleared once and read nowhere; after this it is read exactly here.</action>
  <acceptance_criteria>
    - A shelved alarm neither processes nor notifies.
    - An expired shelf resumes without operator action.
    - The reason is a member of the closed set and names the author and expiry.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_alarm_suppression.py -q -x</automated></verify>
  <done>T6-01 passes.</done>
</task>

<task type="auto">
  <name>Task 2: Bound the shelf, server-side</name>
  <files>custom_components/glt_flow_card/__init__.py</files>
  <action>Enforce the site's shelving maximum — default seven days, configurable — in the `alarms/shelve` handler, refusing a longer expiry with a stable error code. The browser will offer only durations within the bound, but that check is UX; this is the enforcement. Add the audit row shelving does not write today (D13): acknowledgement audits at 1580 and shelving audits nowhere, which makes the *less* reversible of the two the less auditable.</action>
  <acceptance_criteria>
    - A shelve beyond the maximum is refused with a declared code, regardless of what the browser sent.
    - Every shelve writes an audit row with server provenance.
    - The maximum is configuration with a conservative default, not a constant.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_alarm_suppression.py -q -x</automated></verify>
  <done>Shelving is bounded and auditable.</done>
</task>
</tasks>

<output>Create `06-07-SUMMARY.md` when done.</output>
