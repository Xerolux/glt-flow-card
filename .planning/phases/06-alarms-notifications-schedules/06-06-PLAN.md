---
phase: 06-alarms-notifications-schedules
plan: 6
type: execute
wave: 2
depends_on: [4]
files_modified:
  - custom_components/glt_flow_card/alarm_engine.py
  - custom_components/glt_flow_card/__init__.py
autonomous: true
requirements: [ALM-01]
must_haves:
  truths:
    - "Each pending delay carries its own alarm's delay."
    - "The delay is anchored to the first activation and is not restarted by later active states."
    - "Every transition produces a decision record with a reason."
    - "The engine is one module, callable without a running Home Assistant."
---

## Phase Goal

**As an** operator, **I want** a five-second alarm to annunciate in five seconds, **so that** the delay I configured is the delay I get.

<objective>Build the lifecycle engine: per-alarm delays, anchored delays, and decision records.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Extract the engine</name>
  <files>custom_components/glt_flow_card/alarm_engine.py</files>
  <action>Move condition evaluation, hysteresis and transition derivation out of the manager into a module whose functions take state and return decisions. The engine must be callable without a running Home Assistant, because a lifecycle that can only be tested through a full integration is a lifecycle that will be tested shallowly. The manager keeps the Home Assistant coupling — listeners, tasks, storage — and calls the engine.</action>
  <acceptance_criteria>
    - Every engine function is pure over its inputs.
    - No engine function imports from `homeassistant.core`.
    - The existing `_state_active` behaviour is preserved except where a sentinel demands otherwise.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_alarm_lifecycle.py -q -x -k engine</automated></verify>
  <done>The engine is testable in isolation.</done>
</task>

<task type="auto">
  <name>Task 2: Bind the delay per alarm</name>
  <files>custom_components/glt_flow_card/__init__.py</files>
  <action>Fix D2. `pid`, `a`, `e` and `k` are bound as default arguments and `delay` is not, so the coroutine reads the loop's final value. Bind the delay the same way the other four are bound — or better, stop passing the loop variable at all and read the delay from the deepcopied alarm the closure already carries, which removes the class of bug rather than this instance of it.</action>
  <acceptance_criteria>
    - Two alarms on one entity with different delays each annunciate at their own delay.
    - No loop variable is read from an enclosing scope inside a scheduled coroutine.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_alarm_lifecycle.py -q -x</automated></verify>
  <done>T6-02 passes.</done>
</task>

<task type="auto">
  <name>Task 3: Anchor the delay</name>
  <files>custom_components/glt_flow_card/alarm_engine.py, custom_components/glt_flow_card/__init__.py</files>
  <action>Fix D10. The delay exists to suppress a transient, not a persistent fault that happens to be noisy. Record the first activation instant; while the condition remains continuously active, later active state changes must not cancel and recreate the pending task. A genuine clear cancels it and resets the anchor. Every transition emits a decision record carrying outcome, reason and the anchor instant.</action>
  <acceptance_criteria>
    - A sensor oscillating above threshold faster than its delay annunciates at the delay from first activation.
    - A genuine clear resets the anchor.
    - Every transition carries a reason from the closed set.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_alarm_lifecycle.py -q -x</automated></verify>
  <done>T6-03 passes.</done>
</task>
</tasks>

<output>Create `06-06-SUMMARY.md` when done.</output>
