---
phase: 06-alarms-notifications-schedules
plan: 9
type: execute
wave: 2
depends_on: [6]
files_modified:
  - custom_components/glt_flow_card/alarm_engine.py
  - custom_components/glt_flow_card/__init__.py
autonomous: true
requirements: [ALM-01]
must_haves:
  truths:
    - "A state change costs work proportional to the alarms on that entity, not to all alarms."
    - "The index is built by exactly one function."
    - "Every mutation path rebuilds it, and the rebuild is asserted against a full rescan."
---

## Phase Goal

**As an** installation with a thousand entities, **I want** an unrelated state change to cost nothing, **so that** this card is not a tax on the whole instance.

<objective>Index the alarm scan, and prove the index cannot go stale.</objective>

<tasks>
<task type="auto">
  <name>Task 1: The entity to alarm index</name>
  <files>custom_components/glt_flow_card/alarm_engine.py</files>
  <action>Fix D3. Today line 1941 subscribes to the bare bus event with no entity filter, and the handler iterates every project × every alarm and filters afterwards. Build an entity→alarm index in one function, `rebuild_alarm_index(projects)`, and subscribe with `async_track_state_change_event` over the indexed entity list so Home Assistant does the filtering. A cache that misses a rebuild is worse than the scan it replaces — the scan was slow, the stale cache is silently wrong — so there is exactly one builder and every mutation path calls it.</action>
  <acceptance_criteria>
    - Exactly one function constructs the index; grep proves no second construction site.
    - A state change on an entity with no alarm reaches no alarm evaluation.
    - The subscription is entity-filtered, not bare.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_alarm_index.py -q -x</automated></verify>
  <done>The scan is bounded by the index.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the rebuild</name>
  <files>custom_components/glt_flow_card/__init__.py</files>
  <action>Call the builder from every path that changes which entities have alarms: project save, project delete, project import, paste with remapped ids, migration and reload. Then assert it: mutate through each path and compare the index against a full rescan of the current projects. The comparison must be against a rescan, not against a second call to the builder — comparing a function with itself proves it is deterministic, not that it is right.</action>
  <acceptance_criteria>
    - Every mutation path is covered by a test that mutates and compares.
    - The comparison is against an independent full rescan.
    - Re-subscription follows the index, so a newly-alarmed entity is actually watched.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_alarm_index.py -q -x</automated></verify>
  <done>T6-06 passes.</done>
</task>
</tasks>

<output>Create `06-09-SUMMARY.md` when done.</output>
