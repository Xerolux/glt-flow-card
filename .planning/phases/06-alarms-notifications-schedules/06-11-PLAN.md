---
phase: 06-alarms-notifications-schedules
plan: 11
type: execute
wave: 2
depends_on: [5, 6, 7]
files_modified:
  - custom_components/glt_flow_card/notifications.py
  - custom_components/glt_flow_card/__init__.py
  - custom_components/glt_flow_card/const.py
autonomous: true
requirements: [ALM-02]
must_haves:
  truths:
    - "Every notification attempt records service, target, outcome and error."
    - "A target outside the site allowlist is refused and recorded, never called."
    - "A delivery failure never removes, downgrades or hides the alarm."
    - "An unconfigured installation reaches no external recipient."
---

## Phase Goal

**As an** operator, **I want** to know that the page never arrived, **so that** I am not relying on a notification the product believes it sent.

<objective>Build notification and escalation as recorded, allowlisted, failure-visible effects.</objective>

<tasks>
<task type="auto">
  <name>Task 1: The delivery record</name>
  <files>custom_components/glt_flow_card/notifications.py</files>
  <action>Fix D6 on the notification side. `_notify_alarm` calls with `blocking=False`, which makes the outcome unobtainable, and wraps it in a bare `except Exception: return`, which makes the exception unobtainable too. ALM-02 requires recording every attempt and result, so the call becomes blocking with an explicit timeout and the exception is recorded rather than swallowed. Every attempt produces `{service, target, outcome, error, at}` from the closed outcome set, appended through the bounded-history function from Plan 06-10.</action>
  <acceptance_criteria>
    - No `blocking=False` remains on a notification path.
    - No bare `except` discards a notification exception.
    - A timeout is an explicit number and produces a `timeout` outcome, not a hang.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_notification_delivery.py -q -x</automated></verify>
  <done>T6-07 passes.</done>
</task>

<task type="auto">
  <name>Task 2: The allowlist</name>
  <files>custom_components/glt_flow_card/notifications.py, custom_components/glt_flow_card/const.py</files>
  <action>Fix D11. `_notify_alarm` splits a project-supplied string on the first dot and calls whatever it names, with no guard — while schedules check `domain not in allowed` and controls check `SAFE_SERVICE_DOMAINS`, which does not contain `notify`. The project document is operator input; a service string in it is not authorization. Add a per-site notification allowlist, empty by default, checked immediately before the call, with `persistent_notification` as the only entry a default installation may use — it reaches nobody outside the Home Assistant frontend, which is exactly what "conservative default" means here.</action>
  <acceptance_criteria>
    - An unlisted domain or service is refused with a recorded reason and never called.
    - The default allowlist reaches no external recipient.
    - The allowlist is site configuration, not project data.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_notification_delivery.py -q -x</automated></verify>
  <done>T6-08 passes.</done>
</task>

<task type="auto">
  <name>Task 3: Escalation, and failure that stays visible</name>
  <files>custom_components/glt_flow_card/notifications.py, custom_components/glt_flow_card/__init__.py</files>
  <action>Add escalation stages — ordered, bounded, none configured by default — each with its own delay, targets and the priorities it applies to. Deduplicate on a key that survives a restart, so the Plan 06-08 restart safety extends to escalation. Then assert the rule that makes the whole feature honest: a delivery failure must never remove, downgrade, re-order or hide the alarm. An alarm nobody could be told about is more urgent than one they were told about, and the obvious implementation — treat a failed notify as "handled" — gets this exactly backwards.</action>
  <acceptance_criteria>
    - Stages fire in order, at their own delays, only for their configured priorities.
    - A restart does not re-fire a stage that already fired.
    - An alarm whose every delivery failed is still active, unshelved and listed.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_escalation.py -q -x</automated></verify>
  <done>T6-09 and T6-10 pass.</done>
</task>
</tasks>

<output>Create `06-11-SUMMARY.md` when done.</output>
