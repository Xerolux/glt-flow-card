---
phase: 06-alarms-notifications-schedules
plan: 19
type: execute
wave: 4
depends_on: [15, 16, 17, 18]
files_modified:
  - docs/wiki/Alarms.md
  - docs/wiki/Schedules.md
  - docs/wiki/_Sidebar.md
  - README.md
  - README.de.md
autonomous: true
requirements: [ALM-01, ALM-02, SCH-01]
must_haves:
  truths:
    - "The documentation states the defaults and that they are site decisions."
    - "The DST behaviour is documented with both transitions named."
    - "Nothing claims a capability the tests do not prove."
---

## Phase Goal

**As a** site commissioning this card, **I want** the alarm philosophy documented as decisions I can change, **so that** I configure it deliberately instead of inheriting it.

<objective>Document the Phase-6 contract in German and English.</objective>

<tasks>
<task type="auto">
  <name>Task 1: The wiki pages</name>
  <files>docs/wiki/Alarms.md, docs/wiki/Schedules.md, docs/wiki/_Sidebar.md</files>
  <action>Write `Alarms.md` and `Schedules.md` in German, matching the existing wiki. Alarms: the lifecycle, the priority vocabulary, suppression and what each reason means, the delivery record, escalation, and the philosophy table with every default marked as a site decision — including the plain statement that a fresh installation notifies nobody. Schedules: binding kinds and which Home Assistant capability each uses, the authorization boundary, the audit, and the DST section naming both transitions with the preview's two sentences.</action>
  <acceptance_criteria>
    - Every default in the philosophy table appears with its rationale.
    - Both DST transitions are named with concrete dates.
    - No claim appears that a Phase-6 test does not prove.
  </acceptance_criteria>
  <verify><automated>npm run verify:docs-site</automated></verify>
  <done>The wiki documents the phase.</done>
</task>

<task type="auto">
  <name>Task 2: The READMEs</name>
  <files>README.md, README.de.md</files>
  <action>Replace the alarm and schedule sections in both READMEs — English in `README.md`, German in `README.de.md`. Say what the card now does and, as plainly, what it does not: it does not ship a holiday table, it does not reimplement a calendar, and it notifies nobody until a site configures a target. Do not claim a release; none is authorized.</action>
  <acceptance_criteria>
    - Both READMEs describe the same capabilities in their own language.
    - The out-of-scope statements are present.
    - No release, HACS repository or Pages deployment is claimed.
  </acceptance_criteria>
  <verify><automated>npm run verify:docs-site</automated></verify>
  <done>The READMEs are current and honest.</done>
</task>
</tasks>

<output>Create `06-19-SUMMARY.md` when done.</output>
