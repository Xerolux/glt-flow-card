---
phase: 06-alarms-notifications-schedules
plan: 1
type: execute
wave: 0
depends_on: []
files_modified:
  - tools/verify-phase6.mjs
  - tools/phase6-red-gate.mjs
  - test/phase6-gate.test.mjs
  - package.json
  - tests/components/glt_flow_card/alarm_factory.py
autonomous: true
requirements: [ALM-01, ALM-02, SCH-01]
must_haves:
  truths:
    - "The Phase-6 gate binds every requirement, roadmap truth, plan and threat to evidence before any of it exists."
    - "The effect ledger records notification attempts, not only unintended service calls."
    - "The alarm corpus contains conditions a string-membership evaluator gets wrong."
    - "The corpus contains DST transition dates for the site timezone."
---

## Phase Goal

**As a** maintainer, **I want** the gate to exist before the work does, **so that** a Phase-6 claim has somewhere to be checked rather than somewhere to be asserted.

<objective>Stand up the Phase-6 gate, the notification-aware effect ledger and the alarm corpus.</objective>

<tasks>
<task type="auto">
  <name>Task 1: The Phase-6 gate orchestrator</name>
  <files>tools/verify-phase6.mjs, test/phase6-gate.test.mjs, package.json</files>
  <action>Build `verify-phase6.mjs` on the Phase-5 orchestrator's shape: bind requirements, roadmap truths, plan text and threat rows to evidence commands; execute each unique command exactly once; prove the command graph acyclic with exactly one path to the release leaf `test:phase6:release`. Copy nothing blindly — Phase 5's gate was generated from Phase 4's and shipped three residual bugs (roadmap slice bounds, plan regex, threat count). Derive the phase number, the roadmap slice bounds, the plan-file regex and the threat count from one place each, and add a mutation test for every one of those three.</action>
  <acceptance_criteria>
    - The roadmap slice for Phase 6 starts at "### Phase 6:" and ends at "### Phase 7:", derived from the phase number.
    - The plan regex matches `06-NN-PLAN.md` and nothing else.
    - The threat count is read from the register, not written as a literal.
    - The command graph is acyclic and has exactly one path to the release leaf.
  </acceptance_criteria>
  <verify><automated>node --test test/phase6-gate.test.mjs</automated></verify>
  <done>The gate fails honestly with nothing implemented, naming what is missing.</done>
</task>

<task type="auto">
  <name>Task 2: The notification dimension of the effect ledger</name>
  <files>tools/verify-phase6.mjs, tests/components/glt_flow_card/alarm_factory.py</files>
  <action>Extend the effect ledger so every service call a test causes is recorded with its domain, service, target and outcome, and add an assertion that the set of notification targets reached during a suite is exactly the controlled fake. The existing ledger proves *zero* unintended calls; that is no longer sufficient, because this phase's subject is a call that is intended. A test that reaches a real recipient must fail even when it passes.</action>
  <acceptance_criteria>
    - A test that calls a `notify.*` service outside the fixture allowlist fails the ledger.
    - The ledger records target as well as service.
    - The existing zero-unintended-effect assertion still holds unchanged.
  </acceptance_criteria>
  <verify><automated>node --test test/phase6-gate.test.mjs</automated></verify>
  <done>The ledger can tell an intended notification from a real one.</done>
</task>

<task type="auto">
  <name>Task 3: The alarm and schedule corpus</name>
  <files>tests/components/glt_flow_card/alarm_factory.py</files>
  <action>Build a fixture corpus that defeats the evaluators this phase replaces, the way `cad_factory.py` defeats the midpoint router. It must contain: threshold conditions with hysteresis where a string-membership test gives the wrong answer; two alarms on one entity with different delays; a sensor that oscillates faster than its delay; an alarm shelved into the future and one whose shelf has expired; a project whose stored severity strings span all four of today's disagreeing vocabularies; schedules at 02:30 on the spring-forward and fall-back dates for `Europe/Berlin`; and a schedule bound to a calendar without `CREATE_EVENT`. Carry the naive evaluators the corpus defeats, next to the corpus, so the claim is executable.</action>
  <acceptance_criteria>
    - The shipped `activeAlarm` string-membership evaluator visibly disagrees with the intended answer on at least three fixtures.
    - Both DST transition dates are present with an entry in the affected hour.
    - Every stored severity string in use today appears at least once.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_alarm_factory.py -q -x</automated></verify>
  <done>The corpus can express every case the threat register names.</done>
</task>
</tasks>

<output>Create `06-01-SUMMARY.md` when done.</output>
