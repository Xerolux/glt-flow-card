# Resume here — Phase 6

**State:** planning complete, execution not started.

## What exists

| File | What it settles |
|---|---|
| `06-SOURCE-AUDIT.md` | 14 defects D1–D14 with line numbers. None of the three areas is greenfield. |
| `06-CONTEXT.md` | Boundary, implementation decisions, the user's philosophy decision. |
| `06-RESEARCH.md` | The research flag, both halves, resolved. DST measured by execution. |
| `06-PATTERNS.md` | Reused patterns plus two new ones: the decision record and the resolved instant. |
| `06-THREATS.md` | 21 threats T6-01…T6-21, each with an owner plan and a blocking command. |
| `06-VALIDATION.md` | Requirement and success-criterion coverage; bounds and their defaults. |
| `06-UI-SPEC.md` | The UI contract, written before implementation. |
| `06-01…06-20-PLAN.md` | 20 plans across 5 waves. The roadmap carries the itemised list. |

## Start with

`06-01-PLAN.md` — the gate, the notification-aware effect ledger, and the alarm
corpus. Nothing else can be judged until the gate exists.

Wave order: 0 (01–03) → 1 (04–05, RED) → 2 (06–14, backend) → 3 (15–18, browser)
→ 4 (19–20, docs and close).

## Carry these forward

- **Commit source → `npm run build` → `npm run stage:hacs` → commit manifest →
  re-run gates.** Phase 5 lost several cycles to this ordering.
- **A claim about the card is checked against `dist/glt-flow-card.js`.** Phase 5
  found retirements that existed only in files nobody ships.
- **A route must be declared in both `policy.py` and `policy_contract.py`,** and
  every request field must be `vol.Optional(..., default=...)` or the prober
  returns `invalid_format`.
- **No live Home Assistant writes, no real notification recipient, no release.**
  `release_authorized` stays false.

## Known environment limits

`npm run test:phase6:release` needs a Docker engine this container does not have,
so T6-21 is expected to stay `planned` — exactly as T5-16, T4-14, T3-14 and
T2-16 did. `api.github.com` returns 403 through the egress proxy, which is the
source-repository half of `verify:provenance --online`.
