---
phase: 06-alarms-notifications-schedules
plan: 18
type: execute
wave: 3
depends_on: [11, 16]
files_modified:
  - src/v100/project-alarms.js
  - custom_components/glt_flow_card/config_flow.py
  - custom_components/glt_flow_card/const.py
autonomous: true
requirements: [ALM-01, ALM-02]
must_haves:
  truths:
    - "Every setting in the philosophy table is configurable, and ships with the documented conservative default."
    - "An unconfigured installation is quiet toward recipients and loud toward the operator."
    - "The priority vocabulary is the one deliberate exception, and the surface says why."
---

## Phase Goal

**As a** site, **I want** my alarm philosophy to be my decision, **so that** the product does not impose one that is wrong for me.

<objective>Ship the alarm philosophy as configuration with conservative defaults.</objective>

<tasks>
<task type="auto">
  <name>Task 1: The settings</name>
  <files>custom_components/glt_flow_card/config_flow.py, custom_components/glt_flow_card/const.py</files>
  <action>Expose the philosophy table from `06-CONTEXT.md` as options: shelving maximum (7 days), escalation stages (none), escalation targets (none), notify allowlist (empty, explicit opt-in), alarm history bound (bounded, oldest dropped), schedule-run retention window. Extend `migrate_options` on its existing clamping rule — a value outside a new range is clamped to the nearest bound rather than reset, because an installation that chose the old extreme meant "as far as possible", and clamping is idempotent.</action>
  <acceptance_criteria>
    - Every row of the philosophy table is an option with the documented default.
    - Options migration clamps rather than resets, and is idempotent.
    - A fresh installation reaches no external recipient.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_notification_delivery.py -q -x && py -3.13 -m pytest tests/components/glt_flow_card/test_escalation.py -q -x</automated></verify>
  <done>The philosophy is configuration.</done>
</task>

<task type="auto">
  <name>Task 2: The settings surface</name>
  <files>src/v100/project-alarms.js</files>
  <action>Build `glt-flow-card-alarm-settings`, showing each setting with its current value, its default, and one sentence on what it means — the "why this default" column of the philosophy table is the copy. Where a setting is at its conservative default and that default means "nobody is notified", say so in the surface rather than leaving it to be discovered during an incident. State plainly that the priority vocabulary is fixed and why: sites differ on how many classes they use and what they escalate, and both are here, but they do not differ on whether the editor's word and the roll-up's word are the same word.</action>
  <acceptance_criteria>
    - Every setting shows value, default and meaning.
    - "No notification targets configured" is surfaced, not implied by an empty list.
    - Both languages are complete.
  </acceptance_criteria>
  <verify><automated>node tools/run-exact-dist-playwright.mjs --grep=phase-6-alarms</automated></verify>
  <done>The defaults are legible as decisions.</done>
</task>
</tasks>

<output>Create `06-18-SUMMARY.md` when done.</output>
