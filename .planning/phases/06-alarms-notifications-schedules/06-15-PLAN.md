---
phase: 06-alarms-notifications-schedules
plan: 15
type: execute
wave: 3
depends_on: [6, 7, 8]
files_modified:
  - src/generated-bases/glt-flow-card.base.js
  - src/v100/index.js
  - custom_components/glt_flow_card/panels.py
  - custom_components/glt_flow_card/navigation.py
autonomous: true
requirements: [ALM-01]
must_haves:
  truths:
    - "There is one evaluator, and it is the Companion's."
    - "The shipped card's evaluator is reachable and inert, asserted against dist/glt-flow-card.js."
    - "The badges stop reading a design-time config field the engine never writes."
    - "The card issues alarms/list."
---

## Phase Goal

**As an** operator, **I want** every surface to agree about which alarms are active, **so that** what I see on one screen is what I would see on another.

<objective>Retire the second, third and fourth evaluators.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Retire the shipped evaluator</name>
  <files>src/generated-bases/glt-flow-card.base.js</files>
  <action>`activeAlarm` in the generated base is a string membership test — no operator, no threshold, no hysteresis, no delay — so it disagrees with the backend for every threshold alarm, and its acknowledgement path calls a Home Assistant service directly and never reaches the backend. Retire it **reachable and inert**, the way Phase 5 retired the midpoint router and the control confirmations: the entry point stays, and it reaches neither a threshold comparison nor a `callService`. Deleting it would leave nothing to test, and "the string is absent" is a weaker claim than "the function is present and does nothing".</action>
  <acceptance_criteria>
    - The entry point exists in the shipped artifact and reaches no comparison and no service call.
    - The direct acknowledgement service call is gone from the shipped artifact.
    - Phase 5's ordering rule is followed: commit source, build, stage, commit manifest.
  </acceptance_criteria>
  <verify><automated>node --test test/shipped-alarm-truth.test.mjs</automated></verify>
  <done>The second evaluator is inert in the bytes that ship.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the v100 panel to the backend</name>
  <files>src/v100/index.js</files>
  <action>`alarmsPanel` is a third derivation: it posts `alarms/ack` and `alarms/shelve` to the backend but never calls `alarms/list`, so the authoritative state is displayed nowhere in the product. Replace the derivation with a read of `alarms/list` — now enumeration-filtered by the fix in commit `9f53bcb` — and render what came back. The panel sorts, filters, groups and phrases; it decides nothing.</action>
  <acceptance_criteria>
    - `alarms/list` appears in the shipped artifact and is issued on panel open and refresh.
    - No alarm activity is derived in the browser from entity states.
    - Ack and shelve post and re-read; neither paints optimistically.
  </acceptance_criteria>
  <verify><automated>node --test test/shipped-alarm-truth.test.mjs</automated></verify>
  <done>The authoritative state is displayed.</done>
</task>

<task type="auto">
  <name>Task 3: Detach the badges from the config field</name>
  <files>custom_components/glt_flow_card/panels.py, custom_components/glt_flow_card/navigation.py</files>
  <action>`panels.py:187` and `navigation.py:160` read `alarm["state"]`, a design-time config field the engine never writes — the engine writes `self.data["alarm_state"]` — so those badges are permanently detached from the running engine and have been showing a value nobody updates. Read the engine's state, through the Plan 06-02 vocabulary so the roll-up counts what the editor wrote.</action>
  <acceptance_criteria>
    - Neither module reads `alarm["state"]` from project config.
    - The badge count matches the engine's active set over the corpus.
    - A `critical` alarm authored in the editor appears in the roll-up.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_alarm_vocabulary.py -q -x && node --test test/shipped-alarm-truth.test.mjs</automated></verify>
  <done>T6-05 passes.</done>
</task>
</tasks>

<output>Create `06-15-SUMMARY.md` when done.</output>
