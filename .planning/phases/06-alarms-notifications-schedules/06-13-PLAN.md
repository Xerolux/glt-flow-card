---
phase: 06-alarms-notifications-schedules
plan: 13
type: execute
wave: 2
depends_on: [3, 12]
files_modified:
  - custom_components/glt_flow_card/__init__.py
  - custom_components/glt_flow_card/policy.py
  - tests/components/glt_flow_card/policy_contract.py
  - tests/components/glt_flow_card/test_init.py
autonomous: true
requirements: [SCH-01]
must_haves:
  truths:
    - "Every schedule route is declared in both policy tables."
    - "A schedule listing is filtered to the caller's visible projects."
    - "Every schedule edit and every execution outcome is audited with server provenance."
    - "A denial is opaque and carries a declared error code."
---

## Phase Goal

**As an** owner, **I want** schedule editing to have its own authorization boundary, **so that** the thing that runs the plant is not governed only by the project save path.

<objective>Give schedules routes, authorization, enumeration filtering and audit.</objective>

<tasks>
<task type="auto">
  <name>Task 1: The routes</name>
  <files>custom_components/glt_flow_card/__init__.py, custom_components/glt_flow_card/policy.py, tests/components/glt_flow_card/policy_contract.py</files>
  <action>Add `schedules/list`, `schedules/save`, `schedules/delete` and `schedules/preview`, declared in **both** policy tables — the shipped `policy.py` and the test-owned `policy_contract.py`. Phase 5 learned this the hard way: a route declared in only one table passes the prober and fails the contract. Make every request field `vol.Optional(..., default=...)`, which is what stopped the prober returning `invalid_format` on the Phase-5 routes, and map every refusal to a declared error code. Update the route count in `test_init.py`.</action>
  <acceptance_criteria>
    - Every new route appears in both tables and the counts match.
    - The policy prober reports no `invalid_format` or `unknown_error` on any new route.
    - A non-member is denied opaquely with a declared code.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_schedule_routes.py -q -x && py -3.13 -m pytest tests/components/glt_flow_card/test_init.py -q -x</automated></verify>
  <done>Schedules have an authorization boundary of their own.</done>
</task>

<task type="auto">
  <name>Task 2: Enumeration filtering</name>
  <files>custom_components/glt_flow_card/__init__.py</files>
  <action>`schedules/list` is an `enumeration="filter"` route, so the policy guard deliberately does not deny and the handler must filter. Read the decision, call `visible_projects(connection, ids, "schedule.read")`, and return only what the caller may see. Apply the lesson from the `alarms/list` fix in commit `9f53bcb`, including its subtlest part: a `limit` must be applied *after* filtering, or the count of hidden rows leaks through how many visible rows come back.</action>
  <acceptance_criteria>
    - A principal with no membership receives an empty listing, not a denial and not another project's schedules.
    - Hidden rows do not displace visible rows at the limit.
    - An enumeration test exists of the same shape as `test_alarm_enumeration.py`.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_schedule_routes.py -q -x</automated></verify>
  <done>T6-13 passes.</done>
</task>

<task type="auto">
  <name>Task 3: Audit the edit and the execution</name>
  <files>custom_components/glt_flow_card/__init__.py</files>
  <action>Fix D6 on the schedule side. `run_schedules` calls with `blocking=False` and `except Exception: continue`, so a schedule that failed is indistinguishable from one that ran — and neither writes an audit row. Make the call blocking with a timeout, record the outcome as a decision record, and audit both edits and executions with server provenance. A schedule that ran the wrong sequence must be answerable from the audit, not from a guess.</action>
  <acceptance_criteria>
    - No `blocking=False` and no bare `except` remains on the schedule execution path.
    - A failed execution writes an audit row naming the service and the error.
    - Every edit writes an audit row with server provenance.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_schedule_routes.py -q -x</automated></verify>
  <done>T6-14 passes.</done>
</task>
</tasks>

<output>Create `06-13-SUMMARY.md` when done.</output>
