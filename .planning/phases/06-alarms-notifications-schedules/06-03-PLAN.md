---
phase: 06-alarms-notifications-schedules
plan: 3
type: execute
wave: 0
depends_on: [1, 2]
files_modified:
  - schemas/project/5.schema.json
  - src/v100/project-migrations.mjs
  - custom_components/glt_flow_card/project_migrations.py
  - tools/generate-project-validators.mjs
autonomous: true
requirements: [ALM-01, SCH-01]
must_haves:
  truths:
    - "Every field the alarm engine reads is declared."
    - "Every field the schedule runner reads is declared."
    - "The 4 to 5 migration is sequential, receipted and lossless."
    - "A delay_seconds of \"soon\" and a time of \"tea\" are both rejected by the schema."
---

## Phase Goal

**As an** engineer, **I want** a schedule that says `"tea"` to be refused when it is saved, **so that** it is not discovered at the moment it was supposed to run.

<objective>Introduce schema 5 and the sequential 4 to 5 migration.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Schema 5</name>
  <files>schemas/project/5.schema.json, tools/generate-project-validators.mjs</files>
  <action>Generate schema 5 from schema 4, preserving every collection. Close the `alarm` shape at both project and equipment level: `condition` (operator, entity, threshold, active_states), `delay_seconds` (non-negative integer), `hysteresis` (number), `priority` (a member of the closed vocabulary), `notification` (a closed policy shape), `maintenance` and `links`. Close the `schedule` shape: `kind` (instant or interval), `days`, `time` or `from`/`to`, `service`, `entity_id`, `data`, `enabled`, and a closed `binding` shape naming the HA entity and binding kind. Derive every schema index rather than using a literal — Phase 3 found four latent bugs from literal indices and Phase 5 repeated the rule for the same reason.</action>
  <acceptance_criteria>
    - No index into the schema list is a literal.
    - Both alarm shapes and the schedule shape are deny-default.
    - `delay_seconds: "soon"` and `time: "tea"` are both rejected.
    - Both runtimes compile the new validator.
  </acceptance_criteria>
  <verify><automated>npm run generate:contract:validators && node --test test/v100-contract.test.mjs</automated></verify>
  <done>Schema 5 exists and validates in both runtimes.</done>
</task>

<task type="auto">
  <name>Task 2: The 4 to 5 migration</name>
  <files>src/v100/project-migrations.mjs, custom_components/glt_flow_card/project_migrations.py</files>
  <action>Add `stepFourToFive` and its Python mirror on the existing sequential, receipted, dry-run-first machinery. Map stored severity strings through the Plan 06-02 data. Where an existing alarm or schedule carries a field the new shape rejects, the migration reports it and preserves the original value in a quarantine field rather than deleting it — a site's misconfiguration is still its data, and the receipt is where it learns.</action>
  <acceptance_criteria>
    - Nothing in a schema-4 project is dropped.
    - A rejected field is quarantined and reported, never deleted.
    - Both runtimes produce identical receipts over the corpus.
  </acceptance_criteria>
  <verify><automated>node --test test/v100-migrations.test.mjs && py -3.13 -m pytest tests/components/glt_flow_card/test_project_migrations.py -q -x</automated></verify>
  <done>Schema 4 projects migrate losslessly.</done>
</task>
</tasks>

<output>Create `06-03-SUMMARY.md` when done.</output>
