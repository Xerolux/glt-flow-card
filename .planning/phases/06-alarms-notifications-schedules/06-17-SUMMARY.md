# 06-17 Summary — the schedule surface

**Status:** complete. Covered by the exact-dist `phase-6-alarms` group.

Instant and interval entries are visually and semantically distinct, because
Home Assistant does not blur them either. A read-only binding states its reason.
Failed executions are listed with their errors.

The preview carries the two sentences it exists for, authored in each language
rather than string-joined from fragments, and resolves in the site's timezone
passed in from the server.
