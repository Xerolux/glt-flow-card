---
phase: 06-alarms-notifications-schedules
plan: 17
type: execute
wave: 3
depends_on: [12, 13, 14, 16]
files_modified:
  - src/v100/project-schedules.js
  - src/v100/index.js
autonomous: true
requirements: [SCH-01]
must_haves:
  truths:
    - "The preview states both DST answers in words, in both languages."
    - "A binding shows what it cannot do, not only what it can."
    - "Every operation has a non-pointer path."
    - "A state change is announced through a live region, not only by colour."
---

## Phase Goal

**As an** engineer, **I want** the preview to tell me that 02:30 does not exist on 28.03.2027, **so that** I learn it while editing rather than on the morning it did not run.

<objective>Ship the schedule surface and the DST preview.</objective>

<tasks>
<task type="auto">
  <name>Task 1: The editor and the bindings</name>
  <files>src/v100/project-schedules.js</files>
  <action>Replace `showSchedules` with `glt-flow-card-schedule-editor`. An entry declares whether it is an instant or an interval; the UI does not blur the two, because Home Assistant does not either. A binding shows the bound entity, what it can do, and **what it cannot** — a calendar without `CREATE_EVENT` renders read-only with that reason, and an authoring action unavailable to a non-admin says so before it is attempted. Execution history shows failures with the service and the error, which is the visible half of the Plan 06-13 audit.</action>
  <acceptance_criteria>
    - Instant and interval entries are visually and semantically distinct.
    - A read-only binding states its reason.
    - Failed executions are listed with their errors.
  </acceptance_criteria>
  <verify><automated>node tools/run-exact-dist-playwright.mjs --grep=phase-6-alarms</automated></verify>
  <done>Schedules have a real surface.</done>
</task>

<task type="auto">
  <name>Task 2: The preview</name>
  <files>src/v100/project-schedules.js</files>
  <action>Build `glt-flow-card-schedule-preview`: pick a date, see the resolved instants for it, computed by the Plan 06-12 module in the **site's** timezone, never the browser's. On a transition date it states the answer in words — "02:30 gibt es am 28.03.2027 nicht — dieser Eintrag läuft nicht" and "02:30 kommt am 31.10.2027 zweimal vor — dieser Eintrag läuft einmal, um 02:30+02:00" — authored in both languages rather than string-joined from fragments. These two sentences are the reason the preview exists; an engineer cannot derive either from an `HH:MM` field.</action>
  <acceptance_criteria>
    - Both sentences render correctly in German and English.
    - The preview reads the site timezone; a browser in a different zone changes nothing.
    - The preview and the runner agree, proven by the parity corpus, not by inspection.
  </acceptance_criteria>
  <verify><automated>node --test test/schedule-dst-parity.test.mjs && node tools/run-exact-dist-playwright.mjs --grep=phase-6-alarms</automated></verify>
  <done>The DST behaviour is visible before it is executed.</done>
</task>

<task type="auto">
  <name>Task 3: Non-pointer operation and announcements</name>
  <files>src/v100/project-schedules.js, src/v100/project-alarms.js</files>
  <action>Close the workflow as one continuous keyboard scenario, the way Plan 05-15 did: navigate the alarm list, open a detail, acknowledge with a comment, shelve with an expiry, edit a schedule entry, open the preview and change its date — without a pointer, in both languages. Announce state changes through a live region. Re-focus from the live DOM before each step in the test: Phase 5's keyboard traversal failed on German because pressing against a stale `activeElement` after a repaint sent keys to `body`.</action>
  <acceptance_criteria>
    - Every operation is reachable by keyboard in both languages.
    - An alarm activating, an acknowledgement landing and a delivery failing are each announced.
    - Focus is never trapped in a dialog and returns to the row that opened it.
  </acceptance_criteria>
  <verify><automated>node tools/run-exact-dist-playwright.mjs --grep=phase-6-alarms</automated></verify>
  <done>T6-19 passes.</done>
</task>
</tasks>

<output>Create `06-17-SUMMARY.md` when done.</output>
