---
phase: 06-alarms-notifications-schedules
plan: 5
type: execute
wave: 1
depends_on: [1, 2, 3]
files_modified:
  - tests/components/glt_flow_card/test_notification_delivery.py
  - tests/components/glt_flow_card/test_escalation.py
  - tests/components/glt_flow_card/test_schedule_dst.py
  - tests/components/glt_flow_card/test_schedule_routes.py
  - tests/components/glt_flow_card/test_schedule_bindings.py
  - test/schedule-dst-parity.test.mjs
  - test/shipped-alarm-truth.test.mjs
autonomous: true
requirements: [ALM-02, SCH-01]
must_haves:
  truths:
    - "No test reaches a real notification recipient, and the ledger proves it."
    - "The DST tests use real transition dates for the site timezone, not a synthetic offset."
    - "The parity test compares canonical bytes from both runtimes, not two verdicts."
    - "The shipped-artifact test reads dist/glt-flow-card.js."
---

## Phase Goal

**As a** maintainer, **I want** the notification and schedule defects specified as failing tests, **so that** the two areas whose failures are invisible have a place they become visible.

<objective>Specify the notification, escalation, schedule, DST and shipped-truth RED contracts.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Delivery and escalation sentinels</name>
  <files>tests/components/glt_flow_card/test_notification_delivery.py, tests/components/glt_flow_card/test_escalation.py</files>
  <action>Write failing tests for: a notification whose service raises, recorded with its error rather than swallowed (D6); a notification to a domain outside the site allowlist, refused and recorded (D11); a delivery failure that leaves the alarm active, unshelved and surfaced (T6-09); an unconfigured installation reaching no external service at all; and escalation stages firing in order, deduplicated across a restart. Every call goes to the controlled fake the Phase-2 fixtures provide, and the ledger assertion from Plan 06-01 is active in each test.</action>
  <acceptance_criteria>
    - `blocking=False` cannot satisfy the outcome-recording assertions.
    - A test that reached a real `notify.*` service fails the ledger even if its assertions pass.
    - Every test fails on current code.
  </acceptance_criteria>
  <verify><automated>node tools/assert-red.mjs --expected=notification-delivery -- py -3.13 -m pytest tests/components/glt_flow_card/test_notification_delivery.py -q -x</automated></verify>
  <done>T6-07 through T6-10 have failing owners.</done>
</task>

<task type="auto">
  <name>Task 2: Schedule, DST and binding sentinels</name>
  <files>tests/components/glt_flow_card/test_schedule_dst.py, tests/components/glt_flow_card/test_schedule_routes.py, tests/components/glt_flow_card/test_schedule_bindings.py, test/schedule-dst-parity.test.mjs</files>
  <action>DST: assert a 02:30 entry on 2027-03-28 `Europe/Berlin` produces the declared resolution rather than silently not running, and that a 02:30 entry on 2027-10-31 runs exactly once **with the dedupe cache disabled** — the research established that today's single execution comes from a fold-blind key, so a test that leaves the cache on proves nothing. Routes: assert `schedules/*` routes exist, are declared in both policy tables, deny a non-member, and enumeration-filter to visible projects the way Plan 02 established and the `alarms/list` fix reapplied. Bindings: assert a binding to a calendar without `CREATE_EVENT` is refused with that reason, and that a non-admin authoring attempt is refused before the websocket call rather than after. Parity: compare canonical resolution bytes from both runtimes over the committed transition corpus.</action>
  <acceptance_criteria>
    - The fall-back test disables the dedupe cache and still expects one execution.
    - The route tests fail on current code because no route exists.
    - The parity test compares bytes, not verdicts.
  </acceptance_criteria>
  <verify><automated>node tools/assert-red.mjs --expected=schedule-dst -- py -3.13 -m pytest tests/components/glt_flow_card/test_schedule_dst.py -q -x</automated></verify>
  <done>T6-11 through T6-15 have failing owners.</done>
</task>

<task type="auto">
  <name>Task 3: The shipped-truth sentinel</name>
  <files>test/shipped-alarm-truth.test.mjs</files>
  <action>Read `dist/glt-flow-card.js` and assert: the `activeAlarm` entry point exists and reaches neither a threshold comparison nor a `callService`; the card issues `alarms/list` at least once (today it issues it never — `alarms/ack` and `alarms/shelve` appear once each and `alarms/list` not at all); and no alarm state is derived in the browser from entity states. Model this on `test/shipped-dialogs.test.mjs`, which Phase 5 wrote for the same reason: a retirement authored in `src/` that never reached the artifact is not a retirement.</action>
  <acceptance_criteria>
    - The test reads the generated artifact, not the authored module.
    - It fails on current code because `alarms/list` is never issued.
  </acceptance_criteria>
  <verify><automated>node tools/assert-red.mjs --expected=shipped-alarm-truth -- node --test test/shipped-alarm-truth.test.mjs</automated></verify>
  <done>T6-05 has a failing owner.</done>
</task>
</tasks>

<output>Create `06-05-SUMMARY.md` when done.</output>
