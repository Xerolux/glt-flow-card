---
phase: 03-semantic-equipment-provenance
plan: 7
type: execute
wave: 3
depends_on: [03-06]
files_modified:
  - src/v100/project-diff.mjs
  - src/v100/project-bundle.mjs
  - test/v100-diff.test.mjs
  - test/v100-bundle.test.mjs
autonomous: true
requirements: [SEM-01]
must_haves:
  truths:
    - "Semantic diff categorises hierarchy and profile changes with dependency closure."
    - "Bundles carry schema-3 projects without weakening any archive bound."
---

## Phase Goal

**As an** engineer, **I want** the existing diff, closure and bundle tools to understand the new model, **so that** review and transport keep working.

<objective>Integrate the schema-3 collections into diff, dependency closure and bundle handling.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Diff and closure</name>
  <files>src/v100/project-diff.mjs, test/v100-diff.test.mjs</files>
  <action>Teach the semantic diff the new collections: a re-parent is a `move`, a vocabulary change is `config`, a profile version change is its own category, and a datapoint's provenance fields never appear as authored changes because they are derived. Extend the dependency closure so re-parenting a node pulls in the descendants that would otherwise dangle.</action>
  <acceptance_criteria>
    - Re-parenting a node with children produces a closure containing those children.
    - Derived fields never appear as operations.
  </acceptance_criteria>
  <verify><automated>node --test test/v100-diff.test.mjs</automated></verify>
  <done>Review of a semantic change is honest and complete.</done>
</task>

<task type="auto">
  <name>Task 2: Bundles</name>
  <files>src/v100/project-bundle.mjs, test/v100-bundle.test.mjs</files>
  <action>Carry schema-3 projects through `.gltproject` bundles without relaxing a single archive bound: traversal, aliases, collisions, encryption, compression ratio, CRC and size limits stay exactly as they are. Assets remain opaque bytes.</action>
  <acceptance_criteria>
    - Every existing bundle refusal still refuses.
    - A schema-3 project round-trips through a bundle unchanged.
  </acceptance_criteria>
  <verify><automated>npm run test:bundle</automated></verify>
  <done>Transport understands the new model without new risk.</done>
</task>
</tasks>

<output>Create `03-07-SUMMARY.md` when done.</output>
