---
phase: 03-semantic-equipment-provenance
plan: 5
type: execute
wave: 2
depends_on: [03-02, 03-03, 03-04]
files_modified:
  - schemas/project/3.schema.json
  - tools/generate-project-validators.mjs
  - src/v100/project-migrations.mjs
  - test/v100-migrations.test.mjs
  - tests/components/glt_flow_card/test_project_migrations.py
autonomous: true
requirements: [SEM-01]
must_haves:
  truths:
    - "Schema 3 exists in both runtimes from one generated source and schema 2 is frozen."
    - "The 2→3 migration is sequential, receipted, lossless over the whole corpus and idempotent."
---

## Phase Goal

**As an** engineer, **I want** the project contract to express the semantic model, **so that** the hierarchy can be validated at all.

<objective>Introduce schema 3 and the sequential 2→3 migration on the existing Phase-1 machinery.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Schema 3</name>
  <files>schemas/project/3.schema.json, tools/generate-project-validators.mjs</files>
  <action>Author schema 3 from schema 2: keep every existing collection, replace `semantic_model` with a validated containment model, give `sites` the typed hierarchy levels, extend `datapoint` with unit, direction, medium, freshness budget and provenance placeholders, and extend `profile` with version, controls, state signals, alarms, ports, diagnostics, maintenance and symbols. Declare the closed unit, medium, direction and semantic-tag vocabularies. Register `project3` in `PROJECT_SCHEMA_SPECS` so both runtimes regenerate from it. Schema 2 is not edited.</action>
  <acceptance_criteria>
    - `npm run verify:contract:validators` reports no drift after regeneration.
    - Every new vocabulary is closed; an unknown member is a validation error with a stable path.
    - Schema 2 bytes are unchanged.
  </acceptance_criteria>
  <verify><automated>npm run generate:contract:validators && npm run verify:contract:validators && npm run test:contract:js</automated></verify>
  <done>The contract can express the semantic model in both runtimes.</done>
</task>

<task type="auto">
  <name>Task 2: The 2→3 migration</name>
  <files>src/v100/project-migrations.mjs, test/v100-migrations.test.mjs, tests/components/glt_flow_card/test_project_migrations.py</files>
  <action>Add `[2, {from: 2, to: 3, migrate: stepTwoToThree}]` and bump `CURRENT_PROJECT_SCHEMA_VERSION`. The step is pure: it lifts existing sites into the hierarchy, derives no path, leaves unknown fields untouched, and assigns default vocabularies only where schema 2 already implied one. Prove it over the whole fixture corpus including the 100/500/2,000-object sets, and prove dry run and apply agree.</action>
  <acceptance_criteria>
    - Every corpus document migrates without loss and the receipt digests match between dry run and apply.
    - Running the migration twice is a no-op.
    - A schema-2 document that was already invalid stays invalid, with its original errors.
  </acceptance_criteria>
  <verify><automated>node --test test/v100-migrations.test.mjs && py -3.13 -m pytest tests/components/glt_flow_card/test_project_migrations.py -q -x</automated></verify>
  <done>T3-03 is GREEN.</done>
</task>
</tasks>

<output>Create `03-05-SUMMARY.md` when done.</output>
