---
phase: 03-semantic-equipment-provenance
plan: 8
type: execute
wave: 4
depends_on: [03-06]
files_modified:
  - custom_components/glt_flow_card/provenance.py
  - custom_components/glt_flow_card/policy.py
  - tests/components/glt_flow_card/policy_contract.py
  - custom_components/glt_flow_card/__init__.py
  - tests/components/glt_flow_card/test_init.py
autonomous: true
requirements: [PROTO-01]
must_haves:
  truths:
    - "Provenance comes from registries and config entries; a name never infers a protocol."
    - "Communication health reflects availability, disabled state, entry state and freshness."
    - "A hidden project's entity answers exactly as a missing one."
---

## Phase Goal

**As an** operator, **I want** to see where a value comes from and whether it is live, **so that** I never act on a reading the system cannot vouch for.

<objective>Turn PROTO-01 GREEN behind the Phase-2 policy boundary.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Registry-derived provenance and health</name>
  <files>custom_components/glt_flow_card/provenance.py</files>
  <action>Read the entity, device and area registries and the owning config entry, and report integration domain, config entry id/title/state, device identity, area and floor where present. Map a small set of known integration domains to display labels and report every other domain as itself, labelled unknown. Never parse an entity id or friendly name. Derive health from registry presence, `disabled_by`, the state machine's `unavailable`/`unknown`, the config entry state and the age of `last_updated` against the datapoint's freshness budget. Bound device identifiers and connections rather than echoing them wholesale.</action>
  <acceptance_criteria>
    - An entity whose name suggests one protocol and whose registry says another reports the registry's answer.
    - Absent information is `unknown`, never a default.
    - An API missing on the minimum lane degrades to `unknown` instead of raising.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_provenance.py -q -x</automated></verify>
  <done>T3-04 and T3-06 are GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: The authorized route</name>
  <files>custom_components/glt_flow_card/policy.py, tests/components/glt_flow_card/policy_contract.py, custom_components/glt_flow_card/__init__.py, tests/components/glt_flow_card/test_init.py</files>
  <action>Declare `glt_flow_card/provenance/get` in both policy tables with a project-scoped read capability and opaque enumeration, register the handler, and move the exact route count in the lifecycle ledger with it. The handler resolves only datapoints belonging to the named project, so an entity outside it cannot be probed through this route.</action>
  <acceptance_criteria>
    - Hidden and missing answers are byte-identical.
    - The registration oracle passes with the declared and registered sets equal.
    - No provenance data for an unauthorized project appears in any response or error.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_provenance_policy.py tests/components/glt_flow_card/test_policy.py tests/components/glt_flow_card/test_init.py -q -x</automated></verify>
  <done>T3-05 is GREEN.</done>
</task>
</tasks>

<output>Create `03-08-SUMMARY.md` when done.</output>
