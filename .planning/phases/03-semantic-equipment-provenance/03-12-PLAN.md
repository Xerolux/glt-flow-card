---
phase: 03-semantic-equipment-provenance
plan: 12
type: execute
wave: 6
depends_on: [03-08, 03-10]
files_modified:
  - src/v100/entity-mapping.mjs
  - custom_components/glt_flow_card/entity_mapping.py
  - test/entity-mapping.test.mjs
  - tests/components/glt_flow_card/test_entity_mapping.py
autonomous: true
requirements: [MAP-01]
must_haves:
  truths:
    - "Ranking is pure and identical in both runtimes over the shared corpus."
    - "Every candidate carries the reasons that produced its score."
    - "A manual override sorts first and survives re-ranking."
---

## Phase Goal

**As an** engineer, **I want** mapping suggestions I can argue with, **so that** I never accept a binding I do not understand.

<objective>Turn MAP-01's ranking GREEN with identical behaviour in both runtimes.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Explained ranking</name>
  <files>src/v100/entity-mapping.mjs, custom_components/glt_flow_card/entity_mapping.py</files>
  <action>Rank candidates from declared evidence in this order of weight: manual override, device membership, profile slot expectation, area and floor agreement, integration provenance agreement with siblings, unit and device-class compatibility, and — last and never sufficient alone — name similarity. Return each candidate's score together with the reason codes, weights and details that produced it. The function is pure: same inputs, same output, in both runtimes.</action>
  <acceptance_criteria>
    - The near-miss fixture that name similarity alone would prefer does not rank first.
    - Both runtimes agree on exact scores and ordering across the whole iDM corpus.
    - A candidate with no reasons cannot exist.
  </acceptance_criteria>
  <verify><automated>node --test test/entity-mapping.test.mjs && py -3.13 -m pytest tests/components/glt_flow_card/test_entity_mapping.py -q -x</automated></verify>
  <done>T3-10 is GREEN and rankings are explainable.</done>
</task>

<task type="auto">
  <name>Task 2: Overrides as decisions</name>
  <files>src/v100/entity-mapping.mjs</files>
  <action>Store a manual override as a recorded decision rather than a score, so re-ranking cannot overrule it. Removing the override returns the candidate to the ranked list at whatever it now scores.</action>
  <acceptance_criteria>
    - A re-rank after new evidence leaves the override in place and first.
    - Removing an override is reversible and leaves no residue in the ranking.
  </acceptance_criteria>
  <verify><automated>node --test test/entity-mapping.test.mjs</automated></verify>
  <done>An engineer's decision outlasts the model that suggested against it.</done>
</task>
</tasks>

<output>Create `03-12-SUMMARY.md` when done.</output>
