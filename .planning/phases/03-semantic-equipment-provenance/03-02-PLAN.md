---
phase: 03-semantic-equipment-provenance
plan: 2
type: tdd
wave: 1
depends_on: [03-01]
files_modified:
  - test/semantic-model.test.mjs
  - tests/components/glt_flow_card/test_semantic_model.py
autonomous: true
requirements: [SEM-01]
must_haves:
  truths:
    - "One named sentinel per file states the whole hierarchy and vocabulary contract as a gap list."
    - "Cycles, dangling parents, level inversion, multiple parents and over-bounds trees are all expressed as failing expectations."
---

## Phase Goal

**As an** engineer, **I want** the semantic hierarchy validated identically in both runtimes, **so that** navigation and roll-ups cannot walk a broken graph.

<objective>Specify SEM-01 as executable RED contracts before any implementation exists.</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Hierarchy and vocabulary RED contracts</name>
  <files>test/semantic-model.test.mjs, tests/components/glt_flow_card/test_semantic_model.py</files>
  <action>Write the sentinel pair. Assert the level order `site → building → floor → system → subsystem → equipment → datapoint`; exactly one parent per node; a derived path that is never read from stored data; rejection of a self-cycle, a two-node cycle, an n-node cycle, a dangling parent, an inverted level and a second parent; declared depth and breadth bounds enforced at, below and above the limit; and closed unit, medium, direction and semantic-tag vocabularies rejecting an unknown member at every position that accepts one. Each file emits its effect ledger before asserting, prints `EXPECTED_RED[...]` with a gap list, and fails exactly once.</action>
  <acceptance_criteria>
    - Both files are accepted by `tools/assert-red.mjs` as controlled RED.
    - The gap list names each unmet guarantee individually rather than failing on the first.
    - No test asserts an implementation detail — only the contract.
  </acceptance_criteria>
  <verify><automated>node tools/assert-red.mjs --expected=phase3-semantic-model -- node --test test/semantic-model.test.mjs</automated></verify>
  <done>T3-01, T3-02 and T3-12 have failing owner evidence.</done>
</task>
</tasks>

<output>Create `03-02-SUMMARY.md` when done.</output>
