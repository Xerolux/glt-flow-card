---
phase: 03-semantic-equipment-provenance
plan: 17
type: execute
wave: 10
depends_on: [03-16]
files_modified:
  - tools/stage-hacs-packages.mjs
  - tools/validate-hacs-staging.mjs
  - test/hacs-staging.test.mjs
  - tools/verify-phase3.mjs
  - test/phase3-gate.test.mjs
  - package.json
  - .planning/phases/03-semantic-equipment-provenance/03-THREATS.md
  - .planning/phases/03-semantic-equipment-provenance/03-VALIDATION.md
autonomous: true
requirements: [OPS-01, SEM-01, MAP-01, PROF-01, PROTO-01]
must_haves:
  truths:
    - "Every new authored module is packaged, and the drift guard proves it."
    - "One fail-closed orchestrator binds all five requirements, every plan and T3-01..T3-14 to current evidence."
    - "The command graph stays acyclic with exactly one path to the T3-14 leaf."
---

## Phase Goal

**As a** maintainer, **I want** one command that proves Phase 3, **so that** nothing is claimed without evidence.

<objective>Package the new modules and close Phase 3 behind a fail-closed gate.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Packaging</name>
  <files>tools/stage-hacs-packages.mjs, tools/validate-hacs-staging.mjs, test/hacs-staging.test.mjs</files>
  <action>Add every new Companion module to all three `COMPONENT_FILES` lists. The Phase-2 drift guard already fails when an authored module is not staged; confirm it catches each new file before adding it, so the guard is proven rather than assumed.</action>
  <acceptance_criteria>
    - Removing any new module from a list fails the guard.
    - Both HACS categories and the Companion ZIP validate.
  </acceptance_criteria>
  <verify><automated>npm run validate:hacs-staging</automated></verify>
  <done>The new modules actually ship.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The Phase-3 gate</name>
  <files>tools/verify-phase3.mjs, test/phase3-gate.test.mjs, package.json, 03-THREATS.md, 03-VALIDATION.md</files>
  <action>Build the orchestrator on the Phase-2 pattern: parse the threat register's owner commands, bind OPS-01, SEM-01, MAP-01, PROF-01 and PROTO-01, the roadmap's five success criteria and all seventeen plans to evidence, execute each unique command once, and write the evidence manifest only after everything passes. Prove the command graph is acyclic and reaches `test:phase3:release` by exactly one path, with nothing reachable from the leaf calling back. Seed `test/phase3-gate.test.mjs` with the same graph and evidence mutations Phase 2 uses. Mark a threat or validation row `verified` only after its owner command has actually run and passed.</action>
  <acceptance_criteria>
    - Missing, failed, skipped, zero-count, stale, unmapped and orphan evidence each fail closed.
    - `test:phase3` reaches `test:phase3:release` exactly once and the leaf never reaches back.
    - A row is not marked verified on the strength of its parts passing separately.
  </acceptance_criteria>
  <verify><automated>node --test test/phase3-gate.test.mjs && npm run test:phase3</automated></verify>
  <done>T3-14 is owned and Phase 3 closes on evidence.</done>
</task>
</tasks>

<output>Create `03-17-SUMMARY.md` when done.</output>
