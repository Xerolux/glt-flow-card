---
phase: 03-semantic-equipment-provenance
plan: 11
type: execute
wave: 5
depends_on: [03-10]
files_modified:
  - custom_components/glt_flow_card/__init__.py
  - custom_components/glt_flow_card/policy.py
  - tests/components/glt_flow_card/policy_contract.py
  - tests/components/glt_flow_card/test_init.py
autonomous: true
requirements: [PROF-01]
must_haves:
  truths:
    - "Profile instantiation and upgrade are guarded mutations with exact revisions and a lease."
---

## Phase Goal

**As an** engineer, **I want** profile changes to go through the same authority as any other mutation, **so that** they cannot bypass Phase 2.

<objective>Expose profile instantiation and upgrade as fully guarded routes.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Guarded profile routes</name>
  <files>custom_components/glt_flow_card/__init__.py, custom_components/glt_flow_card/policy.py, tests/components/glt_flow_card/policy_contract.py, tests/components/glt_flow_card/test_init.py</files>
  <action>Declare and register `glt_flow_card/profiles/instantiate` and `glt_flow_card/profiles/upgrade` as project-scoped, lease-and-revision-guarded mutations carrying `project.write`, and move the exact route count with them. Both go through the in-lock mutation guard exactly like `projects/apply`; an upgrade that cannot carry an override returns the report rather than committing a partial result.</action>
  <acceptance_criteria>
    - Both routes are refused without a valid lease and an exact revision.
    - The registration oracle stays exactly equal.
    - A rejected upgrade leaves the head untouched.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_equipment_profiles.py tests/components/glt_flow_card/test_policy.py tests/components/glt_flow_card/test_init.py -q -x</automated></verify>
  <done>Profile changes carry the same authority as every other shared mutation.</done>
</task>
</tasks>

<output>Create `03-11-SUMMARY.md` when done.</output>
