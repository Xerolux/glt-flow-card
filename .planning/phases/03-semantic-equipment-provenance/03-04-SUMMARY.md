# 03-04 — One sentinel each for profiles, mapping, state and the surface

> **Reconstructed at close-out**, from this plan, `03-SUMMARY.md` and the code
> at head — not written at execution time. It records what shipped and what the
> plan asked for; it is not a contemporaneous account of the work.

**Status:** complete.

Exactly one named sentinel per area, so a red run names the area rather than a
line number.

**The state contract is the full precedence table, not sampled cases.** Sixteen
inputs resolving to one state is a place where sampling passes and the table is
still wrong: the sample never contains the pair that inverts.
