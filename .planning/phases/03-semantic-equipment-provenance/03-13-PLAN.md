---
phase: 03-semantic-equipment-provenance
plan: 13
type: execute
wave: 6
depends_on: [03-12]
files_modified:
  - custom_components/glt_flow_card/__init__.py
  - custom_components/glt_flow_card/policy.py
  - tests/components/glt_flow_card/policy_contract.py
  - tests/components/glt_flow_card/test_init.py
  - tests/components/glt_flow_card/test_entity_mapping.py
autonomous: true
requirements: [MAP-01]
must_haves:
  truths:
    - "Nothing binds without an explicit acceptance carrying the previewed diff's identity."
    - "An acceptance is undoable as one batch."
---

## Phase Goal

**As an** engineer, **I want** mapping acceptance to be deliberate and reversible, **so that** a suggestion never becomes a silent change.

<objective>Expose ranking, acceptance and undo as guarded routes with no automatic binding.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Rank, accept and undo</name>
  <files>custom_components/glt_flow_card/__init__.py, custom_components/glt_flow_card/policy.py, tests/components/glt_flow_card/policy_contract.py, tests/components/glt_flow_card/test_init.py, tests/components/glt_flow_card/test_entity_mapping.py</files>
  <action>Declare and register `glt_flow_card/mapping/rank` as a project-scoped read and `glt_flow_card/mapping/accept` as a lease-and-revision-guarded mutation, moving the exact route count. Acceptance carries the opaque preview identity from the semantic diff, so a preview whose closure has changed since it was shown is refused rather than applied. Undo reverts the whole accepted batch as a new forward revision, never by rewriting history.</action>
  <acceptance_criteria>
    - There is no route, option or code path that binds a candidate without acceptance.
    - A stale preview identity is refused with a stable code and changes nothing.
    - Undo produces a new revision and leaves the accepted one in the history.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_entity_mapping.py tests/components/glt_flow_card/test_policy.py tests/components/glt_flow_card/test_init.py -q -x</automated></verify>
  <done>T3-09 is GREEN.</done>
</task>
</tasks>

<output>Create `03-13-SUMMARY.md` when done.</output>
