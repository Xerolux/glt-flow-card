---
phase: 03-semantic-equipment-provenance
plan: 15
type: execute
wave: 8
depends_on: [03-12, 03-14]
files_modified:
  - src/v100/project-semantics.js
  - src/v100/project-safety-i18n.mjs
  - src/v100/entry.js
  - test/e2e/project-semantics.spec.mjs
autonomous: true
requirements: [SEM-01, MAP-01, OPS-01, PROTO-01]
must_haves:
  truths:
    - "The semantic tree, provenance card, mapping review and state badge exist in the generated artifact."
    - "Every state and health value carries a non-colour cue in German and English."
    - "No unauthorized entity, device or area data reaches any browser sink."
---

## Phase Goal

**As an** engineer and operator, **I want** the model, its provenance and its state to be legible, **so that** I can act on evidence rather than on a colour.

<objective>Build the Phase-3 surfaces in the exact generated card.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Semantic tree and provenance</name>
  <files>src/v100/project-semantics.js, src/v100/project-safety-i18n.mjs, src/v100/entry.js</files>
  <action>Register `glt-flow-card-semantic-tree` and `glt-flow-card-provenance-card`. The tree shows each node's level as text as well as by indentation, exposes `aria-level`, `aria-setsize` and `aria-posinset`, supports arrow/Home/End navigation, renders an invalid node explicitly with its contract path, and shows a skipped level as a breadcrumb gap rather than an error. The provenance card names the integration domain exactly as reported, states the source registry of every row, bounds device identifiers behind a disclosure, and renders health with shape and text rather than colour alone.</action>
  <acceptance_criteria>
    - Five Project-safety tabs and the single trigger are unchanged.
    - No node, count or provenance row from an unauthorized project is rendered.
    - Complete DE/EN copy for every level, health value and error code.
  </acceptance_criteria>
  <verify><automated>node tools/run-exact-dist-playwright.mjs --grep=phase-3-ui</automated></verify>
  <done>The model and its provenance are legible in the exact artifact.</done>
</task>

<task type="auto">
  <name>Task 2: Mapping review and state</name>
  <files>src/v100/project-semantics.js, src/v100/project-safety-i18n.mjs, test/e2e/project-semantics.spec.mjs</files>
  <action>Register `glt-flow-card-mapping-review` and `glt-flow-card-state-badge`. Candidates list highest first with their reasons inline and never behind a hover; name similarity is always labelled as the weakest evidence; a manual override is marked as a decision and sorts first; acceptance opens the standard diff preview and confirms; undo states what it will revert. The state badge shows the token with a shape or glyph plus text, quality and freshness adjacent, mode qualifiers beside the state, and an evidence panel that reads the same resolved value the badge does.</action>
  <acceptance_criteria>
    - Nothing binds without an explicit confirmation.
    - No state is distinguished by colour alone; the badge and the evidence panel never disagree.
    - Reflow at 320px and 200% zoom, forced-colors and reduced-motion honoured, keyboard-complete with visible focus.
    - The seeded-secret scanner finds nothing in any sink.
  </acceptance_criteria>
  <verify><automated>node tools/run-exact-dist-playwright.mjs --grep=phase-3-ui</automated></verify>
  <done>T3-13 is GREEN.</done>
</task>
</tasks>

<output>Create `03-15-SUMMARY.md` when done.</output>
