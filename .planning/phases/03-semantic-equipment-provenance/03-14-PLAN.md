---
phase: 03-semantic-equipment-provenance
plan: 14
type: execute
wave: 7
depends_on: [03-08, 03-10]
files_modified:
  - src/v100/equipment-state.mjs
  - custom_components/glt_flow_card/equipment_state.py
  - test/equipment-state.test.mjs
autonomous: true
requirements: [OPS-01]
must_haves:
  truths:
    - "A fixed precedence table resolves the sixteen inputs to exactly one state."
    - "Trust outranks activity: a communication error, invalid value or stale reading is never running."
    - "Symbol, quality, freshness, DE/EN label and drill-down are projections of one resolved value."
---

## Phase Goal

**As an** operator, **I want** one honest state per equipment, **so that** what the symbol says is what the evidence supports.

<objective>Turn OPS-01 GREEN with a table-driven resolver and exhaustive pairwise proof.</objective>

<tasks>
<task type="auto">
  <name>Task 1: The precedence table and resolver</name>
  <files>src/v100/equipment-state.mjs, custom_components/glt_flow_card/equipment_state.py</files>
  <action>Declare the ordered precedence as frozen data — communication_error, invalid, stale, fault, interlock, locked, maintenance, local, manual, command_failed, command_pending, warning, running, standby, off — and resolve by finding the first applicable entry. Treat `auto` and `remote` as mode qualifiers reported alongside the state rather than replacing it. Return state, severity rank, quality, freshness, DE/EN accessible label and the evidence that produced the decision.</action>
  <acceptance_criteria>
    - Every ordered pair of inputs resolves as the table says, generated from the table itself.
    - A communication error, invalid value or stale reading never resolves to running, standby or off.
    - The resolver is pure and needs no clock of its own; freshness is decided by a carried timestamp.
  </acceptance_criteria>
  <verify><automated>node --test test/equipment-state.test.mjs</automated></verify>
  <done>T3-11 is GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: One value, four projections</name>
  <files>src/v100/equipment-state.mjs</files>
  <action>Derive the symbol token, the colour token, the accessible label and the drill-down evidence from the single resolved value, so no caller can construct one without the others. Every state carries a non-colour cue.</action>
  <acceptance_criteria>
    - No projection can be produced from anything but a resolved value.
    - Every state has a shape or glyph token distinct from its colour.
    - German and English labels exist for every state and every mode qualifier.
  </acceptance_criteria>
  <verify><automated>node --test test/equipment-state.test.mjs</automated></verify>
  <done>Symbol, quality, freshness, label and drill-down cannot disagree.</done>
</task>
</tasks>

<output>Create `03-14-SUMMARY.md` when done.</output>
