---
phase: 03-semantic-equipment-provenance
plan: 9
type: execute
wave: 4
depends_on: [03-08]
files_modified:
  - custom_components/glt_flow_card/provenance.py
  - custom_components/glt_flow_card/__init__.py
  - tests/components/glt_flow_card/test_phase2_lifecycle.py
autonomous: true
requirements: [PROTO-01]
must_haves:
  truths:
    - "A provenance cache entry can never outlive the runtime generation that produced it."
    - "Unload leaves the Phase-3 resource ledger at zero alongside the Phase-2 counters."
---

## Phase Goal

**As an** operator, **I want** provenance to stay correct across reloads, **so that** a stale cache never claims a dead entity is live.

<objective>Bound the provenance cache and bind it to the runtime generation.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Generation-bound cache and lifecycle</name>
  <files>custom_components/glt_flow_card/provenance.py, custom_components/glt_flow_card/__init__.py, tests/components/glt_flow_card/test_phase2_lifecycle.py</files>
  <action>Give the provenance service a bounded cache keyed by entity and stamped with the runtime generation, invalidated by registry update events and by `async_invalidate`. Add its size to the lifecycle resource ledger so unload must return it to zero. A cache miss is always answerable from the registries; the cache is an optimization, never a source of truth.</action>
  <acceptance_criteria>
    - A cached entry from a previous generation is never served.
    - A registry change invalidates the affected entry without a restart.
    - The ledger returns to zero after unload, with and without live cache entries.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_provenance.py tests/components/glt_flow_card/test_phase2_lifecycle.py -q -x</automated></verify>
  <done>Provenance is fast without becoming a second, stale truth.</done>
</task>
</tasks>

<output>Create `03-09-SUMMARY.md` when done.</output>
