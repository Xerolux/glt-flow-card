---
phase: 03-semantic-equipment-provenance
plan: 4
type: tdd
wave: 1
depends_on: [03-01]
files_modified:
  - tests/components/glt_flow_card/test_equipment_profiles.py
  - test/entity-mapping.test.mjs
  - tests/components/glt_flow_card/test_entity_mapping.py
  - test/equipment-state.test.mjs
  - test/e2e/project-semantics.spec.mjs
autonomous: true
requirements: [PROF-01, MAP-01, OPS-01]
must_haves:
  truths:
    - "Profiles, mapping, state and the exact-dist UI each carry exactly one named sentinel."
    - "The state contract is expressed as the full precedence table, not as sampled cases."
---

## Phase Goal

**As an** engineer and operator, **I want** versioned profiles, explained mappings and one deterministic state, **so that** what I see is what the server means.

<objective>Specify PROF-01, MAP-01, OPS-01 and the Phase-3 UI as executable RED contracts.</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Profile RED contract</name>
  <files>tests/components/glt_flow_card/test_equipment_profiles.py</files>
  <action>Assert that a profile carries id, semantic version, slots, controls, state signals, alarms, ports, diagnostics, maintenance metadata and symbols; that two instantiations of one version are byte-identical; that an upgrade carries every still-addressable override, adjusts what it can, and reports what it cannot rather than dropping it; and that a profile control names only a control id, its bounded input schema and its gates — a profile that names a domain, service or target is rejected.</action>
  <acceptance_criteria>
    - A silently dropped override fails an assertion.
    - A profile carrying `domain`, `service`, `entity_id` or `target` is refused.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_equipment_profiles.py -q</automated></verify>
  <done>T3-07 and T3-08 have failing owner evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mapping RED contract in both runtimes</name>
  <files>test/entity-mapping.test.mjs, tests/components/glt_flow_card/test_entity_mapping.py</files>
  <action>Assert that ranking is pure and produces identical ordering and scores in both runtimes over the iDM corpus; that every candidate carries its reasons; that name similarity alone never ranks a candidate first when stronger evidence contradicts it; that a manual override sorts first and survives re-ranking; that nothing binds without acceptance; and that an acceptance is undoable as a batch. Include the near-miss fixture that name similarity would wrongly prefer.</action>
  <acceptance_criteria>
    - The two runtimes are compared on exact score and order, not on the winner alone.
    - An automatic bind anywhere would fail an assertion.
  </acceptance_criteria>
  <verify><automated>node --test test/entity-mapping.test.mjs</automated></verify>
  <done>T3-09 and T3-10 have failing owner evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: State and exact-dist UI RED contracts</name>
  <files>test/equipment-state.test.mjs, test/e2e/project-semantics.spec.mjs</files>
  <action>Assert the whole precedence table: for every ordered pair of the sixteen inputs, the higher-ranked one wins; a communication error, invalid value or stale reading is never reported as running; the resolved value carries state, rank, quality, freshness, DE/EN label and evidence; and symbol, colour, label and drill-down are projections of that one value. Add the exact-dist sentinel requiring the Phase-3 surfaces to exist in the generated artifact and to render state with a non-colour cue in both languages with no unauthorized data in any sink.</action>
  <acceptance_criteria>
    - The pairwise assertion is generated from the table, so adding a state cannot be forgotten.
    - The exact-dist sentinel uses the committed bytes and the seeded-secret scanner.
  </acceptance_criteria>
  <verify><automated>node --test test/equipment-state.test.mjs</automated></verify>
  <done>T3-11 and T3-13 have failing owner evidence.</done>
</task>
</tasks>

<output>Create `03-04-SUMMARY.md` when done.</output>
