---
phase: 03-semantic-equipment-provenance
plan: 6
type: execute
wave: 3
depends_on: [03-05]
files_modified:
  - src/v100/semantic-model.mjs
  - src/v100/project-contract.mjs
  - custom_components/glt_flow_card/semantic_model.py
  - test/semantic-model.test.mjs
autonomous: true
requirements: [SEM-01]
must_haves:
  truths:
    - "One parent per node, enforced level order, cycle rejection and bounded depth/breadth in both runtimes."
    - "Semantic paths are derived on read and never stored."
---

## Phase Goal

**As an** engineer, **I want** an invalid hierarchy rejected at the contract, **so that** nothing downstream walks a broken graph.

<objective>Turn the SEM-01 hierarchy and vocabulary contracts GREEN.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Containment, cycles and bounds</name>
  <files>src/v100/semantic-model.mjs, src/v100/project-contract.mjs, custom_components/glt_flow_card/semantic_model.py</files>
  <action>Extend `REFERENCE_EDGES` and `ID_COLLECTIONS` so every parent reference is validated by the existing machinery, then add what reference checking cannot do: an acyclicity check that reports the node closing the loop with a stable path, a level-order check, a single-parent check, and declared depth and breadth bounds. Implement `semanticPath` as a derivation over parents. Mirror the rules in the Companion so both runtimes agree.</action>
  <acceptance_criteria>
    - Self-cycles, two-node cycles and n-node cycles are all rejected with the closing node's path.
    - A node with two parents and an inverted level are rejected.
    - Bounds are enforced at, below and above the limit, and validation stays bounded on a hostile document.
    - No stored path exists anywhere in the schema or the code.
  </acceptance_criteria>
  <verify><automated>node --test test/semantic-model.test.mjs && py -3.13 -m pytest tests/components/glt_flow_card/test_semantic_model.py -q -x</automated></verify>
  <done>T3-01 and T3-12 are GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Closed vocabularies</name>
  <files>src/v100/semantic-model.mjs, schemas/project/3.schema.json</files>
  <action>Seed the unit, medium, direction and semantic-tag vocabularies from what the existing fixtures and the iDM profiles actually use, each unit carrying its dimension so conversions and roll-ups can reason about compatibility. Reject an unknown member at every position that accepts one, in both runtimes, with a stable path.</action>
  <acceptance_criteria>
    - Units declare dimensions; two units of different dimensions are never treated as compatible.
    - An unknown member anywhere is a contract error, never a passthrough.
  </acceptance_criteria>
  <verify><automated>node --test test/semantic-model.test.mjs</automated></verify>
  <done>T3-02 is GREEN.</done>
</task>
</tasks>

<output>Create `03-06-SUMMARY.md` when done.</output>
