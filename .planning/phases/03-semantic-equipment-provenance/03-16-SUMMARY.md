# 03-16 — Documentation that does not exceed the evidence

> **Reconstructed at close-out**, from this plan, `03-SUMMARY.md` and the code
> at head — not written at execution time. It records what shipped and what the
> plan asked for; it is not a contemporaneous account of the work.

**Status:** complete.

Both languages describe the same hierarchy, provenance, profile, mapping and
state semantics — one contract, two renderings.

**No claim exceeds the evidence.** In particular no protocol support is asserted
that the registry cannot confirm: the product knows which integration owns an
entity, and that is a different and smaller thing than knowing which fieldbus is
behind it.
