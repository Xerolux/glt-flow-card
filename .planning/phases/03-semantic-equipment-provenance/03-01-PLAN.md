---
phase: 03-semantic-equipment-provenance
plan: 1
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/components/glt_flow_card/registry_factory.py
  - test/fixtures/idm/heat-pump.profile.json
  - test/fixtures/idm/heat-pump.entities.json
  - tools/assert-red.mjs
  - package.json
autonomous: true
requirements: [SEM-01, MAP-01, PROTO-01]
must_haves:
  truths:
    - "Registry fixtures produce entity, device, area and config-entry states covering core, custom, disabled, unavailable and absent cases."
    - "The RED classifier knows every Phase-3 sentinel key and still rejects harness failures, zero-test runs and unrelated failures."
    - "The Phase-3 command scripts exist and are non-recursive from the outset."
  artifacts:
    - path: "tests/components/glt_flow_card/registry_factory.py"
      provides: "Home Assistant registry fixtures for provenance tests"
    - path: "test/fixtures/idm/heat-pump.profile.json"
      provides: "Realistic iDM profile and entity corpus for mapping"
---

## Phase Goal

**As an** engineer, **I want** one validated semantic equipment model whose identity, provenance, mappings, profiles and state agree everywhere, **so that** navigation, permissions, diagnostics and roll-ups rest on the same truth.

<objective>Stand up the Phase-3 fixtures, sentinel registry and command scripts before any contract changes.</objective>
<context>@.planning/phases/03-semantic-equipment-provenance/03-VALIDATION.md</context>

<tasks>
<task type="auto">
  <name>Task 1: Registry fixtures</name>
  <files>tests/components/glt_flow_card/registry_factory.py</files>
  <action>Provide a factory that seeds the entity, device and area registries and config entries of a test `hass`. It must be able to produce: an entity owned by a core integration; one owned by an integration the card has never heard of; one with no device; one disabled by the user and one disabled by the integration; one absent from the state machine; one `unavailable`; one whose config entry is in a failed state; and a device with identifiers, connections, manufacturer, model and an area with and without a floor. Everything is isolated fixture data — no live Home Assistant is contacted.</action>
  <acceptance_criteria>
    - Every case above is constructible in one call and asserted by a self-test.
    - The factory degrades rather than raising where a lane's Home Assistant lacks an API (floors), so both supported lanes can use it.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/registry_factory.py -q</automated></verify>
  <done>Provenance work has a realistic, isolated registry to test against.</done>
</task>

<task type="auto">
  <name>Task 2: iDM fixtures and sentinel registration</name>
  <files>test/fixtures/idm/heat-pump.profile.json, test/fixtures/idm/heat-pump.entities.json, tools/assert-red.mjs, package.json</files>
  <action>Author a realistic iDM heat-pump profile (slots, controls, state signals, alarms, ports, diagnostics, maintenance, symbols) and a matching entity corpus with the units, device classes and names a real installation produces, including near-miss candidates that must not win on name alone. Register the Phase-3 sentinel keys in `tools/assert-red.mjs`. Add `test:phase3:quick`, outer `test:phase3` and the leaf `test:phase3:release`, keeping the leaf non-recursive from the first commit.</action>
  <acceptance_criteria>
    - The corpus contains at least one candidate that name similarity alone would wrongly rank first.
    - `npm run test:phase3:quick` runs and reports the new sentinels as controlled RED.
    - No script reaches `test:phase3` or `tools/verify-phase3.mjs` from the leaf.
  </acceptance_criteria>
  <verify><automated>node --test test/assert-red.test.mjs && npm run test:phase3:quick</automated></verify>
  <done>Wave 0 is complete and every later plan has its harness.</done>
</task>
</tasks>

<output>Create `03-01-SUMMARY.md` when done.</output>
