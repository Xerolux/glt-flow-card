---
phase: 03-semantic-equipment-provenance
plan: 3
type: tdd
wave: 1
depends_on: [03-01]
files_modified:
  - tests/components/glt_flow_card/test_provenance.py
  - tests/components/glt_flow_card/test_provenance_policy.py
autonomous: true
requirements: [PROTO-01]
must_haves:
  truths:
    - "Provenance is asserted to come from registries and config entries, never from an entity id or a friendly name."
    - "Communication health is asserted against unavailable, disabled, failed-entry and stale cases."
    - "An unauthorized entity answers exactly as a missing one."
---

## Phase Goal

**As an** operator, **I want** datapoint provenance and communication health I can trust, **so that** I never act on a value that is not live.

<objective>Specify PROTO-01 as executable RED contracts, including its authorization behaviour.</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Provenance and health RED contract</name>
  <files>tests/components/glt_flow_card/test_provenance.py</files>
  <action>Assert that integration, config entry, device and area provenance is read from the registries seeded by `registry_factory`; that an entity whose id and name strongly suggest a protocol but whose registry says otherwise reports the registry's answer; that an integration the card does not know is reported as its own domain and labelled unknown rather than guessed; and that health resolves correctly for live, stale beyond the freshness budget, `unavailable`, disabled-by-user, disabled-by-integration, absent-from-registry and failed-config-entry cases.</action>
  <acceptance_criteria>
    - A name-based inference anywhere in the implementation would fail at least one assertion.
    - Every unknown is asserted to be `unknown`, never an empty string or a plausible default.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_provenance.py -q</automated></verify>
  <done>T3-04 and T3-06 have failing owner evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Provenance authorization RED contract</name>
  <files>tests/components/glt_flow_card/test_provenance_policy.py</files>
  <action>Assert that a provenance query for an entity inside a project the caller cannot read answers byte-identically to one for a project that does not exist; that a viewer can read provenance for a project they hold and nothing else; that the route is declared in both policy tables; and that no registry data for an unauthorized project reaches any response, count or error detail.</action>
  <acceptance_criteria>
    - Hidden and missing responses are compared for exact equality, not merely for the same code.
    - The route appears in the registration oracle's declared set.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_provenance_policy.py -q</automated></verify>
  <done>T3-05 has failing owner evidence.</done>
</task>
</tasks>

<output>Create `03-03-SUMMARY.md` when done.</output>
