---
phase: 03-semantic-equipment-provenance
plan: 10
type: execute
wave: 5
depends_on: [03-06]
files_modified:
  - custom_components/glt_flow_card/equipment_profiles.py
  - src/v100/equipment-profiles.mjs
  - tests/components/glt_flow_card/test_equipment_profiles.py
autonomous: true
requirements: [PROF-01]
must_haves:
  truths:
    - "Two instantiations of one profile version are identical."
    - "An upgrade carries every still-addressable override and reports what it cannot carry."
    - "A profile names no domain, service or target."
---

## Phase Goal

**As an** engineer, **I want** profiles I can version and reuse, **so that** one modelling decision does not have to be repeated per instance.

<objective>Turn PROF-01 GREEN without giving a profile any authority Phase 2 removed.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Versioned instantiation</name>
  <files>custom_components/glt_flow_card/equipment_profiles.py, src/v100/equipment-profiles.mjs</files>
  <action>Resolve a profile by id and version, instantiate deterministically, and store on the instance only the profile id, the version and the overrides. Refuse a profile that declares a domain, service, entity, device, area or target anywhere — a profile control names a control id, its bounded input schema and its gates, and the Companion still resolves the effect from the verified head.</action>
  <acceptance_criteria>
    - Two instantiations of the same version are byte-identical.
    - A profile carrying an effect field is refused with a stable code.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_equipment_profiles.py -q -x</automated></verify>
  <done>T3-07 is GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Override-preserving upgrades</name>
  <files>custom_components/glt_flow_card/equipment_profiles.py</files>
  <action>Upgrading an instance to a newer profile version carries every override that still addresses something the new version has, adjusts those whose address moved, and returns the ones it cannot carry with the reason for each. Nothing is dropped silently and nothing is invented.</action>
  <acceptance_criteria>
    - An override addressing a removed slot is reported, not discarded.
    - An upgrade that reports nothing carried nothing away.
    - Upgrading twice to the same version is a no-op.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_equipment_profiles.py -q -x</automated></verify>
  <done>T3-08 is GREEN.</done>
</task>
</tasks>

<output>Create `03-10-SUMMARY.md` when done.</output>
