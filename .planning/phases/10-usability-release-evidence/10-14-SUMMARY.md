# 10-14 — Documentation that can be checked

**Status:** complete.

Three wiki pages — Barrierefreiheit, Sprachen, Nachweise — and a section in both
READMEs. Each page ends with what the product does **not** have, because a page
listing only capabilities is a page a reader extrapolates from, and
extrapolating from a supervision product is how someone assumes a plant is being
watched.

The accessibility page states plainly that a clean automated run is consistent
with an unusable product, and that the manual half was not performed here. The
localization page ends with the roughly 150 strings still outside the catalog.
The evidence page explains why the registry is a build step rather than a
document: a document is edited by whoever wants the claim.

**The stale README claim is corrected**, and it was the registry's own worked
example — "`test/` – lightweight validation tests". It now cites the command
that would re-check it rather than asking to be believed.
