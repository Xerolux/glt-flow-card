---
phase: 10-usability-release-evidence
plan: 15
type: execute
wave: 6
depends_on: [14]
files_modified: [.planning/phases/10-usability-release-evidence/10-THREATS.md, .planning/STATE.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md]
autonomous: true
requirements: [I18N-01, A11Y-01, TEST-01]
must_haves:
  truths:
    - "Every threat row is marked from its own owner command, run at head."
    - "A blocked command is recorded with its exact failure output."
    - "REQUIREMENTS.md traceability reflects reality for every phase, not only this one."
---

# 10-15 — Close the register, and the traceability nobody updated

Closes T10-16 as far as it can be closed here.

Run each row's owner command at head; mark each row only from its own run; run a
shared command once per row. Record a blocked command with its **exact failure
output** rather than its likely cause.

T10-16 is expected to stay `planned` — the composed release leaf needs Docker.

**And then the debt this project has been carrying.** `REQUIREMENTS.md`
traceability still reads "Pending" for phases that closed days ago. That is the
same defect as an unsupported claim, in the planning artifacts rather than the
product: a document saying something that stopped being true. All thirty rows get
their real status, and each cites the phase register that supports it.

## Acceptance

Every marked row has its own at-head run. Blocked rows carry exact failure
output. `REQUIREMENTS.md` has no "Pending" row for a closed phase, and no row
claims more than its register shows.
