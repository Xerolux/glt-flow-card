---
phase: 07-trends-energy-reports
plan: 8
type: execute
wave: 2
depends_on: [6]
files_modified:
  - custom_components/glt_flow_card/history_routes.py
  - custom_components/glt_flow_card/policy.py
  - tests/components/glt_flow_card/policy_contract.py
  - custom_components/glt_flow_card/__init__.py
autonomous: true
requirements: [HIST-01]
must_haves:
  truths:
    - "Four routes, declared in both policy tables."
    - "Enumeration filters rather than denying, and the limit is applied after filtering."
    - "Every read and every export is audited with server provenance."
    - "The browser never issues a Recorder request of its own."
---

## Phase Goal

**As a** site owner, **I want** history reads to go through the same boundary as everything else, **so that** the project policy is not silently absent from the one place it was never applied.

<objective>Give history its own routes, authorization, filtering and audit.</objective>

<tasks>
<task type="auto">
  <name>Task 1: The routes, in both tables</name>
  <files>custom_components/glt_flow_card/history_routes.py, custom_components/glt_flow_card/policy.py, tests/components/glt_flow_card/policy_contract.py</files>
  <action>Fix D5 and T7-01. Add `history/series`, `history/statistics`, `history/export` and `history/coverage`, each declared in the shipped policy table *and* the test-owned contract — the deliberate duplication that makes a route present in only one fail the contract while passing the prober. `history.read` sits with the role that can already see the entities the card references; `history.export` is separate, because an export leaves the building.</action>
  <acceptance_criteria>
    - A route in only one table fails the contract.
    - Enforcement is server-side; a browser claim of a role changes nothing.
    - Every route's capability is declared, not inferred.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_history_routes.py -q -x</automated></verify>
  <done>History has a boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Filter, then limit; and audit</name>
  <files>custom_components/glt_flow_card/history_routes.py, custom_components/glt_flow_card/__init__.py</files>
  <action>Make the enumerating routes `enumeration="filter"`, so the policy guard deliberately does not deny and filtering is the handler's job. Apply the limit **after** filtering: applied before, it becomes a count oracle for rows the caller may not see, which Phase 6 established for `schedules/list`. Audit every read and export with server provenance, entity count, window and row count — an export that leaves the building with no audit row is the same defect class as a control with no evidence.</action>
  <acceptance_criteria>
    - A caller sees only their project's rows, and the page is full of their own rows.
    - A limit applied before filtering fails the test.
    - Every read and export writes an audit row.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_history_routes.py -q -x</automated></verify>
  <done>T7-01 passes.</done>
</task>
</tasks>

<output>Create `07-08-SUMMARY.md` when done.</output>
