---
phase: 07-trends-energy-reports
plan: 1
type: execute
wave: 0
depends_on: []
files_modified:
  - tools/verify-phase7.mjs
  - tools/phase7-red-gate.mjs
  - tools/assert-red.mjs
  - test/e2e/fixtures/fake-ha.mjs
  - tests/components/glt_flow_card/conftest.py
  - tests/components/glt_flow_card/recorder_factory.py
  - package.json
autonomous: true
requirements: [HIST-01, ENER-01, REPORT-01]
must_haves:
  truths:
    - "The phase gate derives everything from one PHASE constant, as Phase 6's does."
    - "The effect ledger records every Recorder request with contract, entity count, window and row count."
    - "A test that reaches a live Recorder fails, and its teardown is what fails it."
    - "The fixture Recorder is the only Recorder any test can reach."
---

## Phase Goal

**As an** engineer, **I want** the phase's evidence machinery to exist before any behaviour does, **so that** a plausible wrong number cannot pass unnoticed.

<objective>Stand up the Phase-7 gate, the query-dimension effect ledger and the Recorder fixture corpus.</objective>

<tasks>
<task type="auto">
  <name>Task 1: The gate, derived from one constant</name>
  <files>tools/verify-phase7.mjs, package.json</files>
  <action>Copy the Phase-6 orchestrator's shape and change `PHASE` to 7. Everything else — slug, threat prefix, roadmap slice markers, plan-line pattern, leaf script — derives from it, so nothing needs editing twice. Keep the contiguity check on threat ids rather than a count literal: ids must run from `T7-01` with no gaps or duplicates. Register `test:phase7`, `test:phase7:quick` and the non-recursive `test:phase7:release` leaf; `F7-06` runs the Phase-6 gate.</action>
  <acceptance_criteria>
    - The command graph is proven acyclic with exactly one path to `test:phase7:release`.
    - Every unique command executes once.
    - A missing or duplicated threat id fails the gate.
  </acceptance_criteria>
  <verify><automated>node tools/verify-phase7.mjs --dry-run</automated></verify>
  <done>The gate parses the register, the roadmap slice and the plans, and binds them.</done>
</task>

<task type="auto">
  <name>Task 2: The query dimension</name>
  <files>test/e2e/fixtures/fake-ha.mjs, tests/components/glt_flow_card/conftest.py, tests/components/glt_flow_card/recorder_factory.py</files>
  <action>Extend the effect ledger with a query dimension: every Recorder request a test makes is recorded with its contract (`statistics`, `statistic`, `raw`), entity count, window length and returned row count. Build `RecorderLedger` and a `recorder_fixture` in the Python lane on the shape of Phase 6's `NotificationLedger` — and put the containment assertion in the **teardown**, so a passing test that reached a live Recorder becomes a failing test. Phase 6 established that a check the test itself has to remember to call is a check that gets forgotten.</action>
  <acceptance_criteria>
    - Every request is recorded with all four fields.
    - A test that reaches outside the fixture Recorder fails in teardown, and the failure names the request.
    - The ledger distinguishes "no data returned" from "no request made".
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_recorder_ledger.py -q -x</automated></verify>
  <done>The ledger can prove a query stayed inside its bounds.</done>
</task>

<task type="auto">
  <name>Task 3: The corpora</name>
  <files>tools/research/phase7-probe-statistics-periods.py, tools/research/phase7-probe-resolve-period.py, tests/components/glt_flow_card/fixtures/</files>
  <action>Turn the two research probes into a committed corpus: every named period, every offset, both `first_weekday` values, and the transition dates that produce 23- and 25-hour days and 743- and 745-hour months. Add a Recorder response corpus covering the five fixture classes criterion 4 names — missing, partial, stale, incompatible, Recorder failure — plus the two traps the research found: a window beginning before the statistic exists, and a `sum` of `None`.</action>
  <acceptance_criteria>
    - The corpus is committed data, regenerable by a named command, and the command is not a graph edge.
    - Every fixture class from criterion 4 is present and named.
    - The corpus includes both research traps.
  </acceptance_criteria>
  <verify><automated>node --test test/phase7-corpus.test.mjs</automated></verify>
  <done>Both runtimes have one shared corpus to be compared against.</done>
</task>
</tasks>

<output>Create `07-01-SUMMARY.md` when done.</output>
