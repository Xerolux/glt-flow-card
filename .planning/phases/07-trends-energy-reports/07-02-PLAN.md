---
phase: 07-trends-energy-reports
plan: 2
type: execute
wave: 0
depends_on: [1]
files_modified:
  - src/v100/measured-value.mjs
  - custom_components/glt_flow_card/measured_value.py
  - src/v100/period-vocabulary.mjs
  - custom_components/glt_flow_card/period_vocabulary.py
autonomous: true
requirements: [HIST-01, ENER-01]
must_haves:
  truths:
    - "One closed period vocabulary, mirrored in both runtimes and proven identical."
    - "Every number the phase produces carries value, unit, coverage, gaps, source, period and resolved_at."
    - "value: null with coverage: 0 is a complete answer; zero is not a substitute for it."
    - "An unknown period name or aggregate is refused, not silently defaulted."
---

## Phase Goal

**As a** reader of any number this phase produces, **I want** the evidence for it attached to it, **so that** ignoring the evidence has to be deliberate.

<objective>Close the measured-value shape and the period vocabulary in both runtimes.</objective>

<tasks>
<task type="auto">
  <name>Task 1: The measured value</name>
  <files>src/v100/measured-value.mjs, custom_components/glt_flow_card/measured_value.py</files>
  <action>Define the shape `{value, unit, coverage, gaps, source, period, resolved_at}` in both runtimes, with constructors that refuse to build one without coverage. `source` is a closed set — `statistics`, `raw`, `unavailable` — because "we have no data" and "we did not ask" are different answers. Provide the two constructors the phase needs most: a complete value, and an empty one (`value: null`, `coverage: 0`) with a reason. Fix the audit's D1, D6, D7, D10, D16 and D18 at their root by making the absent case expressible.</action>
  <acceptance_criteria>
    - A value cannot be constructed without coverage.
    - `value: null, coverage: 0` round-trips through both runtimes and both serialisations.
    - Both runtimes produce identical canonical bytes for the corpus.
  </acceptance_criteria>
  <verify><automated>node --test test/measured-value.test.mjs && py -3.13 -m pytest tests/components/glt_flow_card/test_measured_value.py -q -x</automated></verify>
  <done>Absent is expressible, and it is not zero.</done>
</task>

<task type="auto">
  <name>Task 2: The period vocabulary</name>
  <files>src/v100/period-vocabulary.mjs, custom_components/glt_flow_card/period_vocabulary.py</files>
  <action>Close the period names (`day`, `week`, `month`, `year`, `custom`), the aggregates (`min`, `max`, `mean`, `change`, `state`), the sources and the refusal reasons, mirrored in both runtimes with a module-load check that every member has a label in both languages. Fix D12: an unrecognised aggregate is refused with a reason, never silently computed as the mean. Record which contract answers which period name, from the research: `day`, `week`, `month` to the plural command; `year`, offsets and `first_weekday` to the singular command's calendar spec.</action>
  <acceptance_criteria>
    - An unknown period or aggregate is refused with a reason from the closed set.
    - Every member has a German and an English label, checked at module load.
    - The two runtimes' vocabularies are proven equal, not assumed.
  </acceptance_criteria>
  <verify><automated>node --test test/period-vocabulary.test.mjs && py -3.13 -m pytest tests/components/glt_flow_card/test_period_vocabulary.py -q -x</automated></verify>
  <done>Nothing downstream can invent a period or an aggregate.</done>
</task>
</tasks>

<output>Create `07-02-SUMMARY.md` when done.</output>
