---
phase: 07-trends-energy-reports
plan: 4
type: execute
wave: 1
depends_on: [3]
files_modified:
  - tests/components/glt_flow_card/test_history_routes.py
  - tests/components/glt_flow_card/test_history_bounds.py
  - tests/components/glt_flow_card/test_series_coverage.py
  - test/replay-truth.test.mjs
  - tools/phase7-red-gate.mjs
autonomous: true
requirements: [HIST-01]
must_haves:
  truths:
    - "Each sentinel fails for exactly one named reason and no other."
    - "A sentinel asserts an outcome, never the name of a module that must exist."
    - "The RED gate rejects a skipped run, a zero-test run and a harness failure."
---

## Phase Goal

**As an** engineer, **I want** the history contracts written as failing tests before they are built, **so that** the implementation is measured against a specification rather than described by one.

<objective>Specify the history route, bounds, coverage and replay RED contracts.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Routes and bounds</name>
  <files>tests/components/glt_flow_card/test_history_routes.py, tests/components/glt_flow_card/test_history_bounds.py</files>
  <action>Write the sentinels for T7-01 and T7-02: routes declared in both policy tables, enforced server-side, enumeration-filtered with the limit applied after filtering, every read and export audited; and every bound — rows, entities, window — enforced before the query, with a refusal naming the limit or a labelled downgrade to statistics. Assert the *behaviour*: Phase 6 lost a cycle to a sentinel that demanded a `schedule_audit` module when the audit correctly lived in the manager, so a correct implementation failed its own contract. Name outcomes, not files.</action>
  <acceptance_criteria>
    - Each sentinel carries its `EXPECTED_RED[...]` marker and its effect-ledger prefix.
    - Each fails for exactly its own reason at head.
    - No sentinel names an implementation shape.
  </acceptance_criteria>
  <verify><automated>node tools/phase7-red-gate.mjs</automated></verify>
  <done>Two controlled RED sentinels.</done>
</task>

<task type="auto">
  <name>Task 2: Coverage and replay</name>
  <files>tests/components/glt_flow_card/test_series_coverage.py, test/replay-truth.test.mjs</files>
  <action>Write the sentinels for T7-03, T7-04, T7-05 and T7-06. The coverage sentinel drives the five fixture classes from criterion 4 and asserts each produces a **stated** outcome — a test that feeds a Recorder failure and asserts the series is empty has confirmed the defect rather than caught it, and that trap must be closed in the sentinel itself. The replay sentinel asserts that an entity with no history reads as unknown at the replayed instant, not as its live state.</action>
  <acceptance_criteria>
    - The coverage sentinel distinguishes an empty result from an absent query.
    - The replay sentinel fails today for the live-fallback reason and no other.
    - Both are registered in `tools/assert-red.mjs` with their identities.
  </acceptance_criteria>
  <verify><automated>node tools/phase7-red-gate.mjs</automated></verify>
  <done>Four controlled RED sentinels.</done>
</task>
</tasks>

<output>Create `07-04-SUMMARY.md` when done.</output>
