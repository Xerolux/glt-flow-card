---
phase: 07-trends-energy-reports
plan: 17
type: execute
wave: 3
depends_on: [16]
files_modified:
  - src/generated-bases/glt-flow-card.base.js
  - src/v100/core.mjs
  - src/v100/index.js
  - test/shipped-history-truth.test.mjs
autonomous: true
requirements: [HIST-01, ENER-01, REPORT-01]
must_haves:
  truths:
    - "Six browser evaluators are retired reachable and inert."
    - "The assertion reads dist/glt-flow-card.js, not src/."
    - "The browser issues no Recorder request of its own."
    - "Retirement is proven alongside an outcome assertion, never by a grep alone."
---

## Phase Goal

**As an** engineer, **I want** exactly one implementation of each calculation, **so that** two surfaces cannot disagree about the same number.

<objective>Retire the six browser evaluators reachable and inert.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Retire, do not delete</name>
  <files>src/generated-bases/glt-flow-card.base.js, src/v100/core.mjs, src/v100/index.js</files>
  <action>Retire `aggregateSeries`, `integrateEnergy`, `energySummary`, `reportCsv`, `printReport` and `trendCsv` reachable and inert, the way Phase 5 retired the midpoint router and Phase 6 retired `activeAlarm`. Each entry point stays and does nothing, so a test can prove the replacement rather than prove the absence of something nothing checks. Retire `_ensureHistory`'s direct `callApi` at the same time: after this the browser issues no Recorder request of its own.</action>
  <acceptance_criteria>
    - Each function is present in the artifact and computes nothing.
    - No `callApi` to `history/period` remains reachable.
    - The effect ledger shows zero direct Recorder calls from the browser.
  </acceptance_criteria>
  <verify><automated>node --test test/shipped-history-truth.test.mjs</automated></verify>
  <done>One implementation of each calculation.</done>
</task>

<task type="auto">
  <name>Task 2: Assert the shipped bytes, and pair it</name>
  <files>test/shipped-history-truth.test.mjs</files>
  <action>Read `dist/glt-flow-card.js`, not `src/`: Phase 5 found retirements that existed only in files nobody ships while the same defect ran live in the artifact. Then pair the grep with the outcome assertion 07-19 owns, and say so in the test's own documentation — Phase 6's `shipped-alarm-truth` passed throughout while three surfaces reported a confident zero, because a grep can see that a call exists and cannot see whether anything reaches it. A grep is necessary here and it is not sufficient, and the test should not let a reader believe otherwise.</action>
  <acceptance_criteria>
    - The assertion reads the generated artifact.
    - Each retired function is proven inert by structure, not by substring.
    - The test names its own insufficiency and the plan that closes it.
  </acceptance_criteria>
  <verify><automated>node --test test/shipped-history-truth.test.mjs</automated></verify>
  <done>T7-18's artifact half passes.</done>
</task>
</tasks>

<output>Create `07-17-SUMMARY.md` when done.</output>
