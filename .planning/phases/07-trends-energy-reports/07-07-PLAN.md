---
phase: 07-trends-energy-reports
plan: 7
type: execute
wave: 2
depends_on: [6]
files_modified:
  - test/period-parity.test.mjs
  - tests/components/glt_flow_card/fixtures/period_corpus.json
  - tools/generate-period-corpus.mjs
autonomous: true
requirements: [HIST-01]
must_haves:
  truths:
    - "The two runtimes are compared as canonical bytes, not as values."
    - "The corpus is committed data; the generator is not a graph edge."
    - "Every transition date in the corpus is exercised in both directions."
---

## Phase Goal

**As an** engineer, **I want** the axis I verify and the window the report computes to be the same window, **so that** what I checked is what ran.

<objective>Prove both runtimes resolve periods identically.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Byte parity over the corpus</name>
  <files>test/period-parity.test.mjs</files>
  <action>Fix T7-08. Resolve every corpus entry in both runtimes and compare canonical bytes. Phase 6's lesson applies literally: agreeing on every value while disagreeing on every byte is the failure mode, and it comes from serialisation, not arithmetic. Strip milliseconds on the JavaScript side as `canonicalInstant` already does for schedules, and reuse that function rather than writing a second one that drifts.</action>
  <acceptance_criteria>
    - Every corpus entry matches byte for byte.
    - A deliberate one-millisecond perturbation fails the test.
    - The comparison is of canonical bytes, asserted in the test's own structure.
  </acceptance_criteria>
  <verify><automated>node --test test/period-parity.test.mjs</automated></verify>
  <done>T7-08 passes.</done>
</task>

<task type="auto">
  <name>Task 2: The corpus generator is not a graph edge</name>
  <files>tools/generate-period-corpus.mjs, tests/components/glt_flow_card/fixtures/period_corpus.json</files>
  <action>Commit the corpus as data and describe its regeneration as "regenerate with npm run generate:period:corpus" rather than a `generated_by` field naming a command — Phase 5 established the wording and Phase 6 hit the trap again, where a command name in a metadata field was read by the gate as a real graph edge and produced a self-cycle.</action>
  <acceptance_criteria>
    - The gate proves the graph acyclic with the corpus present.
    - Regenerating produces byte-identical output.
    - The corpus covers both transition directions and both `first_weekday` values.
  </acceptance_criteria>
  <verify><automated>node tools/verify-phase7.mjs --dry-run</automated></verify>
  <done>The corpus is evidence, not a dependency.</done>
</task>
</tasks>

<output>Create `07-07-SUMMARY.md` when done.</output>
