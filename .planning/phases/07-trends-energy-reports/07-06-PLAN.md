---
phase: 07-trends-energy-reports
plan: 6
type: execute
wave: 2
depends_on: [4, 5]
files_modified:
  - custom_components/glt_flow_card/period_resolution.py
  - src/v100/period-resolution.mjs
autonomous: true
requirements: [HIST-01]
must_haves:
  truths:
    - "A named period is resolved by asking Home Assistant, never by arithmetic of ours."
    - "Boundaries are local midnights in the site timezone, not UTC epoch multiples."
    - "A 23-hour day, a 25-hour day, a 743-hour month and a 745-hour month all resolve correctly."
    - "The site timezone is Home Assistant's configured timezone, because that is what the Recorder reads."
---

## Phase Goal

**As an** engineer, **I want** a month to be the month the plant experienced, **so that** a period total is comparable with the bill.

<objective>Resolve named periods on local-calendar boundaries.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Delegate the named periods</name>
  <files>custom_components/glt_flow_card/period_resolution.py</files>
  <action>Fix D9 and T7-07. Resolve `day`, `week` and `month` through `recorder/statistics_during_period`'s `period`, and `year`, offsets and `first_weekday` through `recorder/statistic_during_period`'s calendar spec — the research established that the plural command's enum stops at `month`, so reading it alone concludes wrongly that years must be aggregated by hand. Our code names, validates and renders a period; it never computes one. Return the resolved boundaries with their offsets so the surface can state them.</action>
  <acceptance_criteria>
    - Every corpus entry resolves to the boundaries the research measured.
    - A transition day resolves to 23 or 25 hours and a transition month to 743 or 745.
    - An unknown period name is refused with a reason.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_period_resolution.py -q -x</automated></verify>
  <done>T7-07 passes.</done>
</task>

<task type="auto">
  <name>Task 2: The browser resolves for display only</name>
  <files>src/v100/period-resolution.mjs</files>
  <action>Mirror the resolution in JavaScript for axis labels and the picker's preview, and make it structurally impossible for it to answer a query: it returns boundaries and labels, never a request. The authoritative resolution stays server-side, for the reason Phase 6's schedule preview does — resolving in the browser answers for the browser's timezone, and a browser in a different zone from the plant is normal.</action>
  <acceptance_criteria>
    - The browser module cannot issue a query.
    - It produces the same boundaries as the Companion for every corpus entry.
    - Milliseconds are stripped so canonical bytes can match.
  </acceptance_criteria>
  <verify><automated>node --test test/period-resolution.test.mjs</automated></verify>
  <done>Both runtimes resolve, and only one decides.</done>
</task>
</tasks>

<output>Create `07-06-SUMMARY.md` when done.</output>
