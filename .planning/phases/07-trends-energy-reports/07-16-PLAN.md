---
phase: 07-trends-energy-reports
plan: 16
type: execute
wave: 3
depends_on: [14]
files_modified:
  - src/v100/report-model.mjs
  - src/v100/report-renderings.mjs
autonomous: true
requirements: [REPORT-01]
must_haves:
  truths:
    - "Screen, CSV and print derive from one model, never from each other's serialisation."
    - "A value containing a semicolon, newline or quote survives all three renderings."
    - "No exported row carries a value borrowed from another timestamp."
    - "A truncated export says it was truncated."
---

## Phase Goal

**As a** recipient of a report, **I want** the printed table and the CSV to say the same thing as the screen, **so that** I can act on either.

<objective>Derive screen, CSV and print from one model.</objective>

<tasks>
<task type="auto">
  <name>Task 1: One model, three renderings</name>
  <files>src/v100/report-model.mjs, src/v100/report-renderings.mjs</files>
  <action>Fix D21 and T7-16. `printReport` rebuilds its table with `csv.split("\n")` and `line.split(";")` and then strips quotes with a regex, discarding the quoting `csvCell` correctly applied — so a German decimal list, an equipment name or an acknowledgement comment containing a semicolon becomes extra columns, and one containing a newline becomes extra rows. Deriving one rendering from another's serialisation is the defect, not the symptom. All three render from the model.</action>
  <acceptance_criteria>
    - The three renderings agree on a semicolon, a newline, a quote and a comma decimal.
    - No rendering parses another's output.
    - Each rendering carries the period, coverage and source statements.
  </acceptance_criteria>
  <verify><automated>node --test test/report-renderings.test.mjs</automated></verify>
  <done>T7-16 passes.</done>
</task>

<task type="auto">
  <name>Task 2: No borrowed values, and say what produced the file</name>
  <files>src/v100/report-renderings.mjs</files>
  <action>Fix D22, D13 and the second half of T7-18. `trendCsv` joins series by nearest neighbour with no maximum distance, so a sample from four hours away is written into this minute's row with no marker — the file states values that were never measured at the times it attributes them to. Emit the value measured in the interval or an explicit blank. And record what produced the file: aggregate, deadband, bounds, period, timezone and coverage, because the deadband silently thins every export today and a thinned file cannot be reproduced or even interpreted later. A truncated export says so.</action>
  <acceptance_criteria>
    - No row carries a value from outside its interval.
    - Every export states aggregate, deadband, bounds, period, timezone and coverage.
    - A truncated export declares the truncation.
  </acceptance_criteria>
  <verify><automated>node --test test/report-renderings.test.mjs</automated></verify>
  <done>T7-17 passes.</done>
</task>
</tasks>

<output>Create `07-16-SUMMARY.md` when done.</output>
