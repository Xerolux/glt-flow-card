---
phase: 07-trends-energy-reports
plan: 14
type: execute
wave: 2
depends_on: [12]
files_modified:
  - custom_components/glt_flow_card/report_runs.py
  - custom_components/glt_flow_card/history_routes.py
autonomous: true
requirements: [REPORT-01]
must_haves:
  truths:
    - "A run records window, timezone, aggregate, deadband, sources and coverage."
    - "Re-running reproduces the value or names which input changed."
    - "Report ids are content-derived or authored, never minted from the clock."
    - "Run retention is bounded with a configured default."
---

## Phase Goal

**As an** engineer, **I want** a report I can re-run, **so that** the number I sent someone last month can be defended this month.

<objective>Record what a report run was computed from.</objective>

<tasks>
<task type="auto">
  <name>Task 1: The recorded run</name>
  <files>custom_components/glt_flow_card/report_runs.py</files>
  <action>Fix D19 and T7-14. What ships is a snapshot of current display values with a period label nothing reads, so a "Monatsbericht" contains one instant and says so nowhere. A run records `{report_id, version, window, timezone, aggregate, deadband, sources[], coverage, produced_at}` and computes its content from the resolved period. This is Phase 1's receipt pattern and Phase 2's audit provenance applied to a computed artifact rather than a mutation.</action>
  <acceptance_criteria>
    - A run's content comes from the resolved period, not from the current screen.
    - Every recorded input is present and typed.
    - A run with incomplete coverage states it rather than omitting it.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_report_runs.py -q -x</automated></verify>
  <done>A run knows what it was.</done>
</task>

<task type="auto">
  <name>Task 2: Reproducible, or explicit about why not</name>
  <files>custom_components/glt_flow_card/report_runs.py</files>
  <action>Fix D23 and the first half of T7-18. Re-running reproduces the value or names **which input changed** — both halves matter, because a report that silently produces a different number the second time is worse than one that refuses, given the first version has already been sent to someone. Derive ids from content or accept an authored one; never `Date.now()`, which Phase 5 already found and fixed in paste, for the same two reasons: a clock-derived id is not reproducible and collides within a millisecond. Bound run retention with a conservative configured default, as Phase 6 bounded alarm history.</action>
  <acceptance_criteria>
    - A re-run over unchanged inputs reproduces the value exactly.
    - A re-run over changed inputs names the changed input.
    - Ids are stable across runs and retention is bounded.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_report_runs.py -q -x</automated></verify>
  <done>T7-14 passes.</done>
</task>
</tasks>

<output>Create `07-14-SUMMARY.md` when done.</output>
