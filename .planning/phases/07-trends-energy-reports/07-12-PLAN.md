---
phase: 07-trends-energy-reports
plan: 12
type: execute
wave: 2
depends_on: [10]
files_modified:
  - custom_components/glt_flow_card/energy_model.py
  - custom_components/glt_flow_card/history_routes.py
autonomous: true
requirements: [ENER-01]
must_haves:
  truths:
    - "A counter is differenced across the resolved period; a rate is integrated over it."
    - "The two models are never converted into each other implicitly."
    - "Reset handling is the Recorder's `change`, not an implementation of ours."
    - "A period outside the statistic's coverage is out of coverage, never consumption."
---

## Phase Goal

**As an** engineer, **I want** a period total to be the consumption in that period, **so that** the figure can be compared with a bill.

<objective>Difference counters and integrate rates over resolved periods.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Two models, kept apart</name>
  <files>custom_components/glt_flow_card/energy_model.py</files>
  <action>Fix D14, D18 and T7-10. A **counter** is differenced across the resolved period using the Recorder's `change`, which the research established is already reset-aware over a reset-corrected running total — re-implementing meter reset detection would duplicate a well-tested implementation and, since resets are rare and hard to fixture, get it wrong in a way nobody notices for months. A **rate** is integrated over the period with gaps excluded from the integral and the excluded span reported as missing coverage, rather than integrated straight through as today's trapezoid does. Neither model converts into the other implicitly; conversion is a declared operation with a declared unit, the way Phase 6 kept interval and instant schedule bindings apart.</action>
  <acceptance_criteria>
    - A counter total equals the reset-aware change over the resolved period.
    - A rate integral excludes gaps and reports the excluded span.
    - A meter with no declared model is refused, not guessed.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_energy_counters.py -q -x</automated></verify>
  <done>T7-10 passes.</done>
</task>

<task type="auto">
  <name>Task 2: Close the zero-previous-sum trap</name>
  <files>custom_components/glt_flow_card/energy_model.py</files>
  <action>Fix T7-09. The research found it at `statistics.py:1947`: `prev_sum = prev_sums.get(statistic_id) or 0`. When the window begins before the statistic's first recorded row — a sensor added last week, queried for last year — the previous sum is zero and the first bucket's `change` is *the entire accumulated total*. It is not an error, not a null, and not obviously wrong: a plausible large number in the first period of the window, which is exactly this phase's failure shape. Determine the statistic's earliest row and report any period before it as **out of coverage**. Also carry `sum: None → change: None` through to the surface as "no data" rather than coercing it to zero on the way.</action>
  <acceptance_criteria>
    - A window starting before the statistic exists reports out of coverage, not consumption.
    - A null change survives to the result as null.
    - The corpus fixture for this trap passes.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_energy_counters.py -q -x</automated></verify>
  <done>T7-09 passes.</done>
</task>
<task type="auto">
  <name>Task 3: Actually ask the Recorder</name>
  <files>custom_components/glt_flow_card/recorder_query.py, custom_components/glt_flow_card/__init__.py</files>
  <action>**Added during execution, because the planning omitted it.** 07-08 declared the four history routes, 07-09 bounded them and 07-10 computes coverage from an answer — and no plan owned the step that *obtains* the answer. The routes still return `coverage: 0, source: "unavailable"`, which is honest but inert.

Issue the query: `recorder/statistics_during_period` for `day`, `week` and `month`, `recorder/statistic_during_period`'s calendar spec for `year`, and `history/history_during_period` for raw. Feed the answer to `series_coverage.build_series` with the expected instants from `period_resolution`, so coverage is computed where the expectation is known. Every request goes through `history_bounds.decide_query` first, and every response through the query ledger.

A surface that renders an always-empty series would pass its own rendering tests and show an operator nothing, which is exactly the failure T7-22 exists to block — so this must land before 07-18.</action>
  <acceptance_criteria>
    - A route returns real rows from the fixture Recorder, with coverage computed against the resolved period.
    - The browser issues no Recorder request of its own.
    - The query ledger records contract, entity count, window and row count for every request, and no request exceeds a declared bound.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_energy_counters.py tests/components/glt_flow_card/test_history_bounds.py -q -x</automated></verify>
  <done>The routes return what the Recorder holds.</done>
</task>
</tasks>

<output>Create `07-12-SUMMARY.md` when done.</output>
