---
phase: 07-trends-energy-reports
plan: 10
type: execute
wave: 2
depends_on: [9]
files_modified:
  - custom_components/glt_flow_card/series_coverage.py
  - custom_components/glt_flow_card/history_routes.py
autonomous: true
requirements: [HIST-01]
must_haves:
  truths:
    - "Coverage is computed from expected buckets against returned buckets, server-side."
    - "A gap is carried in the result; nothing interpolates across it."
    - "An unreadable binary sample is indeterminate, never off."
    - "An empty response is an empty result with a reason, never a populated window."
---

## Phase Goal

**As an** operator, **I want** a chart to show me where the data stops, **so that** I never read an outage as a steady plant.

<objective>Carry coverage and gaps with every series.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Compute coverage where the data arrives</name>
  <files>custom_components/glt_flow_card/series_coverage.py</files>
  <action>Fix D1, D10, T7-03 and T7-04. The research established that the Recorder omits empty periods rather than emitting them, so absence is the only signal and the expected buckets must be materialised from the resolved period and compared with what came back. Produce a coverage fraction and a gap list on every series. An empty response yields an empty measured value with `coverage: 0` and a reason — never a populated window with nothing in it, which is what ships today.</action>
  <acceptance_criteria>
    - Coverage is a fraction of expected buckets, and 100 % is stated rather than implied by absence.
    - Every gap appears in the list with its interval.
    - An empty response and a full one are distinguishable in the result.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_series_coverage.py -q -x</automated></verify>
  <done>T7-03 and T7-04 pass.</done>
</task>

<task type="auto">
  <name>Task 2: Absent is never a value</name>
  <files>custom_components/glt_flow_card/series_coverage.py</files>
  <action>Fix D6 and D7 and T7-05. A non-numeric sample — `unavailable`, `unknown` — becomes a gap boundary, not a dropped row that lets the line close over it. A binary point with an unreadable sample is **indeterminate**, never `0`: today `unavailable` is not in `ON_STATES` and is therefore recorded as off, so "I could not read the fault contact" becomes "the fault contact is healthy". This is Phase 6's `indeterminate` decision applied to history, and for the same reason.</action>
  <acceptance_criteria>
    - An outage produces a gap, not a straight line.
    - An unreadable binary sample is indeterminate in the result.
    - Neither zero nor the nearest neighbour is ever substituted for an absent sample.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_series_coverage.py -q -x</automated></verify>
  <done>T7-05 passes.</done>
</task>
</tasks>

<output>Create `07-10-SUMMARY.md` when done.</output>
