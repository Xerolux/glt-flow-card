---
phase: 07-trends-energy-reports
plan: 18
type: execute
wave: 3
depends_on: [17]
files_modified:
  - src/v100/project-trends.js
  - src/v100/index.js
  - test/e2e/project-trends.spec.mjs
autonomous: true
requirements: [HIST-01, ENER-01, REPORT-01]
must_haves:
  truths:
    - "Six elements, in German and English, with every string written out in both."
    - "A gap is a break in the line, never a line of any style."
    - "Every chart has a keyboard-reachable tabular alternative."
    - "Operator text is set as text content, asserted by structure in the shipped artifact."
---

## Phase Goal

**As an** operator on a kiosk with no pointer, **I want** to read the plant's history, **so that** the installation I work in is not the one the product forgot.

<objective>Ship the trend and report surfaces.</objective>

<tasks>
<task type="auto">
  <name>Task 1: The six elements</name>
  <files>src/v100/project-trends.js</files>
  <action>Build `glt-flow-card-trend-chart`, `-trend-table`, `-coverage-badge`, `-period-picker`, `-energy-summary` and `-report-designer` to `07-UI-SPEC.md`. A gap is a **break**, not a dashed or lighter segment: on a monochrome kiosk and in forced colours those are the same line. Coverage is stated on every chart including at 100 %, so its absence never means "we forgot to check". Series carry a marker shape and a label as well as a colour, as Phase 6 gave alarm priorities a word and a shape. Replace the three `prompt()` calls on the report path with form fields that validate. Check at module load that every string has both languages.</action>
  <acceptance_criteria>
    - All six elements are defined in the generated artifact.
    - A gap renders as a break, in both languages and in forced colours.
    - No `window.prompt` remains on the report authoring path.
  </acceptance_criteria>
  <verify><automated>node tools/run-exact-dist-playwright.mjs --grep=phase-7-trends</automated></verify>
  <done>The surfaces exist in the shipped artifact.</done>
</task>

<task type="auto">
  <name>Task 2: Keyboard, and text that stays text</name>
  <files>test/e2e/project-trends.spec.mjs</files>
  <action>Fix T7-19 and T7-20. Every chart has a keyboard-reachable table exposing exactly the plotted values, with a gap as a marked row carrying its interval — not a blank cell and not an omitted row. Assert injection by **structure, not substring**: whether the browser parsed something as markup, meaning an element that exists or an attribute that got attached. Escaped text still contains `onerror=` as characters, so a substring search fails a correct implementation, and Phase 6 lost a cycle to exactly that. Assert the other half too: the operator's words must still reach the reader.</action>
  <acceptance_criteria>
    - Every chart's table is reachable and complete without a pointer.
    - Injected markup produces no element and no handler attribute, and the text still renders.
    - The effect ledger shows no service call, no dialog and no script insertion.
  </acceptance_criteria>
  <verify><automated>node tools/run-exact-dist-playwright.mjs --grep=phase-7-trends</automated></verify>
  <done>T7-19 and T7-20 pass.</done>
</task>
</tasks>

<output>Create `07-18-SUMMARY.md` when done.</output>
