---
phase: 07-trends-energy-reports
plan: 20
type: execute
wave: 4
depends_on: [19]
files_modified:
  - tools/verify-phase7.mjs
  - docs/wiki/Trends-Energy.md
  - docs/wiki/Reports.md
  - .planning/phases/07-trends-energy-reports/07-THREATS.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [HIST-01, ENER-01, REPORT-01]
must_haves:
  truths:
    - "Every threat row is marked from its own owner command, run at head."
    - "A row whose evidence is a grep also carries an outcome assertion."
    - "The documentation states what the product does not have, not only what it does."
    - "A blocked command is recorded with its exact failure, never inferred from its parts."
---

## Phase Goal

**As an** engineer, **I want** the phase closed against evidence rather than recollection, **so that** the register means what it says.

<objective>Build out the Phase-7 gate and close the phase honestly.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Document the contract in German and English</name>
  <files>docs/wiki/Trends-Energy.md, docs/wiki/Reports.md</files>
  <action>Write the two pages the way Phase 6 wrote its alarm and schedule pages: what the product does, why each default is what it is, and — the part that matters most here — **what it does not have**. This product reads Home Assistant's Recorder; it is not a historian, has no time-series database of its own and no retention policy of its own, and the current interface implies otherwise. State the coverage rule, the two meter models, the local-calendar periods with their 23- and 25-hour days, and the fact that a refused unit pair is a refusal rather than a guess. Update the sidebar and both READMEs.</action>
  <acceptance_criteria>
    - Both pages exist in German, with the English summary in the README.
    - The "not a historian" statement is explicit.
    - `node tools/verify-docs-site.mjs` passes with the new sources counted.
  </acceptance_criteria>
  <verify><automated>node tools/verify-docs-site.mjs</automated></verify>
  <done>The contract is written down.</done>
</task>

<task type="auto">
  <name>Task 2: Close the register from commands, not memory</name>
  <files>.planning/phases/07-trends-energy-reports/07-THREATS.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <action>Run each threat's owner command at head and mark each row **only** from its own command passing — where two rows name the same command, run it for each row rather than inferring one from the other's result. Any row whose evidence is a grep over `dist/` must also carry its paired outcome assertion, per the register's blocking rule. Record any blocked command with its exact failure rather than its likely cause: Phase 6 found two independent environment limits and had recorded only one. Write `07-SUMMARY.md`, tick the roadmap plan lines, and update `STATE.md` and `.continue-here.md`.</action>
  <acceptance_criteria>
    - Every marked row has its own at-head run.
    - Blocked rows carry the exact failure output, not a paraphrase.
    - The summary states the limitations, including anything the phase raised rather than took.
  </acceptance_criteria>
  <verify><automated>npm run test:phase7</automated></verify>
  <done>The phase is closed against evidence.</done>
</task>
</tasks>

<output>Create `07-20-SUMMARY.md` when done.</output>
