---
phase: 07-trends-energy-reports
plan: 11
type: execute
wave: 2
depends_on: [10]
files_modified:
  - src/v100/replay-truth.mjs
  - src/v100/index.js
autonomous: true
requirements: [HIST-01]
must_haves:
  truths:
    - "A replayed instant reads what was recorded at that instant."
    - "An entity with no history is unknown at the replayed instant, never live."
    - "The surface distinguishes a replayed value from a live one."
---

## Phase Goal

**As an** operator replaying last Tuesday, **I want** to see last Tuesday, **so that** I do not diagnose a fault from today's values.

<objective>Make replay read the record, not the present.</objective>

<tasks>
<task type="auto">
  <name>Task 1: No live fallback</name>
  <files>src/v100/replay-truth.mjs</files>
  <action>Fix D8 and T7-06. `_stateAt` returns the *live* state when an entity has no series, so replaying a past moment shows today's value for everything the Recorder did not keep — mixed into the same view as entities that do have history, with nothing distinguishing them. Return an explicit unknown instead, carrying the reason. An honest blank is worth more than a confident wrong value, and this one is confident in the most misleading possible way: it is the correct current value of the right entity.</action>
  <acceptance_criteria>
    - An entity with no series reads as unknown at the replayed instant.
    - A replayed value and a live value are distinguishable by the caller.
    - The reason accompanies the unknown.
  </acceptance_criteria>
  <verify><automated>node --test test/replay-truth.test.mjs</automated></verify>
  <done>T7-06 passes.</done>
</task>

<task type="auto">
  <name>Task 2: The surface says which mode it is in</name>
  <files>src/v100/index.js</files>
  <action>While replay is active, the surface states the replayed instant and marks every value that could not be resolved at it. A replay that silently mixes recorded and live values is worse than no replay, because the operator has no way to tell which is which — the same argument that made Phase 6 keep `indeterminate` visible rather than collapsing it into a neighbouring state.</action>
  <acceptance_criteria>
    - The replayed instant is stated on screen.
    - Unresolvable values are marked, not blank and not live.
    - The mark is text, not colour alone.
  </acceptance_criteria>
  <verify><automated>node --test test/replay-truth.test.mjs</automated></verify>
  <done>Replay is honest about what it knows.</done>
</task>
</tasks>

<output>Create `07-11-SUMMARY.md` when done.</output>
