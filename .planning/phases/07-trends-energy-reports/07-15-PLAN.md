---
phase: 07-trends-energy-reports
plan: 15
type: execute
wave: 3
depends_on: [14]
files_modified:
  - custom_components/glt_flow_card/report_schedule.py
  - custom_components/glt_flow_card/__init__.py
autonomous: true
requirements: [REPORT-01]
must_haves:
  truths:
    - "A report schedule is validated when it is authored, not when it should have run."
    - "Execution goes through the Phase-6 schedule runner, not a second one."
    - "Every run, successful or failed, is recorded with its outcome."
    - "An unconfigured installation delivers a report to nobody."
---

## Phase Goal

**As an** engineer, **I want** a scheduled report to actually run, **so that** the designer's "Automatik" column means something.

<objective>Execute report schedules through the Phase-6 runner.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Validate at authoring time</name>
  <files>custom_components/glt_flow_card/report_schedule.py</files>
  <action>Fix D20 and T7-15. Today the schedule is free text from a `prompt()` — "Automatik (z.B. 1 07:00) oder leer" — that no parser, validator or runner reads, and the designer renders the string back under a heading that implies it works. Parse and validate the schedule when it is saved, refusing an invalid one **at that moment** rather than discovering it at the time it should have run. This is Phase 6's rule for schedule times, applied to the same class of field.</action>
  <acceptance_criteria>
    - An invalid schedule is refused at save with a reason.
    - A valid schedule resolves to instants through the Phase-6 resolution, DST included.
    - No schedule string is stored unparsed.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_report_schedule.py -q -x</automated></verify>
  <done>An authored schedule is a real schedule.</done>
</task>

<task type="auto">
  <name>Task 2: One runner, and every run recorded</name>
  <files>custom_components/glt_flow_card/__init__.py</files>
  <action>Execute report schedules through the runner Phase 6 built rather than a second one that would drift from it — the product already has four things that disagreed about alarm severity, and a second scheduler is the same mistake in a new place. Record every run with its outcome, successful or failed, as Phase 6 records schedule executions. Delivery targets, if any, go through the Phase-6 notification allowlist: an unconfigured installation delivers a report to nobody, and that default is a site decision.</action>
  <acceptance_criteria>
    - Report runs and schedule runs share one runner and one resolution.
    - A failed run is recorded and distinguishable from one that never ran.
    - No delivery reaches a target outside the allowlist.
  </acceptance_criteria>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_report_schedule.py -q -x</automated></verify>
  <done>T7-15 passes.</done>
</task>
</tasks>

<output>Create `07-15-SUMMARY.md` when done.</output>
