---
phase: 07-trends-energy-reports
plan: 3
type: execute
wave: 0
depends_on: [2]
files_modified:
  - schemas/project/6.schema.json
  - src/v100/project-migrations.mjs
  - custom_components/glt_flow_card/project_migrations.py
  - custom_components/glt_flow_card/schemas/
autonomous: true
requirements: [HIST-01, ENER-01, REPORT-01]
must_haves:
  truths:
    - "Schema 6 closes the trend, energy and report shapes."
    - "The 5→6 migration is sequential and receipted, in both runtimes."
    - "Every field the migration lists is declared in the schema, and the reverse."
    - "A meter with no declared model is refused at validation, not at arithmetic."
---

## Phase Goal

**As an** engineer, **I want** every field this phase reads to be declared, **so that** `period: "sometimes"` fails at validation instead of inside a computed figure.

<objective>Introduce schema 6 and the sequential 5→6 migration.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Schema 6</name>
  <files>schemas/project/6.schema.json, custom_components/glt_flow_card/schemas/</files>
  <action>Fix T7-21. Close three shapes: the trend (period, aggregate, deadband, bounds, source), the meter (entity, model — `rate` or `counter` — unit, price unit, medium, CO₂ factor) and the report (id, version, period, content selection, formats, schedule). A meter with no declared model is refused at validation; today the model is inferred from which code path reads it, which is how one number gets integrated and another multiplied by a price. Generate the standalone validators as every prior schema version does.</action>
  <acceptance_criteria>
    - `period: "sometimes"`, `deadband: "a bit"` and a model-less meter are all refused by validation.
    - Generated validators are byte-identical across two builds.
    - The schema is reachable from the repository-local registry with no remote resolution.
  </acceptance_criteria>
  <verify><automated>node --test test/v100-migrations.test.mjs</automated></verify>
  <done>The shapes are closed.</done>
</task>

<task type="auto">
  <name>Task 2: The 5→6 migration, in both runtimes</name>
  <files>src/v100/project-migrations.mjs, custom_components/glt_flow_card/project_migrations.py</files>
  <action>Add the sequential receipted step. Migrate `historian.bucket_minutes` to a named period where it maps cleanly and report it where it does not — a 43 200-minute bucket is not a month, and pretending it is would carry D9 forward under a new name. Infer each meter's model from its unit where the unit is unambiguous and **report** the ones that need an operator decision rather than guessing. Add `SCHEMA_MIRRORED_FIELDS` and the test Phase 6 needed: the migration's field list and the schema must declare the same fields, or the migration quarantines a field the schema requires and the symptom is a roll-up that silently counts nothing.</action>
  <acceptance_criteria>
    - The migration is sequential from 5 and receipted.
    - A bucket that is not a named period is reported, not silently mapped.
    - The field list and the schema agree, asserted in both runtimes.
  </acceptance_criteria>
  <verify><automated>node --test test/v100-migrations.test.mjs && py -3.13 -m pytest tests/components/glt_flow_card/test_project_migrations.py -q -x</automated></verify>
  <done>T7-21 passes.</done>
</task>
</tasks>

<output>Create `07-03-SUMMARY.md` when done.</output>
