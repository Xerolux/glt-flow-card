---
phase: 07-trends-energy-reports
plan: 19
type: execute
wave: 3
depends_on: [18]
files_modified:
  - test/e2e/project-trends.spec.mjs
  - src/v100/index.js
autonomous: true
requirements: [HIST-01, ENER-01, REPORT-01]
must_haves:
  truths:
    - "Every retired value is proven reached, by rendering the surface and reading the number."
    - "A surface that shows a confident zero fails, even when the artifact contains every call."
    - "The fetch that feeds a surface is throttled and bounded."
---

## Phase Goal

**As an** engineer, **I want** the authoritative number to actually appear on the screen, **so that** the phase does not repeat the defect the last one shipped.

<objective>Prove every retired value is reached, not merely reachable.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Render, open nothing, read the number</name>
  <files>test/e2e/project-trends.spec.mjs</files>
  <action>Fix T7-22, which exists because of what Phase 6 shipped. Retiring `activeAlarm` left it reading `card._alarmState`, and only the alarm panel ever wrote that field — so the toolbar badge, the per-site count and the report's Status column all reported no active alarms until an operator happened to open the modal. `test/shipped-alarm-truth.test.mjs` passed the whole time, because `alarms/list` did appear in the shipped bytes, in the one place nothing else reached. For each retired evaluator, render the surface, open nothing, and assert the number that appears is the authoritative one. A grep cannot tell reachable from reached.</action>
  <acceptance_criteria>
    - Each surface shows the authoritative value after a plain render.
    - A surface showing a confident zero fails with a message naming it.
    - The assertion is confirmed to fail if the fetch is removed.
  </acceptance_criteria>
  <verify><automated>node tools/run-exact-dist-playwright.mjs --grep=phase-7-trends</automated></verify>
  <done>T7-22 passes.</done>
</task>

<task type="auto">
  <name>Task 2: Bounded, so the fix is not a new cost</name>
  <files>src/v100/index.js</files>
  <action>Whatever feeds a surface is throttled and stamps before the request, so a Companion that is refusing or unreachable is asked once per interval rather than once per render — the pattern the Phase-6 follow-up established. Assert that a burst of renders produces no additional request. This phase bounded the backend's query cost in 07-09 and must not hand the cost back to the browser.</action>
  <acceptance_criteria>
    - Ten renders produce no more than one request per interval.
    - A failing backend is retried on the interval, not on every render.
    - The query ledger shows the request count stayed within the bound.
  </acceptance_criteria>
  <verify><automated>node tools/run-exact-dist-playwright.mjs --grep=phase-7-trends</automated></verify>
  <done>The surfaces are fed, and bounded.</done>
</task>
</tasks>

<output>Create `07-19-SUMMARY.md` when done.</output>
