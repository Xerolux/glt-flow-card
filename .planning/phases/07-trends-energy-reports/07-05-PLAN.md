---
phase: 07-trends-energy-reports
plan: 5
type: execute
wave: 1
depends_on: [3]
files_modified:
  - tests/components/glt_flow_card/test_period_resolution.py
  - tests/components/glt_flow_card/test_energy_counters.py
  - tests/components/glt_flow_card/test_energy_units.py
  - tests/components/glt_flow_card/test_report_runs.py
  - test/period-parity.test.mjs
  - test/report-renderings.test.mjs
autonomous: true
requirements: [HIST-01, ENER-01, REPORT-01]
must_haves:
  truths:
    - "The parity sentinel compares canonical bytes from the first commit, never verdicts."
    - "The energy sentinels drive both research traps, not only the happy path."
    - "The rendering sentinel uses values a serialisation round-trip would break."
---

## Phase Goal

**As an** engineer, **I want** the period, energy and report contracts written as failing tests, **so that** the arithmetic is specified before it is written.

<objective>Specify the period, energy, report and rendering RED contracts.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Periods and parity</name>
  <files>tests/components/glt_flow_card/test_period_resolution.py, test/period-parity.test.mjs</files>
  <action>Write the sentinels for T7-07 and T7-08 against the committed corpus, including the 23- and 25-hour days and the 743- and 745-hour months the research measured. Compare **canonical bytes** from the outset, not values: Phase 6's parity work agreed on every value and disagreed on every byte because `toISOString()` writes milliseconds and Python's `isoformat()` omits them at zero, and retrofitting the byte comparison afterwards cost a cycle.</action>
  <acceptance_criteria>
    - The corpus drives both transition directions and both `first_weekday` values.
    - The parity sentinel compares canonical bytes.
    - Each fails for exactly its own reason at head.
  </acceptance_criteria>
  <verify><automated>node tools/phase7-red-gate.mjs</automated></verify>
  <done>Two more controlled RED sentinels.</done>
</task>

<task type="auto">
  <name>Task 2: Energy, runs and renderings</name>
  <files>tests/components/glt_flow_card/test_energy_counters.py, tests/components/glt_flow_card/test_energy_units.py, tests/components/glt_flow_card/test_report_runs.py, test/report-renderings.test.mjs</files>
  <action>Write the sentinels for T7-09 through T7-14 and T7-16 through T7-18. The counter sentinel must drive the research's first trap — a window beginning before the statistic exists, where the Recorder's previous sum defaults to zero and the first period reports the entire accumulated total as consumption. The unit sentinel drives the circular-mean case, where an arithmetic mean of 350° and 10° gives 180°. The rendering sentinel uses a semicolon, a newline, a quote and a comma decimal, because those are exactly the values today's CSV round-trip destroys.</action>
  <acceptance_criteria>
    - Both research traps are driven, not only the ordinary case.
    - The rendering sentinel asserts the three renderings agree, not that each parses.
    - Every sentinel is registered with its marker and prefix.
  </acceptance_criteria>
  <verify><automated>node tools/phase7-red-gate.mjs</automated></verify>
  <done>The full RED wave is specified.</done>
</task>
</tasks>

<output>Create `07-05-SUMMARY.md` when done.</output>
