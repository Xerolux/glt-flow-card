---
phase: 01-trusted-contract-release-foundation
plan: 06
type: execute
wave: 5
depends_on: ["01-05"]
files_modified:
  - src/v100/project-bundle.mjs
  - src/v100/core.mjs
  - custom_components/glt_flow_card/project_bundle.py
  - test/v100-bundle.test.mjs
  - tests/components/glt_flow_card/test_project_bundle.py
autonomous: true
requirements: [SCHEMA-01]
must_haves:
  truths:
    - "A `.gltproject` is completely preflighted before any member is extracted or exposed."
    - "JS and Python reject the same traversal, alias, collision, overlap, type, encryption, CRC/hash, size/count/ratio, reference, and limit failures."
    - "Accepted custom assets remain opaque byte arrays with verified hashes and exact reference closure."
  artifacts:
    - path: "src/v100/project-bundle.mjs"
      provides: "Bounded browser/Node archive preflight and deterministic writer"
    - path: "custom_components/glt_flow_card/project_bundle.py"
      provides: "Equivalent server preflight and controlled extraction"
  key_links:
    - from: "src/v100/project-bundle.mjs"
      to: "schemas/bundle-manifest.schema.json"
      via: "raw manifest validation and exact member closure"
      pattern: "manifest|sha256|assets"
---

<objective>
Implement safe cross-runtime project bundles without active asset inspection.

Purpose: Prevent archive attacks and preserve user assets while supporting verified import/export.
Output: Bounded JS/Python bundle APIs and hostile archive coverage.
</objective>

<execution_context>
@C:/Users/basti/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/basti/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-trusted-contract-release-foundation/01-05-SUMMARY.md
@.planning/phases/01-trusted-contract-release-foundation/01-THREATS.md
@schemas/bundle-manifest.schema.json
@schemas/limits.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reject unsafe archives before extraction in both runtimes</name>
  <files>src/v100/project-bundle.mjs, custom_components/glt_flow_card/project_bundle.py, test/v100-bundle.test.mjs, tests/components/glt_flow_card/test_project_bundle.py</files>
  <behavior>- Central-directory preflight completes before the extraction callback/write count can exceed zero. - NFC/forward-slash path normalization rejects absolute, drive, UNC, NUL/control, empty/dot/dot-dot, backslash, symlink/special, duplicate/case collision and prefix overlap. - Encryption, unsupported compression, CRC/hash mismatch and every canonical archive limit reject identically.</behavior>
  <action>Use approved zip.js in JavaScript and Python standard ZIP inspection. Enumerate the full central directory, normalize logical names, validate types/methods/sizes/ratios/hashes and one canonical raw manifest/project, then permit extraction only into a newly created controlled transaction directory whose resolved paths remain inside it. Require exact declared/reference/member closure. Do not shell-compose paths or incrementally extract while validating.</action>
  <verify><automated>npm run test:bundle &amp;&amp; py -3.13 -m pytest tests/components/glt_flow_card/test_project_bundle.py -q -k "reject or limit or collision or traversal"</automated></verify>
  <done>T-04 owner command proves every hostile archive is rejected before any extraction side effect with stable JS/Python errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Round-trip deterministic bundles with opaque assets</name>
  <files>src/v100/project-bundle.mjs, src/v100/core.mjs, custom_components/glt_flow_card/project_bundle.py, test/v100-bundle.test.mjs, tests/components/glt_flow_card/test_project_bundle.py</files>
  <behavior>- Valid project plus assets round-trip with canonical project bytes and byte-identical assets. - Entry order/metadata are deterministic. - SVG/HTML/script canaries are never parsed, rendered, imported or network-fetched.</behavior>
  <action>Implement deterministic bundle creation after contract validation/migration, using fixed ordering/metadata and per-member SHA-256. Return opaque asset bytes plus allowlisted metadata only; do not sanitize by executing/parsing content. Export compatibility APIs through `core.mjs`. Add active-content canaries and effect spies for imports/network/DOM.</action>
  <verify><automated>npm run test:bundle &amp;&amp; py -3.13 -m pytest tests/components/glt_flow_card/test_project_bundle.py -q -k "roundtrip or opaque or deterministic"</automated></verify>
  <done>Valid bundles round-trip deterministically with exact asset bytes and zero active-content effects.</done>
</task>

</tasks>

<threat_model>
Canonical ledger: `01-THREATS.md`.

| ID | Stable threat description | Plan role | Mitigation / verification |
|---|---|---|---|
| T-04 | ZIP traversal, alias, collision, overlap, bomb | OWNER 01-06-T1 | Full preflight before extraction; ledger command |
| T-05 | SVG/HTML/script asset executes during inspection | supporting opaque-byte control for owner 01-11-T3 | No content parsing/render; bundle active-content tests |
| T-07 | Diagnostics or bundle leaks remote tokens/state | supporting bundle minimization for owner 01-08-T3 | Exact declared members and opaque assets only |
</threat_model>

<verification>Run both runtime bundle suites and assert extraction/DOM/network/service counters stay zero for rejected/active-content inputs.</verification>

<success_criteria>
- T-04 is closed by its exact owner command.
- Bundle decisions match across runtimes.
- Assets are preserved but never executed during Phase-1 inspection.</success_criteria>

<output>Create `.planning/phases/01-trusted-contract-release-foundation/01-06-SUMMARY.md` when done.</output>
