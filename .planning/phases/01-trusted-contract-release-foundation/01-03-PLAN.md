---
phase: 01-trusted-contract-release-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-02"]
files_modified:
  - schemas/project/0.schema.json
  - schemas/project/1.schema.json
  - schemas/project/2.schema.json
  - schemas/bundle-manifest.schema.json
  - schemas/limits.json
  - schemas/diff-policy.json
  - tools/generate-contract-fixtures.mjs
  - test/fixtures/contracts/manifest.json
  - test/contract-fixtures.test.mjs
autonomous: true
requirements: [SCHEMA-01, DIFF-01]
must_haves:
  truths:
    - "The only canonical authored contract is `schemas/project/{0,1,2}.schema.json`, `schemas/bundle-manifest.schema.json`, `schemas/limits.json`, and `schemas/diff-policy.json`."
    - "Schemas describe raw documents before defaults/normalization and retain all existing Lovelace/YAML-compatible project forms through explicit versioned migration."
    - "A deterministic corpus covers golden, malformed, malicious, boundary, and representative 100/500/2,000-object correctness classes without claiming Phase-10 capacity evidence."
  artifacts:
    - path: "schemas/project/2.schema.json"
      provides: "Current Draft 2020-12 raw project contract"
    - path: "schemas/limits.json"
      provides: "Shared JSON/archive/error resource budgets"
    - path: "test/fixtures/contracts/manifest.json"
      provides: "Expected cross-runtime outcomes and fixture classifications"
  key_links:
    - from: "test/fixtures/contracts/manifest.json"
      to: "schemas/limits.json"
      via: "below/at/above boundary expectations"
      pattern: "limit|expected"
---

<objective>
Author the one canonical raw contract and deterministic shared corpus.

Purpose: Give JavaScript, Python, builds, bundles, and tests one bounded source of truth before implementation adapters are written.
Output: Versioned Draft 2020-12 schemas, bundle schema, limits, semantic policy, and shared fixtures.
</objective>

<execution_context>
@C:/Users/basti/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/basti/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-trusted-contract-release-foundation/01-CONTEXT.md
@.planning/phases/01-trusted-contract-release-foundation/01-RESEARCH.md
@.planning/phases/01-trusted-contract-release-foundation/01-THREATS.md
@examples/idm-neo2030.yaml
@src/v100/core.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author versioned raw schemas, limits, and semantic policy</name>
  <files>schemas/project/0.schema.json, schemas/project/1.schema.json, schemas/project/2.schema.json, schemas/bundle-manifest.schema.json, schemas/limits.json, schemas/diff-policy.json, test/contract-fixtures.test.mjs</files>
  <behavior>
    - Draft 2020-12 schemas accept documented existing versions and reject invalid raw type/required/ID/reference structures before defaults.
    - Limits encode 5 MiB JSON, depth 64, 100,000 nodes, string 256 KiB, ID 128, path 512, errors 100; archive limits encode 32 MiB compressed, 256 entries, 16 MiB/asset, 128 MiB expanded, ratio 100:1.
    - Diff policy declares stable identity fields, five categories add/remove/move/binding/config, relevant-order rules, dependency references, and impact vocabulary.
  </behavior>
  <action>Author only the canonical paths listed in this task and reject any second authored schema source. Model absent legacy as version 0, preserve current version 1, and define current version 2 with bounded extensibility/profile/asset references. Keep normalization/defaults out of raw schemas. Use safe anchored/bounded patterns and repository-owned refs only. Add schema metadata tests for IDs, ref closure, limit completeness, and forbidden duplicate canonical paths.</action>
  <verify><automated>node --test test/contract-fixtures.test.mjs --test-name-pattern="schema|limits|policy|canonical paths"</automated></verify>
  <done>The seven canonical contract files are complete, self-contained, and pass structural/policy tests with no duplicate authored schema path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generate bounded golden, hostile, and scale-class fixtures</name>
  <files>tools/generate-contract-fixtures.mjs, test/fixtures/contracts/manifest.json, test/contract-fixtures.test.mjs</files>
  <behavior>
    - Deterministic fixtures cover every stable error code, raw-vs-normalized trap, dangling/duplicate ID, malicious string/path, and immediately below/at/above each limit.
    - Representative 100, 500, and 2,000-object classes exercise contract/diff correctness with fixed seeds and expected digests.
    - Fixture metadata explicitly labels these as Phase-1 bounded correctness evidence, not capacity/performance certification.
  </behavior>
  <action>Create a deterministic generator that emits large fixtures into temporary test output rather than committing bulky generated bodies. Commit the manifest, compact seeds, expected codes/JSON pointers/digests, and provenance to documented examples. Include raw documents that normalization would otherwise mask and hostile archive metadata seeds for Plan 01-06. Add an assertion preventing benchmark/capacity claims in the Phase-1 fixture evidence.</action>
  <verify><automated>npm run test:fixtures</automated></verify>
  <done>Two independent generator runs match, every contract/limit class has an expected outcome, and 100/500/2,000 classes are clearly non-capacity evidence.</done>
</task>

</tasks>

<threat_model>
Canonical ledger: `01-THREATS.md`.

| ID | Stable threat description | Plan role | Mitigation / verification |
|---|---|---|---|
| T-03 | Oversized/deep/regex-hostile JSON | supporting contract/corpus for owner 01-04-T2 | Canonical limits and below/at/above hostile corpus; `npm run test:fixtures` |
</threat_model>

<verification>Run `npm run test:fixtures`; confirm no duplicate authored schema path exists and generated large fixtures remain temporary.</verification>

<success_criteria>
- Canonical authored contract paths are singular and versioned.
- Limits and stable expected outcomes are executable.
- 100/500/2,000 fixtures test correctness without publishing capacity claims.
</success_criteria>

<output>Create `.planning/phases/01-trusted-contract-release-foundation/01-03-SUMMARY.md` when done.</output>
