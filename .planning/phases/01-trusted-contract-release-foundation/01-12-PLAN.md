---
phase: 01-trusted-contract-release-foundation
plan: 12
type: execute
wave: 9
depends_on: ["01-10", "01-11"]
files_modified:
  - tools/resolve-ha-lanes.mjs
  - tools/test-ha-artifacts.mjs
  - test/ha-lanes.test.mjs
  - .github/workflows/validate.yml
  - .github/workflows/build-v1.yml
  - .github/workflows/hacs.yml
  - package.json
  - hacs.json
  - packaging/hacs-plugin/hacs.json
  - packaging/hacs-integration/hacs.json
  - packaging/hacs-integration/README.md
  - README.md
  - README.de.md
  - docs/wiki/Installation.md
autonomous: true
requirements: [HACS-01, SCHEMA-01, DIFF-01]
must_haves:
  truths:
    - "Before tests, official Home Assistant lane candidates are proven to exist and their immutable digest and runner architecture are recorded."
    - "The minimum lane starts from the advertised floor and must pass; otherwise the plan raises the declared minimum to the first verified passing official release with evidence."
    - "A required minimum raise updates root/category metadata and installation documentation atomically, then rebuilds, restages, and reruns both HA lanes plus local HACS validation."
    - "The current lane is discovered from official stable releases at execution, excludes prereleases, and is pinned by digest rather than a hard-coded future tag."
    - "Exact staged artifacts clean-install and upgrade through supported pytest HA harness/container bootstrap, reload, unload and re-setup with preserved data and zero leaked resources."
  artifacts:
    - path: "tools/resolve-ha-lanes.mjs"
      provides: "Official release/tag/digest/architecture preflight and immutable lane evidence"
    - path: "tools/test-ha-artifacts.mjs"
      provides: "Exact staged ZIP/card lifecycle and upgrade runner"
  key_links:
    - from: "tools/test-ha-artifacts.mjs"
      to: "build/release/glt-flow-card-companion.zip"
      via: "manifest hash verification before supported HA bootstrap"
      pattern: "sha256|reload|unload"
---

<objective>
Prove exact artifacts on verified immutable minimum/current Home Assistant lanes and wire CI to the evidence.

Purpose: Avoid nonexistent future images or unsupported APIs while making compatibility claims executable.
Output: Lane resolver, exact-artifact lifecycle/upgrade runner, and CI jobs.
</objective>

<execution_context>
@C:/Users/basti/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/basti/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-trusted-contract-release-foundation/01-RESEARCH.md
@.planning/phases/01-trusted-contract-release-foundation/01-10-SUMMARY.md
@.planning/phases/01-trusted-contract-release-foundation/01-11-SUMMARY.md
@.planning/phases/01-trusted-contract-release-foundation/01-THREATS.md
@hacs.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Resolve immutable HA lanes and automatically own any minimum-version raise</name>
  <files>tools/resolve-ha-lanes.mjs, tools/test-ha-artifacts.mjs, test/ha-lanes.test.mjs, package.json, hacs.json, packaging/hacs-plugin/hacs.json, packaging/hacs-integration/hacs.json, packaging/hacs-integration/README.md, README.md, README.de.md, docs/wiki/Installation.md</files>
  <behavior>- Preflight queries official release/container metadata read-only, rejects nonexistent/prerelease/mutable-only candidates and records tag, immutable digest, architecture and source. - Minimum starts at root `hacs.json`; current derives from official latest stable at execution. - At most 12 official supported candidates are tried in ascending order. - If the floor fails and a later candidate passes, the first passing release becomes the minimum in every listed metadata/doc file, followed by a rebuild, local restage, both-lane rerun and local category validation. - If none passes, no partial metadata change remains and the task fails closed.</behavior>
  <action>Implement `resolve:ha-minimum` as a bounded autonomous loop together with the minimum-probe and two-lane execution modes in `tools/test-ha-artifacts.mjs`. Read the advertised floor, enumerate no more than 12 official non-prerelease supported releases at or above it, and for each prove tag existence, immutable digest and runner architecture before using the supported minimum-probe harness. Resolve/pin current stable independently from official metadata. If the advertised floor passes, preserve declarations and run the final two-lane validation. If it fails, select the first later passing candidate, stage atomic consistent updates to root `hacs.json`, both authored HACS category metadata files, integration staging README, bilingual root READMEs and Installation documentation, then run `npm run build`, `npm run stage:hacs`, `npm run validate:hacs-staging` and `npm run test:ha-artifacts`; commit the metadata replacement only after all pass. Restore prior files and fail if any post-update gate fails. If no candidate passes within the bound, fail closed with evidence and no proposal-only/user checkpoint path. Do not hard-code a future version or infer Python from dates.</action>
  <verify><automated>node --test test/ha-lanes.test.mjs &amp;&amp; npm run resolve:ha-minimum -- --max-candidates=12</automated></verify>
  <done>Two official digest/architecture-pinned lanes pass; when the original floor failed, all seven minimum-version metadata/doc sources contain the first verified passing release and freshly rebuilt/restaged artifacts pass both HA lanes and local HACS validation; otherwise the task failed closed without partial edits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run exact-artifact install, upgrade and lifecycle in supported HA harnesses</name>
  <files>tools/test-ha-artifacts.mjs, test/ha-lanes.test.mjs, .github/workflows/validate.yml, .github/workflows/build-v1.yml, .github/workflows/hacs.yml, package.json</files>
  <behavior>- Both digest-pinned lanes verify artifact hashes, install the local staged integration ZIP and dashboard card, and use supported Home Assistant pytest fixtures/container bootstrap. - Clean setup, duplicate prevention, fake project preview/apply, reload, unload and re-setup pass. - Upgrade from deterministic previous fixture preserves/migrates projects, snapshot history and rollback evidence. - No external Config Entry endpoint, live instance, remote site, service target or plant write is used.</behavior>
  <action>Extend Task 1's exact-artifact runner with the complete supported pytest Home Assistant Config Entry lifecycle and deterministic previous-version upgrade cases, plus official digest-pinned container package/import/startup validation; do not invent an external Config Entry API. Hash-check local stages before install. Exercise both lanes, prior-artifact upgrade, diagnostics build identity, data digests/revisions, recovery and post-unload resource counts. CI builds once, transfers the manifest-hashed stage, downloads browsers/images only after provenance/lane preflight, pins third-party actions to full SHAs, uses read-only permissions, and runs plugin/local-integration validation separately.</action>
  <verify><automated>npm run test:ha-artifacts</automated></verify>
  <done>Both verified lanes pass exact-byte clean-install/upgrade/reload/unload/re-setup with preserved data, matching identity and zero prohibited effects.</done>
</task>

</tasks>

<threat_model>
Canonical ledger: `01-THREATS.md`.

| ID | Stable threat description | Plan role | Mitigation / verification |
|---|---|---|---|
| T-06 | Interrupted apply corrupts head/history | supporting installed recovery control for owner 01-07-T2 | Upgrade/restart/recovery exact-artifact scenarios |
| T-08 | Generated/release artifact differs from reviewed source | supporting HA compatibility control for owner 01-13-T1 | Digest-pinned lanes and manifest-hashed exact installs; `npm run test:ha-artifacts` |
</threat_model>

<verification>Run lane resolver first, then exact-artifact tests. A missing/failing advertised minimum blocks progress until the declared minimum is raised and retested.</verification>

<success_criteria>
- No nonexistent/future/mutable HA tag is assumed.
- Compatibility evidence records official immutable digests and architecture.
- Exact install/upgrade/lifecycle behavior passes without unsupported external APIs or live writes.</success_criteria>

<output>Create `.planning/phases/01-trusted-contract-release-foundation/01-12-SUMMARY.md` when done.</output>
