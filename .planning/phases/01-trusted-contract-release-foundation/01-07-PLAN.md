---
phase: 01-trusted-contract-release-foundation
plan: 07
type: execute
wave: 5
depends_on: ["01-05"]
files_modified:
  - custom_components/glt_flow_card/project_repository.py
  - custom_components/glt_flow_card/project_transactions.py
  - custom_components/glt_flow_card/const.py
  - custom_components/glt_flow_card/__init__.py
  - tests/components/glt_flow_card/test_project_repository.py
  - tests/components/glt_flow_card/test_project_transactions.py
  - tests/components/glt_flow_card/test_websocket.py
autonomous: true
requirements: [SCHEMA-01, DIFF-01, HACS-01]
must_haves:
  truths:
    - "Legacy project data is copied once into versioned split stores with an untouched retained backup and no destructive conversion."
    - "Preview/apply is bound to HA identity, expected revision, base/candidate digests, stable selected IDs and server-recomputed dependency closure."
    - "Mutation uses a PREPARED→verified immutable snapshot→verified head→COMMITTED journal and deterministic startup recovery."
    - "Rollback resolves a server-owned snapshot and creates a new forward revision; it never mutates history."
  artifacts:
    - path: "custom_components/glt_flow_card/project_repository.py"
      provides: "Split versioned projects/snapshots/journal/audit repositories"
    - path: "custom_components/glt_flow_card/project_transactions.py"
      provides: "Authoritative preview/apply/rollback and recovery coordinator"
  key_links:
    - from: "custom_components/glt_flow_card/__init__.py"
      to: "custom_components/glt_flow_card/project_transactions.py"
      via: "project WebSocket handlers delegate all shared mutations"
      pattern: "expected_revision|preview_id|rollback"
---

<objective>
Make shared persistence versioned, immutable, authoritative, and crash recoverable.

Purpose: Prevent stale/tampered apply, forged rollback and partial-store corruption while preserving legacy data.
Output: Split repositories, legacy importer, transaction coordinator, recovery, and WebSocket tests.
</objective>

<execution_context>
@C:/Users/basti/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/basti/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-trusted-contract-release-foundation/01-05-SUMMARY.md
@.planning/phases/01-trusted-contract-release-foundation/01-THREATS.md
@custom_components/glt_flow_card/__init__.py
@custom_components/glt_flow_card/const.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add split versioned stores and safe legacy import</name>
  <files>custom_components/glt_flow_card/project_repository.py, custom_components/glt_flow_card/const.py, tests/components/glt_flow_card/test_project_repository.py</files>
  <behavior>- Active projects, immutable snapshots, journals and audit metadata have independent Store versions/keys. - Empty stores import legacy data exactly once after raw validation/migration and retain the original. - Failure/retry cannot expose a partial import; retention runs only after verified replacement.</behavior>
  <action>Implement repository helpers over Home Assistant Store with deep-copy reads and independent version constants. Stage one-time legacy import copy-on-write, validate/migrate each project, verify canonical digests after read-back, then mark complete. Preserve the untouched legacy payload under a bounded backup key and never delete it automatically. Test injected failures at every stage with temporary stores.</action>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_project_repository.py -q</automated></verify>
  <done>Import, retry, read-back, retention and no-data-loss cases pass against isolated stores.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Journal authoritative preview, selective apply, rollback and recovery</name>
  <files>custom_components/glt_flow_card/project_transactions.py, custom_components/glt_flow_card/__init__.py, tests/components/glt_flow_card/test_project_transactions.py, tests/components/glt_flow_card/test_websocket.py</files>
  <behavior>- Preview returns revision/digests, migration receipt, semantic operations/impact/closure and opaque user-bound ID. - Apply rereads and recomputes everything, rejects stale/forged/incomplete input, and commits only verified state. - Rollback accepts only server-owned snapshot identity plus expected revision/typed confirmation. - Every injected interruption recovers to a verified old or new head.</behavior>
  <action>Route preview/apply/rollback and compatibility save through one coordinator. Persist PREPARED metadata, write content-addressed immutable snapshot, reread/hash, write/re-read active head, then COMMITTED. Bind preview to authenticated HA user/project/revision/digests and accept selected stable IDs only; server revalidates, remigrates, recomputes operations/dependencies/candidate. Rollback creates a new forward transaction from a verified server snapshot. Audit metadata only: user/project IDs, revisions, hashes, selected IDs, result/error. Recover PREPARED journals before availability.</action>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_project_transactions.py tests/components/glt_flow_card/test_websocket.py -q</automated></verify>
  <done>T-01, T-02 and T-06 owner commands pass for happy, concurrent, forged, failure-injected, restart, rollback, authorization and redaction cases.</done>
</task>

</tasks>

<threat_model>
Canonical ledger: `01-THREATS.md`.

| ID | Stable threat description | Plan role | Mitigation / verification |
|---|---|---|---|
| T-01 | Client alters candidate/selection after preview | OWNER 01-07-T2 | HA/revision/digest/ID binding plus server recomputation; ledger command |
| T-02 | User invokes rollback with browser-forged receipt | OWNER 01-07-T2 | Server snapshot identity and forward rollback; ledger command |
| T-06 | Interrupted apply corrupts head/history | OWNER 01-07-T2 | Journal, immutable snapshot, read-back and recovery; ledger command |
</threat_model>

<verification>Run all three canonical pytest modules with fake HA/stores; no live HA, network, service, bus, or plant action.</verification>

<success_criteria>
- Legacy data is preserved with verified split-store import.
- DIFF-01 selective apply is server-authoritative and concurrency safe.
- T-01, T-02 and T-06 owner commands pass.</success_criteria>

<output>Create `.planning/phases/01-trusted-contract-release-foundation/01-07-SUMMARY.md` when done.</output>
