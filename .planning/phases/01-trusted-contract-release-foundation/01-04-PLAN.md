---
phase: 01-trusted-contract-release-foundation
plan: 04
type: tdd
wave: 3
depends_on: ["01-03"]
files_modified:
  - src/v100/project-contract.mjs
  - src/v100/generated/project-validators.mjs
  - custom_components/glt_flow_card/project_contract.py
  - test/v100-contract.test.mjs
  - tests/components/glt_flow_card/test_project_contract.py
  - tools/compare-contract-runtimes.mjs
  - package.json
autonomous: true
requirements: [SCHEMA-01]
must_haves:
  truths:
    - "JavaScript and Python enforce resource budgets on raw input before Draft 2020-12 validation, migration, defaults, or normalization."
    - "Both runtimes return the same validity, capped stable error codes, JSON-pointer paths, canonical serialization, and SHA-256 digest for every shared fixture."
    - "Generated JavaScript validators derive only from the canonical project/bundle schemas and fail drift checks."
  artifacts:
    - path: "src/v100/project-contract.mjs"
      provides: "Raw-first JS contract and canonical evidence API"
    - path: "custom_components/glt_flow_card/project_contract.py"
      provides: "Equivalent Python contract adapter"
    - path: "tools/compare-contract-runtimes.mjs"
      provides: "Byte-level shared-corpus parity runner"
  key_links:
    - from: "src/v100/generated/project-validators.mjs"
      to: "schemas/project/{0,1,2}.schema.json"
      via: "deterministic build-time generation only"
      pattern: "schema|fingerprint"
    - from: "tools/compare-contract-runtimes.mjs"
      to: "custom_components/glt_flow_card/project_contract.py"
      via: "JSON-lines parity protocol"
      pattern: "canonical|digest|errors"
---

<objective>
Implement raw-first JavaScript/Python contract parity against the canonical corpus.

Purpose: Make validation and evidence independent of the runtime that receives a project.
Output: JS and Python adapters, generated validators, stable evidence, and a parity runner.
</objective>

<execution_context>
@C:/Users/basti/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/basti/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-trusted-contract-release-foundation/01-03-SUMMARY.md
@.planning/phases/01-trusted-contract-release-foundation/01-THREATS.md
@schemas/limits.json
@test/fixtures/contracts/manifest.json
</context>

<feature>
  <name>Raw-first bounded cross-runtime project contract</name>
  <files>src/v100/project-contract.mjs, src/v100/generated/project-validators.mjs, custom_components/glt_flow_card/project_contract.py, test/v100-contract.test.mjs, tests/components/glt_flow_card/test_project_contract.py, tools/compare-contract-runtimes.mjs, package.json</files>
  <behavior>
    RED: shared fixtures specify expected raw limit/schema results, stable codes/pointers, canonical bytes, and digests before adapters exist.
    GREEN 1: JavaScript measures bytes/depth/nodes/strings/collections first, then runs generated Ajv validators with all-errors bounded to 100 and emits sorted stable evidence without mutating input.
    GREEN 2: Python performs the same preflight and `jsonschema` evaluation, maps errors identically, and emits the same RFC-style canonical JSON bytes and SHA-256.
    REFACTOR: parity runner compares every scalar/result byte and detects generated-validator/schema fingerprint drift.
  </behavior>
  <implementation>Keep public results serializable: `{valid, errors:[{code,path,params}], schema_version, canonical, digest, limits}`. Reject non-JSON values and do not call normalizers. Compile JS validators deterministically from only `schemas/project/{0,1,2}.schema.json` and `schemas/bundle-manifest.schema.json`; the generated module is an output whose embedded fingerprints are independently checked. Use the canonical Python test root exclusively.</implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement bounded raw-first JavaScript evidence</name>
  <files>src/v100/project-contract.mjs, src/v100/generated/project-validators.mjs, test/v100-contract.test.mjs, package.json</files>
  <behavior>- Raw limits precede validator/default behavior; input is unchanged. - Codes/paths are stable, sorted and capped. - Canonical bytes/digest are deterministic.</behavior>
  <action>Write failing corpus tests, then implement the JS boundary and deterministic validator generation described above. Add focused `test:contract:js` and schema-drift scripts. Do not import UI code or generated dist.</action>
  <verify><automated>npm run test:contract:js</automated></verify>
  <done>All JS corpus and raw-before-normalization assertions pass, including every T-03 limit class.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Python parity and exact comparator</name>
  <files>custom_components/glt_flow_card/project_contract.py, tests/components/glt_flow_card/test_project_contract.py, tools/compare-contract-runtimes.mjs, package.json</files>
  <behavior>- Python fixture results exactly equal JS. - Error cap/order/parameters and canonical digests match. - Any seeded one-byte/code/path drift fails.</behavior>
  <action>Write Python golden tests and the comparator, then implement the Python adapter over canonical schemas/limits. Load repository-owned refs only, disable network resolution, and map validator errors to the shared stable taxonomy. Run both adapters in isolated processes so parity cannot accidentally share implementation state.</action>
  <verify><automated>npm run test:contract:parity &amp;&amp; node --test --test-name-pattern="rejects raw oversized and deeply nested documents" test/v100-contract.test.mjs &amp;&amp; py -3.13 -m pytest tests/components/glt_flow_card/test_project_contract.py -q -k "oversized or deep"</automated></verify>
  <done>Every golden, malformed, malicious, boundary and 100/500/2,000 correctness fixture has byte-equivalent JS/Python evidence; seeded drift fails.</done>
</task>

</tasks>

<threat_model>
Canonical ledger: `01-THREATS.md`.

| ID | Stable threat description | Plan role | Mitigation / verification |
|---|---|---|---|
| T-03 | Oversized/deep/regex-hostile JSON | OWNER 01-04-T2 | Pre-validator budgets and exact parity; `npm run test:contract:parity && node --test --test-name-pattern="rejects raw oversized and deeply nested documents" test/v100-contract.test.mjs && py -3.13 -m pytest tests/components/glt_flow_card/test_project_contract.py -q -k "oversized or deep"` |
</threat_model>

<verification>Run JS focused tests and the exact cross-runtime comparator. No normalization may run before the recorded raw result.</verification>

<success_criteria>
- SCHEMA-01 raw parity and stable diagnostics are executable.
- T-03 owner command passes.
- Generated validator drift is detected from canonical schema fingerprints.
</success_criteria>

<output>Create `.planning/phases/01-trusted-contract-release-foundation/01-04-SUMMARY.md` when done.</output>
