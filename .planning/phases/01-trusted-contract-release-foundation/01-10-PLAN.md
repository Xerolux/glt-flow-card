---
phase: 01-trusted-contract-release-foundation
plan: 10
type: execute
wave: 8
depends_on: ["01-09"]
files_modified:
  - packaging/hacs-plugin/hacs.json
  - packaging/hacs-integration/hacs.json
  - packaging/hacs-integration/README.md
  - tools/stage-hacs-packages.mjs
  - tools/validate-hacs-staging.mjs
  - test/hacs-staging.test.mjs
  - package.json
generated_outputs:
  - build/release/hacs-plugin/glt-flow-card.js
  - build/release/hacs-integration/custom_components/glt_flow_card/
  - build/release/glt-flow-card-companion.zip
autonomous: true
requirements: [HACS-01]
must_haves:
  truths:
    - "Dashboard/plugin and Companion/integration are independently valid local HACS category staging packages derived from the same manifest-hashed build."
    - "The integration stage has one correct `custom_components/glt_flow_card/` root, manifest, schemas, www, translations and deterministic ZIP with no repository/test/cache members."
    - "Phase 1 creates and validates no public Companion repository; any future mirror upload is absent or disabled by default and not a success criterion."
  artifacts:
    - path: "tools/stage-hacs-packages.mjs"
      provides: "Local release-only category staging and deterministic ZIP"
    - path: "tools/validate-hacs-staging.mjs"
      provides: "Independent local plugin/integration category and ZIP validator"
  key_links:
    - from: "tools/stage-hacs-packages.mjs"
      to: "custom_components/glt_flow_card/build-manifest.json"
      via: "copy only exact verified artifact hashes"
      pattern: "build-manifest|sha256"
---

<objective>
Stage and validate two HACS package categories locally without inventing a public Companion endpoint.

Purpose: Prove dashboard and integration packaging/install shapes while keeping publication authorization out of Phase 1.
Output: Authored category metadata, deterministic local stages/ZIP, and independent validation.
</objective>

<execution_context>
@C:/Users/basti/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/basti/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-trusted-contract-release-foundation/01-09-SUMMARY.md
@.planning/phases/01-trusted-contract-release-foundation/01-RESEARCH.md
@.planning/phases/01-trusted-contract-release-foundation/01-THREATS.md
@hacs.json
@custom_components/glt_flow_card/manifest.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stage exact plugin and integration-category packages locally</name>
  <files>packaging/hacs-plugin/hacs.json, packaging/hacs-integration/hacs.json, packaging/hacs-integration/README.md, tools/stage-hacs-packages.mjs, test/hacs-staging.test.mjs, package.json</files>
  <behavior>- Plugin stage contains the exact dist card. - Integration stage contains the exact allowed component tree and normalized deterministic ZIP. - Both hashes/versions match the build manifest. - No remote repository target/token/upload exists on the required path.</behavior>
  <action>Author category-specific metadata and create a local staging tool that consumes only verified Plan 01-09 outputs. Copy the plugin artifact and allowlisted Companion files, normalize ZIP names/order/modes/timestamps, and record hashes in a staging manifest. Treat the integration stage as release-only local evidence, not a public repository. Do not add `user_setup`, secrets, upload scripts or repository creation. If a generic future upload hook already exists, keep it disabled unless an exact target and scoped token are explicitly supplied outside Phase-1 execution, and exclude it from tests/success.</action>
  <verify><automated>npm run stage:hacs &amp;&amp; node --test test/hacs-staging.test.mjs --test-name-pattern="stage|zip|no publication"</automated></verify>
  <done>Both local stages and deterministic ZIP match manifest hashes and contain no upload/public endpoint requirement.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Independently validate both local HACS category shapes</name>
  <files>tools/validate-hacs-staging.mjs, test/hacs-staging.test.mjs, package.json</files>
  <behavior>- Plugin and integration checks run separately. - Integration package passes manifest/layout/hassfest-compatible structural checks. - Wrong category, root, version, hash or extra member fails. - Validation works without publication credentials.</behavior>
  <action>Implement independent local category validation and wire official HACS/hassfest validators only in modes that accept the staged tree. Recompute hashes and inspect ZIP members instead of trusting staging metadata. Add negative mutations for category confusion, extra files, version drift, unsafe roots and stale www/schema copies. Do not treat public discoverability as passing evidence.</action>
  <verify><automated>npm run validate:hacs-staging</automated></verify>
  <done>Both category stages pass independent local validation, all mutations fail, and no target/token is requested.</done>
</task>

</tasks>

<threat_model>
Canonical ledger: `01-THREATS.md`.

| ID | Stable threat description | Plan role | Mitigation / verification |
|---|---|---|---|
| T-08 | Generated/release artifact differs from reviewed source | supporting packaging control for owner 01-13-T1 | Manifest-hash copy, normalized ZIP, independent category validation; `npm run validate:hacs-staging` |
</threat_model>

<verification>Stage and validate locally with no credentials. Confirm no required task creates, uploads to, or claims a public Companion repository.</verification>

<success_criteria>
- HACS-01 has two category-valid local package shapes.
- The exact Companion ZIP is installable by later HA lanes.
- Public mirror publication remains disabled and excluded.</success_criteria>

<output>Create `.planning/phases/01-trusted-contract-release-foundation/01-10-SUMMARY.md` when done.</output>
