---
phase: 01-trusted-contract-release-foundation
plan: 13
type: execute
wave: 10
depends_on: ["01-12"]
files_modified:
  - tools/verify-release-acceptance.mjs
  - tools/verify-phase1.mjs
  - test/phase1-gate.test.mjs
  - .github/workflows/release.yml
  - package.json
  - README.md
  - README.de.md
  - docs/wiki/Installation.md
  - docs/wiki/YAML-Projects.md
  - docs/wiki/Companion-Backend.md
autonomous: true
requirements: [SCHEMA-01, DIFF-01, HACS-01]
must_haves:
  truths:
    - "Release acceptance consumes the already verified staged bytes and never rebuilds; source, manifest, local category stages, exact-dist and HA evidence all agree."
    - "Root release publication, when triggered by an authorized tag, publishes only this repository's verified dashboard and Companion ZIP assets; it neither creates nor uploads to a public Companion mirror."
    - "One fail-closed Phase-1 gate maps every requirement DoD, ROADMAP criterion and T-01..T-08 owner command to exact hashes/results and rejects missing/skipped evidence."
    - "English/German docs accurately distinguish standalone, Companion-enforced shared operations, local integration-category staging and public availability claims."
  artifacts:
    - path: "tools/verify-release-acceptance.mjs"
      provides: "T-08 exact source-to-release acceptance"
    - path: "tools/verify-phase1.mjs"
      provides: "Complete requirement/threat/evidence orchestrator"
    - path: ".github/workflows/release.yml"
      provides: "Least-privilege no-rebuild release of current-repository exact assets"
  key_links:
    - from: "tools/verify-phase1.mjs"
      to: ".planning/phases/01-trusted-contract-release-foundation/01-THREATS.md"
      via: "execute and record every unique owner command"
      pattern: "T-01|T-08|owner"
    - from: ".github/workflows/release.yml"
      to: "custom_components/glt_flow_card/build-manifest.json"
      via: "downloaded artifact hash verification without rebuild"
      pattern: "build-manifest|sha256"
---

<objective>
Close Phase 1 with exact release acceptance, complete evidence mapping and truthful bilingual guidance.

Purpose: Make all SCHEMA-01/DIFF-01/HACS-01 claims auditable without expanding into an unauthorized public Companion repository.
Output: T-08 acceptance, one Phase-1 gate/evidence manifest, least-privilege current-repository release and documentation.
</objective>

<execution_context>
@C:/Users/basti/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/basti/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-trusted-contract-release-foundation/01-12-SUMMARY.md
@.planning/phases/01-trusted-contract-release-foundation/01-THREATS.md
@.planning/phases/01-trusted-contract-release-foundation/01-VALIDATION.md
@.github/workflows/release.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove reviewed source equals tested and releasable artifacts</name>
  <files>tools/verify-release-acceptance.mjs, test/phase1-gate.test.mjs, .github/workflows/release.yml, package.json</files>
  <behavior>- Provenance, lockfile, double-build, generated equality, schema fingerprints, category stages, exact-dist, HA lane evidence, versions and hashes form one chain. - Missing/skipped/mutated evidence fails. - Release job downloads verified stage and does not rebuild. - No Companion mirror target/token/upload is required or executed.</behavior>
  <action>Implement `test:release-acceptance` to independently join source commit, provenance report, build manifest, local HACS staging manifest, Playwright artifact hash, HA lane lock/results and release asset hashes. Seed negative chain breaks. Update the current repository release workflow to accept only the already-green staged artifacts, verify hashes/tag/version, use full-SHA actions/read-only default permissions with isolated contents-write/attestation permissions, and publish current-repository dashboard plus Companion ZIP/checksums/provenance without rebuild. Do not create, infer or publish a separate Companion repository. Any future mirror mechanism must remain absent or explicitly disabled by default and outside success.</action>
  <verify><automated>npm run test:release-acceptance</automated></verify>
  <done>T-08 owner command proves one exact evidence chain and release workflow has no required mirror/publication dependency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit complete Phase-1 evidence and truthful bilingual documentation</name>
  <files>tools/verify-phase1.mjs, test/phase1-gate.test.mjs, package.json, README.md, README.de.md, docs/wiki/Installation.md, docs/wiki/YAML-Projects.md, docs/wiki/Companion-Backend.md</files>
  <behavior>- `npm run test:phase1` runs every required focused suite and all eight ledger owner commands exactly once, refusing zero-test/skipped/missing/stale evidence. - Evidence JSON maps every SCHEMA-01/DIFF-01/HACS-01 DoD and ROADMAP success criterion to command/result/hash. - Docs cover raw errors, migration/rollback, diff closure/conflicts, bundle limits, lifecycle/diagnostics, standalone boundary, exact installation and local staging without claiming public Companion availability or capacity certification.</behavior>
  <action>Build a fail-closed orchestrator over provenance, contract parity, migration/diff, bundle, canonical Python tests, deterministic build, local category stages, exact-dist UI, immutable HA lanes and release acceptance. Record tool/lane/artifact identities and map each requirement/ROADMAP/threat ledger item; source-token checks, syntax alone and screenshots alone never count. Update English/German docs to match tested behavior, explain that HACS plugin is public while Companion integration-category evidence is a local release-only stage unless separately authorized later, and state no physical plant write/capacity claim.</action>
  <verify><automated>node --test test/phase1-gate.test.mjs &amp;&amp; npm run test:phase1</automated></verify>
  <done>Exactly all planned evidence maps pass, docs match behavior, and sign-off can be based on hashes/commands rather than claims.</done>
</task>

</tasks>

<threat_model>
Canonical ledger: `01-THREATS.md`.

| ID | Stable threat description | Plan role | Mitigation / verification |
|---|---|---|---|
| T-08 | Generated/release artifact differs from reviewed source | OWNER 01-13-T1 | Full provenance/build/staging/UI/HA/release evidence chain; `npm run test:release-acceptance` |
</threat_model>

<verification>Run T-08 owner acceptance then the full Phase-1 gate. No mirror upload, live HA, remote site, service, bus or plant write occurs.</verification>

<success_criteria>
- T-08 closes with exact-artifact evidence.
- All requirement/ROADMAP/threat mappings are complete and fail-closed.
- Release and documentation make no unauthorized public Companion or Phase-10 capacity claim.</success_criteria>

<output>Create `.planning/phases/01-trusted-contract-release-foundation/01-13-SUMMARY.md` when done.</output>
