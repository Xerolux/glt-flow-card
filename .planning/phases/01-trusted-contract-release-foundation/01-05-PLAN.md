---
phase: 01-trusted-contract-release-foundation
plan: 05
type: execute
wave: 4
depends_on: ["01-04"]
files_modified:
  - src/v100/project-migrations.mjs
  - src/v100/project-diff.mjs
  - src/v100/core.mjs
  - custom_components/glt_flow_card/project_migrations.py
  - custom_components/glt_flow_card/project_diff.py
  - test/v100-migrations.test.mjs
  - test/v100-diff.test.mjs
  - tests/components/glt_flow_card/test_project_migrations.py
  - tests/components/glt_flow_card/test_project_diff.py
autonomous: true
requirements: [SCHEMA-01, DIFF-01]
must_haves:
  truths:
    - "Migrations run pure sequential 0→1→2 steps on copies, support dry-run, emit receipts/digests, and are idempotent in JS and Python."
    - "Semantic diff emits stable add/remove/move/binding/config operations, ignores only declared irrelevant ordering, reports impact, and computes transitive dependency closure."
    - "Existing core APIs preserve compatible projects while delegating to the hardened contract/migration/diff path."
  artifacts:
    - path: "src/v100/project-migrations.mjs"
      provides: "Pure sequential JS migrations"
    - path: "src/v100/project-diff.mjs"
      provides: "Semantic operations and dependency closure"
    - path: "custom_components/glt_flow_card/project_diff.py"
      provides: "Server-equivalent diff/recompute implementation"
  key_links:
    - from: "src/v100/project-migrations.mjs"
      to: "src/v100/project-contract.mjs"
      via: "validate source raw, migrate copies, validate each target"
      pattern: "validateRaw|schema_version"
    - from: "src/v100/project-diff.mjs"
      to: "schemas/diff-policy.json"
      via: "stable identity, ordering and dependency policy"
      pattern: "dependency|binding|move"
---

<objective>
Deliver deterministic migrations and semantic selective-change computation in both runtimes.

Purpose: Preserve existing data and make review/apply decisions meaningful and server-recomputable.
Output: Pure migration and diff modules, compatibility wiring, and parity tests.
</objective>

<execution_context>
@C:/Users/basti/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/basti/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-trusted-contract-release-foundation/01-04-SUMMARY.md
@.planning/phases/01-trusted-contract-release-foundation/01-THREATS.md
@src/v100/core.mjs
@schemas/diff-policy.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement sequential copy-on-write migration parity</name>
  <files>src/v100/project-migrations.mjs, custom_components/glt_flow_card/project_migrations.py, test/v100-migrations.test.mjs, tests/components/glt_flow_card/test_project_migrations.py</files>
  <behavior>- 0→1 and 1→2 execute separately, never mutate source, and validate each target. - Dry-run and apply produce identical candidate/receipt without persistence. - Re-running current input is unchanged; unsupported future versions fail.</behavior>
  <action>Write shared golden/negative tests, then implement registries keyed by exact from/to version. Each receipt contains ordered steps, source/candidate canonical digests, warnings, and loss-accounting fields. Preserve unknown extension data under the documented namespace and reject transformations that cannot account for data. Keep persistence/backup outside these pure modules.</action>
  <verify><automated>npm run test:migrations &amp;&amp; py -3.13 -m pytest tests/components/glt_flow_card/test_project_migrations.py -q</automated></verify>
  <done>All legacy/current fixtures migrate identically across runtimes, source bytes remain unchanged, and dry-run/idempotence/loss tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement semantic diff, impact, and dependency closure parity</name>
  <files>src/v100/project-diff.mjs, custom_components/glt_flow_card/project_diff.py, test/v100-diff.test.mjs, tests/components/glt_flow_card/test_project_diff.py</files>
  <behavior>- Operations are stable across object/key ordering and use explicit IDs/JSON pointers. - Add/remove/move/binding/config and impact are distinct. - Selection expands transitively to required dependencies and rejects missing/cyclic ambiguity deterministically.</behavior>
  <action>Implement identity/reference-aware comparison from canonical validated projects using only `schemas/diff-policy.json`. Sort operations by stable ID, ignore ordering only where the policy marks it irrelevant, include before/after hashes and impact, and compute a deterministic transitive closure with explicit reasons. Make JS/Python result bytes equivalent and keep apply code out of this pure module.</action>
  <verify><automated>npm run test:diff &amp;&amp; py -3.13 -m pytest tests/components/glt_flow_card/test_project_diff.py -q</automated></verify>
  <done>Five-category, ordering, impact, dependency and malicious reference fixtures pass with equivalent operations/closures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Preserve public core compatibility through the hardened path</name>
  <files>src/v100/core.mjs, test/v100-migrations.test.mjs, test/v100-diff.test.mjs</files>
  <behavior>- Existing `ensureV1`, `migrateProject`, and diff consumers remain compatible. - Raw invalid input remains diagnosable instead of being silently repaired. - Valid existing examples retain semantics.</behavior>
  <action>Delegate existing public helpers to the new contract/migration/diff modules while preserving documented return shapes or adding compatible fields. Keep normalization after raw validation. Add regression tests over existing examples and do not modify generated dist.</action>
  <verify><automated>node --test test/v100-core.test.mjs test/v100-migrations.test.mjs test/v100-diff.test.mjs</automated></verify>
  <done>Existing core tests and new hardened-path regressions pass without lossy normalization.</done>
</task>

</tasks>

<threat_model>
Canonical ledger: `01-THREATS.md`.

| ID | Stable threat description | Plan role | Mitigation / verification |
|---|---|---|---|
| T-01 | Client alters candidate/selection after preview | supporting algorithm for owner 01-07-T2 | Stable operations/closure; `npm run test:diff` |
| T-02 | User invokes rollback with browser-forged receipt | supporting migration evidence for owner 01-07-T2 | Canonical source/candidate digests and receipts; `npm run test:migrations` |
</threat_model>

<verification>Run migration, diff, Python parity and existing core suites; all are pure and perform no persistence or plant action.</verification>

<success_criteria>
- SCHEMA-01 migration behavior is pure, sequential, receipted and equivalent.
- DIFF-01 five categories, impacts and closure are deterministic and equivalent.
- Existing projects/functionality remain compatible.</success_criteria>

<output>Create `.planning/phases/01-trusted-contract-release-foundation/01-05-SUMMARY.md` when done.</output>
