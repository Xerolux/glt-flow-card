---
phase: 01-trusted-contract-release-foundation
plan: 02
type: execute
wave: 1
depends_on: ["01-01"]
files_modified:
  - package.json
  - package-lock.json
  - requirements-test.txt
  - pytest.ini
  - playwright.config.mjs
  - tools/assert-red.mjs
  - tools/run-exact-dist-playwright.mjs
  - test/e2e/fixtures/fake-ha.mjs
  - test/e2e/project-safety.spec.mjs
  - tests/components/glt_flow_card/conftest.py
  - tests/components/glt_flow_card/test_init.py
autonomous: true
requirements: [SCHEMA-01, DIFF-01, HACS-01]
must_haves:
  truths:
    - "Wave-0 installs only the provenance-verified exact toolchain and exposes focused Node, Python, and exact-dist commands."
    - "The exact-dist Project safety and supported pytest Home Assistant lifecycle happy paths fail RED for missing Phase-1 behavior, not broken harness setup."
    - "Fake HA and browser fixtures record filesystem, network, localStorage, WebSocket, service, task, listener, and session effects without reaching live systems."
  artifacts:
    - path: "tools/assert-red.mjs"
      provides: "Controlled RED-result classifier"
    - path: "test/e2e/project-safety.spec.mjs"
      provides: "Exact-dist browser acceptance seed"
    - path: "tests/components/glt_flow_card/test_init.py"
      provides: "Canonical supported HA lifecycle RED seed"
  key_links:
    - from: "package-lock.json"
      to: "tools/provenance-allowlist.json"
      via: "exact dependency and integrity agreement before test execution"
      pattern: "8.20.0|1.62.1|2.8.30"
    - from: "tools/run-exact-dist-playwright.mjs"
      to: "dist/glt-flow-card.js"
      via: "hermetic server loads the generated release path"
      pattern: "dist/glt-flow-card.js"
---

<objective>
Create Wave-0 executable test infrastructure before feature implementation.

Purpose: Replace token-only gates with controlled Node, supported Home Assistant pytest, and exact-dist Playwright RED paths.
Output: Locked tools, canonical Python test root, effect-recording fixtures, and classified RED tests.
</objective>

<execution_context>
@C:/Users/basti/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/basti/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-trusted-contract-release-foundation/01-01-SUMMARY.md
@.planning/phases/01-trusted-contract-release-foundation/01-THREATS.md
@package.json
@test/v100-core.test.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin the verified test and contract toolchain</name>
  <files>package.json, package-lock.json, requirements-test.txt, pytest.ini, playwright.config.mjs</files>
  <action>Require the 01-01 provenance report, then install exact `ajv@8.20.0`, `@playwright/test@1.62.1`, and `@zip.js/zip.js@2.8.30` into the npm lockfile with scripts disabled. Add exact Python test pins `jsonschema==4.26.0` and `pytest-homeassistant-custom-component==0.13.316`. Configure pytest discovery only under `tests/components/glt_flow_card/`. Add focused scripts for provenance, fixtures, contract JS/parity, migrations, diff, bundle, Python, exact-dist, release, HA lanes, and the Phase-1 gate. Configure Playwright for Chromium, one deterministic worker, trace/screenshot on failure, no external base URL, and ignored temporary output.</action>
  <verify><automated>node tools/verify-provenance.mjs --online &amp;&amp; npm ci --ignore-scripts &amp;&amp; npm ls --depth=0 &amp;&amp; py -3.13 -m pip install --dry-run -r requirements-test.txt</automated></verify>
  <done>Lockfiles contain only approved exact candidates, pytest discovers the canonical root, and all named scripts/config parse.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Establish the exact-dist Project safety RED path</name>
  <files>tools/assert-red.mjs, tools/run-exact-dist-playwright.mjs, test/e2e/fixtures/fake-ha.mjs, test/e2e/project-safety.spec.mjs</files>
  <behavior>
    - A hermetic local page loads `dist/glt-flow-card.js`, injects deterministic HA state/WebSocket results, and records all external effects.
    - The happy path expects Project safety→Validate→Migrate/compare→Apply→Verify and currently fails only because the UI is absent.
    - Any `callService`, non-loopback network request, or unexpected localStorage write fails immediately.
  </behavior>
  <action>Build the exact-dist runner and fake HA fixture without importing authored UI modules. Add a happy-path Playwright test with readiness assertions that distinguish harness failure from the expected missing feature. Implement `assert-red.mjs` to accept only the named missing-behavior assertion and reject launch, syntax, timeout, resource-load, or fixture failures. Do not patch generated artifacts.</action>
  <verify><automated>npm run test:e2e:red</automated></verify>
  <done>The exact-dist test records a controlled RED result and all prohibited side-effect spies remain zero.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Establish the supported HA pytest lifecycle RED path</name>
  <files>tests/components/glt_flow_card/conftest.py, tests/components/glt_flow_card/test_init.py, tools/assert-red.mjs</files>
  <behavior>
    - Supported pytest Home Assistant fixtures create a Config Entry, setup, reload, unload, and re-setup without using an invented external API.
    - Exact command/listener/task/session/runtime counts are observable.
    - The seed fails only because resource-exact lifecycle behavior is absent.
  </behavior>
  <action>Create canonical fake-HA fixtures under `tests/components/glt_flow_card/` using the package's supported Home Assistant pytest helpers and temporary storage. Instrument callbacks, tasks, sessions, stores, WebSocket registrations, and service attempts. Add a lifecycle seed test and classify its expected RED failure. Never connect to a live HA instance, remote site, physical bus, or plant service.</action>
  <verify><automated>node tools/assert-red.mjs --expected=missing-lifecycle-cleanup -- py -3.13 -m pytest tests/components/glt_flow_card/test_init.py -q</automated></verify>
  <done>The supported harness boots reliably and produces only the expected missing-lifecycle RED evidence.</done>
</task>

</tasks>

<threat_model>
Canonical ledger: `01-THREATS.md`.

| ID | Stable threat description | Plan role | Mitigation / verification |
|---|---|---|---|
| T-03 | Oversized/deep/regex-hostile JSON | harness support for owner 01-04-T2 | Add bounded fixture/test scripts; controlled RED infrastructure |
| T-05 | SVG/HTML/script asset executes during inspection | harness support for owner 01-11-T3 | Browser network/service/DOM effect ledger |
| T-06 | Interrupted apply corrupts head/history | harness support for owner 01-07-T2 | Failure-injection capable fake stores |
| T-08 | Generated/release artifact differs from reviewed source | harness support for owner 01-13-T1 | Exact-dist path and locked verified tools |
</threat_model>

<verification>Run all three task commands; record RED evidence and leave Wave-0/sign-off pending until later plans make the same paths GREEN.</verification>

<success_criteria>
- No human checkpoint remains.
- Canonical Python paths use `tests/components/glt_flow_card/` exclusively.
- Node, Python/HA, and exact-dist behavior paths are executable before implementation.
</success_criteria>

<output>Create `.planning/phases/01-trusted-contract-release-foundation/01-02-SUMMARY.md` when done.</output>
