---
phase: 01-trusted-contract-release-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tools/provenance-allowlist.json
  - tools/verify-provenance.mjs
  - test/provenance.test.mjs
autonomous: true
requirements: [SCHEMA-01, HACS-01]
must_haves:
  truths:
    - "The five exact Phase-1 dependency candidates are accepted only when official registry metadata, source ownership, version, integrity hashes, and lifecycle-script policy match a committed allowlist."
    - "Registry/source verification is automated, read-only, reproducible, and requires no human package approval."
    - "Any missing version, source-owner drift, integrity mismatch, unexpected lifecycle script, or unlisted transitive install request blocks Wave 0."
  artifacts:
    - path: "tools/provenance-allowlist.json"
      provides: "Committed exact package identities and allowed provenance"
    - path: "tools/verify-provenance.mjs"
      provides: "Read-only npm/PyPI/source/integrity verifier"
    - path: "test/provenance.test.mjs"
      provides: "Deterministic allowlist and tamper tests"
  key_links:
    - from: "tools/verify-provenance.mjs"
      to: "tools/provenance-allowlist.json"
      via: "fail-closed exact tuple and integrity comparison"
      pattern: "allowlist|integrity|repository"
---

<objective>
Automate the package-legitimacy gate before any Phase-1 install.

Purpose: Remove human checkpoints while keeping supply-chain verification read-only and fail-closed.
Output: A committed provenance allowlist, verifier, and tamper tests for the exact approved candidates.
</objective>

## Phase Goal

**As an** engineer or administrator, **I want to** validate, migrate, compare, install, and upgrade GLT projects, **so that** project data and shipped artifacts remain trustworthy.

<execution_context>
@C:/Users/basti/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/basti/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-trusted-contract-release-foundation/01-RESEARCH.md
@.planning/phases/01-trusted-contract-release-foundation/01-THREATS.md
@package-lock.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Commit exact registry and source provenance policy</name>
  <files>tools/provenance-allowlist.json, test/provenance.test.mjs</files>
  <behavior>
    - The only accepted candidates are `ajv@8.20.0`, `@playwright/test@1.62.1`, `@zip.js/zip.js@2.8.30`, `jsonschema==4.26.0`, and `pytest-homeassistant-custom-component==0.13.316`.
    - Each entry declares ecosystem, canonical registry URL, canonical source owner/repository, version, expected integrity/hash set policy, and allowed lifecycle scripts.
    - Duplicates, ranges, mutable labels, missing owners, absent hashes, or unexpected scripts fail tests.
  </behavior>
  <action>Create a sorted JSON allowlist for the five exact candidates using the official npm/PyPI identities and linked upstream repositories recorded by research. Store immutable expected npm integrity values or PyPI file SHA-256 sets after read-only lookup; allow only explicitly reviewed lifecycle scripts and make the default none. Add schema-like tests that reject ranges, latest tags, incomplete records, unknown fields, duplicate names, source-owner drift, missing hashes, and lifecycle-script additions. The allowlist is authored policy, not generated lockfile output.</action>
  <verify><automated>node --test test/provenance.test.mjs --test-name-pattern="allowlist"</automated></verify>
  <done>The committed policy contains exactly five complete immutable identities and all malformed/tampered policy cases fail.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verify official metadata and downloaded integrity read-only</name>
  <files>tools/verify-provenance.mjs, test/provenance.test.mjs</files>
  <behavior>
    - Online mode queries official npm/PyPI JSON endpoints and linked public source metadata without publishing, installing, or writing outside a temporary directory.
    - Downloaded package/archive bytes match registry integrity/hashes and allowlist identities; metadata/source/script drift fails.
    - Fixture mode proves every failure class without network access.
  </behavior>
  <action>Implement a Node verifier using built-in fetch/crypto and temporary directories. Query official registry metadata for exact versions, compare canonical repository ownership and lifecycle scripts, download candidate archives to the temp directory, verify npm integrity or PyPI SHA-256, and delete temporary bytes on completion. Never execute package scripts, import the downloaded package, mutate lockfiles, publish, authenticate, or call GitHub write APIs. Support recorded-response fixtures for deterministic negative tests. Emit a canonical provenance report consumed by Plan 01-02.</action>
  <verify><automated>node --test test/provenance.test.mjs &amp;&amp; node tools/verify-provenance.mjs --online --output=.planning/tmp/phase01-provenance.json</automated></verify>
  <done>All five exact candidates verify against official read-only sources, tampered fixtures fail, and no dependency or external state is changed.</done>
</task>

</tasks>

<threat_model>
Canonical ledger: `01-THREATS.md`.

| ID | Stable threat description | Plan role | Mitigation / verification |
|---|---|---|---|
| T-08 | Generated/release artifact differs from reviewed source | supporting control for owner 01-13-T1 | Exact provenance policy and read-only integrity verification; `node --test test/provenance.test.mjs` |
</threat_model>

<verification>
Run the tests and online verifier before any install. The verifier is read-only except for temporary/output evidence and performs no publication, Home Assistant, or plant action.
</verification>

<success_criteria>
- Plan is fully autonomous.
- All five exact candidates have machine-verifiable provenance and integrity.
- Any provenance drift blocks Plan 01-02.
</success_criteria>

<output>Create `.planning/phases/01-trusted-contract-release-foundation/01-01-SUMMARY.md` when done.</output>
