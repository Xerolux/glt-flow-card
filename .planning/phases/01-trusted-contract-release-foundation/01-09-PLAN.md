---
phase: 01-trusted-contract-release-foundation
plan: 09
type: execute
wave: 7
depends_on: ["01-06", "01-08"]
files_modified:
  - tools/build.mjs
  - tools/verify-release.mjs
  - tools/apply-v100.mjs
  - package.json
  - test/release-build.test.mjs
generated_outputs:
  - dist/glt-flow-card.js
  - dist/schemas/project/0.schema.json
  - dist/schemas/project/1.schema.json
  - dist/schemas/project/2.schema.json
  - dist/schemas/bundle-manifest.schema.json
  - custom_components/glt_flow_card/www/glt-flow-card.js
  - custom_components/glt_flow_card/schemas/project/0.schema.json
  - custom_components/glt_flow_card/schemas/project/1.schema.json
  - custom_components/glt_flow_card/schemas/project/2.schema.json
  - custom_components/glt_flow_card/schemas/bundle-manifest.schema.json
  - custom_components/glt_flow_card/build-manifest.json
  - docs/editor/app.js
autonomous: true
requirements: [SCHEMA-01, HACS-01]
must_haves:
  truths:
    - "One staged build bundles the card once, byte-copies dist/www, updates only the marked editor region, and copies schemas only from canonical authored paths."
    - "A canonical non-circular manifest records source/tool/version/path/size/SHA-256 identity without timestamps, randomness, secrets or absolute paths."
    - "Two isolated builds are byte-identical and independent verification rejects generated drift, schema/compiler drift, version disagreement or dist/www inequality."
  artifacts:
    - path: "tools/build.mjs"
      provides: "Single deterministic staged build"
    - path: "tools/verify-release.mjs"
      provides: "Independent double-build and drift verifier"
  key_links:
    - from: "tools/build.mjs"
      to: "schemas/project/{0,1,2}.schema.json"
      via: "copy/compile only canonical authored contracts"
      pattern: "schemas/project|bundle-manifest"
    - from: "tools/verify-release.mjs"
      to: "custom_components/glt_flow_card/build-manifest.json"
      via: "independent hash/version recomputation"
      pattern: "sha256|manifest"
---

<objective>
Create and independently verify one deterministic source-to-artifact build.

Purpose: Eliminate generated drift before HACS staging, exact-dist UI and HA lane tests.
Output: Sole build orchestrator, compatibility delegate, canonical manifest and double-build verifier.
</objective>

<execution_context>
@C:/Users/basti/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/basti/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-trusted-contract-release-foundation/01-06-SUMMARY.md
@.planning/phases/01-trusted-contract-release-foundation/01-08-SUMMARY.md
@.planning/phases/01-trusted-contract-release-foundation/01-THREATS.md
@tools/apply-v100.mjs
@src/v100/entry.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build all generated runtime/schema outputs from authored sources once</name>
  <files>tools/build.mjs, tools/apply-v100.mjs, package.json, test/release-build.test.mjs</files>
  <behavior>- A temporary stage is fully validated before output replacement. - Card bytes are built once and dist/www are exactly equal. - Only canonical schemas are compiled/copied. - Manifest is canonical and deterministic.</behavior>
  <action>Implement `tools/build.mjs` as sole producer: compile validator output from canonical schema paths, bundle `src/v100/entry.js` once for ES2022, assemble final browser bytes once, byte-copy to dist/www, replace only the marked editor section, stage canonical schemas, and emit sorted manifest fields for commit/dirty marker, versions, tool versions and artifact hashes/sizes. Exclude time/random/absolute paths/self-hash. Validate stage then atomically replace outputs. Make `apply-v100.mjs` a delegate with no package/changelog mutation.</action>
  <verify><automated>npm run build &amp;&amp; node --test test/release-build.test.mjs --test-name-pattern="single build|manifest|schema copies|dist www"</automated></verify>
  <done>All declared generated outputs come from one validated stage, dist/www match byte-for-byte, and schema paths trace to canonical sources.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Independently prove double-build equality and reject drift</name>
  <files>tools/verify-release.mjs, package.json, test/release-build.test.mjs</files>
  <behavior>- Separate temporary builds have identical path sets/bytes/manifests. - Independent hashes/versions/schema fingerprints match. - One-byte, version, generated-source or missing-output mutations fail specifically.</behavior>
  <action>Build twice into separate temporary roots and independently enumerate/compare outputs without trusting manifest claims. Recompute SHA-256/sizes/version authorities and validator/schema fingerprints; compare fresh output with checked-in generated outputs. Add negative mutations and `verify:release`. Leave HACS packaging/category logic to Plan 01-10.</action>
  <verify><automated>npm run verify:release</automated></verify>
  <done>Clean verification passes twice and every seeded drift class fails with artifact-specific evidence.</done>
</task>

</tasks>

<threat_model>
Canonical ledger: `01-THREATS.md`.

| ID | Stable threat description | Plan role | Mitigation / verification |
|---|---|---|---|
| T-08 | Generated/release artifact differs from reviewed source | supporting deterministic-build control for owner 01-13-T1 | Single build, manifest, double-build, equality/drift gate; `npm run verify:release` |
</threat_model>

<verification>Run build and independent release verification; ensure the only authored schema inputs are canonical paths.</verification>

<success_criteria>
- Build and verification are separate implementations.
- Generated outputs are deterministic and source-traceable.
- HACS packaging is not mixed into this plan.</success_criteria>

<output>Create `.planning/phases/01-trusted-contract-release-foundation/01-09-SUMMARY.md` when done.</output>
