---
phase: 01-trusted-contract-release-foundation
plan: 08
type: execute
wave: 6
depends_on: ["01-07"]
files_modified:
  - custom_components/glt_flow_card/__init__.py
  - custom_components/glt_flow_card/config_flow.py
  - custom_components/glt_flow_card/const.py
  - custom_components/glt_flow_card/diagnostics.py
  - custom_components/glt_flow_card/manifest.json
  - custom_components/glt_flow_card/strings.json
  - custom_components/glt_flow_card/translations/en.json
  - custom_components/glt_flow_card/translations/de.json
  - tests/components/glt_flow_card/test_init.py
  - tests/components/glt_flow_card/test_options.py
  - tests/components/glt_flow_card/test_diagnostics.py
autonomous: true
requirements: [HACS-01, SCHEMA-01]
must_haves:
  truths:
    - "Setup, reload, unload and re-setup leave one active runtime while loaded and zero owned listeners/tasks/sessions/managers after unload."
    - "Every retained option is range-validated, has an observable effect, reloads atomically, and preserves prior effective configuration on failure."
    - "Diagnostics expose contract/store/recovery/build metadata through an explicit allowlist and exclude project, asset, entity-state, token, secret and remote URL bodies."
  artifacts:
    - path: "custom_components/glt_flow_card/__init__.py"
      provides: "Idempotent Config Entry lifecycle"
    - path: "custom_components/glt_flow_card/diagnostics.py"
      provides: "Allowlisted Home Assistant diagnostics"
  key_links:
    - from: "custom_components/glt_flow_card/config_flow.py"
      to: "custom_components/glt_flow_card/__init__.py"
      via: "validated options update listener and atomic reload"
      pattern: "add_update_listener|async_reload"
---

<objective>
Finish the Companion Config Entry lifecycle, options, localization and diagnostics.

Purpose: Make repeated Home Assistant operations leak-free and supportable without disclosing sensitive plant/project data.
Output: Resource-exact lifecycle, effective options, localized flow metadata and diagnostics tests.
</objective>

<execution_context>
@C:/Users/basti/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/basti/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-trusted-contract-release-foundation/01-07-SUMMARY.md
@.planning/phases/01-trusted-contract-release-foundation/01-THREATS.md
@custom_components/glt_flow_card/config_flow.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make Config Entry lifecycle resource-exact</name>
  <files>custom_components/glt_flow_card/__init__.py, tests/components/glt_flow_card/test_init.py</files>
  <behavior>- Commands register once and resolve only the current loaded entry. - Unload cancels/awaits tasks, unsubscribes callbacks, closes owned clients and removes runtime data. - Recovery completes before availability; unloaded commands return stable unavailable.</behavior>
  <action>Use one runtime object per Config Entry with a compatibility accessor for the verified minimum lane; do not assume an unavailable `runtime_data` API. Register WebSocket commands once at component scope and resolve the current runtime per call. Track and close every owned resource, remove YAML duplicate-manager behavior, and recover transactions before setup succeeds.</action>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_init.py -q</automated></verify>
  <done>Repeated setup/failure/reload/unload/re-setup proves exact one/zero resource counts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Give Config Flow options validated atomic effects</name>
  <files>custom_components/glt_flow_card/config_flow.py, custom_components/glt_flow_card/const.py, custom_components/glt_flow_card/__init__.py, tests/components/glt_flow_card/test_options.py</files>
  <behavior>- User flow creates one entry and aborts duplicates. - Retention/TTL options affect repositories/cleanup. - Failed option reload retains prior runtime/effective values. - Insecure or no-effect legacy options migrate to secure defaults or disappear.</behavior>
  <action>Keep only version retention, audit retention and lock TTL unless another existing option has executable Phase-1 effect. Validate explicit ranges, stage updates, reload atomically and restore previous effective state on failure. Never allow an option to bypass raw validation, revision checks or server recomputation.</action>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_options.py -q</automated></verify>
  <done>Single-instance, ranges, effects, legacy migration and failed-reload cases pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add localized metadata and allowlisted diagnostics</name>
  <files>custom_components/glt_flow_card/diagnostics.py, custom_components/glt_flow_card/manifest.json, custom_components/glt_flow_card/strings.json, custom_components/glt_flow_card/translations/en.json, custom_components/glt_flow_card/translations/de.json, tests/components/glt_flow_card/test_diagnostics.py</files>
  <behavior>- English/German setup/options/errors load. - Diagnostics include versions, effective limits/options, counts, digest prefixes, recovery state and build identity. - Seeded project/state/token/secret/URL/asset/audit-body canaries are absent.</behavior>
  <action>Construct diagnostics from an explicit metadata allowlist, not recursive redaction. Include only support evidence named above and never remote configuration bodies or raw exceptions. Update manifest/strings/translations consistently and test serialized output against distinct canaries.</action>
  <verify><automated>py -3.13 -m pytest tests/components/glt_flow_card/test_diagnostics.py -q -k "redact or canary or allowlist" &amp;&amp; py -3.13 -m compileall -q custom_components/glt_flow_card</automated></verify>
  <done>T-07 owner command and localized metadata tests pass with no sensitive canary present.</done>
</task>

</tasks>

<threat_model>
Canonical ledger: `01-THREATS.md`.

| ID | Stable threat description | Plan role | Mitigation / verification |
|---|---|---|---|
| T-06 | Interrupted apply corrupts head/history | supporting recovery lifecycle for owner 01-07-T2 | Recover before entry availability; lifecycle tests |
| T-07 | Diagnostics or bundle leaks remote tokens/state | OWNER 01-08-T3 | Explicit metadata allowlist and canary absence; ledger command |
</threat_model>

<verification>Run all canonical component tests and compileall using fake Home Assistant only.</verification>

<success_criteria>
- Lifecycle and options satisfy HACS-01 install/reload/unload expectations.
- T-07 owner command passes.
- No live HA or plant write occurs.</success_criteria>

<output>Create `.planning/phases/01-trusted-contract-release-foundation/01-08-SUMMARY.md` when done.</output>
