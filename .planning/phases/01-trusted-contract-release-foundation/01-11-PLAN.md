---
phase: 01-trusted-contract-release-foundation
plan: 11
type: execute
wave: 8
depends_on: ["01-07", "01-09"]
files_modified:
  - src/v100/project-safety.js
  - src/v100/project-safety-i18n.mjs
  - src/v100/index.js
  - src/v100/entry.js
  - src/v040-extension.part01
  - test/e2e/fixtures/fake-ha.mjs
  - test/e2e/project-safety.spec.mjs
generated_outputs:
  - dist/glt-flow-card.js
  - custom_components/glt_flow_card/www/glt-flow-card.js
  - docs/editor/app.js
autonomous: true
requirements: [SCHEMA-01, DIFF-01, HACS-01]
must_haves:
  truths:
    - "One Project safety action adjacent to Projects opens the approved five-tab native-feeling dialog/full-screen mobile UI in English and German."
    - "Validate→Migrate→Review→Apply→Verify exposes raw errors, receipts, five semantic categories, impacts, dependency-locked selection, revision conflicts, recovery and rollback evidence."
    - "Shared failures never silently fall back to local storage; Project safety performs zero HA service or plant writes."
    - "Exact generated dist passes keyboard/focus, 200% zoom, desktop/mobile, light/dark/high-contrast, forced-colors, reduced-motion and all specified states."
  artifacts:
    - path: "src/v100/project-safety.js"
      provides: "Approved Project safety workflow"
    - path: "test/e2e/project-safety.spec.mjs"
      provides: "Exact-dist UI-SPEC and T-05 evidence"
  key_links:
    - from: "src/v100/project-safety.js"
      to: "glt_flow_card/projects"
      via: "hass.callWS preview/apply/rollback only"
      pattern: "callWS|expected_revision|preview_id"
    - from: "test/e2e/project-safety.spec.mjs"
      to: "dist/glt-flow-card.js"
      via: "Plan 01-02 hermetic exact-dist runner"
      pattern: "dist/glt-flow-card.js"
---

<objective>
Implement and prove the complete approved Project safety UI over authoritative APIs.

Purpose: Let engineers validate, migrate, compare, selectively apply, bundle and inspect evidence without weakening backend safety.
Output: Authored UI modules and exact-dist Playwright coverage; dist/www/editor remain generated outputs only.
</objective>

<execution_context>
@C:/Users/basti/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/basti/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-trusted-contract-release-foundation/01-UI-SPEC.md
@.planning/phases/01-trusted-contract-release-foundation/01-07-SUMMARY.md
@.planning/phases/01-trusted-contract-release-foundation/01-09-SUMMARY.md
@.planning/phases/01-trusted-contract-release-foundation/01-THREATS.md
@src/v100/index.js
@src/v040-extension.part01
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the accessible Project safety shell and read-only tabs</name>
  <files>src/v100/project-safety.js, src/v100/project-safety-i18n.mjs, src/v100/index.js, src/v100/entry.js, test/e2e/fixtures/fake-ha.mjs, test/e2e/project-safety.spec.mjs</files>
  <behavior>- Exactly one action adjacent to Projects opens Overview, Validate, Migrate &amp; compare, Bundles and Evidence. - Approved DE/EN copy, stable codes/pointers and metadata-only evidence render. - Modal semantics, focus trap/return, Escape, tabs and mobile full-screen work.</behavior>
  <action>Implement the UI-SPEC shell with semantic dialog/tab markup, native buttons, visible focus, `aria-live`, HA CSS variables, 200% reflow, forced-colors and reduced-motion. Render untrusted values as text, never unsafe HTML. Wire read-only views to pure contract/bundle APIs and fake Companion responses. Mark shared operations unavailable without Companion; preserve safe explicit standalone validation.</action>
  <verify><automated>npm run build &amp;&amp; npm run test:e2e -- --grep="shell|validate|bundles|evidence|keyboard|locale"</automated></verify>
  <done>Exact dist satisfies shell, copy, read-only, focus, responsive and locale contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire authoritative migrate, compare, selective apply, conflict and rollback</name>
  <files>src/v100/project-safety.js, src/v040-extension.part01, test/e2e/fixtures/fake-ha.mjs, test/e2e/project-safety.spec.mjs</files>
  <behavior>- Five guided steps always preview before mutation. - Five diff categories/impacts and dependency closure render; required dependencies cannot be deselected. - Calls send only expected revision, opaque preview ID, selected stable IDs and required confirmation. - Shared rejection remains failed with no localStorage fallback or service call.</behavior>
  <action>Implement the state machine over Companion preview/apply/rollback. Never trust client candidate/closure as authority or auto-retry conflicts. Show distinct loading, empty, validation, migration, diff, applying, success, revision conflict, recovery, bundle, evidence and unavailable states with approved copy. Harden the v0.4 ProjectStore compatibility seam so shared failure cannot become local success; standalone local behavior remains explicit.</action>
  <verify><automated>npm run build &amp;&amp; npm run test:e2e -- --grep="migrate|diff|selective apply|conflict|rollback|no fallback|no service"</automated></verify>
  <done>Exact-dist workflow, dependency locks, server-bound calls, failures, recovery and rollback states pass with zero forbidden effects.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Complete exact-dist state, accessibility and opaque-asset evidence</name>
  <files>src/v100/project-safety.js, test/e2e/fixtures/fake-ha.mjs, test/e2e/project-safety.spec.mjs</files>
  <behavior>- Dist hash matches build manifest and Companion www before Chromium. - Desktop 1440x900, mobile 390x844, 200% zoom, themes/media preferences and keyboard-only completion pass. - SVG/HTML/script canaries never execute, inject DOM, fetch network or call services.</behavior>
  <action>Complete all 17 UI-SPEC scenarios and visual/accessibility matrices using deterministic fake HA. Assert no horizontal page scroll/clipped primary action, focus containment/restoration, status announcements and typed confirmation. Test opaque asset metadata without active preview. Regenerate outputs only with `npm run build`; do not list or edit dist/www/editor as authored targets.</action>
  <verify><automated>npm run build &amp;&amp; npm run test:e2e -- --grep="opaque asset|xss|network|no service" &amp;&amp; npm run test:e2e</automated></verify>
  <done>T-05 owner command and complete exact-dist UI-SPEC suite pass.</done>
</task>

</tasks>

<threat_model>
Canonical ledger: `01-THREATS.md`.

| ID | Stable threat description | Plan role | Mitigation / verification |
|---|---|---|---|
| T-01 | Client alters candidate/selection after preview | supporting UX control for owner 01-07-T2 | Send stable IDs/revision/opaque preview only; browser forgery/conflict tests |
| T-02 | User invokes rollback with browser-forged receipt | supporting UX control for owner 01-07-T2 | Typed confirmation and server snapshot selection only |
| T-05 | SVG/HTML/script asset executes during inspection | OWNER 01-11-T3 | Opaque metadata-only display and effect spies; `npm run build && npm run test:e2e -- --grep="opaque asset|xss|network|no service" && npm run test:e2e` |
| T-07 | Diagnostics or bundle leaks remote tokens/state | supporting UI minimization for owner 01-08-T3 | Evidence metadata allowlist and text rendering |
</threat_model>

<verification>Run the complete exact-dist suite after build. Generated dist/www/editor are outputs only and no live HA/plant write occurs.</verification>

<success_criteria>
- Complete approved UI-SPEC behavior is shipped and tested from exact dist.
- Shared mutation remains authoritative and no-fallback.
- T-05 owner command passes.</success_criteria>

<output>Create `.planning/phases/01-trusted-contract-release-foundation/01-11-SUMMARY.md` when done.</output>
