{
  "schema_version": "1.0",
  "project": "GLT Flow Card",
  "milestone": "v1.1",
  "created_at": "2026-09-01T20:39:55.9074407+02:00",
  "updated_at": "2026-09-02T09:00:00Z",
  "status": "in_progress",
  "pause_reason": null,
  "current_position": {
    "phase": "04",
    "phase_name": "runtime-operations-drilldown",
    "stage": "phase-verification",
    "plans_created": 17,
    "tasks_planned": 17,
    "plans_implemented": 17,
    "implementation_started": true,
    "resume_file": ".planning/phases/04-runtime-operations-drilldown/.continue-here.md"
  },
  "completed": [
    "Phase 1: all 13 implementation plans and all 14 recorded code-review fixes are committed; full post-fix gates pass.",
    "Phase 2 planning: context, UI contract, research, validation, patterns, threats, source audit and 17 plans are committed.",
    "The bounded final Phase 2 plan check passed and is recorded in 02-PLAN-CHECK.md; the T2-16 recursion correction is consistent across every planning artifact.",
    "Python 3.13 gates are portable: tools/python-launcher.mjs resolves the pinned interpreter on the Windows py launcher and on Linux without weakening the pin.",
    "Plan 02-01: threat register status column, seven-principal authenticated HA fixtures, controlled fake service, Phase-2 resource ledger, the twelve-entry strict RED classifier with mutation tests, and the extended browser effect ledger with seeded secret scanning.",
    "Plans 02-02 through 02-05: all twelve Phase-2 RED sentinels exist and are accepted by the classifier - policy matrix, access and subscriptions, evidence pagination, leases, collaboration guard, merge, configured controls, control evidence, migration and lifecycle, browser reducers, exact-dist fixture seed, and the complete exact-dist UI.",
    "Plan 02-06: the deny-default policy boundary and the server-owned ACL are GREEN; T2-01, T2-02 and T2-03 pass their owner commands.",
    "Plan 02-08: connection-bound exclusive engineering leases are GREEN (T2-10).",
    "Plan 02-07: membership administration, per-event subscriptions and opaque cursors are GREEN (T2-04, T2-05).",
    "Plan 02-09: the in-lock mutation guard and bounded three-way merge are GREEN (T2-11, T2-12).",
    "Plan 02-10: trusted evidence separated from bounded browser telemetry is GREEN (T2-09).",
    "Plan 02-11: configured controls resolve from the verified head and dispatch at most once (T2-06, T2-07, T2-08).",
    "CI was red on main before this branch; both root causes are fixed - shallow checkouts fabricated the build manifest's provenance, and the parity gates had no Python dependencies. A third issue found on the branch: the Phase-2 modules were missing from the HACS packaging lists.",
    "Plan 02-12: the fail-closed browser authority reducer, the persistent authority state bar, the capability-aware Overview cards, the in-dialog access work surface and the separated trusted/untrusted evidence sections are GREEN (T2-13 core). The plan also added the missing glt_flow_card/capabilities/get route: the UI contract and the exact-dist fixture had assumed it since the RED wave, but no backend plan declared it.",
    "Plan 02-13: the collaboration and configured-control state machines and their lease, conflict, control-confirmation and evidence surfaces are GREEN (T2-13, T2-14). Every one of the twelve Phase-2 sentinels now reports implemented; the phase gate is 12 implemented, 0 controlled RED, 0 broken.",
    "Plan 02-14: legacy authority retires conservatively on upgrade - the 30-3600s lock window is replaced by the 60-900s lease window with clamping rather than resetting, persisted locks are dropped rather than minted into leases, and legacy audit rows are kept labelled legacy_untrusted (T2-05, T2-15).",
    "Plan 02-15: the HA lanes now fail on a collapsed run, on any skip and on any deselect, the pytest cache no longer poisons the bind-mounted workspace, and verify:release consumes the staged packages instead of ignoring them.",
    "Plan 02-16: English and German READMEs and the Companion, YAML-project and installation wiki pages state the tested Phase-2 contract; three stale claims (Designer role, TTL locks) were corrected. The docs workflow validates sources and build determinism before either delivery job.",
    "Plan 02-17: tools/verify-phase2.mjs binds SEC-01, COLLAB-01, five roadmap truths, all 17 plans, T2-01..T2-16 and resolved A1-A6 to current evidence, and proves its own command graph is acyclic with exactly one path to the T2-16 leaf. T2-01 through T2-15 are marked verified in the threat register and validation map; T2-16 stays planned because its owner needs a Docker engine this environment does not have.",
    "Phase 3 planning: context, source audit, research, patterns, threats, validation, UI spec and 17 plans across 11 waves are committed; the roadmap's Plans: TBD entry for Phase 3 is replaced.",
    "Plans 03-01 through 03-04: the seven Phase-3 RED sentinels, the misleading-name registry factory (modbus_named_knx, knx_named_modbus), the IDM heat-pump corpus with its four mapping traps, and the extended effect ledger.",
    "Plans 03-05 through 03-07: schema 3 generated from schema 2 preserving every collection, schemas/vocabularies.json with unit dimensions, the sequential 2 to 3 migration in both runtimes, and semantic-model.mjs with its Python mirror proven to return identical verdicts on a shared malformed corpus.",
    "Plans 03-08 and 03-09: registry-derived provenance that infers nothing from a name, the project-scoped glt_flow_card/provenance/get route (route count 37 to 38), and its generation-stamped cache in the lifecycle ledger.",
    "Plans 03-10 and 03-11: versioned parametric equipment profiles whose controls are deny-default, so a profile control cannot name a domain, service or target; an upgrade carries every still-addressable override and reports what it cannot.",
    "Plans 03-12 and 03-13: assignment-based entity mapping with explained reasons, binding nothing without acceptance, with manual overrides stored as decisions.",
    "Plan 03-14: the frozen operational-state precedence table with pairwise proof generated from the table itself; trust outranks activity.",
    "Plans 03-15 through 03-17: the four semantic surfaces in the generated artifact, bilingual documentation, and tools/verify-phase3.mjs on the Phase-2 orchestrator pattern with 22 gate mutation tests. T3-01 through T3-13 are marked verified; T3-14 stays planned because its owner needs a Docker engine this environment does not have.",
    "Phase 3 exposed and fixed four latent index bugs: project_contract.py split its schema list at literal index 3 and project_bundle.py selected the bundle manifest as _ALL_SCHEMAS[3], so adding schema 3 shifted both onto the project contract and bundle manifests were being validated against it. Both now derive.",
    "Phase 3 exposed and fixed a packaging gap: the HACS stager and its independent validator each keep their own file list and both were missing schema 3 and the vocabulary file, so ha-artifacts went red with FileNotFoundError while every local gate passed. Both lists are corrected and the drift guard now walks the schema directory as well as *.py.",
    "Phase 4 planning: context, source audit, research verified against the vendored HA 2026.2.3, patterns, threats, validation, UI spec and 17 plans across 13 waves.",
    "Plans 04-01 through 04-04: ten Phase-4 sentinels, a two-project operations corpus, an effect ledger that records and refuses callApi and window dialogs with per-effect attribution, and all ten controlled-RED sentinels.",
    "Plans 04-05 through 04-08: panels/get composing the panel server-side with a filtered control list and no dispatch target; views/subscribe reading a snapshot and its sequence in one critical section under a per-connection budget; navigation/resolve bounded and re-authorized per use; navigation/portfolio summing counts after the project filter. Route count 38 to 42.",
    "Plans 04-09 through 04-12: the address-as-state navigation reducer, the panel render model, the nine-state outcome presentation with no retry, and honest gap detection that never interpolates.",
    "Plan 04-13: five operations surfaces in the generated artifact, and the legacy operate path retired and proven inert - including a canOperate that granted control to everyone whenever no permission list was configured at all.",
    "Plans 04-14 through 04-17: lifecycle and packaging, bilingual documentation and a wiki page, and tools/verify-phase4.mjs with 22 gate mutation tests. T4-01 through T4-13 verified; T4-14 stays planned for lack of Docker.",
    "Phase 4 corrected its own threat model during execution: the first corpus hid objects inside one project as though authorization were per-object, but AccessService.async_assign has no object granularity. The corpus is now two projects and T4-04 moved to the portfolio total, where the leak actually is.",
    "Phase 4 added test_lane_portability.py after the same lane-only bug appeared twice: a component test that reaches for src/, a top-level test/ directory or a host binary now fails locally instead of only in ha-artifacts."
  ],
  "remaining": [
    "Run npm run test:phase4 (which subsumes phases 2 and 3) where Docker and the package-provenance endpoints are reachable, then mark T4-14, T3-14 and T2-16 verified.",
    "Complete the Phase 2, 3 and 4 code reviews, security audits, UI audits, drift checks, and independent goal verification.",
    "Write the individual plan summaries for Phases 3 and 4; only the consolidated summaries exist.",
    "Plan and complete Phases 5 through 10 - all still carry Plans: TBD - and the final milestone/release verification.",
    "Close the still-open consolidated Phase 1 security, UI, drift, and goal verification."
  ],
  "decisions": [
    "A lease-guarded route validates its bearer at the policy boundary before the handler runs; plan 02-09 adds the second, decisive check inside the coordinator lock.",
    "A filtered collection is reachable by every authenticated principal and protected by omitting rows; refusing the call would itself leak that rows exist.",
    "Component-wide surfaces authorize against the union of a principal's project capabilities; a user with no assignment anywhere still holds nothing.",
    "Authorization is decided synchronously at the WebSocket boundary, before a handler is scheduled.",
    "Each RED test file carries exactly one product-completeness sentinel and reports every unmet guarantee as a gap list, so a controlled RED fails exactly once.",
    "No release is created at this checkpoint.",
    "Live Home Assistant, remote-site, physical-bus, and plant writes remain outside authorization.",
    "The browser never derives a role-to-capability matrix of its own. Until the server returns one, RoleMatrixDisclosure renders its declared unavailable state: a client-side matrix would be exactly the browser-derived authority this phase exists to prevent.",
    "A threat row is marked verified only after its owner command has actually run and passed. T2-16 stays planned rather than being marked on the strength of its constituent parts passing separately.",
    "Vocabulary data lives in schemas/vocabularies.json beside limits.json rather than in a foreign x-vocabularies keyword inside the schema: a foreign keyword is something every consumer has to be taught about, and it breaks Ajv strict mode. A drift test asserts the schema enums equal these keys.",
    "A profile control is deny-default via unevaluatedProperties: false, so it cannot name a domain, service or target at all; the server still resolves every control from the verified head.",
    "Entity mapping is an assignment problem, not a per-slot ranking: a candidate that fully covers another slot's name is skipped for this slot. An undeclared role token is an eligibility rule, not a score penalty.",
    "The containment path is derived from the hierarchy rather than stored, so it cannot drift from the graph it describes.",
    "Provenance is project-scoped and describes only entities the project references, so the route cannot become a registry search; a hidden project answers byte-identically to a missing one.",
    "Authorization is per project, so restriction is per project. Phase 2's ACL assigns one role per (project, user); a corpus that hid objects from a member would model an authority the product does not have.",
    "A portfolio total is summed from the already-filtered project set. Computing it across every project and filtering the rows afterwards hides the row and not the number.",
    "An authorized count of zero is reported as no count at all: a rendered zero distinguishes an empty authorized scope from an unauthorized one.",
    "The trend region renders a declared history_unavailable state until Phase 7. Building a history path in Phase 4 would duplicate HIST-01's ownership.",
    "A component test may not reach outside custom_components/ and tests/, nor shell out to a host binary: the HA lane workspace contains neither."
  ],
  "blockers": [],
  "open_verification": [
    "Phase 1 consolidated security, UI, drift, and goal verification.",
    "T2-16, T3-14 and T4-14 are all owned by the composed test:phaseN:release leaf, which needs Docker and has never been run as one command. ha-artifacts is green on this branch, but that is one of the leaf's four parts.",
    "npm run test:phase2, test:phase3 and test:phase4 all fail closed here at the Phase-1 gate, whose dependency-provenance step this network policy answers with HTTP 403."
  ],
  "human_actions_required": [],
  "background_processes": [],
  "git": {
    "branch": "claude/chatgpt-continuation-hi3y86",
    "remote": "origin",
    "remote_branch": "claude/chatgpt-continuation-hi3y86",
    "pull_request": "https://github.com/Xerolux/glt-flow-card/pull/3",
    "push_authorized": true,
    "release_authorized": false,
    "working_tree_expected_after_commit": "clean"
  },
  "environment": {
    "python": "tools/python-launcher.mjs resolves Python 3.13; a repository .venv is used on Linux",
    "playwright": "set PLAYWRIGHT_CHROMIUM_EXECUTABLE when the container Chromium revision differs from the pinned Playwright version",
    "gate": "npm run test:phase4 reports every Phase-4 sentinel as controlled RED, implemented, or broken; node tools/phase4-red-gate.mjs runs the sentinel classifier alone"
  },
  "resume": {
    "first_action": "Read .planning/phases/04-runtime-operations-drilldown/.continue-here.md. All 17 Phase-4 plans are implemented and every sentinel reports implemented.",
    "next_gate": "Run npm run test:phase4 in an environment with Docker and reachable package-provenance endpoints, then mark T4-14, T3-14 and T2-16 verified.",
    "after_gate": "Phase 2, 3 and 4 review passes; then plan Phase 5 (CAD Engineering & Extension Platform), which still carries Plans: TBD."
  }
}
